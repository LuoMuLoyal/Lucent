# Lucent 部署加固计划：容错性、可控性与线上可排查性（2026-07-16）

来源：2026-07-16 部署与生产运维专项审查（`deploy/` 全部内容、`Dockerfile`、3 个 GitHub workflow、`src/common/{logger,metrics,queue}`、健康探针、配置校验、AdminJS、相关 ADR 交叉核对）。

**决策记录（2026-07-16）**：确认**砍掉蓝绿双 slot，改单 slot 停机部署**。理由：共享数据库下蓝绿挡不住 schema 风险（旧 #3）、回滚实现已坏（旧 #1）、维护成本高（cron 双触发、SSE drain、内存双份）；单 slot 停机部署下旧代码先停、再 migrate、再启新代码，破坏性迁移反而更安全。代价：每次发布 15~45s 停机窗口，低峰时段发布，已确认可接受。

每条完成并落地到 docs 后，从本文件删除对应章节。

**进度（2026-07-17）**：#1~#14 及 #15 的大部分子项已完成并落地 docs（见
`docs/02-logs/migration-log/2026-07-17.md`），对应章节已删除。唯一剩余项如下。

---

## 低优先级

### 15. LLM 调用无熔断器（#15 唯一剩余子项）

已有超时（`AI_MODEL_TIMEOUT_MS=10s`）+ 错误分类重试（`llm-retry.helper.ts`），连续故障时无快速失败。可用简单计数器或 `opossum` 补齐。**工作量**：中。

---

**与架构审查计划的衔接**：`2026-07-16-architecture-review.md` #7 中"cron 蓝绿双触发需加 Redis 锁"随本次拓扑取消而消失，worker 拆独立进程、failed job 死信告警、异步任务结果持久化仍在那边跟踪，本文件不重复。
