info:
  name: 发送验证码
  type: http
  seq: 6

http:
  method: POST
  url: '{{baseUrl}}/api/v1/auth/verification-code'
  body:
    type: json
    data: |-
      {
        "email": "user@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
