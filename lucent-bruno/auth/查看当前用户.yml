info:
  name: 查看当前用户
  type: http
  seq: 4

http:
  method: GET
  url: '{{baseUrl}}/api/v1/auth/me'
  headers:
    Authorization: 'Bearer {{accessToken}}'
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
