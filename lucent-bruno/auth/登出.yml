info:
  name: 登出
  type: http
  seq: 3

http:
  method: POST
  url: '{{baseUrl}}/api/v1/auth/logout'
  headers:
    Authorization: 'Bearer {{accessToken}}'
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
