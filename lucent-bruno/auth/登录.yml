info:
  name: 登录
  type: http
  seq: 2

http:
  method: POST
  url: '{{baseUrl}}/api/v1/auth/login'
  body:
    type: json
    data: |-
      {
        "email": "user@example.com",
        "password": "Abc12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
