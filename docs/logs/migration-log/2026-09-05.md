# 2026-09-05

## chore(contract): openapi-generator 版本提升到 7.25.0

`openapitools.json` 的 generator-cli version 由 7.22.0 升到 7.25.0。OpenAPI 3.2 双轨方案经实测不可行:dart-dio 生成器对 OAS 3.1/3.2 的 required+nullable 字段有上游已知 bug(openapi-generator #21510/#23866),任一表达(`anyOf [T,null]`、`nullable:true`、数组 type)都会把可空字段生成成非空 Dart 类型,影响面达全量模型;且生成器输入侧仍不支持 `openapi: 3.2.0`(validate 报 attribute openapi is unexpected),bootstrap 侧降级折叠又引入结构漂移。最终决定放弃版本升级,spec 保持 OAS 3.0 直出,export-openapi 维持既有 openApiSchema 3.0 兼容过滤,只交付生成器版本提升与 Luminous 侧 dio 升级。
验证:`pnpm export:openapi` 通过且导出 spec 与升级前零 diff;lint 与 typecheck 通过,单测 54 项通过。客户端再生成与 dio 升级见 Luminous 当日迁移日志。

## refactor(api): OpenAPI 组件名与 operationId 语义化改革 (AIP-190/136)

Lucent OpenAPI 契约命名改革一次性迁移:143 个 operationId 从机械名(`<Controller>_<action>_v1`)改为领域语义 `VerbNoun` camelCase(如 `accountControllerChangeEmailV1` → `changeEmail`、`reportsControllerGenerateSummaryAsyncV1` → `enqueueSummaryGeneration`),全局唯一 0 冲突(operationIdFactory 注入命名查找表 + Set 去重断言)。

- 组件名去机械后缀:响应组件 81 个去 `Dto`(`AccountResponseDto` → `AccountResponse`)、请求体组件 73 个改为 `<MethodName>Request`(`AccountControllerChangeEmailV1Request` → `ChangeEmailRequest`);`ProblemDetailsDto`/`SseProblemDetailsDto` 按 Task 5 决策保留(RFC 9457 稳定错误契约,决策固化于 response-schema.registry.ts 注释)。
- 内联对象提升:export-openapi.ts 的 `promoteInlineObjects` 扩展为结构性自动规则(非白名单)——对象属性内联、顶层数组 items、`oneOf`/`anyOf`/`allOf` 组合分支内对象、嵌套数组 items 全部提升为 `<Parent><Field>` 命名组件(180 处),从根上消除 dart-dio 的 `_inner`/`_one_of_` 派生名;跳过 `additionalProperties` 自由 Map(避免 `$ref` 与 `additionalProperties` 并存的非法 schema)。
- 契约产物:`docs/reference/generated/openapi.json` 现为 123 paths / 337 schemas,0 个 `Controller`/`V1`/`_inner`/`_200_response` 机械名,0 悬空引用。
- 受影响测试同步:`test/contract/contract.e2e-spec.ts`、`today-analysis.contract.spec.ts` 更新为新组件名。

验证:Lucent `pnpm build`、`pnpm test:ci`(314 文件 3670 用例)、`pnpm lint:check`(0 errors)、`pnpm export:openapi` 全部通过。Luminous 侧客户端重生成与调用点迁移见 Luminous 当日迁移日志。

## fix(review): 落实 09-04 代码审查 warning/suggestion 项

按 `plans/Lucent-review-09-04.md` 逐项处置(审查于 openapi 命名改革前产出,先以当前代码核对再修):

- **W-1 runtime 信号收口**:新增 `src/config/env/runtime-signal.ts` 的 `isRunningFromSource()`,以目录名向上探测(`src` = 源码运行,`dist`/`build`/`lib` = 编译镜像),替换 `app.module.ts` 与 `i18n.module.ts` 两处硬编码 `basename(dirname(...)) !== 'dist'` 判据;未来 build 输出目录改名只改一处。
- **W-2 logger 时序耦合**:`throttler.config.ts` 的模块级 `logger` 上移到 imports 之后、`buildThrottlerOptions` 之前,消除函数体对后置声明的 TDZ 依赖。
- **W-4 缓存维护日志补 traceId**:`dashboard.service.ts` 全部 5 处 `logger.warn` 结构化字段补 `traceId`/`spanId`(经 `getActiveTraceIds()`),队列/预热场景无活动 span 时优雅为空。
- **S-1 缓存 set fail-open**:`dashboard.service.ts` 缓存 `set` 失败不再 `throw`,仅告警并直接返回已计算结果(读路径不受写缓存故障影响);`dashboard.service.spec.ts` 新增「set 失败仍返回响应」用例。
- **W-5 JSON 解析错误可观测**:`setup-app.ts` 将 `SyntaxError` 的 message 拼入 `BadRequestException` detail(`Malformed JSON payload: <原因>`),400 problem+json 响应与过滤器日志均可还原行列;fuzzing e2e 的 400 契约不受影响(22 用例通过)。
- **W-6 脚本类型收口**:`import-medicine-knowledge.ts` 的 `redisStoreFromUrl(redisUrl: string)`、`stripNamespacePrefix`、`listMedicineCacheKeys` 补显式类型,新增 `CacheKeyEnumerator` 接口承接 `keys(pattern)` 调用(上游 `RedisStore` 类型缺 `keys`,调用点窄 cast 并注释)。

验证:`pnpm typecheck`、`pnpm typecheck:tools`、`pnpm lint:check`(0 errors)、`pnpm build` 全部通过;相关单测(throttler/dashboard/i18n/import-medicine)14+4 项通过;`fuzzing.e2e-spec.ts`(22)与 `startup-order.e2e-spec.ts`(3)通过。

## chore(lint): 清零 oxlint/unicorn 告警,`lint:check` 恢复 --max-warnings=0 门禁

一次性清理 61 条 lint warning(此前 `pnpm lint:check` 因 oxlint 告警被 `--max-warnings=0` 阻断),全部为风格/现代 API 迁移,无行为变更:

- **node: 协议导入**:8 处 `import { randomUUID } from 'crypto'`(today-suggestion rules/collectors)与 `writer.service.ts` 的 `createHash` 改 `node:crypto`;`prisma.config.ts` 的 `path` 改 `node:path`。
- **字符串/克隆/编码**:`rule-version-registry.service.ts`、`risk-check.prompt.ts`、`computation.service.ts`、`summary.service.ts`、`proposal-draft-extractor.ts` 的拼接改模板字面量;`meal-analysis.types.ts` 的 `JSON.parse(JSON.stringify())` 改 `structuredClone`;两处 `charCodeAt` 改 `codePointAt`;`logger.config.ts` 十六进制转义改 `\u001B` 风格。
- **集合/遍历**:`next-step.service.ts` 白名单数组改 `ReadonlySet`;`router.ts` 的 summary-point 工具筛选改 Set + `.has()`;3 处 `.reverse()` 改 `toReversed()`;`risk-detection.service.ts`、`context.service.ts`、`cron-jobs.service.ts`、`stats.service.spec.ts` 的重复 push 合并为单次调用;`manager.service.ts` 去除无用的 `no-useless-switch-case` 分支;`context.service.ts` 的 `while` 日期遍历改为毫秒 for 循环(满足 `no-unmodified-loop-condition`)。
- **对象 API**:`domain-failure.ts`、`message-text.utils.ts`、`review.spec.ts` 的 `hasOwnProperty.call` 改 `Object.hasOwn`;`notifications.service.ts` 的 `Object.assign({}, ...)` 改对象展开;`llm.config.ts` 三元改 `||`;`verification-code.service.ts` 改默认参数。
- **测试文件**:mock 工厂 `function ()` 改箭头函数(7 处);`sse-connection-registry.service.spec.ts`、`startup-order.e2e-spec.ts` 的 `EventEmitter` mock 改 `EventTarget`(补 `once` 兼容层);`env-file-paths.spec.ts` 的 `typeof x === 'undefined'` 改直判;`fuzzing.e2e-spec.ts` 十六进制转义改 `\u0000`。

验证:`pnpm lint:check` 0 warnings / 0 errors 通过(`--max-warnings=0` 门禁恢复)。
