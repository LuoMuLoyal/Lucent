# 2026-09-16

## test(config): 补缓存跳过 Redis 分支的用例

（来源：2026-09-11 至 09-15 五日审查报告的 warning 项，报告已处理完毕、文件已删。）

### 背景

`CacheConfigService` 在 `OPENAPI_EXPORT_SKIP_REDIS === 'true'` 时退回内存 store，
这条分支是给「没有 Redis 的 CI 里跑 OpenAPI 导出」用的，但此前没有用例覆盖：
两个既有用例一个只给 `REDIS_URL` 传 undefined，另一个对所有 key 都返回同一个
`rediss://` 字符串（因此恰好走的是 `!== 'true'` 那一侧）。

### 变更

- `src/config/services/cache.config.spec.ts`：补两条用例——`OPENAPI_EXPORT_SKIP_REDIS`
  为字面量 `'true'` 时退回内存且不建 Keyv store；为其它值（`'false'`）时仍走 Redis。
  按 key 分派的 mock 取代了原先「所有 key 同一个返回值」的写法。

### 验证

- `npx vitest run src/config/services/cache.config.spec.ts` 全绿。
- 反向验证：临时去掉 `OPENAPI_EXPORT_SKIP_REDIS` 判断，skip 用例如期失败；
  恢复后通过。

## docs(daily-records): 记录餐食分析 v1 历史记录的展示表现

（来源：2026-09-11 至 09-15 五日审查报告的 warning 项，报告已处理完毕、文件已删。本轮无行为改动。）

### 背景

餐食分析 v2 重构把 `payload` 收敛成只有 `mealAnalysis` 一个键，并要求 `version: 2`。
但这次重构**改变了已存在记录的可见行为**，此前只在类型文件里写了形状差异，没写用户可见后果：

- v1→v2 新增的热列（状态/标题/热量/revision）在旧行上全是默认值（null / 0），
  读路径据此按「没有分析」处理；
- `mealAnalysisPayloadSchema` 的 `version: 2` 校验让 v1 的 `mealAnalysis` 被判为
  形状不匹配，`parseMealRecordPayload` 直接置 null。

合起来的结果是：**迁移之前已经分析过的餐食记录，客户端不再显示分析结果。**

### 变更

- `src/modules/daily-records/types/meal-analysis.types.ts`：在读写路径的头注释里补上这段
  表现，并写明为什么不回填——v1 的自由形状 `mealInput` 与人工确认快照，和 v2 的一次
  多模态直出模型没有可靠映射，猜出来的分析结论比「没有结论」更糟；用户对该记录重新
  发起一次分析即可拿到 v2 数据。
- `docs/TODO.md`：登记 B7（P3），把「若产品要求旧记录可见」的验收口径固定为**重新分析**
  而非数据迁移。

### 验证

- `pnpm docs:verify` 通过；`npx eslint` 对改动文件无告警。
- 本轮不改代码行为，未新增测试。

## fix(assistant): 餐食 digest 上限提示不再谎报「你请求了 N 条」

（来源：2026-09-11 至 09-15 五日审查报告的 warning 项，报告已处理完毕、文件已删。）

### 背景

`limitCapped` 的判定是 `analyzed.length > limit.value`——「窗口内已分析餐食多于单次返回
上限」，与模型是否传过 `limit` 无关。但提示文案回落成 `requestedLimit ?? digest.limit`，
于是模型**没传** `limit` 时渲染出：

> Requested 20 meals, but the digest returns at most 20 analyzed meals.

两个数字相同，且它根本没请求过。这段文本是给 LLM 读的（写进 `ambiguities` 让它知道自己
拿到的是哪一段），自相矛盾的说明会直接污染模型对结果完整性的判断。

注意报告里的推断（`requestedLimit` 为 null 时回落）方向对，但成因不是 `clampPositiveInt`
——那个函数的 `capped: true` 必然伴随非空 `requested`；真正的成因是上面 `limitCapped` 的
判定本身与「请求」无关。

### 变更

- `src/modules/assistant/tools/shared/tool-constants.ts`：
  `MEAL_DIGEST_LIMIT_CAP_MESSAGE` 接受 `requestedLimit: number | null`，为 null 时改说
  「窗口内已分析餐食多于单次返回上限，只含最新 N 条」。同时给 `MEAL_DIGEST_DAYS_CAP_MESSAGE`
  同样的 null 分支——它的 `?? windowDays` 目前不可达（封顶必伴随非空 `requested`），
  但保留同样的兜底措辞，免得将来封顶条件变化时又出现同类自相矛盾的句子。
- `src/modules/assistant/tools/read/read.service.ts`：两处调用点不再 `?? ` 回落，直接把
  可空值交给文案函数。

注：这两段文案都不是面向用户的界面文案，是工具返回里给模型看的 `ambiguities`。

### 验证

- `src/modules/assistant/tools/read/read.service.spec.ts` 补两条用例：未传 `limit` 时提示
  不含「Requested」且给出上限数字；传了 `limit: 99` 时仍如实说「Requested 99」。
- 反向验证：临时恢复 `?? digest.limit`，新用例如期失败并**原样重现**了上面那句
  `Requested 20 meals, but the digest returns at most 20 analyzed meals.`；恢复后通过。
- `npx vitest run` 两个相关 spec 全绿；`pnpm typecheck`、`npx eslint` 无问题。

## fix(daily-records): 过期回收改用独立的作业丢失失败码

（来源：2026-09-11 至 09-15 五日审查报告的 warning 项，报告已处理完毕、文件已删。**跨仓契约变更**。）

### 背景

`MealAnalysisSweeperService` 回收超时仍停在 `analyzing` 的记录时写
`analysis_failed(model_timeout)`，但它自己的注释（以及 `MEAL_ANALYSIS_STALE_AFTER_MS`
的注释）写的是「作业已经丢失：进程崩溃、队列丢单、Redis 数据被清」——判定的是
**作业没跑完就没了**，不是**模型调用超时**。两者对用户的补救动作一样（重试），但故障
归因完全不同，混成一个码会让「模型超时」的告警淹没有真实基础设施故障的时段。

（`model_timeout` 仍在用，且用法正确：`worker.service.ts` 的模型调用超时映射。）

### 变更

- `schemas/meal-analysis.schema.ts`：失败原因码词表新增 `job_lost`，并写明它与
  `model_timeout` 的区别是责任方（谁该被叫醒）。
- `services/meal-analysis/sweeper.service.ts`：落 `job_lost`（含日志文案）。
- `constants/meal-analysis.constants.ts`、`dto/record-item.dto.ts`：同步注释与契约描述。
- `docs/reference/generated/openapi.json`：`pnpm export:openapi` 重导出。
- 对侧（Luminous）：`analysis_summary_card.dart` 增加 `job_lost` 的 l10n 分支并新增
  `recordMealAnalysisFailureJobLost`（中「分析中断了，请重试」/ 英），跑 merge + gen-l10n；
  `dart run scripts/contract/bootstrap.dart` 重生成客户端。

### 验证

- 契约影响面已核对：该字段在契约里是普通 `string`（不是生成枚举），所以新增取值
  **不产生**生成客户端的编译破坏——`openapi.json` 与 `generated/lucent_api` 的 diff
  都只有描述文本变化。
- 客户端有 `_ =>` 通用兜底，未知码不会白屏；新增分支由既有参数化用例覆盖，
  该用例显式枚举每个码，因此**必须同步加一项**才真正覆盖（已验证：临时删掉分支，
  用例如期报 `Found 0 widgets with text "分析中断了，请重试"`）。
- `sweeper.service.spec.ts` 期望值同步为 `job_lost`。
- Lucent：`npx vitest run` 全绿（含 17 个 daily-records spec）、`pnpm lint:check` 0 warning
  0 error、`pnpm typecheck` 通过。
- Luminous：`flutter analyze` 无问题、`flutter test test/record` 全绿、
  `dart run scripts/docs/verify.dart --verify` 通过。

## test(daily-records): 补餐食 payload 写入服务的单元用例

（来源：2026-09-11 至 09-15 五日审查报告的 warning 项，报告已处理完毕、文件已删。本轮无行为改动。）

### 背景

`MealPayloadWriterService` 是「客户端提交 → 落库 payload」的唯一闸门，四个公共方法各自
带独立的早返回（无既有分析、图片数量不为 1、状态不是 analyzing、无 payload 等），
但它此前没有自己的 spec——只被 `records.service.spec.ts` 的端到端路径间接覆盖，
单条分支（尤其「多张图不置 analyzing」「revision 覆盖与状态判定读法不同」）测不到。

### 变更

- `src/modules/daily-records/services/meal-payload-writer.service.spec.ts`：新增，18 条用例
  按方法分组，覆盖每个早返回、客户端菜名编辑（标记 `source: 'user'`、不重算结论）、
  恰好一张图时的入队占位与 revision 递增、热列投影、以及入队的四种分支。

### 验证

- `npx vitest run src/modules/daily-records` 全绿；`npx eslint`、`pnpm typecheck` 无问题。
- 用例有效性：临时把 `attachments.length !== 1` 放宽成只判 null，5 条用例如期失败；
  恢复后通过。
- 写用例时发现并**如实钉住**了一处既有实现差异：`enqueueAnalysisIfNeeded` 的
  `analysisStatus` 判定直接取原始字段，而 `sourceRevision` 走 `parseMealRecordPayload`
  的契约校验。半截 payload 因此会以 revision 0 入队。这是既有行为、正常写入路径不产生
  半截形状，本轮不改（只加用例记录差异，改动其一时必须同时面对）。

## test(reports,assistant): 锁住 SSE 头确实走到了裸 socket

（来源：2026-09-11 至 09-15 五日审查报告的 warning 项，报告已处理完毕、文件已删。本轮无行为改动。）

### 背景

`prepareSse` 直接对裸 response `writeHead`，因此绕过了框架 reply 管线——只有把
`reply.getHeaders()` 显式传进去，插件注册的头（`@fastify/cors`、`vary: Origin`）才能到
socket；少了它浏览器会以 CORS 失败为由直接断开流。

三个 SSE 端点的**实现**都是对的（五处 `prepareSse` 调用全都传了 `getHeaders()`），
但测试对这条的覆盖参差不齐。核对实际情况如下（与报告的推测有出入）：

- `today-analysis`：用真实 `prepareSse` 断言 `writeHead` 同时收到 CORS 头与
  `Content-Type` —— 已经是对的，无需改动。
- `assistant`：只断言 `prepareSse` 收到了第 4 个参数，且 fixture 的
  `getHeaders()` 返回 `{}` —— 断言存在，但传空对象时**丢掉 `getHeaders()` 也照样通过**，
  实际没有防护力。（另一个端点 `regenerateLastMessage` 的断言早已带真实头。）
- `reports`：`getHeaders: () => ({})` 写死，且完全没有对 `writeHead` 的断言 —— 真缺口。

`prepareSse` 本身（含 CORS 透传与键冲突时 SSE 协议头优先）已有专项 spec，覆盖良好，
缺的是「各端点确实把 `getHeaders()` 传下去了」。

### 变更

- `src/modules/reports/reports.controller.spec.ts`：`makeMockReply` 的 `getHeaders`
  改为可注入（默认仍为空对象），SSE 用例带上真实 CORS 头并断言 `writeHead` 收到
  CORS 头与 `Content-Type: text/event-stream; charset=utf-8`。
- `src/modules/assistant/assistant.controller.spec.ts`：SSE 用例的 fixture 带上真实
  CORS 头，断言随之改为校验真实头对象（空对象断言 ⇒ 真实头断言）。

### 验证

- 反向验证（两个仓库同样的手法）：临时把对应端点的第 4 个参数（`reply.getHeaders()`）
  去掉，两条新断言分别如期失败；恢复后通过。
- `npx vitest run`（assistant + reports + common/api/sse）全绿；
  `npx eslint`、`pnpm typecheck` 无问题。
- 两个实现文件最终无改动（`git diff` 为空），本轮只加强测试。

## refactor(today-analysis): 睡眠上下文字段与载荷命名对齐

（来源：2026-09-11 至 09-15 五日审查报告的建议项，报告已处理完毕、文件已删。）

### 背景

`TodayAnalysisContextService.buildSleepContext` 从睡眠记录的 `payload` 里读 `startedAt` /
`endedAt`，投影出来的 `SleepContext` 却叫 `startAt` / `endAt`——同一份数据在同一条链路
上有两个名字，改动映射时极易只改一侧（尤其 `startedAt` 与 `startAt` 只差两个字母，读错
不会报错，只会静默取到 null）。

### 变更

- `src/modules/today-analysis/services/pipeline/context.service.ts`：类型声明、空结果、
  局部变量与返回对象统一为 `startedAt` / `endedAt`，与落在 `payload` 里的键名一致。
- 三个构造该类型的 spec fixture 同步改名。

### 验证

- `npx vitest run src/modules/today-analysis` 全绿；`npx eslint`、`pnpm typecheck` 无问题。
- 全仓 grep 确认 `SleepContext` 已无 `startAt`/`endAt` 残留。
- **未**一并改 `assistant/tools/read/read.service.ts` 里同名的 `startAt`/`endAt`：那是
  助手工具返回给模型的载荷（内联对象字面量，不在 `SleepContext` 类型上），改名会改变
  模型看到的结构，属于另一个决策，本轮不动。

## refactor(today-suggestion): 饮食失衡并列顺序改名为 `tieBreakRankOf`

（来源：2026-09-11 至 09-15 五日审查报告的建议项，报告已处理完毕、文件已删。）

### 背景

`diet-imbalance.service.ts` 里的 `WATCH_KIND_PRIORITY` / `priorityOf` 命名暗示「优先级」，
容易被读成参与触发判定；实际上它**只**作为 `.sort()` 的并列打破器（出现天数相同时谁占
文案主位），触发与否只看出现天数（`days >= DIET_IMBALANCE_MIN_DAYS`）。名字与职责不符
会让后来者误以为调整顺序会改变触发行为。

### 变更

- 常量改名 `WATCH_KIND_TIE_BREAK_ORDER`，注释写明「顺序即优先级、不参与是否触发的判断」。
- 方法改名 `tieBreakRankOf`，并补注释说明「不在表里的维度排到最后」是刻意兜底（将来新增
  维度忘记录入顺序时排后面，而不是静默排到最前）。

### 验证

- `npx vitest run src/modules/today-suggestion` 全绿；`npx eslint` 无问题。
- 全仓 grep 确认无 `priorityOf` / `WATCH_KIND_PRIORITY` 残留。
- 纯改名，无行为变化。

## fix(common): Error 日志的堆栈改回字符串并清掉空 error 字段

（来源：2026-09-11 至 09-15 五日审查报告的 warning 项，报告已处理完毕、文件已删。报告的归因有误，
实际缺陷与描述不同，见下。）

### 背景

`logger.error(new Error('boom'))` 是 NestJS 的惯用写法，nest-winston 会把 Error 当**元数据**
转发，Winston 于是产出：

```json
{
  "context": "CtxTwo",
  "error": {},
  "level": "error",
  "message": "boom",
  "stack": ["Error: boom\n    at ..."]
}
```

两个真实的缺陷（用 nest-winston + 本仓 format 实测复现，非推测）：

- `stack` 是**数组**而不是字符串。开发格式的 `devConsoleFormat` 里 `printf` 只认
  `typeof info['stack'] === 'string'`，所以**开发环境下 Error 的堆栈从来没被打印过**；
  JSON 消费者（VictoriaLogs / Loki）也拿到不符合字符串预期的形态。
- `error: {}` 序列化后不含任何信息（Error 的自有可枚举属性为空），却留在日志里冒充数据。

报告原本的说法（`_msg` 在 `manuallyRegisterLogger` 里丢失）在本仓不可达，已实测排除；
但顺着它检查这条链路时发现了上面两个真问题。

### 变更

- `src/common/logger/logger.config.ts`：新增 `errorNormalizeFormat`，接在两种格式的
  `otelTraceFormat` **之前**，让两个 sink 看到同一形态：
  - 数组 `stack` 拼成单个字符串（空数组则整个删掉）；
  - `error` 为空对象时删掉；带真实数据时原样保留；
  - 已经是字符串的 `stack` 不动。

### 验证

- `src/common/logger/logger.config.spec.ts` 新增 6 条用例：数组 stack 展平、空 error 删除、
  有内容的 error 保留、字符串 stack 不动、空数组 stack 删除，以及**开发格式下堆栈确实
  被打印**（直接断言输出含 `at one`）。
- 反向验证：临时从两种格式里摘掉 `errorNormalizeFormat`，6 条中 4 条如期失败；
  恢复后通过。
- `npx vitest run src/common/logger` 全绿；`npx eslint`、`pnpm typecheck` 无问题。

## 说明：本日条目中的来源引用已改写

本日以上条目原本以路径形式引用 plan 文件（即 2026-09-11 至 09-15 的五日审查报告）。
该报告的全部条目已处理完毕、文件已按「计划执行完毕整体删除」的约定删除，因此把九处
**来源指针**改写成了不带路径的描述。改写只动了当日的来源行（同一次提交内的新增内容），
未删除任何既有条目的实质内容——`docs:verify` 的 append-only 守卫按行数统计会把这次
改写计为删除行，故在此显式说明缘由。

## fix(medicine-import): 药品叙事文本解码 XML 实体并剥离内联标记

### 背景

DrugBank 的 prose 字段原样携带 XML 实体与伪 Markdown，例如
`The intravenous LD&lt;sub&gt;50&lt;/sub&gt;`、`**Indicated** for:[label,L6616]&#13;`。
清洗链路此前只做 `.strip()`，因此这些字符原样入库并原样渲染到药品详情页，用户看到的是
`&#13;` / `&lt;sub&gt;` / `**` 字面量。

### 变更

- `scripts/import/medicine/parsers/common.py`：新增 `clean_narrative_text()`——先做实体解码
  再做标签剥离（顺序不可颠倒，否则 `&lt;sub&gt;` 与字面 `<sub>` 会走成两条路径），
  处理 `<br>`/块级标签为换行、剥离 `[label,L6616]` 式引用标记、还原 `**bold**` 强调、
  把 `[vitamin K]` 这类交叉引用拆成裸文本，最后收敛空白与连续空行。
- `scripts/import/medicine/parsers/drugbank_drugs.py`：新增 `narrative_text()`，**只**用于
  prose 字段（description/indication/mechanism/pharmacodynamics/toxicity/metabolism/
  absorption/half-life/protein-binding/route-of-elimination/volume-of-distribution/clearance）。
  标识符、URL、日期字段仍走 `normalize_text()`，内容不被改写。
- `DrugDataBase` 侧重跑 `drugbank-drugs` 导入。

### 验证

- 新增 `scripts/import/medicine/drugbank-narrative-cleaning.spec.ts`（随 `pnpm test:tools`
  运行，数据集缺失时自动跳过），覆盖实体解码、下标保留数字（`LD&lt;sub&gt;50` ⇒ `LD50`
  而非丢数字）、引用标记剥离不吃正文、换行保留。
- 重跑导入后全库 19842 行，残留脏标记仅 1 行——核实为 `TOP**` 脚注符号，属源文本真实内容，
  清洗器正确地未剥离；用例中一并钉住该行为，避免后续把合法星号误删。

## feat(medicine-import): 补入 DrugBank 药物的靶点作用类型

### 背景

`all.csv` 与 `pharmacologically_active.csv` 的表头完全相同，**都没有** `Actions` /
`Known Action` 列（解析器的 `get_first_value(row, "Actions", "Action")` 回退分支从不触发）。
实测该表 `actions`、`known_action` 两列全表为空，`relation_kind` 恒为来源数据集名。
真正的作用类型在 `full database.xml` 的
`<targets>/<enzymes>/<carriers>/<transporters>` 里，实测 38 个取值。

### 变更

- `scripts/import/medicine/parsers/drugbank_drugs.py`：新增 `parse_xml_targets()`，读取四类
  关系并提取名称、物种、`<actions>`、`known-action`；产物经 `xml_targets` 字段随药物记录输出。
- `scripts/import/medicine/import-medicine-knowledge.ts`：`xml_targets` 是旁路数据而非
  `drugbank_drugs` 的列，upsert 前剥离、流式解析结束后由新增的 `applyXmlTargetActions()`
  统一落库。该函数按靶点名称（大小写与首尾空白不敏感）关联 `drugbank_targets`，
  回填 `actions`/`known_action`，并以真实关系类型插入 CSV 中不存在的关系行。

### 验证

- 重跑导入：32464 条关系被回填，2467 个靶点名称未匹配（多为 XML 中的非人类物种条目）。
  全表关系数 58766，其中带 actions 者 22678，关系类型分布为 target 22906 / enzyme 5426 /
  transporter 2757 / carrier 884。
- ⚠️ XML 的靶点 id（`BE0000451`）与 CSV 的数字 id（`451`）是**不相交的两套 id 空间**，
  因此只能按名称关联，无法建立稳定外键。名称匹配对同一名称的多条 CSV 记录会取其中一条。

## fix(medicines): 详情页靶点列表去掉 CSV 与 XML 的重复条目

### 背景

上一条把 XML 的靶点作用类型补进来之后，同一个药物→靶点对会同时存在两行：CSV 导入的行
（`relation_kind` 为数据集名，无 `actions`）与 XML 导入的行（`relation_kind` 为真实关系类型，
带 `actions`）。详情接口把两行都返回，于是靶点列表出现重复条目，且其中一条没有任何作用类型。
实测 Imatinib 因此从应有的 28 条膨胀到 38 条。

### 变更

- `src/modules/medicines/adapters/drugbank.service.ts`：`toTargets()` 先收集 XML 已覆盖的
  靶点，再丢弃与之一一对应的 CSV 行；XML 未提及的 CSV 行保留（全库有 6022 对属于此类，
  丢弃会丢数据）。同一靶点的不同关系类型（target/enzyme/carrier/transporter）**不合并**，
  因为每条边各带自己的 actions。

### 验证

- 实测 Imatinib：38 → 28 条，全部带 actions，类型分布 target 10 / enzyme 8 / transporter 8 /
  carrier 2。
- `src/modules/medicines/adapters/drugbank.service.spec.ts` 补三条用例：XML 覆盖时丢 CSV 行、
  CSV 独有靶点保留、同一靶点不同关系类型不合并。`pnpm test` 全绿（3768 条）。

## feat(prisma): 新增 DrugBank 序列数据两张表

### 背景

`DrugDataBase/unziped/` 的三份 FASTA(`protein.fasta` / `gene.fasta` /
`drug sequences.fasta`)此前没有任何表承载。三份文件的形状各不相同,直接沿用单列唯一键会丢数据,
故先落结构。

### 变更

- `prisma/models/medicine-knowledge.prisma`:新增 `DrugbankTargetSequence` 与
  `DrugbankDrugSequence`,并补 `DrugSourceImport` / `DrugbankDrug` 的反向关系字段。
- `prisma/migrations/20260916230000_drugbank_sequences/migration.sql`:手写建表 SQL。

### 验证

- `prisma validate` 通过;`pnpm db:migrate` 对 dev(15432)与 test(5432)两库均应用成功。
- `prisma migrate status` 报 "Database schema is up to date!",无未应用迁移。
- 直接查 `pg_constraint` / `pg_indexes` 核对:两表主键、两个外键
  (`import_run_id` SET NULL、`drugbank_id` CASCADE)、两个唯一索引与两个普通索引均按设计落库。
- 注:`prisma migrate diff --from-config-datasource` 在本仓会把**所有**表的外键报成
  "Removed foreign key"(连未改动的 `drugbank_targets` 也如此),该方向不可用作漂移判据。

### 数据形状依据(实测)

- `protein.fasta` 5096 条 / 5096 个不同 UniProt id;`gene.fasta` 4726 条 / 4726 个不同 id。
  两者表头格式**完全相同**,只能靠 `source_dataset` 区分:同一 UniProt id 在 protein 是
  20 字母氨基酸序列(610 aa),在 gene 是 4 字母核苷酸序列(1833 nt),确为不同数据。
- `drug sequences.fasta` 615 条 / **335 个**不同药物 id,**一药多序列**(172 个药物多于一条,
  最多 11 条),故链描述必须进唯一键。(药物 id, 描述)实测 615 个组合全部唯一。
- 表头链描述取值开放(实测 `sequence` / `heavy chain` / `SUBUNIT_1` / `(FSH)` / `Heavy chain:` 等
  16 种以上),故原样保存,不强拆 name/chain 两列。
- 靶点 FASTA 表头的括号里带药物 id 清单(每个靶点 1~171 个,均值 5.3),
  该映射只在此处出现,保留为 `drugbank_ids`。
- 不与 `drugbank_targets` 建外键:其 `uniprot_id` 可空且非唯一,无法承载外键。
