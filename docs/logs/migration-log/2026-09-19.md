# 2026-09-19

## fix(semantica): fork 自持前完成 §7 必须项修复（7 处，独立仓 `semantica/`）

（来源：`plans/2026-09-17-semantica-oag-english-side-plan.md` §7 fork 清单，
该计划仍在执行中、未删；本次只做英文侧，未触碰中文侧。）

### 背景

工作区根目录的 `semantica/` 是上游 `semantica-agi/semantica` 的 fork 克隆
（origin = `LuoMuLoyal/semantica`，upstream = 官方仓）。检查发现它当时是
**零改动的干净克隆**：HEAD 与 `upstream/main` 完全一致，没有任何 fork 分支
或本地提交，即 §7 的 12 项 fork 改动**一项都没做**。

本次按 §7 的"必要性"列，完成其中 **必须** 与 **值得** 的全部 7 项
（第 1、2、3、4、9、10、11、12 项；第 5–8 项按下述边界不做）。

每处修复先在源码上逐条核实缺陷真实存在（行号按当前 `main` 校准），
再改，再用试点仓的对照实验复验。

### 变更（`semantica/`，8 个提交，一修复一提交）

| #   | §7 项 | 文件                                                               | 内容                                                                                                                                                                      |
| --- | ----- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 9   | 必须  | `graph_store/age_store.py`                                         | `_infer_return_cols` 改为引号/括号感知的词法扫描：新增 `_find_return_body` 与 `_split_top_level_commas`，跟踪单/双引号、反引号与三种括号；反引号别名归一为可用 SQL 标识符 |
| 10  | 必须  | `reasoning/datalog_reasoner.py`                                    | 规则体按顶层逗号切分，`!=` / `<>` 捕获为 `InequalityConstraint` 并在绑定后求值；`DatalogRule.inequalities` 默认空列表保持向后兼容                                         |
| 11  | 值得  | `reasoning/datalog_reasoner.py`                                    | `load_from_graph` 对无任何受支持访问器的对象抛 `TypeError`（原为静默返回 0，让推理对着空事实集跑）                                                                        |
| 12  | 必须  | `ontology/ontology_generator.py`                                   | `_resolve_xsd` 不再重复加前缀：识别 `xsd:` 前缀、展开完整 XSD IRI、保留其他词汇表的 QName，并折叠已产生的 `xsd:xsd:`                                                      |
| 1   | 必须  | `embeddings/provider_stores.py`                                    | `OpenAIStore` 支持 `base_url`（参数 → `OPENAI_BASE_URL`），仅在该值存在时下发，并拒绝非 HTTP(S) scheme                                                                    |
| 3   | 必须  | `embeddings/text_embedder.py`                                      | 新增 `assert_real_embeddings()`：对 `get_method() == "fallback"`（128 维 SHA-256 哈希向量）fail fast                                                                      |
| 2   | 必须  | `semantic_extract/methods.py`                                      | `get_text_embedder()` 的模型参数化：显式参数 → `SEMANTICA_SIMILARITY_EMBEDDING_MODEL` → 原 `bge-small-en-v1.5` 默认；缓存按解析后的模型名区分                             |
| 4   | 值得  | `provenance/postgres_storage.py`（新增）、`provenance/__init__.py` | PG 版 `ProvenanceStorage`：jsonb 载荷 + 索引列、`ThreadedConnectionPool`、`transaction()` / `savepoint()` 与 SQLite 后端同形                                              |

### 关键点

- **第 4 项的隐藏陷阱已避开**：`get_chain_head()` **不在**
  `ProvenanceStorage.__abstractmethods__` 里（基类默认 `return None`，注释写
  "该后端不支持链式"）。只实现 5 个抽象方法会**静默关闭哈希链** —— 写入看似正常，
  `verify_chain()` 却报 `chain_break`。实现里显式覆盖，共 6 个方法。
- **第 10 项补了一处试点未覆盖的缺陷**：事实参数**保留原文引号**
  （`inhibits('a', 'E')` 存为 `"'a'"`），而约束里的常量通常不带引号，
  直接比较会让 `D != 'a'` 恒真。已统一按去引号后的值比较（`_normalise_term`）。
- **第 12 项的自动派生仍不可用**：本次只修了"重复前缀"这一条。
  `sh:minCount` / `sh:maxCount` 依旧只在词条显式声明 `cardinality.min`
  或 `required` 时才发出 —— 这是**有意保留**的：自动发明约束会在 CI 里挡掉合法数据
  （源码 `#1105` 注释即为此）。落地时 SHACL 形状按 §6.4 手写。

### 边界（不做，非遗漏）

§7 第 **5–8 项**（`deduplication` 的 token 预筛 / blocking key、`decision_query`
的 CJK bigram 回落、`split/methods.py` 的中文句切分、`rdf_parser` 的语言标签）
全部标注为「按需」，且**都属中文侧能力**。§2 的决策是
**Semantica 只承接英文侧**（中文走 LightRAG + 边表 + SQL/CTE），
因此这四项在英文侧范围内**不适用**，本次不做。

### 验证

- 在 `semantica/` 新增 6 个回归测试文件（52 项），覆盖：AGE 列推断的引号/括号/
  转义/`DISTINCT`/`RETURN *`、Datalog 的两种不等号写法与不含约束的对照、
  常量不等式、`load_from_graph` 的两种可接受形态与抛错、SHACL 前缀与完整 IRI、
  `base_url` 优先级与 scheme 拒绝、fallback 自检、相似度嵌入模型优先级、
  PG 后端的抽象方法覆盖与 `get_chain_head` 覆盖。
- 用试点仓 venv（Python 3.11）跑 `semantica/` 全量测试：
  **改动前 60 failed / 7068 passed，改动后 60 failed / 7120 passed**
  （+52 为新增用例）。逐条比对失败集合：**新增失败 0、消失 0**，
  即既有失败全部与本改动无关（`vector_store` / `llm_*` / `qdrant` 等
  依赖缺失或既有时序问题）。
- 第 4 项在真实 PG（dev `127.0.0.1:15432`）上跑通：写入 / 检索 / 血缘 /
  `verify_chain()` 返回 `{'valid': True, ...}` / PROV-O 导出为真 RDF / `clear()`。
- 另在试点仓加了 5 个针对 fork 源码的核对脚本
  （`verify_fork_return_cols.py` / `verify_fork_datalog.py` /
  `verify_fork_embeddings.py` / `verify_fork_similarity_embedder.py` /
  `verify_fork_pg_provenance.py`），沿用试点既有的对照实验风格。

### 顺带发现（未改，供后续参考）

`tests/export/test_jsonld_document_iri.py::test_exported_at_still_varies_between_exports`
在上游基线上就是**不稳定用例**（实测 12 次里失败 4 次）：它断言两次导出
的微秒级时间戳不同，同一微秒内会碰撞。与本改动无关，未修。

## feat(semantica-service): 新建英文侧 OAG sidecar 骨架

（来源：同一计划的 §6.5 落点切分与 §8.2 执行顺序。新仓在**工作区根目录**
`semantica-service/`，独立 git 仓，与 `semantica/`（fork 镜像仓）互不隶属；
代码不放 `Lucent/deploy/`，理由见 §6.5。计划仍在执行中、未删。）

### 背景

计划 §8.2 把落地切成 S0–S4，本次做 S2 的第一步：把 sidecar 服务立起来。
**不参考同目录的 `semantica-oag-pilot/`** —— 那是一次性可行性验证，
与新项目无继承关系。

### 变更（`semantica-service/`，6 个提交）

| 提交           | 内容                                                                                                                     |
| -------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `chore`        | `uv init --package` 骨架（src 布局）；移除 uv 默认的 `[project.scripts]` 入口（它指向不存在的 `semantica_service:main`） |
| `build(deps)`  | 依赖集 + ruff/mypy/pytest 工具链；semantica 用 `[tool.uv.sources]` 从 fork `main` 装，**不装 PyPI 0.6.8**                |
| `feat(config)` | pydantic-settings 配置：AGE DSN、图名、连接池上下限、`statement_timeout_ms`、推理规模/超时上限                           |
| `feat(graph)`  | `StorePool`：按请求借还的 AGE store 池 + 每连接 `statement_timeout`                                                      |
| `feat(api)`    | FastAPI 应用 + `/health`（AGE 不可达时返回 503）                                                                         |
| `docs`         | README + `.env.example`；`.env` 加入 `.gitignore`                                                                        |

### 关键点

- **所有端点都是同步 `def`**。`psycopg2` / Datalog fixpoint / pyshacl 都是
  阻塞或 CPU-bound；`async def` 会把它们放上事件循环，一慢全停。
  FastAPI 对同步 handler 自动走线程池。
- **池是"store 池"而非"连接池"**：`ApacheAgeStore` 内部持有单个 `self._conn`，
  因此按请求借还一个 store，而不是共享单例（实测单连接并发 16 慢 2.9×）。
- **`statement_timeout` 的传递方式踩了坑并已修正**：`connect()` 接受
  `**options` 但**除 `connection_string` 外全部忽略**（docstring 自己写着
  "currently unused"），直接传参是静默 no-op；而 `statement_timeout` 是服务端
  GUC、不是 libpq 连接选项，只能经 `options=-c statement_timeout=...` 传，且
  该值含空格**必须加引号**，否则 libpq 报
  `invalid dsn: invalid connection option "statement_timeout"`。
  现按 DSN 传并加引号，连接后再 `SHOW statement_timeout` 复核，不符即拒启。
- `/health` **不只看进程存活**：它真借一条连接读图规模，AGE 不可达时返回 503，
  避免负载均衡把流量送到依赖已挂的进程。

### 验证

- `uv sync` 装到的是 **`semantica==0.7.0` from git `42519e65`**（即本日推送的
  fork 提交）；已在装好的包里复核 fork 修复全部到位：`DatalogRule.inequalities`
  存在、`load_from_graph` 抛 `TypeError`、`_resolve_xsd('xsd:string')` 返回
  `xsd:string`、`PostgresStorage` 可导入。
- `uv run ruff check` / `ruff format --check` / `mypy src tests`（strict）全通过；
  `uv run pytest` 17 passed。
- 真实启动 `uvicorn` 验证：AGE 未运行时 `/health` 返回 503 且错误体准确报出
  `Connection refused`（修正 DSN 前报的是 `invalid dsn`）；`/openapi.json`
  正常暴露 `/health`，可供 Lucent 生成客户端。
- mypy 对 `semantica.*` 单独加 `ignore_missing_imports`：该包既无 `py.typed`
  也无 stub，属客观未类型化；我方代码仍在 strict 下。`types-psycopg2` 以
  dev 依赖引入，未放宽规则。

### 尚未做（S2 后续）

`/reason`、`/schema`、`/validate` 三个端点、vendor wheel 树，
以及 `Lucent/deploy/semantica/` 配置与 compose 服务定义。

## feat(docker): AGE 镜像改为自建（PG18 + pgvector + AGE 1.7.0）

（来源：`plans/2026-09-27-apache-age-introduction-plan.md`，仍在执行中、未删。
本次同时修正了该计划中一个**已实测不成立的镜像前提**。）

### 修正的前提

计划原定三环境直接用社区镜像 `dyingbleed/postgres-rag:18-trixie`，
并据此写了「不引入新容器」「第三方镜像供应链风险」两处结论。

**实测该镜像仅发布 `arm64/linux`**（Docker Hub API 查 tags 的 `images` 字段），
而 dev/test/prod 均为 amd64 —— **直接不可运行**。原记录的「AGE 只有 arm64、
需 QEMU 顶着」亦由此更正：那说的是该社区镜像，不是 AGE 本身。

计划 §八 待确认 1 已预留退路（「不达标则按同一配方自建」），故改自建，
计划中 §一 决定 1/6、§2.1、§2.2、§3.1、§3.4、§五 P0、§六 风险表、
§七 文档动作、§八 待确认共 9 处一并更新。

### 变更（`semantica-service/`）

新增 `docker/postgres-age/`：`FROM pgvector/pgvector:pg18` + 编译 AGE
`PG18/v1.7.0-rc0`，只 COPY `age.so` / `age.control` / `age--1.7.0.sql` 三个产物，
不把编译工具链带进最终镜像。基底已含 pgvector，省去一层编译。

新增 `scripts/check_age_connection.py`：对真实 AGE 的连通性自检（手动执行，
不进测试套件——测试必须能无库运行）。

### 构建踩到的两点

- 基础镜像**无 `git`**；改用 GitHub tarball + `curl` 获取源码（顺带不带 `.git`）。
- slim 基底**无 `ca-certificates`**，`curl` 直接报
  `error setting certificate file`；补装该包。

### 验证

- 镜像构建成功；容器起来后 `CREATE EXTENSION age` → `age 1.7.0`、
  `vector 0.8.5`（**未用 1.8.0**，避开 #2500 SIGSEGV）。
- **P0 验收通过**：建独立 database `lucent_graph`、建扩展后
  `SELECT * FROM ag_catalog.ag_graph` 返回空结果（0 rows）。
- Cypher 往返通过：`CREATE (n:Drug {name:'Aspirin'})` → `MATCH ... RETURN n.name`
  返回 `"Aspirin"`。
- **fork 修复在真实 AGE 上生效**：`RETURN n.name AS \`drug name\` ORDER BY n.name`的返回列被正确推断为`drug_name`—— 即修复`69baa02e`（`\_infer_return_cols`
  引号感知）所要解决的行为。
- semantica（fork）经 `ApacheAgeStore` 连上该容器，`get_stats()` 返回
  `{'node_count': 1, ...}`。
- `ruff check` / `ruff format --check` / `mypy src tests`（strict）通过；
  `pytest` 17 passed。运维脚本的 `T201 print` 以 `per-file-ignores` 定向豁免
  （输出即其接口），未关闭整条规则。
- Lucent 侧 `pnpm docs:verify` / `docs:links` 通过。

## fix(compose): 数据库镜像归属与命名更正

（接上一条。上一条把 Dockerfile 放在 `semantica-service/`，**该归属有误**，
本条目更正；上文保留为当时的记录，不回改。）

### 归属更正

该镜像服务的是**本仓三环境 compose 里的 `postgres` 服务**，与
semantica-service 是否部署无关。放在那边等于让 Lucent 的部署依赖
semantica-service 的源码——Coolify 的构建上下文也够不到仓外路径。

已移交本仓 `docker/postgres-age/`；`semantica-service/` 侧对应删除
（见该仓 `refactor: 数据库镜像定义移交 Lucent 仓`）。

### 命名规范化

原 `lucent-postgres-age:pg18-age1.7.0` 是把技术栈拼在一起，既长又把
「一个扩展」当成了镜像身份。改为 **`lucent-db:18`**：

- 它**首先是 Lucent 的主库镜像**，AGE 只是额外带的扩展 → 按定位命名，
  将来增删扩展不必改名
- 标签只写 PG major；AGE 版本由构建参数 `AGE_REF` 决定，生产用短 sha
  标签保证可复现（同 `LUCENT_IMAGE` 的做法），故不必把扩展版本拼进标签
- 经 **`${LUCENT_DB_IMAGE}`** 注入：dev 有默认值，staging/prod 必填。
  与既有 `LUCENT_IMAGE` 同法，**仓库不写死 registry**

### 验证（对 dev 真实环境）

- `lucent-postgres-dev` 用新镜像重建后 healthy；`pg_available_extensions`
  含 `age 1.7.0`。主库 `lucent` 的既有扩展未受影响。
- dev 建出 `lucent_graph` 并启用 age 后，
  `SELECT * FROM ag_catalog.ag_graph` 返回空结果（0 rows）—— P0 验收项。
- **`/health` 从 503 变 200**：semantica-service 经真实 DSN 连上
  `lucent-db:18` 的 `lucent_graph`，返回图规模与连接池占用。
- **fork 修复在真实 AGE 上生效**：`RETURN n.name AS \`n name\` ORDER BY n.name`的返回列被正确推断为`n_name`—— 即修复 `69baa02e` 所要解决的行为。
- `docker compose config -q` 对三环境均通过；`LUCENT_DB_IMAGE` 覆盖实测生效。
- `pytest` 23 passed（新增 6 个时长解析用例）。

### 期间修掉的两个自身缺陷

- **默认 AGE 端口写错**（15433 → 15432）：初版凭印象填，与 dev 实际端口
  不符；改为对齐 `docs/reference/environment-variables.md` 的记录，
  并让自检脚本读服务配置而非硬编码 DSN。
- **语句超时校验误报**：`SHOW statement_timeout` 把 `15000ms` 规范化显示为
  `15s`，而校验按字符串精确比较，导致**配置正确却被判为未生效并拒绝启动**
  （`/health` 因此返回 503）。改为解析带单位的时长，`min` 不被误判为 `ms`。

## feat(assistant): 英文侧 OAG 接线 —— `reason_over_ontology` 工具落地

（来源：`plans/2026-09-17-semantica-oag-english-side-plan.md` 的 S5 / §3.5 / §3.6。
同日 `semantica-service` 与 `Luminous` 各有配套改动，见下。）

### 分工（计划 §3.5 ②）

**NL→Cypher 的生成留在 Lucent**（复用 `AI_LANGUAGE_*` 角色），sidecar 只做只读校验 +
执行。因此 sidecar 不需要 LLM 凭据，Lucent 侧也没有对应的 API key 项——两边的凭据模型
不同是刻意的，不是遗漏。

### 变更（`Lucent/`，本仓）

| 类别   | 内容                                                                                                                                                                                                           |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 客户端 | `semantica-client.service.ts`：与 LightRAG 客户端同形（判别式失败；超时/不可达/鉴权/被拒/畸形响应分类）。差别有二：**没有 API key**；把 sidecar 的结构化 `detail.kind` 归一出来，作为重试回路的判据            |
| 生成   | `cypher.schema.ts` / `cypher.prompt.ts` / `cypher-generator.service.ts`：继承 `BaseLlmGeneratorService`（结构化输出、重试、熔断、指标、OTel span 已在其内实现一次）                                            |
| 工具   | `ontology-reasoning.service.ts`：读 schema → 生成 → 执行；被拒则把 sidecar 的报错回喂模型重生成，最多 3 次且总预算 30s；envelope 带 `cypher` / `sourceTier` / `verifiability`                                  |
| 注册   | 工具名与读工具表、来源映射、新增 `ASSISTANT_OAG_TOOL_NAMES`（与 LightRAG 那条线**各自独立判定**）、工具描述与参数、知识子图依赖序、router 优先级、dispatch、缓存键纳入 toolArgs、policy 门控、module providers |
| 超时   | `ONTOLOGY_TOOL_EXECUTION_TIMEOUT_MS = 45s`：一条链路含一轮模型生成（模型自身 10s 超时）+ sidecar 往返，20s 的通用预算会把"正常重试"直接判死                                                                    |
| 配置   | `SEMANTICA_ENABLED` / `SEMANTICA_BASE_URL` / `SEMANTICA_TIMEOUT_MS`（默认 20s，须大于 sidecar 的 `statement_timeout`）；`deploy/semantica/.env.example`；三份 compose 的 `semantica` 服务（profile 门控）      |
| 审计   | confirm 路径补 `assistant.proposal.confirm`（计划 §8.3 登记的缺口）：`resourceType` / `resourceId` + `proposalIds` + `decision` + `status`；用户填的自由文本 `note` 不进审计表                                 |
| 契约   | 能力 DTO 的 `z.enum(ASSISTANT_TOOL_NAMES)` 随新增工具变化 → `pnpm export:openapi` + Luminous 重新生成客户端                                                                                                    |

### 变更（`semantica-service/`，独立仓）

新增 `POST /query`（只读 Cypher 的校验 + 执行）：字面量/注释感知的写关键词守卫、单语句
约束、`limit` 1..500 与截断标记、六类结构化 `detail.kind` 供重试回路判据。177 tests
passed，ruff / mypy(strict) 通过。

附带改动：把 `rebuild_target_edges.py` 按仓内锁定的 ruff 版本重排（**仅空白**）——该文件
在原提交状态下就过不了 `ruff format --check`，不是本次引入的问题。

### 变更（`Luminous/`）

工具名 l10n（`assistantToolReasonOverOntology`，zh/en 片段 → `arb_tools.dart merge` →
`flutter gen-l10n`）、`ui_formatters.dart` 的两处映射（工具显示名 + DrugBank 来源徽标）、
`dart run scripts/contract/bootstrap.dart` 重新生成客户端。

### 验证

- 单测：助手模块 553 passed（全仓 `pnpm test` 通过）；
- 门禁：`typecheck` / `lint:check` / `arch:check` / `docs:verify` / `docs:links` / `build` 通过；
  Luminous 侧 `flutter analyze` 与助手测试通过；
- **真机链路**（真实 sidecar + 真实 AGE 图 3278 节点 / 211,630 边 + 真实 `qwen3.7-plus`）：
  「Which drugs interact with warfarin?」一次生成即通过守卫，返回 10 行带原文描述的真实
  相互作用；共享靶点类查询在图上存在的药上返回带类型的结果（`ACTS_ON` 多跳 + `p.name`）。
- **本次抓到并修掉的真实陷阱**：图上名称按 DrugBank 原样**大写**存储，`{name: $x}` 精确
  匹配在大小写不一致时**静默返回 0 行**——那会被如实说成"DrugBank 没有这条断言"，
  即"看起来像答案的错答案"。prompt 已强制 `toLower()` 匹配，实测确认修复后同一问题返回
  真实行。

### 仍未做（诚实清单）

- **sidecar 镜像 / Dockerfile**：三份 compose 的服务定义已就位（profile 门控，避免
  `docker compose up` 去拉不存在的镜像），但 `semantica-service` 仓还没有 Dockerfile，
  容器化部署仍不可用；dev 目前靠本机 `uvicorn` + `.env.development` 直连。
- **PROV-O 引用未接**：envelope 目前带执行过的 Cypher + 来源等级作为可复核路径；
  sidecar 尚未暴露 provenance 端点，fork 的 PG provenance 后端只在库侧可用。
- 计划 S0（英文侧评测集）与 S3（§4.1 词表 → 手写 SHACL/OWL）未做。
- `plans/2026-09-27-apache-age-introduction-plan.md` 的状态行与 §2.1 已过期
  （仍写"未开工 / 待自建"，而 dev 已用自建 `lucent-db:18`、AGE 1.7.0 且 P0 验收通过）。

## fix(assistant): 英文侧 OAG 直连评测暴露的三处修正

（来源：`semantica-oag-pilot/eval-2026-09-19/` —— 11 道复杂/多跳/多关系问题**绕过
Lucent 直连 sidecar** 的评测，产物与报告在同目录。）

评测本意是度量能力（结论：10/11 与源表 SQL 复算的 GT 一致，唯一红项归因于 `/reason`
的 `load_from_graph` 导出桥，不是自然语言→Cypher 的问题），但它反过来暴露了接线侧
三个真实缺陷。三处互相独立，一修复一提交。

| #   | 文件                                                                                             | 内容                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `agent/runtime/classify.ts`、`agent/runtime/tool-keyword-rules.ts`                               | `classify.ts` 里**第三份**硬编码知识工具集合（`KNOWLEDGE_TOOL_NAMES`）漏了 `reason_over_ontology`：命中该工具的消息被判成"读个人数据"，路由到 read 子图后只绑个人记录工具。实测表现是走 Lucent API 问 "What drugs interact with warfarin?" 时模型自称"我只有你的用药记录"——**工具存在但永远到不了模型**。关键词规则同时补齐：原 `/interaction/i` 漏掉最常见的 `interact` 词形，`shared target` / `metaboli[sz]ed by` 也过于字面 |
| 2   | `tools/ontology/cypher.prompt.ts`、`ontology-reasoning.service.ts`、`shared/tool-definitions.ts` | 零行措辞：原文 "no edges matching this question" 易被读成"不存在该关系"。评测证实**源表的相互作用列表本身就是单向的**（19,842 行里只有 4,218 个药出现在源列；本次子图范围内 895 个药中 829 个），故"图上无此边"与"临床上无该相互作用"不等价——prompt 与工具描述都改为显式声明这一点                                                                                                                                              |
| 3   | `tools/ontology/semantica-client.service.ts`                                                     | `eslint-disable-next-line` 写在了 `catch` **体内部**，指向 `return` 而非 `catch` 行 → `no-silent-catch` 仍报错、附带一条 unused directive 警告（上一批提交时 `lint:check` 被中断，未复验）                                                                                                                                                                                                                                      |

### 验证

- `pnpm lint:check` 通过（0 error / 0 warning）；
- `classify.spec.ts` 新增回归：三条英文药学问法都必须提供 `reason_over_ontology`。
  断言只钉"工具被提供"、不钉 intent —— `get_current_medicines` 的 `/drug/i` 会把英文
  问法一并命中，intent 落 knowledge 还是 mixed 取决于这条**预存在**的路由特性；
- `ontology-reasoning.service.spec.ts` 的零行断言随之更新；
- 助手模块 `vitest run src/modules/assistant` 554 passed。

### 仍未做（承接上文）

- 「PROV-O 引用未接」不变：本轮只做了直连评测，provenance 仍未暴露到 sidecar 端点；
- 评测顺带暴露的两条线上问题仍待修：`/reason` 导出桥的双 LIMIT 且无 ORDER BY
  （上限 50,000，211,630 条边实际只加载约 23% 且每次不同）、密集多跳必须靠
  `WITH ... LIMIT` 收窄且上限须高于中间基数（否则静默丢行）。

## feat(provenance): PROV-O 端到端引用（边上 prov → sidecar → envelope → SSE → 来源条）

（本节的验证产物在 `semantica-oag-pilot/provenance-2026-09-19/`；跨仓改动同时在
`semantica-service/` 与 `Luminous/` 各留一份提交。）

### 背景：链路原本断在每一处接缝

上一轮直连评测证明 OAG 能答对问题，但"可审计"只是口径：图上没有边级溯源，sidecar
没有 provenance 端点，envelope 没有引用位，SSE 投影只转发
`coverage/confidence/ambiguities/source/disclaimer`（`result` 整个丢掉）。两端都
"没接"。

### 变更（`semantica-service/`，独立仓）

| 提交               | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `feat(provenance)` | id 方案（`lucent:<来源表>/<行键>[/<动词>]`，可读、可解析、写读共用一份定义）、`ProvenanceReader`（懒连接、失败记一次）、PROV-O 渲染（`prov:Entity` / `prov:Activity` / `prov:Agent` / `wasDerivedFrom` 指向来源行，JSON-LD 与 Turtle 由同一张 rdflib 图序列化）、`POST /provenance`、`GET /provenance/verify`、`/query` 内联解析（`provenance` / `provenance_missing` / `provenance_truncated` / `provenance_error` 四件套：把"没有记录"与"没有库可问"分开） |
| `feat(scripts)`    | `scripts/add_edge_provenance.py`：图上写 `prov`（每关系类型一条 Cypher，211,630 条边 176s）＋审计库灌入（220,011 条：边 211,630 + 多动词来源行 8,379 + 导入活动 2），哈希链在 Python 侧一次算完，批插 47s；`--verify` 双向校验（图上 id 全部可解、库里没有图上没有的 id、`verify_chain` 无断链）                                                                                                                                                             |

**bulk 与库自身写路径的关系**：`ProvenanceManager._save_entry` 每条 3 次往返
（实测 100 条/s，211k 条要一小时），所以灌入走批量；链算式用的是库自己的
`compute_checksum`，而两端一致性由 `verify_chain` 在事后重新推导校验——不是"两条路径
等价"的口头假设，是事后可验证的结论。`--via-manager` 保留库自身写路径作为参照。

### 变更（`Lucent/`，四个原子提交）

| 提交                                                                  | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `feat(assistant): 本体推理带 PROV-O 引用并透传到客户端来源条`         | envelope `result.citations`（id / 来源表 / 行键 / 原文 / 活动 / 校验和）/ `citationCount` / `citationsTruncated` / `citationsMissing` / `citationsError`；`verifiability` 由行数与引用数共同决定（有行无引用 = `uncited`，不再是 `citable`）；行回来了却没有引用时**强制重写一次**（提示具体、只一次，属性类问题不会被反复回炉）；SSE 投影新增 `citations` 与 `executedQuery`（问→查询→证据三段都可复核）；系统提示词要求引用 `[prov: <id>]` 且不得引用结果里没有的 id |
| `fix(assistant): NL→Cypher 补充引用要求、角色链与酶/ATC 词汇匹配规则` | 三条实测规则：关系必须返回 `r.prov`；多跳角色链两侧用各自的关系类型（`(X)-[:SUBSTRATE_OF]->(e:Protein)<-[:INHIBITS]-(i)`，两侧同一类型会返回 0 行）；酶按 `gene_name` 问（"CYP3A4" ⟷ "Cytochrome P450 3A4"）、ATC 只有 `code` 没有名称                                                                                                                                                                                                                                 |
| `fix(assistant): 补齐底物/CYP/ATC 类问法的关键词路由`                 | `/substrate/`、`/cyp\d/`、`/p450/`、`/atc/` 等：实测这两类问句原先根本不会被提供该工具                                                                                                                                                                                                                                                                                                                                                                                 |
| `fix(assistant): 图节点超时改为模型调用加一次工具预算`                | `ASSISTANT_NODE_TIMEOUT_MS = AI_MODEL_TIMEOUT_MS + max(工具预算) + 5s`（默认 60s）。原先节点默认取模型超时 10s，**比工具自己的 45s 预算还小**：慢查询被节点超时掐断、LangGraph 重试三次，客户端拿到每次尝试的半截答案，最后以 `Node "agent" exceeded its run timeout` 收场——工具预算等于从未生效                                                                                                                                                                       |

### 变更（`Luminous/`）

`AssistantToolCitation` 模型 + `_mapCitations` 映射（未知键剥离而非整块丢弃）、来源条
的引用区（原文 / 来源表·行键 / id，点击复制 id 便于复核）、l10n 片段
`assistantSourceCitationsLabel`，以及映射与渲染两组测试。

### 验证（真机：真实 AGE 图 3,278 节点 / 211,630 边 + 真实模型 + 真实 SSE）

- **审计面**：`GET /provenance/verify` → `valid: true`，`total_entries: 220011`，
  `broken_links: []`；`--verify` 双向一致（图上 211,630 个 id 全部可解，库里 0 个
  多余 id）；随机取一条相互作用边，能从 `r.prov` 一路解到
  `lucent.drugbank_drugs.drug_interactions` + `drugbank_id=…, drugbankId=…` + 原文。
- **端到端（走 Lucent 的 SSE，模拟前端）**：9 题（多跳 / 多关系 / 角色链 / 范围外 /
  阴性对照），冷缓存下 7 题通过；q01 单题拿到 50 条引用且逐条可在审计库解出，
  q02 17 条、q03 10 条、q04 4 条、q09 57 条。确定性：同一条 Cypher 连跑 5 次结果
  完全一致（`identical: true`）；同一问题重复提问引用集合一致（模型改写 Cypher 的
  列序/别名不影响语义）。
- **本轮修掉的三个真实缺陷**（都是"只有走完整链路才看得见"的）：
  1. `ProvenanceReader.verify_chain` 返回驼峰键、而响应模型是蛇形：链报告恒为
     `total_entries: 0`，**看起来像"审计库是空的"**（测试曾用同形状的桩把这个 bug 一起
     钉住了，已改为按模型契约断言）；
  2. 图节点超时 10s 低于工具预算 45s（见上表）；
  3. 我自己写的迁移脚本漏了对侧范围过滤，给 200,182 条边写了 742,440 条记录——
     由 `--verify` 的"库里没有图上没有的 id"当场抓出（该检查项因此留着）。
- **未能完成的一步（诚实记录）**：词汇规则的复测卡在模型供应商配额/凭据上
  （`The assistant model rejected our credentials or exhausted its quota`，快失败，
  连续两次不同时间点复现），因此 7/9 那一轮的数字来自**词汇规则之前**的代码；
  规则本身已提交并有单测，但没有新一轮真机数字。

### 仍未做

- 上一节列的"仍未做"中，`/reason` 导出桥与传输层重试仍未修（已移入 `docs/TODO.md`）；
- ATC 类名与相互作用双向边属于**数据缺口**（不是接线问题），已记入 TODO；
- 评测时务必先清 `assistant:tool:*` 缓存：工具结果缓存 1 小时，跨轮次的同题会直接
  复用上一轮的信封，本轮第一次跑的 0 引用结果就是缓存旧信封造成的假象。
