# Migration Log — 2026-07-25

## 审查后续行动：barrel 残留清理与导入格式化

来源：`plans/Lucent-review-2026-07-24.md` 审查行动计划。

### A1: `today-suggestion.module.ts` import 格式化

- `src/modules/today-suggestion/today-suggestion.module.ts` — 移除 barrel 重构（d0be2f89）遗留的 import 语句间多余空行，按逻辑分组排列（NestJS 框架 → 跨模块 → 模块内 providers，按子目录字母序）。

### A2: `admin/` 子目录死 barrel 清理

删除 3 个使用 `export *` 的子目录 barrel，它们无任何消费者（`admin/setup.ts` 及所有 service/spec 均直接使用深路径导入）：

- 删除 `src/admin/services/index.ts`
- 删除 `src/admin/types/index.ts`
- 删除 `src/admin/constants/index.ts`

同时将 7 个 admin 源文件中通过 barrel 解析的 `'../types'` / `'../constants'` 导入改为深路径 `'../types/admin.types'` / `'../constants/admin.constants'`。

### A3: 空模块 barrel 清理

- 删除 `src/modules/today-suggestion/index.ts`（0 字节空文件，无导出无消费者）。
