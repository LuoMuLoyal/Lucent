# 2026-09-20

## docs: README 与 AI 安全文档对齐当前实现（英文侧 OAG / LightRAG / AGE）

### 背景

对 Lucent 根 README 做了一次事实核对，发现它落后于已经落地的检索与推理分层：
`Key Features` 仍写"三个独立向量检索源"，没有体现英文侧 OAG、LightRAG 中文散文检索、
Apache AGE 图后端；`Stack` 仍写 PostgreSQL / Prometheus，未反映 pgvector + AGE 自建镜像
与 VictoriaMetrics；`dev:stack` 说明仍描述为纯 `pgvector/pgvector:pg18`，而实际镜像已是
叠加 AGE 的自建 `lucent-db:18`（`docker/postgres-age/`），且图数据落在独立 database
`lucent_graph`。另外发现三处指向已不存在路径的引用。

### 变更

- `README.md`
  - 简介补充 ontology-grounded drug reasoning。
  - `Key Features`：Auth 补设备会话与敏感操作重认证；Health Records 补 health events；
    AI Pipeline 补 proposal-based writes；新增 **Proactive Suggestions** 与
    **Ontology-Augmented Generation (English side)** 两条；Medicine Knowledge 改为写明
    三源强制分离（中文散文走 LightRAG / DrugBank passage 走自有 pgvector 表 / 中文产品走 SQL）。
  - `Stack`：PostgreSQL → PostgreSQL 18 (pgvector + Apache AGE)；prom-client/Prometheus →
    prom-client/VictoriaMetrics（ADR-0016）；补 LightRAG 与 Semantica + AGE sidecar（ADR-0021）。
  - 本地基础设施说明：改为自建 `${LUCENT_DB_IMAGE:-lucent-db:18}` 镜像、AGE 只服务英文侧
    OAG 且图在独立 `lucent_graph`、LightRAG 继续用纯 SQL 表；补 sidecar 在 compose profile
    中默认不启动、未启用时对应工具报 `retrieval_unavailable` 而非静默返回空结果。
  - **修正失效引用**：`../Luminous/docs/product/Product_Vision.md` →
    `product-vision.md`（实际文件名小写）；`tool/bootstrap_generated_sources.dart` /
    `tool/verify_lucent_openapi_sync.dart` → `scripts/contract/bootstrap.dart` /
    `scripts/contract/verify_openapi.dart`（Luminous 侧脚本已迁移到 `scripts/`）；
    删除 `scripts/import/food/` 条目（该目录不存在，实际只有 `scripts/import/medicine/`）。

- `docs/reference/assistant-safety.md`
  - 新增 **Knowledge-Source Boundaries** 一节，把已经实现的知识分层边界写成文档：
    中文散文 / 中文产品目录 / 英文 DrugBank 三路强制分离；源选择与 `verifiability` 由服务端
    赋值而非模型；有行无引用标 `uncited`；规则推导结论必须标 derived 且不得与直接断言的
    数据同形呈现；未复核的跨来源 `SAME_AS` 不得被结论级推理消费；**缺少断言 ≠ 临床上不存在**
    （截断的图必须声明范围）；服务不可用必须如实上报、不得静默降级为空结果。
    该节内容与 `src/modules/assistant/README.md` 的 OAG 小节、ADR-0021 及
    `Luminous/docs/product/product-safety-privacy.md` 保持一致。
  - front-matter `updated` → 2026-09-20。

### 验证

- 文档中引用的全部路径逐条 `Test-Path` 复核存在（含 Luminous 侧相对路径）。
- 未改动任何代码，未触碰 `docs/reference/generated/`。

## fix(graph): ATC 类名决策落定（不导入），图侧数据缺口核实

### 背景

计划 D1 是决策项：1,467 个 `ATCClass` 节点只有 `code`/`level`，没有类名。
三个候选里默认 ②（不导入，产品层只呈现 code）。落决策前先在源库核实名称是否
真的不存在，避免把"导入漏了字段"误判成"数据本来就没有"。

### 结论

源库侧确认名称在整条链路上都不存在：

- `drugbank_drugs.atc_codes` 是 JSON 数组且只含代码（如 `["B01AB07"]`，3,257 行有值）；
- `information_schema` 中没有任何名称含 `atc` 的列，也没有任何 ATC 名称表；
- 图侧 `ATCClass` 的 `name`/`title`/`description` 计数均为 0。

即这是数据源本身没有，而非导入丢字段。故维持选项 ②：只呈现 code，类名交给
模型/UI 解释；若产品明确要求类名，需单独立项导入公开 ATC 索引并先确认许可与来源。

### 变更

- `docs/TODO.md`：追加上条决策与核实依据，作为该决策的记录位置。

### 验证

- 三条查询均在 dev 源库与 AGE 图库上实跑并留痕（见上）。

## feat(assistant): OAG 推理与图谱硬化落地（R1/R2/R4/R5/D1/D2/S1/S2）

### 背景

计划 `plans/2026-09-19-semantica-reasoning-and-graph-hardening-plan.md` 的
sidecar 侧工作面已实施完毕，本条目记录 Lucent 视角的关键结论与对下游的影响。
代码变更全部落在 `semantica-service` 仓（该仓独立提交），Lucent 侧只改文档。

### 关键结论（均为真机实测，非推断）

- **交互边单向会让规则得出自信的错误答案**。导入后 895 个药物里 80 个从未作为
  源出现（华法林：出边 0、入边 530），任何只沿出边遍历的规则都会得出
  "华法林不与任何药物相互作用"。已在导出期补全对称关系的反向边并保留原始方向，
  单向交互从 34,496 降到 0。**这意味着同一问题在修复前后会给出不同答案**，
  此前基于该数据的评测结论需重新审视。
- **SHACL 命名空间写错会静默通过**。同一形状在正确命名空间报 895 条违规、
  在错误命名空间报 0 条且 `conforms`。已加门禁拒绝这类形状。
- **规则必须显式给范围**。Datalog 没有否定，"未推出"是它对"不存在"的唯一回答，
  因此图支持的推理必须声明范围，空范围直接 422 而不是返回空结论。

### 变更

- `plans/2026-09-19-semantica-reasoning-and-graph-hardening-plan.md`：
  状态同步。已完成阶段改为落地内容；未完成的保留 **R3（Lucent 侧
  `reason_over_rules` 工具接线）** 与 **E1/E2（评测集固化）**，这两项在 Lucent 侧。
  并记下三处与原计划不同的决策依据（R5 不落词表 YAML、R1 分页不用 keyset、
  D2 改落导出期），避免后来者当作遗漏重做。
- `docs/TODO.md`：ATC 类名决策落定（选项 ②，不导入）及源库核实依据。
- `docs/reference/assistant-safety.md` 的 Knowledge-Source Boundaries 一节
  已包含 derived 结论不得与断言同形呈现等约束，与本轮实现一致，未再改动。

### 对下游的影响

- **评测集（E1/E2）必须重跑**：交互边方向修复改变了可答问题集合，
  修复前的评测结果不再代表当前行为；重跑前先清 `assistant:tool:*` 缓存
  （工具结果缓存 1 小时，跨轮复用会拿到过期 envelope）。
- **R3 未做**：sidecar 已能返回带归因的结论（`rule` + `conclusions` +
  前提 `citations`），但 Lucent 还没有工具调用它，模型暂时看不到这些结论。

### 验证

- sidecar 仓：`ruff` / `ruff format` / `mypy --strict` 全通过，`pytest` 全绿。
- 五个真机验证脚本全部通过（确定性导出、结论→前提→`prov` 引用链、
  形状确实约束到图、重试与日志计数、单向边归零）。
- 本仓：`pnpm docs:verify` 与 `pnpm docs:links` 通过。

## R3：规则推理接入 Lucent，评测集扩到 11 题（11/11）

### 变更范围

**Lucent 新增工具 `reason_over_rules`**（`src/modules/assistant/tools/ontology/rule-reasoning.service.ts`）。
与 `reason_over_ontology` 的分工是**断言与推导**，不是"另一个端点"：前者读图上写了
的边，后者按规则推出图上没写的边——依据不在任何一个三元组里，而在两个三元组的
**连接**上（"A 抑制的酶恰好是 B 的代谢酶"）。

硬约束落地方式：让推导与断言在**类型上**就分开。新增 `SemanticaReasonOutcome` /
`SemanticaDerivedConclusion`，与断言的 `SemanticaQueryOutcome` 各自独立；envelope
里 `cypher` 换成 `rule` + `query` + `derivedNote`（明确告诉模型"这是推出来的，
要这么说，不要把断言说成事实"）。同名同形会让下游按名字取值时把规则当成查询。

入参 `intent` + `query` + **必填**范围 + `limit`。范围必填的理由是代价：推导随
子图规模超线性增长，不给范围就是把分钟级计算塞进秒级工具调用，而超时会被读成
"图上没有"。全变量模式（`potential_ddi(?A, ?B)`）同样在边界拒绝——那不是提问，
是让工具把子图的全部推导倒进上下文（单药范围实测 20,116 条）。

`limit` 走独立的 `SEMANTICA_REASON_DEFAULT_LIMIT`（2000 边）/ `_MAX_LIMIT`
（20000 边），刻意不复用 `SEMANTICA_MAX_LIMIT`（25/100 **行**）：一个是"喂进
不动点引擎的边数"，一个是"返回给模型的行数"，当一个数就会在两头都错。

**sidecar 修掉两个叠在一起的静默缺陷**（`semantica-service`）：

1. `conclusions` 完全无视 `query`。请求 `potential_ddi(db00682, ?B)` 时返回规则推出
   的**每一对**——单药范围实测 20,116 条，而该查询只命中 67 条。响应的两半在回答
   不同的问题。修复中踩到第二个坑：绑定行**只含变量**（`[{B: 'db04951'}, ...]`，
   常量 `db00682` 不在行里），所以常量必须从查询模式取、自由变量从绑定行取。
2. 前提恢复的上限作用在收窄**之前**。`premises_for_conclusion` 对全部 20,116 条排序
   取前 500，而查询真正问的 67 条排得靠后——**一条引用都没有**，倒是 500 条无关的
   拿到了引用。改为收窄在前、上限在后：67 条结论、67 条全部带引用、
   `conclusions_complete: true`。

还修了一处**本层的静默降级**，由真机验证当场抓出：sidecar 的 422
`scope_matched_nothing` 被 `parseErrorDetail` 丢成 `null`（它只认 `/query` 那一组
kind），于是信封说成 "Rule reasoning is unavailable"。这是把"你的药名写错了、
换个名字再问"报成"推理坏了"，模型对后者的反应是放弃这条推理路径。现在两个端点
各有词表，并按 kind 分流：可修正的输入问题走独立信封（明说 blame the scope），
基础设施故障才走 unavailable；反向用例（连不上的 sidecar 不得被说成范围问题）
也在验证里。

### 验证

- 本仓：`pnpm lint:check` 0 警告 0 错误；`pnpm test:ci` 3869 通过；`pnpm build` 通过。
- 真机验证 `semantica-oag-pilot/lucent-oag-eval/verify-rules.mjs` **11/11**：正常推导 18 条结论
  全部带规则 id 与前提引用（源自 23,620 条推导事实，即按查询收窄确实生效）、
  空范围拒绝、未命中范围 refusal、全变量模式拒绝、真故障不被误报、空结果归因正确。
- 评测集 `node --experimental-strip-types semantica-oag-pilot/lucent-oag-eval/run.ts` **11/11**，
  覆盖 11 个类别，退出码 0。
- `semantica-service`：`ruff` / `mypy` 全通过，`pytest` 339 通过。
- 纠错过程记入 `semantica-oag-pilot/CORRECTIONS.md`（首轮 5 个）与
  `CORRECTIONS-r3.md`（本轮 3 个）。

### 已知未修

`REASONING_TIMEOUT_S` 覆盖不动点，不覆盖导出阶段。导出侧已从 57s 降到
12.7s（50,000 边），但在更小的预算下仍可能击穿调用方的超时预期。

## feat(assistant): 中文散文检索开放图模式，超时三层同步调整

### 背景

`lightrag-eval/results/mode-comparison.md` 结论 4 把 `leaflet` 建图保持 off 定性为
「**待验证前的默认**，不是已验证的结论」，结论 5 与 line 132-134 则点明启用图模式
**必须同步调整三层超时**（原话：客户端 8s 会把所有图模式查询打成超时失败，
「工具级 `TOOL_EXECUTION_TIMEOUT_MS` 20s 也未必够」）。本次按该药方落地：
把"允不允许图模式"从硬编码改成**跟着索引状态走的配置**，并把三层超时按实测值调够。

### 变更

- `src/config/env/env-keys.enum.ts` / `sidecar-defaults.ts` /
  `environment.validation.ts`：新增 `LIGHTRAG_GRAPH_TIMEOUT_MS`（默认 60000，
  上限 120000——不与 `LIGHTRAG_TIMEOUT_MS` 共用 60000 天花板，实测最慢的 `global`
  已到 28.5s，顶格后没有余量）与 `LIGHTRAG_GRAPH_SOURCES`（默认 `leaflet`，
  逗号分隔，**空串是合法值**，含义是回到"图模式全禁"）。
- `src/modules/assistant/tools/retrieval/lightrag.types.ts`：删掉
  `LIGHTRAG_SOURCES_WITHOUT_GRAPH` 这个硬编码数组，改为 `parseGraphSources()`。
  语义从"未评测前不给用"改成"**这些来源的图已经建好了**"——那是一件运维事实，
  不是一个代码分支：建图是独立长任务（约 $0.057/doc），开关必须能跟着索引状态走
  而不必重新发版。未知值丢弃而不报错（配置笔误 ≠ 请求参数错误）。
- `src/modules/assistant/tools/retrieval/knowledge.service.ts`：`parseMode` 的白名单
  改为查 `LIGHTRAG_GRAPH_SOURCES` 解析出的集合；拒绝理由不变（仍指向"requires a
  knowledge graph"）。
- `src/modules/assistant/tools/retrieval/lightrag-client.service.ts`：新增
  `resolveTimeoutMs(mode)`，`request()` 改为按调用接收超时。`naive` 用 8s、
  图模式用 60s——图模式实测 16–29 秒，共用 8 秒预算等于把它们全部打成超时。
  超时原因里的数字也改用本次调用的值，不再恒报 `this.timeoutMs`。
- `src/modules/assistant/tools/shared/tool-constants.ts` + `tools/tool.service.ts`：
  新增 `RETRIEVAL_TOOL_EXECUTION_TIMEOUT_MS`（65s）并登记为
  `search_cn_medicine_knowledge` 的覆盖值。必须**大于**客户端图模式超时，
  否则客户端还没报"检索超时"，工具层就先判死，信封里只剩一句笼统的
  "Tool execution timed out."。
- `src/modules/assistant/agent/runtime/graph.ts`：`ASSISTANT_NODE_TIMEOUT_MS` 的
  `Math.max` 纳入新常量，否则图节点预算会低于工具预算，重演
  「节点在工具能跑完之前就被取消」那个已经修过一次的缺陷。
- 测试：`knowledge.service.spec.ts` 默认值补两个新键，并把
  `rejects graph modes for leaflets until evaluation passes` 换成两个用例——
  「已建图则放行并透传 mode」与「配置为空则拒绝」；`lightrag-client.service.spec.ts`
  增加 `resolveTimeoutMs` 的分档断言。
- `src/modules/assistant/tools/shared/tool-definitions.ts`：**改写 `mode` 的模型可见
  描述**。原文是「Only "naive" is accepted today — the knowledge graph that
  "local"/"global"/"hybrid"/"mix" require is not built」。**不改这一处，门开了模型也
  不会去用**——这是"改动生效"的关键一步，只放行白名单只算把闸抬起来、没通电。
  新描述给出两档的取舍（单跳事实用 `naive`；需要跨文档连接才用图模式）、
  图模式慢的原因（先调模型抽关键词），并写明 `qa` 只接受 `naive`。
- 文档：`src/modules/assistant/README.md` 的检索小节、
  `docs/reference/environment-variables.md` 的 LightRAG 段（含三层超时的嵌套关系）、
  `docs/TODO.md`（把"未评测前只接受 naive"这条待办改写为新状态）。

### 已知边界（必须与运维状态一致）

**默认值是"门开着"**（`LIGHTRAG_GRAPH_SOURCES` 默认 `leaflet`），而 `mode` 的 enum
一直列着 `local/global/hybrid/mix`。因此**没建图却按默认启动时，图模式拿到的是空结果
而非报错**——只有 `coverage.reason` 会说明。这个默认值成立的前提是 `leaflet` 侧的图
已经建好；索引状态与 `LIGHTRAG_GRAPH_SOURCES` 必须一致，未建图时应把它设成空串
（回到"图模式全禁"）。

另外要留意口径：**中文侧的图是让模型从文本里抽实体关系建出来的**（建图 约 $0.057/doc），
与英文侧 OAG「确定性 schema 映射、知识入库这一层没有 AI 参与」是**相反**的构造方式。
两者都叫"图"，但对外表述时不能混为一谈。

### 验证

- `pnpm typecheck` 通过；`pnpm lint:check` 通过；单元测试全绿（含本次改动的三个 spec）。
- 未改动 `docs/reference/generated/`，未触碰 Luminous 与 sidecar 仓。

## fix(assistant): 补齐审批审计的失败路径，收口三份审查报告的遗留项

（来源：三份审查报告 `plans/Lucent-review-09-16.md`（已删）/
`plans/Lucent-review-09-18.md`（已删）/ `plans/Lucent-review-09-19.md`（已删），
全部实施完毕。剩余 suggestion 已移交 `docs/TODO.md`。）

本条目合并记录三份报告的处置，不再逐日拆分——三份报告在核对时大部分条目已在
更早的提交中修复（09-16 的 W1/W3 见 09-17 日志、W2 见 09-18 日志），本次只对
**在代码上仍成立**的条目动手。

### 变更

**09-19 C-1（真正的问题）：审批失败路径不留审计**

`assistant.controller.ts` 的 `confirmProposal` 把 `logFireAndForget` 写在
`await unwrapResult(...)` **之后**。`confirmProposal` 链路上任一步抛错
（`applyApprovedProposals` 写库失败、`resumeConversation` 失败）都会越过这行
直接进全局 filter —— 也就是**最需要留痕的那条路径恰恰不写审计**。用户点了
approve、界面只显示 5xx、审计表里什么都没有。

按报告推荐的方案 2 修复：审计移入 `AssistantProposalConfirmService`，用
`.map()` / `.mapErr()` 覆盖成功与失败两条路径，controller 不再持有"成不成都来
留痕"的语义负担。

- `services/proposal-confirm.service.ts`：注入 `AuditLogService`；新增
  `auditConfirm()`，成功写 `effectiveStatus = result.status`，失败写
  `'failed'` + `failureCode`。用户填的自由文本 `note` 仍不进审计表。
- `assistant.controller.ts`：删掉审计调用（留注释指向新位置）。

**09-19 W-1：`decision` / `status` 恒等**

原结构里 `decision`（用户请求）与 `status`（服务端结果）在所有路径上取值相同，
审计消费者无法区分二者，且将来加 `apply_failed` 也没有位置放。改为
`requestDecision` / `effectiveStatus`(+`failureCode`)：成功路径二者仍相等，
失败路径 `effectiveStatus = 'failed'` —— "用户批了但写库失败"与"用户拒绝"从此
在审计里可区分。

**09-19 W-2：policy 重复维护 sidecar 元组**

`policy.service.ts` 本地 `new Set(...)` 包了两份已存在的工具名数组。Set 化本身
合理（查询比 `includes` 快且意图更清楚），问题在**重复声明**：新增一个工具要改
两处且两处没有理由不一致。改为在 `tool-types.ts` 导出
`ASSISTANT_RETRIEVAL_TOOL_NAME_SET` / `ASSISTANT_OAG_TOOL_NAME_SET`，调用点直接读。

**09-19 W-6：`internal` 被当作可重试**

`internal` 是 sidecar 自身的 bug，语句没有问题，回喂模型重写不会改变结果——
三次重试就是三次同样的坏结果，只把工具耗时与 token 成本翻几倍。

- `semantica.types.ts`：从 `isRetryableSemanticaFailure` 的可重试集合里移除。
- `cypher.prompt.ts`：删掉 `internal` 的 RETRY_HINTS 行。**同时修掉它的兜底**——
  `RETRY_HINTS[kind] ?? RETRY_HINTS['internal']` 在删掉该键后会变成 undefined；
  改为未登记的 kind 不输出"怎么改"一句：只有知道原因才谈得上纠正方向，硬套提示
  就是让模型去修一个我们并不了解的错误。

**09-19 S-1：`previousErrorKind` 是裸 `string`**

取值来源都是封闭集合（sidecar 两组词表 + 本层 `missing_provenance`），而它的唯一
用途是查 RETRY_HINTS —— 写成 `string` 时拼错只会静默降级成通用提示。收窄为
`SemanticaErrorKind | 'missing_provenance' | null`。

**09-18 W1：HTTP 状态码分类散落三处**

LightRAG 客户端、Semantica 客户端、`provider-failure.ts` 各自维护一份状态码分类，
且集合互不相同。新增 `src/common/constants/http-status.ts` 单点导出，三处统一
引用（`provider-failure.ts` 原先是裸字面量 `status === 429`）。

**09-18 W2：三处 `toDomainFailure` 复制粘贴**

`stream-orchestrator` / `agent/runtime` / `proposal-confirm` 各有一份逐字相同的
实现，`classifyProviderFailure` 每扩展一次就要同步改三处，漏一处会把可诊断的
上游故障静默降级回笼统 500。抽到 `services/domain-failure.ts`。

**09-18 W3：sidecar 默认值双份维护（**顺带抓到一处真实漂移**）**

`environment.validation.ts` 的 zod `.default()` 与客户端 `FALLBACK_*` 常量各写一遍。
核对时发现已经漂了：Semantica 客户端的兜底超时是 **15000**，而 zod 默认是
**20000** —— 直接构造 `ConfigService` 的测试路径拿到的值与生产不一致。
新增 `src/config/env/sidecar-defaults.ts` 单点导出，schema 与两个客户端共同引用。

**09-18 S1：`bypass` 用字符串字面量点名拒绝**

提到 `lightrag.types.ts` 的 `LIGHTRAG_REJECTED_MODES`，`parseMode` 改为查词表；
将来上游再加同类危险模式只需改词表。

### 核对结论：三条报告主张不成立，未按其改动

- **09-19 W-3（节点级 `cachePolicy` 会跨用户串味）**：**实测证伪**。用真实
  LangGraph 建最小复现图（同样的 `cachePolicy: { ttl }` + 同样的 `setNodeDefaults`），
  两个仅 `userId` 不同的调用 → 节点执行 **2** 次，即缓存键吃的是整个 state 输入，
  用户维度的差异天然造成 miss。报告设想的"新节点只吃 `state.userId` 会被串味"
  不成立，故未加断言、未删缓存。
- **09-16 W2（`xml_targets` 旁路字段会静默送进 SQL）**：不成立。
  `scripts/shared/db-upsert.ts` 的绑参是 `spec.columns.map(col => record[col])`，
  只读声明过的列，多余键根本不会被读取。
- **09-16 S4（header 大小写未归一）**：归属错误。`git show c5a1d66a~1` 里
  `shouldBypassCache` 已存在，是既有代码；且 `Access-Control-Allow-Origin: 'true'`
  与 `MEDICINES_BYPASS_CACHE_HEADER` 是两个不同的 header，不会互相误判。

### 未采纳的 suggestion

09-19 S-2（`normalizeLimit` 静默夹紧）/ S-7（citations drop 无 metrics）、
09-18 S2（`INTERNAL_IDENTIFIER` 正则覆盖不全）/ S3（`Task 10` 未进台账）/
S5（下线的旧工具关键词残留）已登记进 `docs/TODO.md`，不在本次改动范围。

### 验证

- `pnpm typecheck` 通过；`pnpm lint:check` 0 warning 0 error；`pnpm test:ci` 全绿。
- 新增/更新用例：`core.service.spec.ts` 增 3 条审计断言（成功路径、**写库失败仍留痕**、
  拒绝路径）；`assistant.controller.spec.ts` 的审计断言改为
  `not.toHaveBeenCalled()`（controller 不再写，避免与 service 层重复计数）。
- 未改动 `docs/reference/generated/`（本轮无 API 契约变化），未触碰 Luminous。
