# 2026-08-31

## docs(governance): Phase 0 文档存在性审计与检查器适配

按 plans/2026-08-31-doc-governance-overhaul-plan.md(实施完毕文件已删)执行 Phase 0:

- 逐篇四问审计完成(40 篇),审计表回填计划 §11;存活文档面裁决为 12 篇,
  达成 -50% 压缩硬目标;被归档/下沉断言承接方逐篇注明(specs、openapi.json、
  compodoc、模块 README、ADR、eslint-plugins)。
- `doc-coverage-lib.ts` / `check-docs-updated.ts` 适配新 docs 布局:新增
  explanation/reference/howto 活跃模式与 front-matter 门禁(reference/howto 必带,
  explanation/ADR/generated 豁免),迁移日志路径同时识别新旧目录,
  EXEMPT_UNREFERENCED_PATTERNS 覆盖新布局;旧编号布局条目保留至 Phase 2
  重建后裁剪。测试 34 例全绿。
- AGENTS.md 文档规则段缩减:六向裁决词汇 + 预置保留 + 布局一句 + 硬生命周期,
  删除四象限长表与数量上限表;修正失实的 `{ code, message, data }` envelope
  断言(实际为直返资源 + Problem Details,ADR-0012);各处文档路径引用同步
  指向新布局。

验证:`pnpm vitest run --config vitest.tools.config.ts scripts/hooks/doc-coverage-lib.spec.ts` 全绿。

## docs(governance): Phase 1 指令单一来源

- CLAUDE.md 指针化:删除全部规则正文,改为 `@AGENTS.md` 引用,规则唯一事实源
  收敛到 AGENTS.md。
- 入口排查:GEMINI.md、.github/copilot-instructions.md 均不存在,无其他规则副本
  入口;根目录 CONTEXT.md / todo.md / ROADMAP.md / CHANGELOG.md 的归档与合并
  按 Phase 0 审计裁决随 Phase 2 执行。

## docs(governance): Phase 2 docs 重建收尾(索引/瘦身/合并/审计落档)

- docs/README.md 重写为唯一索引:布局一段 + 六向裁决一句版 + 存活文档清单 +
  plans/backlog.md 链接 + 模块 README 索引(25 模块 + src/common,按域分组)+
  生成物/归档说明。
- explanation/architecture.md 瘦身:删除依赖图、队列数量、端口表、事件全表等数字快照,
  保留 HTTP 边界、分层与模块纪律、AI 管道分层(吸收 assistant-contract 的
  bounded-linear/common-llm/copy 本地化小节)、领域事件模式一段(细节一句指向代码)、
  可观测性一段;每节尾附相关 ADR 相对链接。
- reference/environment-variables.md 合并 archive/environment.md 仍正确内容(YAML 加载
  顺序、本地 dev 基线、Docker dev 栈、脚本清单要点),以 package.json scripts、
  src/config/env 与 yaml-loader、docker-compose.dev.yml 为准;文首注明本地环境与运行时
  基线也在此文件。
- plans/backlog.md 头部补「唯一 TODO 台账,完成即删行」说明;git 历史根 todo.md 的
  P2-F(medicines ResultAsync 迁移)经 grep 核对 src/modules/medicines 现状已由
  ResultAsync/createDomainFailure 实现,判定完成,不并入台账。
- 新建 archive/2026-08-31-doc-governance-audit.md:Phase 0 四问审计明细落档(本批归档
  28 篇 + 保留新路径 13 篇 + 生成物 2 类),头部注明一次性分析只进不出。
- 存活文档旧路径清零:glossary(补 front-matter、删 Security PIN 废弃条目)、
  assistant-safety(补 front-matter、子文档链指向 archive)、deployment(两处 how-to
  wikilink 失效已修)、howto 5 篇(01-reference/02-logs/contracts/openapi.json 路径、
  wikilink 指向新布局,quadrant 统一为 howto);各篇 updated: 2026-08-31。

验证:`node scripts/hooks/check-links.ts` 对存活文档零断链;遗留断链仅两类——archive 与
旧迁移日志内的历史链接(归档/账本只进不出,不改),README 模块索引指向的 23 个
模块 README 待 Phase 2.3 落盘后自然消除。异常记录:scripts/contract/export-openapi.ts
仍写 docs/openapi.json、compodoc.json 仍输出 docs/compodoc,与 generated/ 新路径待
生成器侧联动修正。

## docs(governance): Phase 2 收尾补记(生成器/检查器/联动)

- 生成器切换:`export-openapi.ts` 输出改写 docs/reference/generated/openapi.json,并新增
  generated region 回填机制(模块 README 中
  `<!-- @generated openapi:BEGIN prefix=/api/v1/<模块> -->` 区块由导出脚本按前缀回填端点摘要,
  幂等零 diff);compodoc.json 输出改写 docs/reference/generated/compodoc。
- 检查器增强:check-links 豁免 docs/archive/** 与 docs/reference/generated/**,并跳过
  行内反引号内的括号记号(历史日志中的字面记号不再误报);孤儿检查宇宙扩展到模块 README
  与 plans/\*.md,doc-map 规则可引用 docs/ 之外的承接文档。
- doc-map.yaml 全量改写:路径切新布局,docs_any_of 指向各模块 README(结构固化承接点);
  glossary/backlog 入 docs_info。
- 修复 docs/README.md 模块索引相对链接(多一层 ../ 导致解析出仓库根);历史日志 2026-07-15
  中 ADR-0008 链接为单行指针修复(01-reference → reference,不改写叙事)。
- 删除根 todo.md:P2-F(medicines ResultAsync)经核实已完成,完成即删;backlog 唯一台账。

验证:pnpm docs:verify 全绿;pnpm docs:links 对存活文档零断链。

### Phase 2 收尾(路径清零与生成物联动验证)

- 活跃面旧路径清零:check-docs-updated 帮助文本、check-links 注释示例、deploy 注释、
  .obsidian 默认新建目录、README 资源表(去重 environment 行、CI 叙述改为"CI 重导出并校验
  已提交产物");.gitignore/.prettierignore/.gitattributes/PR 模板的生成物路径切至
  docs/reference/generated/;oxlint 忽略 docs/reference/generated/\*\*。
- export-openapi 落地验证:JSDoc 中 `modules/*/README` 的 `*/` 会提前闭合块注释致语法错,
  改写为 `<name>`;两次导出幂等零 diff(123 paths / 264 schemas);模块 README region 回填,
  契约测试改读新路径。复跑 docs:verify / docs:links / test:tools 全绿。
- oxlint 基线修复:today-analysis 冷启动 spec 正则 \u4e00-\u9fff 转义大小写自动修正,oxlint 恢复 0 error。

### Phase 3 — 可执行约束(观察期)

- 新增 `pnpm arch:check`(warn 观察期,exit 0 不阻断),聚合:dependency-cruiser 依赖图
  (.dependency-cruiser.cjs,5 规则:R1 跨模块深路径 7、R3 common→modules 12、R4 直连 keyv 1,
  R2/R5 为 0;另默认环检查 5 环);eslint.arch.config.ts 代码模式(no-empty 0、service 裸
  throw 12、no-magic-numbers 1033、测试 : any 54;注:ESLint 9.26 起 `-c` 为叠加,arch 配置
  必须配 --no-config-lookup 独立运行);AST 约定脚本 scripts/hooks/check-ast-conventions.ts
  (DTO 缺 @Is\* 849、端点缺显式鉴权姿态 111;--strict 预留转 error;配套 spec 16 例)。
- check-links 扩展路径存在性校验:docs 活跃面 + 模块/common README + plans/{backlog,README} +
  根入口文档;带日期计划为时点快照豁免、迁移日志豁免、占位符/大写段跳过以保证零误报。
  首轮清零真实失效:plans/README 死链清单与旧路径、README adminjs.setup.ts、
  ADR-0015/0016 指向已删计划、deployment SSE registry 路径。
- AGENTS.md 增 Architecture Checks 段与 e2e 约定(跨用户 404、list limit 上限)。
- 验证:arch:check exit 0;test:tools 50/50;typecheck:tools 通过;docs:links(88 markdown
  断链 0 + 61 文件缺失路径 0);docs:verify 通过。

### Phase 4 — doc-map 退役(pre-commit 去阻断化)

- check-docs-updated.ts 重构为双模式:`--report`(默认,`--warning-only` 为退役别名)只打印
  覆盖映射报告不阻断;`--verify` 保留结构检查并新增迁移日志 append-only 守卫
  (diff HEAD --numstat,单日志文件删除行 >5 报违规,自 pre-commit 阻断迁入)。
  删除 `--staged`、SKIP_DOC_CHECK 旁路与阻断横幅。
- 钩子精简:pre-commit 仅剩 lint-staged;pre-push 汇总 `lint:check + build + test:ci +
arch:check`;`docs:check` 脚本切 `--report`(pnpm prepare 重装 hooks)。
- AGENTS.md 工具行与守卫归属更新;工作区根 AGENTS.md 同步「阻断式门禁已退役」叙述。
- CI 现状:lucent-ci.yml 仅在 E2E 前重导出 openapi.json 供契约测试,无「已提交产物与
  代码一致」的 diff 校验步骤——记 plans/backlog.md。

验证:docs:verify 全绿(含 append-only 守卫);docs:check 报告模式 exit 0;test:tools 50/50;
lint:check 通过;arch:check exit 0(观察期基线不变)。

### 收尾:计划生命周期终结

- 缺陷修复:`.gitignore` L19 无锚定 `logs` 规则误伤 `docs/logs/`,迁移日志自项目启动以来
  从未入库(Phase 0-3 提交均未含日志文件,append-only 守卫对未跟踪文件不可见);
  改为 `/logs` 仅匹配根目录,60 个日志文件随 Phase 4 提交首次入库。
- 遗留项入账 plans/backlog.md(G1-G5):G1 doc-map 两周观察后整体退役、G2 arch:check
  warn→error 转级与违规清理(基线已记录)、G3 CI openapi.json 一致性 diff 校验补齐、
  G4 契约-代码差异清单、G5 跨仓联动(Luminous 未提交修改、站点治理另开计划)。
- plans/2026-08-31-doc-governance-overhaul-plan.md 执行完毕整体删除;Phase 0 条目引用处
  加「已删」标注;doc-map.yaml 头部退役指引改指 backlog G1。

验证:docs:verify / docs:links / test:tools 复跑通过。
