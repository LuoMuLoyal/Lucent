# 2026-09-04

## test(e2e): 启动顺序用例独立生命周期避免重复关闭

`test/e2e/core/startup-order.e2e-spec.ts` 重构:moduleRef 的关闭职责收拢到 afterAll 兜底,单个用例内关闭后置标志避免 afterAll 重复 close;用例顶部幂等 init,消除对测试声明顺序的隐式依赖。SSE fake response 抽为 `makeFakeSseResponse()` helper,与 registry 单测工厂同构;断言强化为 end 恰一次、write 命中 `event: error` 帧且不少于一次、closeAll 后 registry 大小归零。
验证:typecheck 含 test 目录通过;e2e 运行依赖 Redis,本地未执行,留待 CI。

## fix(validation): JSON 解析失败保留原始错误为原因

`src/setup-app.ts` 的 JSON 解析失败 catch 绑定原始错误,以 `BadRequestException` 第二参数 cause 携带 SyntaxError,状态码仍为 400,安全模糊测试契约不变,过滤器日志可还原行列位置;NestJS 12 重映射背景注释保留。
验证:typecheck 与 lint 通过。

## fix(reports): 缓存键维护失败改结构化日志

`src/modules/reports/dashboard/dashboard.service.ts` 五处缓存键维护失败路径由 `String` 模板日志改为结构化 `logger.warn` 携带 err 与上下文,winston 可落 stack 与 cause;无响应面变化。
验证:dashboard.service 相关 spec 通过。

## refactor(config): 移除 S3 校验空占位函数

`src/config/env/environment.validation.ts` 删除 `assertS3StorageEnvironment` 空占位函数及其在 `validatedEnvSchema.check` 链上的调用;S3 校验已下沉 storage module 与 YAML 配置。
验证:environment.validation spec 通过。

## refactor(config): Throttler 改注入工厂并加固测试模块挂载判定

`src/app.module.ts` 与新增 `src/config/services/throttler.config.ts`:ThrottlerModule 由 `useClass` 改为 `useFactory` 注入 ConfigService,options 构造上提为模块级 `buildThrottlerOptions`,原 `ThrottlerConfigService` 委托同一函数,避免重复实例化;测试支撑模块挂载在 NODE_ENV 判定外增加非 dist 编译产物运行判据,编译镜像误配不再挂载。
验证:throttler.config spec 通过,typecheck 与 lint 通过。

## fix(admin): prisma internals 失败改走异常过滤器

`src/admin/services/prisma-module.service.ts` 保留 named/default 互操作回退;失败路径裸 throw 改为 `InternalServerErrorException`,进入 NestJS 异常过滤器统一记录。
验证:prisma-module.service spec 通过。
