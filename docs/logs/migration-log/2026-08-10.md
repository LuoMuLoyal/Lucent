# 2026-08-10 Proactive Suggestion Runtime

## Task 4 review hardening

- 继续完成 Task 4 的审查修复：worker 对已完成同版本任务幂等短路，`markReady`/`markFailed` 只更新 pending materialization；新增测试后 9 个 spec、123 tests 通过。
- 本次验证：`pnpm typecheck`、`pnpm lint:check`（0 warnings）、`pnpm format:check`、`pnpm build`、`pnpm exec prisma validate`、`pnpm docs:check`、`pnpm docs:verify`、`pnpm docs:links` 和 `git diff --check` 通过。

## Task 5 baseline observation runtime

- 成功 recompute 在建议结果缓存完成后，把同一批 pipeline signals 交给 BaselineService；collector 只有显式 `observedValue` 且 `coverage.sufficient === true` 才产生 observation，缺省、非有限值、空 trend 和未知值不会写入 baseline，明确覆盖的 `0` 仍会保留。
- 新增 `user_suggestion_baseline_observations`，以 `userId + dimension + localDate` 唯一约束保证按日幂等；每次 observation 写入（含重复重算）都会在事务内 reconciliation aggregate baseline。
- baseline 写入失败保留已生成建议并使其仍可读取，同时将 materialization 标记为固定错误码 `BASELINE_OBSERVATION_FAILED`，不再标记 ready。
- 本次定向验证：7 个 spec、86 tests 通过；`pnpm typecheck` 和 `pnpm exec prisma validate` 通过。
- 规格审查后补充底层 `recordObservation` 的有限值保护及回归测试，防止绕过 signal 入口写入非有限 observation。
- 修复带 baseline 回调的建议结果缓存命中路径：重放同日期 pipeline signals 后再执行 observation 写入，普通缓存命中仍跳过 pipeline。
- 根据质量审查，将 observation 与 aggregate reconciliation 放入事务，重复日期会重建 aggregate，条件更新避免旧重算降低覆盖天数，并拒绝非法日历日期。

## Task 6 medication slot runtime

- Medication collector 不再从 date-only 午夜推导当前分钟；它使用 `now()`，并在用户 profile timezone 中把 `scheduledFor + scheduledTime` 组合成真实时间点。非法 profile timezone 回退 `Asia/Shanghai`；DST gap、fold 和无效日历日期有确定处理，不伪造 overdue instant。
- collector 按 reminder slot 生成 `planned`、`taken`、`skipped`、`unconfirmed` 和 `overdueUnconfirmed` 状态；同一药品同日多个槽位独立评估，summary 同时正确统计有提醒槽位和无提醒药品。
- `MedicineDoseLogReaderPort` 的 lean fact 增加 `reminderId`。新日志按 reminderId 精确匹配；历史空 reminderId 仅在 medicine+scheduledTime 唯一时 fallback，歧义日志不套用到任一同时间槽位。
- `MissedDoseRuleService` 只消费 `overdueUnconfirmed`；legacy `pending_dose` 仍保留为兼容 signal，但不再触发漏服规则，候选 copy 使用“尚未确认”语义，并拒绝非有限或不完整 payload。

验证：3 个定向 spec、54 tests 通过；`src/modules/today-suggestion` 回归 37 files、400 tests 通过；`pnpm typecheck`、`pnpm lint:check`（0 warnings）、`pnpm format:check`、`pnpm build`、`git diff --check` 通过。

## Task 7 Today Analysis materialization

- 新增 `user_today_analysis_materializations`，以 `userId + localDate` 唯一保存 Today Analysis 的 source/computed version、生成次数、活动 claim、computedAt、冷却时间和错误状态；新增 `HealthEventKind` 与历史分析 `sourceVersion` 字段。
- 新增 Today Analysis 触发 listener 和版本化队列：普通 daily record 不触发；symptom record、health-event create/end、symptom check-in、dose log 及 dose/health-event 原因的 suggestion materialization 才能触发。job id 固定为 `userId + localDate + sourceVersion`，每日最多 3 次生成。
- `GET /today-analysis` 只读取 materialization 和最近成功的摘要；`POST /today-analysis/refresh` 保留显式刷新，增加 5 分钟冷却、版本 fence 和幂等处理。pending/stale/failed 时保留旧摘要，不把读取路径升级为 LLM 生成。
- API contract 已通过 `pnpm export:openapi` 更新；相关文档同步到 current-state、architecture、assistant/reminder contracts。本计划 Task 7 已实施完毕，相关计划段已删除。
- 验证：development/test PostgreSQL 均通过 `pnpm db:migrate`；today-analysis 定向测试、today-suggestion/health-events/daily-records/medicine-dose-logs 回归、`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm build` 和 `pnpm exec prisma validate` 通过。

## P1 daily-record update event kind

- 修复 daily-record 更新事件按旧 kind 发出的错误：旧日期携带旧 kind，目标日期携带更新后的 kind，并按日期和 kind 去重；普通记录不会误触发 Today Analysis，symptom 转换和 symptom 移动会覆盖正确日期。
- 新增 records service 定向回归测试，覆盖普通记录更新/移动、转为 symptom 和 symptom 移动。
- 本次验证：daily-records 定向回归 17 个 spec、158 tests 通过；records service 定向 spec 40 tests 通过；目标文件 format/lint、`pnpm typecheck` 和 `pnpm docs:check` 通过。

## P1 Today Analysis stale claim fence

- 过期 Today Analysis claim 回收后使用单调的 `sourceVersion + generationCount` 生成新的 `activeVersion`，旧 worker 的 ready/failed completion 不再命中新 claim；生成次数仍在成功 claim 时递增，达到每日上限时不会因回收过期 claim 而绕过 cap。
- 新增定向 store 回归测试，覆盖旧 worker completion rejection 和每日生成上限。
- 本次验证：store 定向 spec 8 tests 通过；目标文件 format/lint、`pnpm typecheck`、`pnpm docs:check` 和 `git diff --check` 通过。

## P1 Today Analysis timezone and read fence

- Today Analysis 的显式日期保持原值；省略日期统一通过用户 profile timezone 和共享 date helper 解析，controller、queue job id 与生成 materialization 使用同一 localDate。
- `readCurrent` 仅在 materialization ready 且 sourceVersion、computedVersion 与摘要 sourceVersion 完成 fence 时返回摘要；pending、stale、failed、empty 不再泄露未物化摘要。
- 本次验证：5 个定向 spec、58 tests 通过；目标文件 format/lint、`pnpm typecheck` 和 `pnpm docs:check` 通过。
