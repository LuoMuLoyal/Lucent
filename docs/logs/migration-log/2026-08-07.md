# 2026-08-07 代码审查修复

## 审查背景

基于 2026-08-07 全仓库审查（`dev` 分支，commit `01be37d3`），修复 6 项 🟡 警告级别问题。
计划文件：`plans/Lucent-review-2026-08-07.md`（实施完毕文件已删）。

## risk-check.service.ts 异常规范化 + 关联查询分页

- `src/modules/medicines/services/risk/risk-check.service.ts`
  - LLM 模型未配置时裸 `throw new Error(...)` 改为 `throw new ServiceUnavailableException`，
    附带 `ResultCode.EXTERNAL_SERVICE_ERROR`。
  - `evaluateStaticCheck` 中 `allergies`、`conditions`、`currentMedicines` 三个关联查询
    各添加 `take: 100` 上限，防止极端数据量下内存压力与查询超时。
- `src/modules/medicines/services/risk/risk-check.service.spec.ts`
  - "模型未配置"测试断言从 `Error` 改为 `ServiceUnavailableException`。

## assertClientRateLimit 速率限制硬化

- `src/modules/auth/services/identity/verification-code.service.ts`
  - `assertClientRateLimit` 对缺失 `clientKey` 不再直接 return 跳过，
    改为使用默认桶 `'unknown'` 确保任何调用路径都有限流。
- `src/modules/auth/services/identity/verification-code.service.spec.ts`
  - 新增测试：不传 `clientKey` 时仍触发限流（429）。
  - 新增测试：不传 `clientKey` 时创建默认桶（`vcode:rl:client:` + hash of `'unknown'`）。

## chunkArray 提取为公用函数 + jpush.provider.ts 规范化

- `src/common/helpers/infra/array.utils.ts`
  - 新增 `chunkArray<T>(items: readonly T[], size: number): T[][]`。
- `src/common/index.ts`
  - 补充导出 `chunkArray`。
- `src/modules/notifications/services/jpush.provider.ts`
  - 删除局部 `chunk` 函数，改用 `common` 导出的 `chunkArray`。
  - JPush API 调用失败裸 `throw new Error(...)` 改为 `throw new ServiceUnavailableException`，
    附带 `ResultCode.EXTERNAL_SERVICE_ERROR`。
- `src/common/helpers/infra/array.utils.spec.ts`
  - 新增 `chunkArray` 测试：空数组、size > length、刚好整除、非整除、不可变性。
- `src/modules/notifications/services/jpush.provider.spec.ts`
  - 非 2xx 响应测试断言从 `Error` 改为 `ServiceUnavailableException`。

## 密码修改通知日志级别提升

- `src/modules/auth/services/identity/credential.service.ts`
  - `_notifyPasswordChanged` 通知失败日志级别从 `warn` 提升为 `error`，
    附加 `event: 'password_change_notification_failed'` 结构化标签便于告警系统捕获。

## 验证

- `pnpm test:ci`：280 files / 2831 tests 全部通过
- `pnpm lint:check`：0 warnings
- `pnpm typecheck`：通过
- `pnpm build`：通过
