# 2026-09-03

## 全仓 ESM 切换(计划 2026-09-01-nestjs12-upgrade-plan 第二步)

根 `package.json` 切 `"type": "module"`,`.swcrc` 输出改 ESM;相对导入与 `#generated` 引用按
nodenext 补扩展名;CJS 运行时 API 迁移;脚本/部署与自研可观测性栈随迁验证。

### 构建与模块格式

- `package.json` 加 `"type": "module"`;`.swcrc` `module.type` commonjs → es6(保留
  legacyDecorator/decoratorMetadata/keepClassNames,`nest-cli.json` typeCheck 不变)。
- `prisma/schema.prisma` generator `moduleFormat` cjs → esm;`scripts/dev/fix-generated-prisma-internal.ts`
  SWC 输出同步改 es6——`generated/prisma/*.js` 现为 ESM,与根 `type: module` 一致(原 CJS 产物会被
  Node 按 ESM 解析而崩溃)。
- tsconfig 前序已对齐(nodenext + `resolvePackageJsonExports` + ES2023 + isolatedModules),本轮未再改。

### 导入扩展名(幂等 codemod)

- 新增 `scripts/dev/esm-import-extension-codemod.ts`:批量为相对 import/export/dynamic
  import/`require` specifier 补 `.js`(指向 `.ts` 文件或目录 `index.ts` → `/index.js`),
  `#generated/prisma/client` 补 `.js`;已带后缀、裸 specifier、无落点者跳过并报告,重跑幂等。
- 对 src/test/scripts/deploy 共 943 个 `.ts` 扫描,重写 3475 处 specifier、涉及 780 文件。
- tsc(nodenext 强制扩展名校验)+ vitest + build + e2e 四重兜底全过。

### 运行时 API 迁移

- `__dirname` → `dirname(fileURLToPath(import.meta.url))`:setup-app.ts(读 package.json 版本与
  Scalar standalone 资源路径)、tracing.ts(首个 import,预载 env)、config/yaml/yaml-loader.ts、
  i18n/i18n.module.ts(翻译目录与 dev typesOutputPath)、test/contract e2e(openapi.json 定位)。
- `require`:`victorialogs-transport.ts` 改 typed ESM import(`require('winston-transport')` 退役,
  类成员补 `override`);两处 PDF 字体路径 `require.resolve` → `createRequire(import.meta.url).resolve`;
  `admin/setup.ts` 的 `new Function('specifier','return import(specifier)')` hack 退化为普通
  `import()`(SWC ESM 构建不再把动态 import 编译成 require)。
- ioredis 类型导入在 ESM nodenext 下由 default 改 named type import(`{ Redis }`)。
- CJS interop 失败处置:`@prisma/internals` 的 `getDMMF` named export 无法被 cjs-module-lexer 静态
  识别(动态重导出)→ 动态 import 后具名/`default` 回退装载(同时兼容 vitest 的模块 mock);
  `cos-nodejs-sdk-v5` 为 default import、`better-auth` 走 ESM 出口、`@scalar/*`/adminjs 系为动态
  default——均无需 createRequire。遗留清单见 `docs/TODO.md`。

### 验证(闸门收敛记录)

- `pnpm check` 全绿:lint(oxlint/eslint 0 errors)/ format:check / typecheck / typecheck:tools /
  build / test:ci(单测 + eslint 插件 + tools)/ test:e2e:ci(30 文件 444 例全过)。
- `pnpm export:openapi` 复跑:123 paths / 264 schemas,`openapi.json` 零 diff(合同未变)。
- dist ESM 产物启动冒烟(dev,本地 Postgres/Redis):`/api/v1/health` 200 且 database/cache 组件
  up,BullMQ 队列启用、RedisService connected、cron repeatable 注册、LifecycleService 启动、
  Scalar `/api/docs` 200;`OTEL_ENABLED=true` 下 `tracing.ts`(main.ts 首个 import)正常,`@opentelemetry/sdk-node`
  以 CJS 依赖被 ESM 消费无碍。
- 容器化冒烟暴露**既有打包缺陷**(与 ESM 无关,此前容器冒烟未覆盖到应用启动点):
  `@scalar/nestjs-api-reference`(setup-app.ts 静态导入)与 `@adminjs/fastify`(admin/setup.ts 动态导入,
  main.ts 无条件注册 admin 面板)此前位于 devDependencies,生产镜像 `pnpm prune --prod` 后容器启动
  缺包即崩——均移至 dependencies;重建镜像后 dev 姿势容器启动与健康检查通过。
- Docker deploy compose 级冒烟(deploy:smoke)需完整部署环境,同前归计划全量闸门复查,未阻塞本步合并。

## zod 校验迁移:机制验证与 environment 试点(计划 2026-09-01-nestjs12-upgrade-plan 第三步)

首个模块试点(environment),产出可复制的模块迁移模板与机制结论。

### 机制结论(经验证)

- 参数装饰器 `{ schema }` 元数据由 `StandardSchemaValidationPipe` 消费;已全局注册于 class-validator
  `ValidationPipe` 之后,无 `schema` 元数据的参数为 no-op,两管道可并存过渡,全量 e2e 无回归。
- **OpenAPI zod 直出零配置成立**:Swagger 从参数 schema 元数据自动生成 query/body 参数(schema 级
  type/minimum/maximum/description),移除手工 `@ApiQuery` 后无重复、无缺失。
- 语义映射口径:`.optional()` 对应 `@IsOptional`;`z.coerce.number()` 对应 `@Type(() => Number)` +
  `@IsNumber`(数字字符串可用、非法值报错、空串按 `Number('') === 0`);`.min/.max` 对应 `@Min/@Max`;
  未知键默认 strip(计划默认),拒绝未知键的既有全局语义按端点需要以 `.strict()` 显式保留。
- example 丢失:zod 无原生 example 元数据,description 经 `.describe` 保留;example/nullable 语义补全
  留待 zod-openapi 元数据阶段(见 docs/TODO.md)。

### environment 模块试点

- `snapshot-query.dto.ts`:类 DTO 改为 zod schema + `z.infer` 类型(导出类型名不变);查询端点控制器
  `@Query({ schema })`,删除重复的 `@ApiQuery` 手写文档。
- e2e 新增 3 个语义用例(非法数字 400、空串 lat 按 0 接受、未知键 400 strict),environment e2e 共 7 例
  通过;模块 unit 25 例通过。

### 验证与合同

- typecheck / eslint / format 通过;全量 e2e 30 文件 447 例通过(双管道并存无回归)。
- `export:openapi`:123 paths / 264 schemas;合同 diff 仅 environment snapshot 的 query 参数
  (description 由参数级移入 schema 级、example 移除),**无结构变化**。
- Luminous 联动:`dart run scripts/contract/bootstrap.dart` 已执行——生成物仅 dart format 噪音与
  参数 doc 注释差异(description 读参位置变化),结构零变化,已还原不提交。bootstrap 的 build hooks
  步骤当前失败(与本次合同 delta 无关),结构性合同批次前需先修流水线(见 docs/TODO.md)。

## zod 校验迁移:assistant 批次(请求侧)

按批量迁移手册将 assistant 三个请求 DTO 切换为 zod4 schema(`z.infer` 类型保留原名),控制器
Body 参数消费 `{ schema }` 元数据:

- `dto/confirm-proposal.dto.ts`:请求侧 `ConfirmAssistantProposalDto` → `confirmAssistantProposalSchema`
  (proposalIds 数组 `.min(1)`、decision `z.enum`、note `.max(500).optional()`);同文件纯响应类
  `AssistantConfirmResultDto` / `AssistantConfirmResultResponseDto` 保持 class + `@ApiProperty` 不动。
- `dto/rename-conversation.dto.ts` → `renameConversationSchema`;`@IsNotEmpty` 语义以 whitespace
  refine 等价保留(拒绝空/纯空白 title,且不改动存储值),`@MaxLength(48)` → `.max(48)`。
- `dto/stream-messages.dto.ts`:`assistantInputMessageSchema` + `streamAssistantMessagesSchema`;
  `@ValidateNested`/`@Type` 嵌套改为 schema 引用,messages `.min(1)/.max(20)`,子对象与顶层均
  `.strict()` 保留原 forbidNonWhitelisted 语义,conversationId `.max(64).optional()`。
- `assistant.controller.ts`:confirm/rename/stream 三个 `@Body` 切 `@Body({ schema })`,DTO 导入改
  `import type`;响应侧 DTO 与 `@ApiResponse` 不动。模块 barrel(index.ts)不导出这些 DTO,无改动。
- e2e 新增 14 个语义用例(空/纯空白 title 400、缺字段/空数组/非法 decision 400、嵌套 role 非法 400、
  messages 超 20 条 400、未知键 400 strict 覆盖三个端点、合法 rename 200),assistant e2e 全绿。
- 验证:typecheck 全仓零错;assistant 模块 unit 39 文件 485 例全过;改动文件 eslint 零告警。
- openapi 导出与合同联动、提交由批量收尾统一执行,本批未单独跑 export:openapi。

## medicine-dose-logs 请求侧 DTO → zod(zod 批量迁移批次 9)

`create/update/mark-dose-log` 三个请求 DTO 由 class-validator 类迁为 zod4 schema + `z.infer` 类型
(导出类型名不变);控制器 3 个 Body 参数切 `@Body({ schema })`(Query/Param 标量参数无对应 DTO,
维持原样);响应侧 `dose-log-response.dto.ts` 未动;barrel 无 DTO 具名导出,无需改动。

### 映射决策

- `@IsEnum(DoseLogStatus)` → `z.nativeEnum(DoseLogStatus)`(Prisma 为 const object 导出而非 TS enum)。
- `@IsDateString` → `z.iso.date()`:仅收日历日期 `YYYY-MM-DD`,与 ApiProperty 文档及 service
  `parseDateOnly` 消费口径一致;完整 datetime 先前通过校验后也会在 service 产出 Invalid Date,无真实依赖。
- 可空可选(`healthEventId`/`doseText`/`note` 的 `string | null`)→ `.nullish()`;纯可选 → `.optional()`。
- `@IsNotEmpty`/`@MaxLength` → `.min(1)`/`.max(n)`;`@IsUUID` → `z.uuid()`;`@Matches` → `.regex`。
- 未知键沿用全局 `forbidNonWhitelisted` 语义,`z.object(...).strict()`。
- description 经 `.describe` 保留;example 不迁移(按 runbook,补全走 TODO)。

### 验证

- typecheck:medicine-dose-logs 零错(并行批次每日记录/product-events/用户健康上下文曾有中间态
  报错,不属本模块)。
- 模块 unit(controller/service/repository)全绿;e2e 新增语义用例(非法 status、非法日期、
  空串 IsNotEmpty 对等、未知键 strict、update 非法 status)后全绿。
- `export:openapi` 与 Luminous 合同联动留给并行协调层统一执行,本批未跑。

## zod 请求侧迁移:today-analysis 与 today-suggestion(批量 runbook 批 10/11)

按 `批量 runbook` 执行两个模块的请求侧 DTO 迁移(计划仍在执行中,未删)。

### today-analysis(批 10)

- `dto/generate-today-analysis.dto.ts`:类 DTO 改为 zod schema `generateTodayAnalysisSchema` +
  `z.infer` 类型 `GenerateTodayAnalysisDto`。映射:`@IsOptional` → `.optional()`;
  `@IsDateString` → `z.iso.date()`;description 用 `.describe` 保留;`.strict()` 保留全局
  forbidNonWhitelisted 语义(未知 body 键 400)。
- 控制器 refresh / generate / generate/async / generate/stream 四个 Body 参数切
  `@Body({ schema: generateTodayAnalysisSchema })`,import 拆为 schema 值导入 + 类型导入。

### today-suggestion(批 11)

- `dto/feedback.dto.ts` 仅请求侧 `SuggestionFeedbackDto` 迁移(`@IsIn(FEEDBACK_VALUES)` →
  `z.enum(SuggestionFeedback)`——zod4 将 nativeEnum 并入 z.enum,原生 TS 枚举直传;eslint
  no-deprecated 对 `z.nativeEnum` 报错),description 保留;同文件的响应/文档 DTO
  `SuggestionFeedbackDataDto`/`SuggestionFeedbackResponseDto` 保持类形式不动。
- 控制器 submitFeedback 的 Body 参数切 `@Body({ schema: suggestionFeedbackSchema })`,
  import 拆值/类型两行。

### 决策口径

- 两模块 barrel 未导出上述 DTO 名(核实为 no-op),services 层本已 `import type`,无需改。
- `date` 收紧为 YYYY-MM-DD 日历日:class-validator 宽松 isISO8601 曾接受的完整 datetime 现拒绝
  (字段语义与 example 均为日期);显式 `null` 不再按缺省接受(zod `.optional()` 不含 nullable)。
- 未知键经 `.strict()` 保留原 forbidNonWhitelisted 拒绝行为;枚举错误自定义 message。

### 验证与未覆盖点

- typecheck 两模块零错(全仓快照含并行批 product-events 瞬时未完成错误,串扰与本批无关);
  两模块 vitest 全绿。
- e2e 语义用例已新增并实跑绿:today-analysis `POST /generate` 未知 body 键 400(strict 对等);
  today-suggestion `POST /today/suggestions/:id/feedback` 非法枚举 400 +
  `code=VALIDATION_FAILED`、未知 body 键 400。既有非法日期 400/VALIDATION_FAILED 用例在 zod
  下保持绿(运行窗口全树曾因并行批 health-events `z.iso()` 过渡态无法装配 AppModule,修复后
  两文件 25 例全绿)。
- 未执行:`export:openapi` diff 审查、Luminous bootstrap 联动——按批次边界留待批量闸门复查。

## medicines 模块请求侧 DTO → zod(批次 16)

- `dto/query.dto.ts`:`MedicineSearchQueryDto`/`MedicineDetailQueryDto` 类改为
  `medicineSearchQuerySchema`/`medicineDetailQuerySchema` + `z.infer` 类型(导出类型名不变)。
  - `@Type(() => Number)` + `@IsInt` → `z.coerce.number().int()`;`page`/`pageSize` 字段默认值
    (1/20)以 `.default(...)` 表达;`@Min/@Max` → `.min/.max`。
  - 全仓唯一 `@Transform`(q 字段:字符串 trim、非字符串丢为 undefined)用 `z.preprocess`
    等价迁移:preprocess 结果接内部 `.optional()` 的 `z.string().max(200)`——长度检查作用于
    trim 后值,与装饰器管线一致;数组/数字等非字符串输入仍被丢弃(同原 `trimOptionalString`),
    而非 400。
  - 未知 query 键 `.strict()` 保留全局 forbidNonWhitelisted 语义。
- `dto/recognize-medicine.dto.ts`:`RecognizeMedicineDto` → `recognizeMedicineSchema`
  (`.strict()`);`@IsUrl({ require_tld: false })` → `z.url()`(localhost 等无 TLD host 仍接受;
  无 scheme 串收紧为 400——契约本即 http(s) 公网 URL),`@MaxLength(2048)` → `.max(2048)`。
- `dto/risk/risk-check-request.dto.ts`:`RunRiskCheckDto`/`RiskCheckCandidateDto` →
  `runRiskCheckSchema` + 嵌套 `riskCheckCandidateSchema`(`.strict()` 顶层与嵌套),
  `@IsEnum` → `z.enum(...)`,`@ValidateNested`+`@Type` → 子 schema 引用。
- 控制器 `GET /medicines`、`GET /medicines/:id`、`POST risk-check`、`POST recognize(/async)`
  参数改为 `@Query({ schema })/@Body({ schema })`,类型按 `z.infer` 导入;本批无重复手工
  `@ApiQuery`/`@ApiBody` 可删,响应侧 DTO 与 `@ApiResponse({type})` 不动。
- barrel 未导出本批请求 DTO,维持原状;medicines 内部 service 均为 `import type`,不受影响。
  **本批为首个 body schema 批次**:requestBody zod 直出与 body 校验链路未在真实 HTTP 下实证,
  已新增 e2e 探针用例(未知 body 键/嵌套缺失 400)充当验收点。
- 语义差异:query 空串数值经 `Number('')===0` 后触 `.min(1)` 拒绝(与原 `@Type` 后 `@Min`
  一致)、非数字 400、数组 q 静默丢弃与原 `@Transform` 一致、未知键 400(strict)。
- 验证:medicines 模块 unit vitest 全绿;typecheck 本模块(含 test/e2e/medicines 两个 e2e 文件)
  零错(仓库其余报错为并行批 health-events/account 过渡态)。未执行:medicines e2e 实跑(需
  DB 栈)、`export:openapi` diff 审查、Luminous bootstrap 联动——按批次边界留待批量闸门复查。

## zod 请求侧迁移:product-events(批量 runbook 批 7)

按 `批量 runbook`(已删除)迁移 product-events 模块请求侧 DTO。

- `dto/create-product-event.dto.ts`:类 DTO → zod4 schema(`createProductEventSchema` +
  `createProductEventBatchSchema`),`z.infer` 类型保留原名。映射:`@IsEnum` → `z.enum(Prisma 常量对象)`
  (zod4 合并 API;`z.nativeEnum` 已 deprecated,eslint 禁);`@IsString`/`@MaxLength`/`@IsNotEmpty` →
  `z.string().min/max`;`@IsDateString` → `z.iso.date().or(z.iso.datetime({ offset: true }))`
  (保留日历日与带 UTC 偏移 datetime;无偏移 datetime 相对宽松 isISO8601 收窄为拒绝);数组
  `ArrayMinSize/ArrayMaxSize` → `.min(1)/.max(50)`;事件与批次对象均 `.strict()` 保留全局
  forbidNonWhitelisted 未知键拒绝。
- `dto/funnel-query.dto.ts` → `productFunnelQuerySchema`(两日期 optional、同口径、`.strict()`)。
- `product-events.controller.ts`:POST Body 与 GET funnel Query 切 `@Query/@Body({ schema })`,DTO
  import 拆 schema 值导入 + `import type`;该控制器无重复手工 `@ApiQuery/@ApiBody` 可删。
- barrel `index.ts`:DTO 具名导出改 `export type`(MAX 常量保持值导出;schema 不进 barrel)。
- `services/events.service.ts` DTO 引用改 `import type`。
- `product-events.controller.spec.ts`:`new XxxDto()` 与 class-validator `validate` 全清,改类型化
  字面量 + schema `safeParse` 断言(未知键/非法枚举/非 ISO/无偏移 datetime/超长/缺 clientEventId/
  空批/超 50/缺 events/顶层未知键,断言 zod issues JSON 含目标字段)。
- e2e:强化既有「userId 白名单」与「非法枚举」POST 断言补 `code=VALIDATION_FAILED`;funnel 新增
  非法日期 400 与未知 query 键 400(strict)各一例。POST 端点 10 req/min 限流预算已被既有用例占满,
  不再新增 POST 语义用例(相应 schema 语义由 unit 覆盖)。

### 验证与未覆盖点

- typecheck 全仓 product-events 零错(并行批 account/health-events 中间态报错与本批无关);
  module unit 4 文件 70 例全绿;改动文件 eslint/prettier 零告警。
- 未执行:`export:openapi` 与 Luminous 合同联动(任务边界禁跑,留批量收尾闸门统一实证 body
  requestBody 直出与 `z.enum` 渲染);本批 e2e 本地未跑通——health-events 并行迁移中
  `z.iso()` 误用使 app 装配阶段崩溃(非本批代码问题)。

## zod 请求侧迁移:reports(批量 runbook 批 15)

按 `批量 runbook`(已删除)迁移 reports 模块四个请求侧 DTO 文件,
控制器 Body7/Query2 参数切 `{ schema }`(6 个 Param 为字符串标量,无 DTO,不动);响应/文档 DTO
(clinic-summary-response、event-review-response、report-dashboard-response、report-summary-\*、
share-list 等)保持 class + `@ApiProperty` 不动;模块 barrel 本就只具名导出 `ReportRange`
(type)与 range 常量,无本批 DTO 导出,无需改动。

- `dto/report-dashboard-query.dto.ts`:常量(REPORT*RANGE*\*/REPORT_SUPPORTED_RANGES)与
  `ReportRange` 类型保留;`ReportDashboardQueryDto` 类 → `reportDashboardQuerySchema` + `z.infer`
  类型。`@IsIn` → `z.enum(REPORT_SUPPORTED_RANGES)`(as const 元组)。
- `dto/generate-report-summary.dto.ts`:`GenerateReportSummaryDto` 类 → `generateReportSummarySchema`
  (共享 custom-range 日期对校验)。
- 两处 custom 日期对规则(`range === custom` 时双日期必填且为日历日)以导出的
  `refineReportRangeDates` superRefine 等价落地:非 custom 时杂散/非法日期仍放行被消费方忽略,
  保留原 `@ValidateIf` 语义;窗口边界(未来 endDate、startDate ≤ endDate、30 天上限)仍在 service 层。
- `dto/clinic-summary-request.dto.ts`:`ClinicSummaryRequestDto` 类 → `clinicSummaryRequestSchema`;
  常量 CLINIC_SUMMARY_MAX_RANGE_DAYS / CLINIC_SUMMARY_SELECTABLE_FIELDS 保留。日期对条件
  (eventId 缺失且给出任一日期时双日期必填且为日历日)用 superRefine 等价 `@ValidateIf` 语义;
  `selectedFields` → `z.array(z.enum(...)).min(1).max(长度)`。
- `dto/event-review-list-query.dto.ts`:`EventReviewListQueryDto` 类 → `eventReviewListQuerySchema`;
  `@IsEnum(HealthEventStatus)` → `z.enum(HealthEventStatus)`(zod4 合并 API,Prisma const object
  导出);`limit` `@Type(() => Number)`+`@IsInt`+`@Min/Max` → `z.coerce.number().int().min(1).max(100)`;
  cursor 维持普通字符串,畸形游标仍在 EventReviewService.resolveCursor 以 VALIDATION_FAILED 拒绝。
- 四个 schema 均 `.strict()` 保留全局 forbidNonWhitelisted 未知键拒绝;description 用 `.describe`
  保留(example/default 按 runbook 不迁移);`@IsDateString` 日期字段(日历日口径)统一
  `z.iso.date()` 收紧(拒绝完整 datetime,与下游 parseDateOnly 消费口径一致)。
- **空 body 语义**:原 class-validator 管道对无 payload POST 实例化出空对象(空 scope,e2e 覆盖
  clinic preview/share/export 无 body 201);Nest 把缺失 JSON body 以 `undefined` 交给 standard-schema
  管道,两个 body schema 以 `z.pipe(z.unknown().transform(空值→{}), 字段 schema)` 等价归一。
- `reports.controller.ts`:7 个 `@Body` 与 2 个 `@Query` 切 `@Body({ schema })/@Query({ schema })`,
  DTO import 拆 schema 值导入 + `import type`;`listReviews` 参数默认 `new EventReviewListQueryDto()`
  换类型化字面量 `= {}`。
- 验证:reports 模块 unit vitest 20 文件 250 例全绿;typecheck 本模块零错(全仓剩余报错为并行批
  account 过渡态,与本批无关);改动文件 prettier 通过。
- 未执行:reports e2e 实跑(需 DB 栈)、新增语义 e2e 用例、`export:openapi` diff 审查与 Luminous
  bootstrap 联动——按批次边界留批量收尾闸门;body schema 经 `z.pipe` 包装后 OpenAPI 渲染待
  export 实证。

## zod 请求侧迁移:account(批量 runbook 批 12)

按 `批量 runbook`(已删除)迁移 account 模块两个请求侧 DTO,
控制器两处 `@Body` 参数切 `{ schema }`;纯响应 DTO(response.dto.ts 的 AccountDto/
AccountResponseDto/AccountEmailResponseDto)保持 class + `@ApiProperty` 不动。

- `dto/update.dto.ts`:`UpdateAccountDto` 类 → `updateAccountSchema` + `z.infer` 类型;
  `@IsOptional` → `.optional()`、`@IsString` → `z.string()`、`@MaxLength(20)` →
  `.max(20)`;description 经 `.describe` 保留。
- `dto/unlink-identity.dto.ts`:`UnlinkIdentityDto` 类 → `unlinkIdentitySchema` +
  `z.infer` 类型;`@IsNotEmpty` → `.min(1)`(仅拒绝空串,空白串仍放行,与原语义一致)。
- 两个 schema 均 `.strict()` 保留全局 forbidNonWhitelisted 的未知键拒绝。
- `account.controller.ts`:`updateAccount` 与 `unlinkIdentity` 的 `@Body` 切
  `@Body({ schema })`,DTO import 拆 schema 值导入 + `import type`;`account.service.ts`
  对 DTO 的 import 改 `import type`(仅类型位使用)。
- `account.service.ts` 内部微调:update 载荷条件展开从 `dto.nickname !== undefined` 守卫改为
  守卫局部 const(`nickname !== undefined`),语义等价(zod 推断类型下 TS 无法对间接 const 别名
  收窄,class-validator 类 DTO 时代可编译;避免裸 `undefined` 落入 UserUpdateInput)。
- 本模块 barrel(index.ts)为空,无 DTO 导出,无需改动;本批未引入跨模块 import/导出。
- 验证:typecheck 本模块零错(全仓剩余报错为并行批 health-events 等过渡态,与本批无关);
  account 模块 unit vitest 35 例全绿。
- 未执行:account e2e 实跑与新增语义用例、`export:openapi` diff 审查与 Luminous bootstrap
  联动——按批次边界留批量收尾闸门。

## zod 请求侧迁移:medicine-reminders(批量 runbook 批 17)

按 `批量 runbook`(已删除)迁移 medicine-reminders 模块五个请求侧
DTO 文件,两个控制器共 5 个 `@Body` 参数切 `{ schema }`(Query3 为标量字符串 query 经控制器
parseBoolean/parseLimit 处理、Param2 为 `:id` 字符串路径参数,均无对应请求 DTO,不动);
纯响应/文档 DTO(`dto/response.dto.ts`、`dto/reminder-delivery-response.dto.ts`)保持
class + `@ApiProperty` 不动;模块 barrel(index.ts)只导出服务与响应类型 `MedicineReminderItemDto`,
不导出本批请求 DTO,无需改动。

- `dto/create.dto.ts`:`CreateMedicineReminderDto` 类 → `createMedicineReminderSchema` + `z.infer`
  类型;`@IsOptional` 按字段形状 `.optional()`/`.nullish()`(可空类型 `string | null` 用 `.nullish()`,
  与 dose-log 口径一致);`@IsString`+`@IsNotEmpty`/`@MaxLength` → `.min(1)`/`.max(n)`;
  `scheduledHour`/`scheduledMinute` → `z.number().int().min/max`(必填);`daysOfWeek`
  `@IsArray`+`ArrayMinSize(1)`+`ArrayMaxSize(7)`+each 校验 → `z.array(z.number().int().min(0).max(6))
.min(1).max(7).nullish()`;`@IsBoolean` → `z.boolean()`。
- `dto/update.dto.ts`:`UpdateMedicineReminderDto` 类 → `updateMedicineReminderSchema`;全部字段
  optional/nullish 化,与 create 同口径映射;`note` 保留 update 独有 `@IsNotEmpty`(`.min(1)`),
  与 create 的 note(仅 `.max(500)`)区分。
- `dto/upsert-group.dto.ts`:`UpsertReminderSlotDto` 与 `UpsertMedicineReminderGroupDto` 两个类 →
  `upsertReminderSlotSchema` + `upsertMedicineReminderGroupSchema`(`z.infer` 类型保留原名);
  `@ValidateNested`+`@Type` 嵌套数组 → `z.array(upsertReminderSlotSchema).min(1)`;
  group 必填 `currentMedicineId` → `z.string().min(1)`;顶层与嵌套 slot schema 均 `.strict()`。
- `dto/local-capability.dto.ts`:`LocalCapabilityStateDto` 类 → `localCapabilityStateSchema`;
  `@IsIn(['active','unavailable','disabled'])` → `z.enum([...])`(与 constants 导出的
  `LocalCapabilityState` 联合类型同值);`.strict()`。
- `dto/reminder-delivery-receipt.dto.ts`:`ReminderDeliveryReceiptDto` 类 →
  `reminderDeliveryReceiptSchema`;`@Matches`(scheduledDate `YYYY-MM-DD`、scheduledTime `HH:mm`
  24h)用 `.regex` + 原 message 保留;`reminderId` `@IsString`+`@IsNotEmpty` → `.min(1)`;
  `.strict()`(e2e 已覆盖非法日期/时间 400,含「完整 ISO 时间戳 400」用例)。
- `medicine-reminders.controller.ts`:create/update/upsertGroup 三个 `@Body` 切
  `@Body({ schema: xxxSchema })`,DTO import 拆 schema 值导入 + `import type`;该控制器无
  `@ApiBody` 手写文档装饰器,响应侧 `@ApiResponse({ type })` 不动。
- `reminder-deliveries.controller.ts`:recordReceipt/reportLocalCapability 两个 `@Body` 切
  `@Body({ schema })`,删除引用已类化请求 DTO 的重复 `@ApiBody({ type })`(requestBody 交由
  schema 元数据直出,首个 body schema 批次的 OpenAPI 渲染留 export 实证);`@ApiQuery`
  (date/limit 标量)与 GET list 处理逻辑不动。
- `reminder-deliveries.controller.spec.ts`:`new ReminderDeliveryReceiptDto()` +
  class-validator `validate` 用例清除,改 schema `safeParse` 断言(类型化字面量 +
  `.success` 期望),`class-validator` import 移除。
- 语义差异口径:`@IsDateString`(startDate/endDate)与 dose-log 一致按文档口径
  `z.iso.date()` 收紧(仅日历日 YYYY-MM-DD);显式 `null` 仅对声明可空字段
  (`.nullish()`)放行,纯 optional 字段不再接受 null;未知键 `.strict()` 保留全局
  forbidNonWhitelisted 拒绝。
- 验证:typecheck 本模块零错;模块 unit vitest 9 文件 150 例全绿;改动文件 prettier
  通过;eslint 未跑(任务边界,按批次收尾统一)。
- 未执行:`export:openapi` 与 Luminous 合同联动、e2e 实跑(需 DB 栈)——按批次边界留批量收尾闸门。

## auth 批(zod 请求侧迁移)

- 范围:auth 模块 13 个请求 DTO(credentials 4 + password 7 + shared 2)类 → schema + `z.infer` 类型;
  三个控制器 local/oauth/session 对应 @Body/@Query 切 `{ schema }`;common/validators 文件
  `auth.decorators.ts` 与 `auth.decorators.spec.ts`(auth 批负责,spec 改写为 schema 断言)。
- 自定义复合验证器落地(common/validators/auth.decorators.ts):
  - `strongPasswordSchema({ messagePrefix?, notEmptyMessage? })`:`z.string().min(8).max(32).regex(PASSWORD_PATTERN)`,
    常量/文案与原 `IsStrongPassword` 同一来源;
  - `verificationCodeSchema({ exactLength? })`:默认 `.length(6)`,`exactLength:false` 仅 `.min(1)`;
  - `emailAddressSchema({ message?, notEmptyMessage? })`:`z.string().pipe(z.email(...))`,格式/空串文案保留。
  - class-validator 装饰器 `IsStrongPassword`/`IsEmailAddress` 保留导出(testing-support 仍在 import);
    `IsVerificationCode` 仅 auth 曾引用且现无外部消费者,已删导出(常量 `VERIFICATION_CODE_LENGTH` 保留)。
  - `auth.decorators.spec.ts`:原 8 个 DTO-class + `validate()` 用例全部改写为对上述 zod 片段的
    `safeParse` schema 断言(长度/字符集/空串/可选/自定义前缀),文件不再依赖 class-validator。
- 控制器:auth 三个控制器 @Body18/@Query1 切 `{ schema }`;oauth.controller 删除引用已类化请求 DTO 的
  重复 `@ApiBody({ type })` 与 wechat-web GET redirect 的手工 `@ApiQuery`(requestBody/query 由 schema
  元数据直出,OpenAPI 渲染留 export 实证)。
- barrel `auth/index.ts`:已转 DTO 名改 `export type` 并同量导出 schema 值(changeEmail/changePassword/
  deleteAccount/setPassword/oauthAuthorize/oauthCallback/oauthCodeCallback),供 account 等跨模块
  `{ schema }` 接入;其余导出(服务/守卫/响应 DTO)不动。
- 语义差异口径:邮箱由 validator.js isEmail → zod `z.email()`(IDN/unicode 邮箱收紧,ASCII/大小写/e2e
  夹具不受影响);未知键 `.strict()` 保留 forbidNonWhitelisted 拒绝;验证码/强密码消息沿用原文案。
- 验证:typecheck 全仓过滤 auth 与 common/validators 零错;vitest src/modules/auth + src/common/validators
  30 文件 436 例全绿;改动文件 prettier 通过。
- 未执行:`export:openapi` 与 Luminous 合同联动、auth e2e 实跑(需 DB 栈);account.controller 对
  auth-shared 请求 DTO 的接入属 account 批(并行在途),本批未触碰。

## zod 请求侧全面替换:批量执行完成(计划 2026-09-01-nestjs12-upgrade-plan 第三步)

请求侧迁移主体完成并合入。13 个子 agent 按批量 runbook(执行完毕已删)并行转换全部请求侧 DTO;
本条目为汇总,逐批细节见上方各批小节。

### 范围与语义口径

- 请求侧(62 个 DTO)全部 class-validator → zod4 schema + `z.infer` 类型(类型名不变);纯响应/文档
  DTO(45)与 typeonly(3)未动,`@ApiProperty` 仍服务响应文档,待响应侧机制。
- 覆盖模块:environment(模板)、legal-documents/files/user-settings/notification-preferences/
  data-export/testing-support、assistant、auth(13,含自定义复合验证器 port 与 spec 改写)、account、
  today-analysis/today-suggestion、medicine-dose-logs、medicine-reminders、daily-records、
  health-events、user-health-context、reports、product-events、medicines(含唯一 `@Transform`)、
  notifications(收尾补迁)。
- 统一口径:.strict() 保留 forbidNonWhitelisted;coerce 仅 query(空串按 0);日期字段 YYYY-MM-DD
  (`z.iso.date()`);需同时接受 datetime 的字段改 `string + refine`
  (`src/common/validators/iso-datetime.schema.ts` 新增)——避免请求侧 `anyOf` 破坏 Luminous 生成器;
  `z.enum(Prisma const)` 替代 deprecated nativeEnum;null 语义按列可空/清除对齐(旧 `@IsOptional`
  放行 null 的隐患收紧);`@IsNotEmpty` 等义 min(1)/空白 regex;description 经 `.describe` 保留,
  example 丢失留 TODO。

### 集成修复

- account/auth 共享请求 DTO 跨批接缝:auth barrel 改 `export type` + 导出 schema 值,account.controller
  改 `import type` + `@Body({schema})`,删除冗余 `@ApiBody`。
- reports 空 body 归一由 `z.pipe(unknown→{})` 改 `.default({})`(前者使 OpenAPI requestBody 渲染成
  空 schema;空 body 合法语义保留)。
- OpenAPI zod 直出实证:query/body/param 自动生成(类型/min/max/enum/description);请求侧类组件
  移除、内联渲染(components 264→193)。
- 语义差异 e2e 分批补充(错误码 `VALIDATION_FAILED`、strict 未知键、coerce、日期边界)。

### 验证与合同联动(阻塞项如实记录)

- `pnpm check` 全绿:lint/format/typecheck/typecheck:tools/build/单测/e2e(含各批新增语义用例);
  `pnpm export:openapi`:123 paths / 193 components,逐批 diff 审查无意外漂移。
- openapi 请求 schema 内联且无命名组件 → Luminous 再生成按 operation 派生模型名
  (`XxxControllerXxxV1Request`),全仓既有 DTO 名调用点失效(flutter analyze 539 处),pre-commit
  门禁拦截;已还原 Luminous 工作树,合同同步作为阻塞项入 docs/TODO.md(需决策请求 schema
  组件化命名或 Luminous 调用点全面适配,另含 bootstrap 旧文件清理/format 漂移/hooks 本机失败)。
  后端 zod 迁移不依赖该同步,独立合入。

## Luminous 合同联动完成(承接上节阻塞项)

bootstrap 流水线已修(build.yaml 排除 tool/luminous_lints 独立包、生成前清理旧产物、build_runner 失败重试),client 按 zod 请求合同再生成(请求模型 per-op 化),七个并行切片完成全仓调用点迁移;Luminous `flutter analyze` 0 issues、全量 `flutter test` 复跑通过,已分组提交(流水线/再生成/适配/日志)。
遗留决策项(date wire):新生成客户端把 date 型参数定为 DateTime(序列化可能带时间),Lucent 端 zod date 仍 date-only——record/health_event 等 list/body 的 date wire 形态需 e2e 确认或统一在 Luminous data 层以 date-only 提交,入 TODO。

## zod 收尾:全局管道切换与 class-validator/transformer 移除

- setup-app 全局 ValidationPipe(transform/whitelist/forbidNonWhitelisted/formatValidationErrors)退役,仅保留 StandardSchemaValidationPipe——请求侧 DTO 已全部 schema 化,管道单一化。
- 删除 formatValidationErrors/collectValidationMessages 与 setup-app.spec.ts(仅测该格式化)。
- common/validators/auth.decorators.ts 移除 class-validator 装饰器(IsStrongPassword/IsEmailAddress,已无消费者),保留常量与 zod 片段工厂。
- pnpm remove class-validator class-transformer(直接依赖清零,源码/测试无残留;scripts/arch 的 AST 规则文本引用为历史规则字符串,非真实 import)。
- 验证:pnpm check 全绿(lint/format/typecheck/typecheck:tools/build/单测/e2e)。

## 响应侧机制试点(app-info)

- 全局注册 StandardSchemaSerializerInterceptor(@SerializeOptions({schema}) 校验出站;无 schema no-op)。
- 响应 OpenAPI 由 export 期注册器提供:注册器(response-schema.registry)集中登记 path/method/组件名(沿用旧 DTO 名)与 zod schema;export-openapi.ts 在 createDocument 后以 z.toJSONSchema 注入 components 并让 200 响应指向 $ref——组件名稳定,Luminous 客户端无需第二波重命名。
- 语义:出站校验 + 命名组件 + @ApiProperty 可退役;试点经 typecheck/tools/lint/启动 e2e/export 验证。

## zod 响应侧转换:notifications 模块(同批 files/user/testing-support/health-data 审计)

按响应侧试点机制转换 notifications 的纯响应/文档 DTO;同批其余目标模块审计后无 class
响应 DTO 可转,仅报告。

### notifications

- `dto/response.dto.ts`:纯响应类(NotificationListItemDto/NotificationDetailDto/
  NotificationListDataDto/NotificationListResponseDto/NotificationDetailResponseDto/
  UnreadCountDataDto/UnreadCountResponseDto)→ zod4 schema + `z.infer` 类型别名(导出
  类型名沿用旧类名);请求侧 createNotificationSchema/CreateNotificationDto 与类型常量
  原样保留。响应 schema 不带 `.strict()`/`.default()`;description 经 `.describe` 保留;
  字段按实际 wire 全键必填、nullable 字段用 `.nullable()`(service 恒定输出全键)。
- `actionPayload` 保持宽松(`z.unknown().nullable()`):Prisma JSON 列可含历史数组/标量,
  出站校验收紧为 record 会把既有数据打成 500;services/notifications.service.ts 随之
  移除多余类型断言(原 cast 指向 Record,现已非必要)。
- `notifications.controller.ts`:7 个带返回体方法挂 `@SerializeOptions({ schema })`;
  `@ApiResponse({ type })` 类引用随类类型化删除(4xx/5xx `ProblemDetailsDto` 保留);
  文件底部 registerResponseSchema 六项(GET list→NotificationListResponseDto、
  GET unread-count 与 PATCH mark-all-read→UnreadCountResponseDto、GET/PATCH
  detail 三端点→NotificationDetailResponseDto),组件名沿用旧类名,export 期 200 自动
  指向 $ref。
- 说明:POST `/notifications`(201)不再声明响应 schema——注册器只回填 200,且旧文档把
  201 误标为 NotificationListResponseDto(列表信封)与真实单条 wire 不符,顺势删除
  type 仅留 description;真实创建形状由 list item schema 承担出站校验。
- 新增 `dto/response.dto.spec.ts`:出站解析契约(actionPayload 宽容含数组/标量、非法
  enum/缺字段拒绝、未知键剥离、detail/list/count 形状)。
- 验证:notifications vitest 全绿;改动文件 eslint/prettier 零告警;全仓 typecheck 通过。
  未执行:export:openapi 组件 diff 与 Luminous 联动、notifications e2e 实跑(需 DB
  栈)——按批次边界留批量收尾闸门。嵌套组件副作用:zod 内联渲染使
  NotificationListItemDto 与 UserNotificationType 独立组件消失(items/type 内联),
  Luminous 再生成需在联动批次适配(同请求侧内联先例)。

### files / user / testing-support / health-data(审计结论)

- files:无 class 响应 DTO(仅 `CreatePresignedUploadResult` typeonly interface,
  typeonly 口径未动)。
- user:无 controller/DTO(纯内部 UserService 模块),无纯响应 DTO。
- testing-support:`@ApiExcludeController`,响应为 typeonly interface,无纯响应 DTO。
- health-data:Lucent 无此模块,无纯响应 DTO。
- 根级:src/app.dto.ts(Health\* 响应 DTO)不在本批模块清单,未动(归属待协调确认);
  src/common/api/problem-details.dto.ts 的 ProblemDetailsDto/SseProblemDetailsDto 属
  错误契约(export extraModels + ApiExceptionFilter 动态产出)与 SSE 结构,明确不动。

## zod 响应侧转换:daily-records 与 health-events 批次

按响应侧 runbook 将两个模块的纯响应/文档 DTO 由 `@ApiProperty` 类转为 zod4 schema +
`z.infer` 类型(导出类型名不变),控制器成功响应端点接 `@SerializeOptions({ schema })` +
文件尾 `registerResponseSchema`(组件名沿用旧类名)。响应 zod 统一口径:无
`.strict()`/`.default()`,字段按 mapper 实际出站形状 required + `.nullable()`(可空列落
`null`,数组/必填键恒在场),description 经 `.describe` 保留;嵌套结构引用子 schema
(`dailyRecordAttachmentSchema`、`healthEventCheckInResponseSchema`/`healthEventCoverageSchema` 等)。

- daily-records:`record-data/record-item/record-summary/record-response` 四个 DTO 文件 +
  `candidates/record-candidate-response.dto.ts` 全转;**同文件请求 schema 不动**;为保持模块
  响应面一致与接线完整,`record-attachment.dto.ts` 与 `candidates/record-image-upload.dto.ts`
  的响应类(`DailyRecordAttachmentDto`、`DailyRecordImageUploadDto/ResponseDto`)同批转 zod
  (该两文件请求侧 schema 原样保留)。响应类型名(DailyRecordItemDto 等)照旧由模块 barrel
  `export type`,zod schema 值不进 barrel。
- daily-records.controller.ts 接线:list/summary/get/create/update/generate-candidates/
  presign-upload 六类成功响应;注册组件:DailyRecordListResponseDto(GET list)、
  DailyRecordSummaryResponseDto(GET summary)、DailyRecordResponseDto(GET/:id、POST 201、
  PATCH/:id)、DailyRecordCandidateResponseDto(POST generate)、DailyRecordImageUploadResponseDto
  (POST presign-upload 201)。
- health-events:`event-response.dto.ts` 全转(CheckInResponse/Coverage/Item/ListData/Response/
  NullableResponse),controller 各成功端点接线。active 端点原
  `getSchemaPath(HealthEventItemDto)` nullable 手写 schema 改注册
  `HealthEventNullableResponseDto`(schema = item `.nullable()`);注册组件:HealthEventResponseDto
  (POST 201、GET/:id、PUT check-ins、POST end)、HealthEventListResponseDto(GET list)、
  HealthEventNullableResponseDto(GET active)。
- **201 注记**:export-openapi 现仅把注册组件的 200 响应回写 `$ref`,两个 201 主成功体
  (daily-records POST create、POST presign-upload;health-events POST create)已按稳定组件名
  登记并注释,导出脚本支持 201 回写后自动生效。错误响应 `@ApiResponse`(ProblemDetailsDto)
  与请求侧 DTO 均未动。
- 验证:typecheck 全仓过滤 daily-records/health-events 零错(全仓报错为并行批 assistant/
  user-health-context/today-suggestion 过渡态,与本批无关);模块 vitest 22 文件 244 例全绿;
  改动文件 prettier/eslint 零告警。`export:openapi` 与 Luminous 合同联动按批次边界留闸门复查。

## zod 响应侧转换:medicines / medicine-dose-logs / medicine-reminders(批次)

按响应侧机制试点(app-info + notifications/daily-records 先例)转换三个模块的纯响应/
文档 DTO → zod4 schema + `z.infer` 类型别名(导出类型名沿用旧类名),控制器
`@ApiResponse({ type })` 改为 description 并挂 `@SerializeOptions({ schema })`,文件尾
registerResponseSchema 登记组件(path 用真实导出路由,含 `user` RouterModule 前缀)。

### medicines

- `dto/detail.dto.ts`:DrugbankDrugInteractionDto/DrugbankMedicineDetailDto/
  CnMedicineDetailDto/MedicineDetailDataDto → schema;detail 变体按 `kind` literal 做
  `z.discriminatedUnion`;源数组 `z.array(z.string()).nullable()`;externalIdentifiers/
  externalLinks 保持 `z.unknown().nullable()`(Prisma Json? 历史值不可收紧)。
- `dto/search.dto.ts`:MedicineSearchItemDto/MedicinePaginationDto/MedicineSearchDataDto
  → schema;`MedicinePagination`/`MedicineSearchResult` 两个 interface 原样保留;source
  常量数组 `MEDICINE_KNOWLEDGE_SOURCES`(source.dto.ts)原样保留供 `z.enum`。
- `dto/response.dto.ts`:MedicineSearchResponseDto/MedicineDetailResponseDto 空 extends
  包装类 → 直接指向 data schema 的响应 body 别名。
- `dto/safety-tip-response.dto.ts`:item schema + `medicineSafetyTipsResponseSchema`
  (数组 body;outbound 校验用 item schema,注册用数组 schema)。
- `dto/recognition-response.dto.ts`:MedicineRecognitionResultDto/Async 响应 → schema
  (jobId/result 均 optional,队列优先/同步回退两种 wire 均通过)。
- `dto/risk/risk-check-response.dto.ts`:六个枚举 union 类型原样导出(本地 `as const`
  数组供 `z.enum`,值与旧 union 一致);Finding/CoverageIssue/RedFlag/Response/Record/
  Records 及其 Response 包装 → schema;`createdAt`/`updatedAt` 用 `z.iso.datetime()`
  保留 `format: date-time`(客户端 DateTime 契约不变);overallRecommendation 等仅在
  LLM 路径出现,取 optional。
- `medicines.controller.ts`:删除 `@ApiExtraModels` 与全部响应类值导入(改 `import type`
  或 schema);GET safety-tips/search/detail、GET/POST risk-check、POST recognize/async
  挂 SerializeOptions + 文件尾 register 六项;POST recognize(sync)与
  GET recognize/status 无响应类型保持原样。`medicines.service.ts` 对 safety-tip 类的
  value import 改 `import type`。
- 共享/跨界:模块 barrel 仅 `export type { MedicineSearchItemDto }`(assistant lookup 消费),
  zod 别名类型下原样可用;adapters/services/utils 均以 `import type` 消费 detail/search
  类型,零值导入残留。

### medicine-dose-logs

- `dto/dose-log-response.dto.ts`:DoseLogItemDto/ListData/Response 包装 → schema
  (status `z.enum(DoseLogStatus)` 走 Prisma const object;键全必填、可空列
  `.nullable()`,与 `toItem` 恒定全键 wire 一致;日期/时间保持纯 string 不带 format)。
- `medicine-dose-logs.controller.ts`:GET list、POST create、POST mark、PATCH update
  改 description + SerializeOptions + 文件尾 register 四项(201/200 主成功体均登记)。

### medicine-reminders

- `dto/response.dto.ts`:MedicineReminderItemDto/ListData/List/Item 响应 → schema
  (daysOfWeek 可空 number 数组;startDate/endDate/note 等 `.nullable()`)。
- `dto/reminder-delivery-response.dto.ts`:ReminderDeliveryItemDto/List 响应/Receipt
  响应(`{ item }`)/LocalCapability 响应(`{ state }`)→ schema;state 与请求侧
  `localCapabilityStateSchema` 枚举一致。
- 两个 controller:reminders(list/create/patch/group)与 deliveries(list/receipts/
  local-capability)同步 description + SerializeOptions + 文件尾 register 七项。

### 口径与验证

- 共享类型保留:detail/search/risk 的枚举 union 类型与 source.dto 常量数组不改名;
  dose-log/reminder 未导出内层类不额外扩散导出面;barrel 导出类型
  (MedicineSearchItemDto/MedicineReminderItemDto)zod 别名下继续可用。
- 注册器限制如实记录:export-openapi 现仅回填 `responses['200']`——dose-log create/mark、
  reminder create、deliveries receipts 的 201 主成功体按稳定组件名登记(daily-records
  先例),导出脚本支持 201 回写后自动生效;safety-tips 为数组 body,组件 schema 用
  `medicineSafetyTipsResponseSchema`,形状待 Luminous 再生成核对。
- 验证:typecheck 过滤本模块零错(全仓报错为并行批 assistant/user-health-context/
  today-suggestion 过渡态,与本批无关);vitest 三个模块 28 文件 385 例全绿(含三 controller
  spec);改动文件 eslint/prettier 零告警;代表 wire 样本对各 schema 出站解析冒烟通过。
  未执行:export:openapi 组件 diff、Luminous 联动、medicines/dose-log/reminder e2e 实跑
  (需 DB 栈)——按批次边界留批量收尾闸门;嵌套组件内联副作用
  (DrugbankMedicineDetailDto/CnMedicineDetailDto 等独立组件随 union 内联,source 枚举与
  子结构不复独立组件)同先例在联动批次适配。

## zod 响应侧转换:assistant / user-health-context / today-suggestion / today-analysis

按响应侧机制试点与 notifications/daily-records/health-events 先例,四个模块的纯响应/文档
DTO → zod4 schema + `z.infer` 类型(导出类型名沿用旧类名;空 extends 包装类折叠为同形状
类型别名)。响应 zod 统一口径:无 `.strict()`/`.default()`,description 经 `.describe` 保留,
字段按 class 声明全键/可空/可选映射,枚举用 `z.enum`(TS 原生 enum、Prisma const object 与
`as const` 数组均可),jsonb 自由字段保持 `z.unknown()`/`z.record(…, z.unknown())` 不收紧;
assistant capabilities 内嵌 user-settings 的 assistantContext 形状改为本地 zod 镜像并移除
跨模块 value import(用户设置模块自身另批转换,解耦)。共享响应类型仅保留类型导出
(today-analysis stream 的 `TodayAnalysisStreamErrorDto` = common `SseProblemDetailsDto`
type-only;`ProblemDetailsDto` 4xx 文档装饰器未动)。

### assistant

- `dto/proposed-action.dto.ts`(含私有 PreviewField/ProposalTarget 子 schema)、
  `stream-response.dto.ts`、`conversation-response.dto.ts`、`conversation-list-response.dto.ts`、
  `capabilities-response.dto.ts`、`clear-memory-response.dto.ts` 全转 zod;能力清单用
  ASSISTANT_TOOL_NAMES/ASSISTANT_CONTEXT_SOURCES/ASSISTANT_TOOL_DISABLED_REASONS 常量
  `z.enum`。
- `assistant.controller.ts`:capabilities/open/rename/delete/memory/latest-clear 六个 200
  端点挂 SerializeOptions + 文件尾注册(组件沿用 AssistantCapabilitiesResponseDto/
  AssistantConversationResponseDto/AssistantClearMemoryResponseDto/AssistantClearResultResponseDto);
  conversations 列表为数组 body——出站校验用 item schema、注册用
  `assistantConversationListResponseSchema`(组件名沿用旧 item 类名,形状留 Luminous 再生成核对);
  latest 为可空 body——注册 `.nullable()` 组件(名沿用 AssistantConversationDataDto),运行时
  SerializeOptions 用非空 data schema(serializer 对 null 直通);confirm(200)的
  AssistantConfirmResultResponseDto 属请求批残留类,未在本次文件清单,保持 class 不动;两条
  SSE(stream/regenerate)非 JSON body,文档保持 `text/event-stream`,不接线。

### user-health-context

- `dto/response.dto.ts`:HealthContextResponseDto/DataDto/ResponseData 全折叠为单
  `healthContextResponseSchema` 类型别名;嵌套 Summary/Profile/Allergy/Condition/MedicineItem/
  EmergencyContact 为模块内子 schema,枚举走 Prisma const object。
- `user-health-context.controller.ts`:全部 11 个端点(200 与 201)挂 SerializeOptions;
  8 个 200 端点 + 3 个 POST 201 端点均按稳定组件名 `HealthContextResponseDto` 文件尾登记
  (201 注记:export 支持 201 回写后自动生效);错误 `@ApiResponse`(ProblemDetailsDto)不动。

### today-suggestion

- `suggestion-response.dto.ts`(EvidenceItem/SuggestionAction/ObservedMetric/SuggestionItem;
  evidence `kind` 按 class 保持 `z.string()`——规则实际输出多种 kind,不收紧为文档枚举)、
  `suggestion-history.dto.ts`、`explanation.dto.ts`、`suggestion-history-query.dto.ts`
  (仅响应部分;`SuggestionHistoryQueryDto` 查询类原样保留)转 zod。
- `today-suggestion.controller.ts`:GET /today/suggestions 与 GET history(200)挂
  SerializeOptions + 注册(TodaySuggestionsResponseDto/SuggestionHistoryResponseDto);
  POST explain(201)与 explain/async(202)挂 SerializeOptions + 按稳定组件名
  (SuggestionExplanationResponseDto/SuggestionExplanationAsyncResponseDto)登记并注记;
  POST feedback 的响应类在 feedback.dto.ts(不在本批文件清单)保持 class 不动。

### today-analysis

- `analysis-response.dto.ts`(Data/ReadData/ReadResponse/RefreshPending/RefreshReady/AsyncJob/
  AsyncResult/AsyncStatus 及嵌套 Bullet/ObservedMetric/Metric)、
  `recommendation-response.dto.ts`(+ 数组 body schema)、`analysis-stream-response.dto.ts`
  (SSE 帧文档;summary 转 zod,error 共享 ProblemDetails 仅类型导出,result 帧保持类型联合)
  转 zod。
- `today-analysis.controller.ts`:GET read(200)挂 SerializeOptions + 注册
  TodayAnalysisReadResponseDto;recommendations 数组出站 item schema + 注册数组组件
  (TodayRecommendationResponseDto);refresh(201)/generate(200 union)/generate/async(202)
  三端点为多态响应——不满足注册器"单组件 200"形状,保留 oneOf `$ref` 装饰器文档,成员组件
  (TodayAnalysisDataDto/ReadDataDto/RefreshPending/RefreshReady 锚 refresh;AsyncJob/Result/
  Status 锚 async)按旧类名登记供 $ref 解析,导出脚本支持 201/202/多态回写后自动生效;
  generate/status 与两条 SSE 无响应类型/schema 保持原样。
- `today-analysis.contract.spec.ts` 改写:read 断言 SerializeOptions schema 引用与 200
  type 移除;refresh/generate/async 断言 status 与 oneOf `$ref` 文档保持。

### 验证与未覆盖点

- 全仓 typecheck 零错;四个模块 vitest 99 文件 1171 例全绿(含改写后的 contract spec);
  改动文件 eslint/prettier 零告警。
- 未执行:export:openapi 组件 diff 审查与 Luminous 联动(嵌套组件内联副作用同先例留联动
  批次);四个模块 e2e 实跑(需 DB 栈)——Serializer 出站校验与 201/SSE/联合端点行为未在
  真实 HTTP 下实证;数组/可空组件的 Luminous 再生成形状(assistant conversations/latest、
  today-analysis recommendations)需闸门核对。

## 响应侧 zod 转换批(legal-documents/notification-preferences/notifications/user-settings/data-export/account)

响应侧机制(app-info 试点)推开的第二批:六模块纯响应/文档 DTO 类 → zod schema + `z.infer` 类型(类名保留),
controller 响应端点接 `@SerializeOptions({schema})` + 文件尾 `registerResponseSchema`(path 用 export 后真实
路径,method 小写,componentName 沿用旧 DTO 类名 → Luminous 模型名不变)。响应 schema 一律不带 `.strict()`/
`.default()`(出站放行未知字段与缺省),描述经 `.describe` 保留。

- legal-documents:`dto/response.dto.ts` 四个响应类(LegalDocumentListItemDto/DetailDto/ListDataDto 及
  ListResponse/DetailResponse 空壳)统一收敛为 legalDocumentListItemSchema/legalDocumentDetailSchema/
  legalDocumentListSchema 三份 zod + 五个类型导出;controller 两个 GET 接 SerializeOptions 并注册
  `/api/v1/legal-documents`(get,LegalDocumentListResponseDto)、`/api/v1/legal-documents/{docType}`
  (get,LegalDocumentDetailResponseDto)。
- notification-preferences:`dto/response.dto.ts` 两个类 → notificationPreferencesSchema(分钟字段
  `.int().nullable()`),controller GET/PATCH 接 SerializeOptions 并注册 `/api/v1/user/notification-preferences`
  (get/patch,NotificationPreferencesResponseDto)。
- notifications:`dto/response.dto.ts` 剩余纯响应类(NotificationListItemDto/DetailDto/ListDataDto/
  ListResponse/DetailResponse/UnreadCountDataDto/UnreadCountResponseDto)→ 五份 zod(list item/detail/list/
  unread count;detail = list extend readAt)+ 类型别名;actionPayload 走 `z.unknown().nullable()`(历史任意 JSON);
  新增 `dto/response.dto.spec.ts` 锁定出站形状;controller 六个 200 端点接 SerializeOptions 并注册
  `/api/v1/user/notifications`(get,NotificationListResponseDto;post,NotificationListItemDto — 主成功为
  201,注册器固定 200 不接线,留报告)、`/unread-count`(get,UnreadCountResponseDto)、`/{id}`(get,
  NotificationDetailResponseDto)、`/{id}/read`、`/{id}/unread`(patch,NotificationDetailResponseDto)、
  `/mark-all-read`(patch,UnreadCountResponseDto);POST create(201)实际返回单条 item,旧文档 type 误标
  ListResponseDto,现按真实 wire 注册 NotificationListItemDto。
- user-settings:`dto/response.dto.ts` 三个类 → assistantContextSettingsSchema + userSettingsDataSchema;
  barrel index 改 `export type { AssistantContextSettingsDto }`(原为类值导出);controller GET/PATCH 接
  SerializeOptions 并注册 `/api/v1/user/settings`(get/patch,UserSettingsResponseDto)。
- data-export:`dto/export-response.dto.ts` 三个纯响应类(DataExportRequestDataDto/RequestResponseDto/
  LatestResponseDto)→ dataExportRequestDataSchema + latest nullable 变体;controller POST(201)与
  GET latest 接 SerializeOptions 并注册 `/api/v1/user/data-export-requests`(post,DataExportRequestResponseDto;
  主成功 201,注册器固定 200 不接线,留报告)、`/api/v1/user/data-export-requests/latest`(get,
  DataExportRequestDataDto,nullable 经 schema 表达——组件名沿用旧 ref 所指的 DataExportRequestDataDto,
  与旧 openapi 手工 nullable allOf 语义对齐,数组/可空组件的 Luminous 再生成形状留联动闸门核对)。
- account:`dto/response.dto.ts` AccountDto/AccountResponseDto/AccountEmailResponseDto(及内部
  AccountIdentityDto)→ accountDataSchema + accountEmailDataSchema;controller 六个 200 响应端点接
  SerializeOptions 并注册 `/api/v1/account`(get/patch,AccountResponseDto)、`/api/v1/account/email`
  (post,AccountEmailResponseDto)、`/api/v1/account/identities/{identityId}`(delete,AccountResponseDto)、
  `/api/v1/account/identities/wechat-web/callback`、`/api/v1/account/identities/wechat-mobile/callback`
  (post,AccountResponseDto);account.service 对 AccountDto 的导入改 `import type`。wechat-web authorize
  端点返回 auth 模块 OAuthAuthorizeResponseDto(响应 DTO 仍类式、不在本批),未改装饰器。
- 类型别名:同文件请求 schema/常量(zod 请求 schema、USER*\* 常量、DATA_EXPORT*\* 常量)保留;被删响应类
  无任何 `new Xxx()` 值实例(controller spec 全为类型化字面量),跨模块消费者仅 import type,TS 自动 elide。
- 验证:pnpm typecheck 全仓零错;vitest 六模块 23 文件 228 例全绿;改动文件 prettier 通过。
- 未执行:`export:openapi` 与 Luminous 合同联动(批次收尾统一,嵌套组件内联与 201/可空语义核对同留闸门)、
  e2e 实跑(需 DB 栈)——Serializer 出站校验在真实 HTTP 下未实证。

## zod 响应侧迁移:reports / product-events / environment / auth(响应类全转)

按响应侧试点(app-info)机制转换四个模块的纯响应/文档 DTO:类 + `@ApiProperty` → zod4
schema(`z.infer` 类型导出旧类名,wire 形状/描述保留,响应 zod 无 strict/default),控制器成功
响应端点移除 `@ApiResponse type`,接 `@SerializeOptions({ schema })` 并在文件底部逐路径
`registerResponseSchema`(组件名沿用旧类名);多状态(201/202)端点同样登记,注册器/导出脚本
当前只回写 200 的局限与嵌套内联语义留批次闸门。

### reports

- `dto/clinic-summary-response.dto.ts`:14 个嵌套/数据/响应类 → clinicSummaryCoverageEntry/
  Coverage/Profile/Allergy/Condition/Medicine/WaterEntry/SleepEntry/NoteEntry/Response/
  ExportAsyncResponse/ShareScope/ShareData/ShareResponse schema(共享 ShareScope 供 share-list
  dto 复用);`ClinicSummaryResponseDto`/`ShareResponseDto` = 数据 schema 别名。
- `dto/clinic-summary-share-list.dto.ts`:ListItem/ListData/ListResponse → schema(scope 引用
  clinic-summary-response 的 shareScope schema)。
- `dto/event-review-response.dto.ts`:14 类 + 3 纯类型联合(保留)→ SectionFacts/Section/Sections/
  Event/TodayCheckIn/CheckInCoverage/ObservedSource/CoverageSummary/SourceTimestamps/Data/
  ListData schema;`EventReviewResponseDto`=Data 别名、`NullableResponseDto`=Data `.nullable()`、
  `ListResponseDto`=ListData 别名。
- `dto/report-dashboard-response.dto.ts`:ObservedMetric/Metric/Trend/Finding/Pattern/Data →
  schema,ResponseDto = Data 别名(废弃字段描述经 `.describe` 保留为 wire 字段)。
- `dto/report-summary-response.dto.ts`:CoverageDimension/Coverage/ObservedPattern/LowRiskAction/
  SummaryData/AsyncResponse → schema,`ReportSummaryResponseDto`=Data 别名。
- `dto/report-summary-stream-response.dto.ts`:SSE 文档类 Summary/Result → schema(仅类型导出,
  不注册——body 是 text/event-stream 非 JSON 200)。
- `reports.controller.ts`:dashboard/summary generate(+async 202)/clinic preview·share·shares·
  shared·export-async/reviews current·list·byId 共 11 个 JSON 响应端点接 SerializeOptions 并
  注册 `/api/v1/user/reports/...` 11 条(reviews/current 用 data `.nullable()` 表达 resource-or-null,
  与原手工 nullable allOf 语义对齐);PDF/SSE/204/job-status 端点不带 schema 不动。

### product-events

- `dto/funnel-response.dto.ts`:6 类 → funnelDailyCounts/OptionalCounts/Totals/Window/Data
  schema,`FunnelResponseDto`=Data 别名。
- `product-events.controller.ts`:GET funnel 接 SerializeOptions 并注册
  `/api/v1/user/product-events/funnel`(FunnelResponseDto);POST record(201 无 body DTO)不动。
- barrel `index.ts`:funnel 响应类值导出改 `export type`(跨模块仅 type 消费)。

### environment

- `dto/snapshot.dto.ts`:整文件纯响应——常量数组与联合类型(ENVIRONMENT_DATA_SOURCES/
  POLLEN_LEVELS/UV_LEVELS/AIR_QUALITY_LEVELS)保留;6 个 Indicator 类 + EnvironmentSnapshotDto/
  ResponseDto → pollen/uv/airQuality/temperature/humidity/environmentSnapshot schema
  (`z.enum(常量数组)` 沿用 enumName 值域,组件名 EnvironmentSnapshotResponseDto 不变)。
- `environment.controller.ts`:GET snapshot 接 SerializeOptions 并注册
  `/api/v1/environment/snapshot`(EnvironmentSnapshotResponseDto)。

### auth

- `dto/shared/auth-response-common.dto.ts`:UserBrief/UserFull/Tokens/CooldownMessage →
  userBrief/userFull/tokens/cooldownMessage schema(类型导出保留)。
- `dto/shared/auth-responses.dto.ts`:Register/Login/OAuthAuthorize/VerifyEmail 数据 shape 与
  Refresh/SendVerificationCode/ForgotPassword(继承共享 schema)→ register/login/oauthAuthorize/
  verifyEmail response schema + refresh/sendVerificationCode/forgotPassword = tokens/
  cooldownMessage schema 别名;旧名仅作类型导出,跨模块共享响应类型按规则只留类型
  (auth barrel `OAuthAuthorizeResponseDto` 改 `export type` + 导出 `oauthAuthorizeResponseSchema`
  供 account 等 `{ schema }` 接入)。
- `dto/shared/session-list-item.dto.ts`:SessionListItem → sessionListItem schema + 数组
  sessionList schema(200 body 是裸数组,SerializeOptions 走 item schema、注册走数组 schema)。
- 控制器:local(register 201/login/send-verification-code/verify-email/forgot-password 200 五端点)、
  oauth(5 家 authorize 200 + 6 家 callback 200,均 LoginResponseDto/OAuthAuthorizeResponseDto)、
  session(list 200/refresh 200)接 SerializeOptions 并逐路径注册 `/api/v1/auth/...`
  (register 201 主成功,注册器固定 200 不接线,留报告);204/302 端点不动。

### 验证与未覆盖点

- typecheck:本批四模块零错(全仓剩余报错为并行 account 批对 auth-shared 响应的装饰器类引用
  过渡态,与本批无关——auth barrel 已导出 schema 供其切换);vitest 四模块 55 文件 727 例全绿;
  改动文件 prettier/eslint 通过。
- 未执行:`export:openapi` 与 Luminous 合同联动(批次收尾统一;201/202 状态码回写、裸数组与
  `.nullable()` 组件的 Luminous 再生成形状、嵌套对象内联语义核对同留闸门)、e2e 实跑(需 DB 栈)
  ——Serializer 出站校验在真实 HTTP 下未实证。

## 响应侧全量(六个并行批)与集成

- reports/product-events/environment/auth、assistant/UHC/today、daily-records/health-events、medicines/dose/reminders、六模块组(legal/notif/settings/data-export/account)、查漏组全部完成响应 DTO→zod+接线;每批范围 vitest 绿、typecheck 收敛;account/auth 跨批接缝已补。
- export 期注册器扩展:成功码 200→201→202 回写;嵌套/子模型随顶层组件内联(components 193→84,旧响应子组件退役)。
- 遗留:全量 pnpm check 与 Luminous 再生成(预期第二波客户端适配)与 Phase 4 收尾待下一轮。

## 响应侧序列化运行时修复

Serializer 出站校验暴露服务端 Date 对象 vs schema string 的 500(reports shares):控制器边界归一(Date→ISO,容忍已字符串),控制器 spec 断言同步;全量 pnpm check 绿。

## openapi 导出兼容层与宽松 schema 回退

- `scripts/contract/export-openapi.ts` 新增 `openApiSchema` 清洗:把 zod `toJSONSchema` 产出的
  JSON-Schema 收敛为 OpenAPI 3.0 可接受子集——剔除 `$schema`/`propertyNames`/`const` 等仅
  JSON-Schema 关键字、`anyOf` 中的 null 分支折叠为 `nullable: true`,并对 properties/items/
  additionalProperties/allOf/anyOf/oneOf 递归处理;单组件转换加 try/catch,失败打印组件名后终止,
  避免大面积静默丢组件。
- `dto/clinic-summary-share-list.dto.ts`:share 列表时间戳字段从容忍 `z.union([string,date])`
  回退为纯 `z.string()`——该联合会被 `z.toJSONSchema` 拒绝,date 归一已由控制器边界负责,wire
  契约保持 ISO 字符串。
- `docs/reference/generated/openapi.json` 随清洗逻辑重新导出(components 84,零转换错误)。
- `src/modules/{health-events,daily-records}/…controller.ts`:13 条响应注册路径修正为与导出路径
  一致——补 RouterModule `user` 前缀(`/api/v1/{...}` → `/api/v1/user/{...}`),路径参数 `:id`/
  `:date` 改 OpenAPI `{id}`/`{date}` 花括号形式。此前两条不匹配被导出脚本静默跳过,对应端点
  在客户端生成时无成功响应 schema(`Response<void>`);修正后 114 条注册全部命中真实 operation。
- `export-openapi.ts` 对未命中 operation 的注册改为显式报错(逐条打印后抛错终止),防止此类
  "注册了却没接线" 的契约缺口再以静默方式漏出。

## 机器可读错误码 ADR(计划 Phase 3 决策文档补写)

- 新增 `docs/reference/adr/0017-machine-readable-error-code-catalog.md`:成文「稳定 code 注册表 =
  ProblemCatalog」决策——Problem Details `code` 的唯一来源、`ApiExceptionFilter` 对出站 code 做
  status/注册表一致性兜底(显式 code 仅在登记且 status 匹配时采纳,否则按 status 回落默认码)、
  zod/Standard Schema 校验失败统一 VALIDATION_FAILED + 结构化 errors、i18n 标题/详情唯一映射点、
  新码流程(注册表 + 双语 key + 按需 e2e)。
- 纯文档补写:注册表(problem-catalog.ts)、过滤器、DomainFailure 映射与各模块 e2e 断言(含
  VALIDATION_FAILED)此前已实现;ADR-0012 索引追加 0017 条目。

## 收尾基线同步与 TODO 收敛(计划 Phase 4 前置)

- `AGENTS.md` Current Baseline 更新:NestJS 11 → NestJS 12(ESM、SWC builder)+ zod 4 + Standard
  Schema(请求/响应校验,不引入 class-validator);错误契约引用补 ADR-0017;注明 RouterModule
  `user` 前缀与 export-openapi 注册路径对账语义。
- `README.md` Stack 与 `docs/README.md` 索引行同基线更新(NestJS 12 / ESM / zod 4)。
- `docs/TODO.md` 删除两条已完成项:「Luminous 合同联动阻塞」(请求 schema 命名决策已落地:
  Luminous 按 per-op 模型名全面适配 + bootstrap 清理/重试修复)与「date wire 口径」(方案 A
  落地:date-only 字段 string+refine,客户端 date 参数 String,见当日与 2026-09-03 早间日志);
  front-matter updated 同步当日。OpenAPI example 元数据项经裁定维持延后(条目保留)。
- 验证:`pnpm docs:verify`、`pnpm docs:links`、`pnpm format:check` 全绿(纯文档变更)。

## NestJS 12 激进升级计划实施完毕(计划文件已删)

- 计划 `plans/2026-09-01-nestjs12-upgrade-plan.md` 全阶段完成,按约定删除;`plans/README.md`
  Current Plans 移除对应条目。持久决策与事实已落:ADR-0012/0017、当日迁移日志、AGENTS/README/
  docs 索引基线(NestJS 12 + ESM + zod)、docs/TODO.md 延后清单(CJS 遗留、serializer 边角、
  `@nestjs/observe` 复议条件、OpenAPI example 维持延后)。
- 收尾验证(本地可达部分):`pnpm check`(lint/format/typecheck/build/unit/e2e,486 e2e 用例全绿)、
  `pnpm arch:check`(观察期 warn 不阻断)、`pnpm docs:verify`、`pnpm docs:links`、Luminous 全仓
  analyze 0 + `flutter test` 3653 绿;deploy-compose Docker smoke 属生产宿主部署后冒烟
  (deploy/smoke.ts 走 SSH+渲染密钥),本地不可执行——dev compose config 校验通过,运行时冒烟由
  e2e 全量(真实 Docker Postgres/Redis 上的完整应用启动)覆盖。staging 流量下 OTel 基线对照与
  Docker smoke 留待部署流程执行。
