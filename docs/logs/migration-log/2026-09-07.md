# 2026-09-07

## fix(auth): resetPassword 密码变更通知改 fire-and-forget，补控制器异常分支测

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#3 / #4 落实:

- `credential.service.ts`：`changePassword` / `setPassword` / `resetPassword` 三处的
  `_notifyPasswordChanged(userId)` 不再经 `lift()` 串进 ResultAsync 链，改为
  `void this._notifyPasswordChanged(userId)` fire-and-forget——通知失败只记 error
  日志、不影响成功路径（密码已写入 DB 且 JWT 已撤销后，通知失败不再让整个
  reset 变成 5xx，避免"用户被强制登出却收不到提示通知"的不一致被放大为主路径失败）。
- `local.controller.spec.ts`：`resetPassword` 新增两个断言——非 DomainFailure
  基础设施异常（如 DB 断开）经 `unwrapResult` 逃逸到全局 filter 转 5xx 且不丢失
  原始异常；验证码失效折叠为 `DomainFailureException`（`AUTH_VERIFICATION_CODE_EXPIRED`）。

注：#3 推荐的「把 verify 移到最后」与「先 cacheDel 主动消费」未采纳——当前
`verify` 成功后即删除缓存中的验证码（一次性消费），不存在"verify 未消费导致 token
仍有效"的 TOCTOU；把 account 查询提前到 verify 之前反而会破坏防枚举（OAuth-only
账号无需有效验证码即可被探测出 `AUTH_PASSWORD_NOT_SET`）。

验证：`pnpm typecheck` 通过；credential / local.controller / auth.service spec
全绿（53 + 13 + 27）。

## fix(reports): dashboard cache get 失败改 fail-open 与 cache set 对称

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#10 落实:

- `dashboard.service.ts`：`cache.get` 失败不再 `throw error`（fail-closed → 5xx），
  改为记 warn 后 `cached = undefined` 走 DB 重算路径，与下方 `cache.set` 的
  fail-open 策略对称——cache 是 accelerator、DB 是 source of truth，读失败不应
  把健康请求变成 5xx。
- `dashboard.service.spec.ts`：新增「cache get 失败时回退 DB 重算」用例
  （断言 `contextService.build` 被调用、结果正常返回）。

验证：`pnpm typecheck` 通过；dashboard.service.spec 5 tests 全绿。

## refactor(config): bootstrap 阶段 NODE_ENV 直读统一走 EnvKey.NODE_ENV

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#1 / #2 落实:

- `app.module.ts`：`isTestRuntime` 的 `process.env['NODE_ENV']` 改为
  `process.env[EnvKey.NODE_ENV]`（与 `app.config.ts` 同源类型化键）。
- `i18n.module.ts`：`watch` 与 `typesOutputPath` 门控两处裸字符串
  `process.env['NODE_ENV']` 同样改为 `EnvKey.NODE_ENV`；补充注释说明
  bootstrap 阶段 ConfigService 不可用、此直读是合理豁免。
- 其余 `process.env['NODE_ENV']` 位于 config 初始化层（`yaml-loader` /
  `env-file-paths.ts`）与测试文件，属于既有约定范围，未动。

验证：`pnpm typecheck`、`pnpm lint:check`（0 warnings / 0 errors）通过；全量
单测相关文件无回归。

## fix(auth): verification-code 构造期 TTL/限流参数 fail-fast 校验

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#12 落实:

- `verification-code.service.ts` 构造函数从 yaml 读取
  `codeTtlMs` / `cooldownMs` / `rateLimitMax` 后做 sanity check：
  - `codeTtlMs <= 0` 抛错（否则验证码无限期有效）
  - `cooldownMs <= 0` 抛错（否则冷却永远失效）
  - `cooldownMs > codeTtlMs` 抛错（否则冷却超过验证码有效期，语义矛盾）
  - `rateLimitMax <= 0` 抛错（否则限流形同虚设）
    构造函数异常属 DI 初始化期，豁免 `error-handling/no-bare-throw-error`
    （Nest 启动即失败，fail-fast 而非带病运行）。
- `verification-code.service.spec.ts` 新增 4 个构造器校验用例（zero codeTtl /
  负 cooldown / cooldown 超 codeTtl / zero rateLimitMax）。

验证：`pnpm typecheck` 通过；verification-code.service.spec 21 tests 全绿。

## fix(auth): OAuth config 哨兵值 not-configured 统一归一化为空字符串

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#13 落实:

`.env.development` / `.env.production` 中微信（web + mobile）与微博的
`APP_ID` / `APP_SECRET` 已填 `not-configured` 占位以消除启动告警
（前端已隐藏入口，见 09-06 migration log），但该字符串本身是非空值，若未来
有人误填入真实 env 或占位未清理，OAuth provider 的 `!config.appId` 判断会放行
并拿这个假值去请求第三方 API（静默失败或账户被锁）。

- `oauth.config.ts`：新增 `resolveCredential()` 私有函数——将 `not-configured`
  （case-insensitive，含前后空格）统一归一化为空字符串；所有 provider 的
  `appId` / `appSecret` 经此函数处理。`redirectUri` 维持原样（它是 URL，不会
  与 sentinel 混淆；且未配置时 provider 已有 `!config.redirectUri` 守卫）。
- `oauth.config.spec.ts`：新增 2 个用例——sentinel 原值归一为空（WeChat web
  - Weibo）、大小写 + 空格归一（WeChat mobile）。`keysToClean` 补充
    `WEIBO_*` 与 `WEIBO_REDIRECT_URI` 键以防止测试间污染。

验证：`pnpm typecheck`、`pnpm lint:check`（0/0）通过；`oauth.config.spec` 8
tests + 6 个 provider spec（61 tests）全绿。

## test(security): CORS 预检断言防止 Access-Control-Allow-Methods 回退

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#7 落实:

`test/security/fuzzing.e2e-spec.ts` 新增 `CORS preflight` describe：发送
`OPTIONS /api/v1/user/daily-records/123`（Origin: `https://web.example.com` +
`Access-Control-Request-Method: DELETE`），断言响应头
`access-control-allow-methods` 包含 `DELETE`。若未来有人误将 `setup-app.ts` 的
`methods` 回退为默认值（仅 `GET,HEAD,POST`），CI 会在 e2e 阶段捕获该回归。

## test(config): runtime-signal BUILD_OUTPUT_NAMES 断言扩展文档化

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#8 落实:

- `runtime-signal.ts`：`BUILD_OUTPUT_NAMES` 由模块私有常量改为 `export`，
  供 spec 和未来可能的参数化场景使用。
- `runtime-signal.spec.ts`：新增断言覆盖 `BUILD_OUTPUT_NAMES` 内容
  （`['build','dist','lib']` 排序）——未来若 `compilerOptions.outputPath`
  新增目录名（如 `out`），需同时更新集合与本断言，否则 CI 自动报红。
  注释说明扩展方式与审查意图。

验证：`pnpm typecheck` 通过；全量单测 315 文件 / 3691 tests 全绿。

## docs(auth): verification 场景限流说明注释 + 日志约定示例表

按 `plans/Lucent-review-09-06.md`（实施完毕文件已删）#5 / #9 / #11 / #14 落实:

- `config/default.yaml`：`verification` 块补注释说明所有场景
  （register / login / set-password / forgot-password / change-email /
  delete-account）共享同一套窗口/上限/冷却；明确 forgot-password 从 Better Auth
  内部限流迁移到 `VerificationCodeService` 同一路径（09-06），与 register/login
  参数一致（#5）。
- `docs/reference/logging-conventions.md`：规则 5（结构化与模板并用）下新增
  「结构化 vs 模板」对照示例表，明确「有上下文的失败用 `(message, meta)` 结构化、
  纯状态/调试用模板字符串」的取舍（#14）；`updated` 前移至 2026-09-07。
- 确认项（无代码改动）：#11 `logging-conventions.md` 已在 `docs/doc-map.yaml` 与
  `docs/README.md` 链接矩阵登记；#9 OpenAPI Dto 后缀重命名对下游 `Luminous`
  的影响——该重命名发生于 09-06，`export:openapi` 本轮未再触发（无 contract
  变更），Luminous 侧 `generated/lucent_api` 由 09-06 的 bootstrap 同步过，无
  遗留 import 依赖旧组件名，无需进一步处理。

验证：`pnpm docs:verify` 通过。

## refactor(reports): exportPdf/exportSharedPdf 从 ClinicSummaryService 迁到 ClinicSummaryPdfService

按领域拆分计划（P2.5）落实——PDF 导出编排（build view → render PDF）属渲染
领域，摘要视图组装（buildClinicSummary / getSharedSummary）属数据领域：

- `pdf.service.ts`：新增构造函数依赖 `ClinicSummaryService`（单向 `pdf → summary`，
  无循环依赖），新增 `exportPdf(userId, locale, options)` 与
  `exportSharedPdf(token, locale)`（调用 summary 的视图方法 + 自身 `buildPdf`）。
- `summary.service.ts`：删除 `exportPdf` / `exportSharedPdf` 与 `pdfService` 字段
  （构造签名 7 参 → 6 参，`ClinicSummaryPdfService` import 一并移除）。
- `pdf-queue.service.ts`：worker 回调改用 `ClinicSummaryPdfService.exportPdf`；
  同步回退路径注释同步更新。
- `clinic-summary.controller.ts`：4 处导出调用改用新注入的
  `clinicSummaryPdfService`（PDF 下载 / scoped-sync / fallback / 共享 PDF）。
- specs：`reports.controller.spec.ts` 新增 `ClinicSummaryPdfService` mock 并把
  9 处断言迁移；`summary.service.spec.ts` 的 `withEventReview` 移除多余 pdf 参数，
  field-drift 测试改用真实 `ClinicSummaryPdfService`（`buildPdf` 走 mock 捕获输入）；
  `pdf-queue.service.spec.ts` 无需改动（已用 PdfService mock）。

验证：reports 相关 typecheck 干净（全仓仅剩 assistant/tools 另一代理在建文件的
6133，与本变更无关）；全量单测 316 文件 / 3685 tests 全绿。

## refactor: 按领域拆分超大文件（daily-records / assistant / today-analysis / today-suggestion / auth）

按 `plans/2026-09-07-large-file-domain-split-plan.md`（实施完毕文件已删）P0–P2 落实，
全部按领域而非机械截断拆分，文件名表明职责：

- **daily-records**：`records.service.ts`（847 行）拆出
  `records-validator.service.ts`（创建载荷校验 / 睡眠终态校验）与
  `meal-payload-writer.service.ts`（餐食载荷组装 / 分析入队）。
- **assistant core**：`core.service.ts`（698 行）拆出
  `proposal-confirm.service.ts`（提案确认 / 应用写入）与
  `stream-orchestrator.service.ts`（流式消息编排 / 能力协商），core 变纯委托门面。
- **assistant runtime**：`runtime.service.ts`（741 行）拆出
  `agent/stream.service.ts`（generateStream / 流式预生成内容 / chunk 解析）。
- **assistant proposal**：`proposal.service.ts`（457 行）拆出
  `daily-record-proposal.service.ts`（create/update/delete 记录提案）与
  `settings-proposal.service.ts`（用户设置提案），原文件保留为委托门面。
- **assistant lookup**：`tools/medicine/lookup.service.ts`（447 行）拆出
  `drugbank-lookup.service.ts`（DrugBank 详情查询），原文件保留国内药品查询。
- **today-analysis**：`context.service.ts`（676 行）拆出
  `trigger-evaluator.service.ts`（shouldTriggerForDimension / 基线聚合）。
- **today-suggestion**：`collectors/medication.service.ts`（614 行）拆出
  `medication-time-resolver.service.ts`（时区/DST 调度时刻计算）；
  `collectors/record.service.ts`（590 行）拆出
  `sleep-trend-builder.service.ts`（睡眠 episode 解析与趋势构建）。
- **auth**：`credential.service.ts`（636 行）拆出
  `password-management.service.ts`（changePassword / setPassword / changeEmail /
  验证码 / forgotPassword / resetPassword），credential 只保留 register/login。
- **reports**：`reports.controller.ts`（814 行）拆出
  `clinic-summary.controller.ts` 与 `event-review.controller.ts`；
  `summary.service.ts`（679 行）的 `exportPdf`/`exportSharedPdf` 迁入
  `pdf.service.ts`（依赖方向翻转为 pdf → summary，单向无环）。

spec 全部随拆迁移/委托化；新服务均注册进各自模块 providers；无 API 契约变更
（controller 端点路径与 DTO 不变），无需 `export:openapi`。

验证：`pnpm typecheck` 通过；全量单测 316 文件 / 3691 tests 全绿（eslint-plugins
子配置无匹配属已知噪音）；`pnpm lint:check` 0 errors / 0 warnings。
