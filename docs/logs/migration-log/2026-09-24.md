# 2026-09-24

## 09-19 审查遗留（R 批 / B 批）收口

- **本体推理入参夹紧对模型可见**：`tools/ontology/ontology-reasoning.service.ts` 的
  `normalizeLimit()` 改为返回 `OntologyLimitResolution`（`limit` / `requestedLimit` /
  `limitCapped`），`buildEnvelope` 在发生夹紧时输出 `query.requestedLimit` 与
  `limitCapped`，并追加一条 `ambiguities`（文案 `ONTOLOGY_LIMIT_CAP_MESSAGE`，与
  `MEAL_DIGEST_*_CAP_MESSAGE` 同构）。sidecar 调用参数不变；原先模型要 200 行只拿到
  100 行且无从得知，表现为反复要更大的窗口。
- **引用 schema 漂移的指标**：新增计数器 `assistant_tool_citations_dropped_total{reason}`；
  `assistant/services/stream-orchestrator.service.ts` 的 `extractCitations()` 因 schema
  不匹配丢弃时计数（原先只 `logger.warn`，"客户端永远看不到 citations 但不知道为什么"
  只能在日志里翻）。
- **建议卡内部标识符白名单**：`today-suggestion/schemas/copy.schema.ts` 新增
  `KNOWN_INTERNAL_ACTION_LABELS`（由 `ACTION_LABEL_TEMPLATES` 的 key + `default` +
  `short` 派生）与 `COPY_TEMPLATES[*].actionKeys`；`isInternalIdentifier()` 先查白名单、
  再退回原 `INTERNAL_IDENTIFIER` 正则，大小写敏感以免误杀正常文案（如 `Record`）。
- **风险检查候选预检的可观测性**：`medicines/services/risk/risk-check.service.ts` 在把
  候选详情异常包装成 400 `VALIDATION_FAILED` 之前 `logger.warn` 记录来源、标识与原始
  message/stack；响应契约不变。
- **候选预检与详情缓存的口径**：确认 `bypassCache=false` 会经 `getOrSetDetail` 预热共享
  详情缓存，「预检不落库」只指 risk-check records 与 records 缓存；已在候选前置检查处
  注明，无行为变更。
- **已下线工具的关键词规则残留**：核实 `src/` 内已无 `search_medical_qa_corpus` /
  `search_medicine_leaflets` 残留，该台账条目本身已过期，删除。

## 跨仓契约：微博 OAuth 全链路移除

- 删除 `providers/weibo-oauth.provider.ts`（及 spec）；同步清理 `oauth.controller.ts`
  两个端点与对应 `registerResponseSchema`、`oauth.dto.ts` 的两个 schema 与推断类型、
  `auth.service.ts` 与 `services/oauth/facade.service.ts` 的 weibo 方法、
  `state.service.ts` 的 weibo 回跳路径、`oauth.types.ts` 的 `OAUTH_PROVIDER_WEIBO`、
  `auth.module.ts` 的 provider 注册。
- 配置面：`EnvKey.WEIBO_*`、`environment.validation.ts` 的 schema、`oauth.config.ts` 的
  `weibo` 项，以及 `docs/reference/environment-variables.md`（front-matter 同步更新）、
  `README.md`、`.env.development.example` / `.env.production.example` 中的对应条目；
  `plans/_naming-lookup.json` 的 weibo 命名映射一并清理。
- 重新导出契约后 `docs/reference/generated/openapi.json` 中 weibo 路径与 schema 归零；
  未跟踪的本地 `.env.development` / `.env.production` 保留未动（已无任何读取方）。

## 契约：`POST /api/v1/medicines/recognize` 响应 schema 补齐

- 新增 `MedicineRecognitionResponse`（`dto/recognition-response.dto.ts`，即识别结果
  schema 的响应别名），`medicines.controller.ts` 的同步端点补 `@ApiResponse` +
  `@SerializeOptions({ schema })` 与 `registerResponseSchema`；
  `medicines.service.ts` 的 `recognizeMedicine` 返回类型收敛为该 schema 的推断类型。
- 生成客户端的响应体现在是类型化对象，Luminous 的 scan 可据此删掉手写 Dio +
  `coerceToStringMap` 解析（对侧同日落地）。

## 台账卫生

- `docs/TODO.md`：删除已完成或已决策的条目（R1/R2/R3/R5、B5/B6、G2、
  `/medicines/recognize` 响应 schema、微博移除清单）；Semantica 段落收敛为「主链、推理与
  硬化已全部落地 + 4 条未闭环项」，并去掉与台账自身重号的 R/D/S/E 编号；R4 落为具名条目
  「health-events ownership shim 的移除」，三处 `TODO(error)` 注释改引该条目、不再引用
  已失效的 `Tasks 8.2/10`；front-matter 与 Last updated 更新为当日。
- 计划 `plans/2026-09-19-semantica-reasoning-and-graph-hardening-plan.md`
  （实施完毕，文件已删）：其非目标（不接生成能力 / Rete / SPARQL / 时态推理 / 决策智能、
  不引入嵌入与 NER 消解链路、推理结论不作诊断建议）按该计划自身声明的归宿补进
  `src/modules/assistant/README.md`；`plans/README.md` 索引同步移除；两条「已知未修」
  （导出阶段读取不受 `REASONING_TIMEOUT_S` 约束）与既有未闭环项留在 TODO。
- **G2 观察期裁决**：`no-empty`（观察期内 0 违规，oxlint 不覆盖）转 error 并
  迁入 `eslint.config.ts`；AST 规则 `dto-validator-missing` 退役（其 class-validator 前提
  已随 zod 迁移消失，命中全部落在响应类上）；其余规则维持 warn 且不再计划转 error，
  `--strict` 不接入门禁。`AGENTS.md` 的 arch:check 小节按结论改写。

## 验证

- `pnpm test:tools` 通过（含退役后的 AST 检查 spec）。
- `pnpm export:openapi`：TSC 0 issue，契约重导出成功且响应 schema 注册路径与真实
  operation 对账无报错。
- `pnpm docs:verify` / `pnpm docs:links` 通过。
- `pnpm arch:check` / `pnpm lint:check` 通过（新增的 `no-empty` error 规则无违规）。
- 微博残留按 `src|test|scripts|prisma` 全量 grep 归零。
