# Migration Log - 2026-07-01

## Lucent

- Synced assistant/public docs to the real retrieval direction: source-split assistant RAG, assistant-only medical QA boundary, and entity-scoped DrugBank retrieval.
- Renamed assistant retrieval tool contracts to `search_medicine_leaflets`, `search_medical_qa_corpus`, `resolve_drugbank_entity`, and `search_drugbank_passages`.
- Added assistant-side vector cursor helpers plus new DrugBank entity-resolution and scoped passage-search services.
- Updated assistant runtime planning/prompt state to carry bounded retrieval-loop metadata and explicit source-priority / no-keyword-fallback instructions.
- Added focused Jest coverage for assistant retrieval dispatch, medical QA retrieval, DrugBank entity resolution, DrugBank passage search, cursor round-trip behavior, and bounded runtime planning.
- Added a first `rebuild-drugbank-rag-index.ts` script skeleton for assistant-side DrugBank narrative passage indexing smoke checks.
- Finished the remaining implementation cleanup: leaflet retrieval now uses the new tool name end-to-end, returns page/cursor metadata, and no longer keeps stale `get_medicine_leaflet_context` envelope values.
- Fixed the leaflet index rebuild script to match the current `cn_medicine_leaflets` schema and enrich vector metadata with linked product names.
- Re-exported `docs/openapi.json` so generated contract output matches the current assistant tool surface.
- Verified the backend with focused assistant suites, full typecheck/build, both RAG script smoke checks, and the full Jest suite.
- Raised the Lucent toolchain baseline from pnpm 10 to pnpm 11.x, pinned CI to 11.9.0, and clarified the local compatibility/recommended-version split in repo docs.
- Added contract-artifact enforcement to Lucent CI: the workflow now re-exports OpenAPI and fails when committed `docs/openapi.json` is stale relative to backend code.
- Migrated Lucent's pnpm 11 build-script policy to the official `allowBuilds` map so local/CI installs no longer depend on interactive `pnpm approve-builds`.
- Cleared the assistant-module ESLint/Prettier backlog, added a repo-local `.gitattributes`, and kept Prettier on `endOfLine: auto` so Windows/Linux newline differences stop surfacing as code-quality failures.
