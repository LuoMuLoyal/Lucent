# 2026-07-03 Security PIN replacement for 2FA

## Schema

- Removed `twoFactorEnabled`, `twoFactorSecret`, and `twoFactorRecoveryCodes` from `User`.
- Added `securityPinEnabled`, `securityPinHash`, `securityPinChangedAt`, and `securityElevationVersion` to `User`.
- Migration: `prisma/migrations/20260703125744_replace_two_factor_with_security_pin/migration.sql`.

## Domain

- New `SecurityPinModule` with `SecurityPinService` and `SecurityElevationGuard`.
- PINs are 6-digit numeric strings hashed with argon2id.
- Enabling, changing, or disabling a PIN increments `securityElevationVersion`, invalidating prior elevation tokens.
- Successful PIN verification mints a 15-minute signed JWT elevation token (`scope: security_elevation`).
- Elevation tokens are read from the `x-security-elevation` header as `Bearer <token>`.

## Settings API

- `POST /api/v1/settings/security-pin/enable`
- `POST /api/v1/settings/security-pin/verify`
- `POST /api/v1/settings/security-pin/change`
- `POST /api/v1/settings/security-pin/disable`

All endpoints require authentication. Verify returns an elevation token and expiry.

## Sensitive route protection

The following routes now require a valid elevation token via `SecurityElevationGuard`:

- `POST /api/v1/account/password`
- `POST /api/v1/account/email`
- `DELETE /api/v1/account/identities/:identityId`
- `POST /api/v1/user/data-export-requests`
- `GET /api/v1/user/data-export-requests/latest`

## Auth cleanup

- Removed `AuthTwoFactorService` and `two-factor.dto.ts`.
- Removed runtime 2FA checks from credential login; login no longer returns `requiresTwoFactor` or `tempToken`.
- Updated `User` mock fixtures across auth specs to use new Security PIN fields.

## Contract

- Regenerated `docs/openapi.json`.

## Meal Analysis closeout

- Added a lightweight safety/length filter to `MealAnalysisVisionService` before stored results are persisted:
  - Maximum lengths: meal description 200 chars, food name 100 chars, portion text 100 chars.
  - Strips `<script>` / `<style>` blocks, remaining HTML tags, and control characters.
  - Drops food items and meal descriptions that fail the shared `AiSafetyPolicyService` (medical/diagnosis patterns).
- Wired `AiSafetyPolicyService` into `DailyRecordsModule` providers.
- Aligned downstream consumers with the shared read-rule matrix:
  - **Today**: `analyzing` meals stay as plain records; `analysis_failed` is surfaced as missing meal-analysis data; `unconfirmed`/`partial` meals are explicitly labeled as estimates.
  - **Report**: `ReportsContextService` now computes a `mealEstimateBreakdown` (confirmed, estimated, partial, analyzing, failed days) in addition to the existing `mealEstimateSeries`.
  - **Assistant**: system prompt now requires the assistant to explicitly flag unconfirmed/partial meal records as estimates and treat `analysis_failed` as unavailable evidence.
- Expanded ADR-0005 to document the shared read-rule matrix and the vision safety filter decision.
- Added/updated tests for `MealAnalysisVisionService`, `TodayAnalysisContextService`, `ReportsContextService`, `ReportsAiSummaryContextService`, and related summary generator/service specs.
- Deleted completed plan file `plans/2026-07-01-meal-analysis-food-composition-plan.md`.

## Assistant RAG + DrugBank closeout

- Added durable chunk tables for assistant RAG sources:
  - `medical_qa_chunks` (question/answer/safety_label)
  - `drugbank_passage_chunks` (drugbank_id, source_field, chunk_text, chunk_index)
- Migration: `prisma/migrations/20260703065114_add_assistant_rag_chunk_tables/migration.sql`.
- Rewrote `scripts/import/medicine/rebuild-drugbank-rag-index.ts` to chunk DrugBank narrative fields, persist to `drugbank_passage_chunks`, and optionally embed into `drugbank_passage_embeddings` via PGVectorStore. Supports `--limit`, `--embed-limit`, `--embed-batch-size`, and `--dry-run`.
- Added a product-level vector resolve stage to `AssistantToolLeafletReadService`: `search_medicine_leaflets` now aggregates chunk scores over `leaflet_embeddings` to resolve the product before retrieving chunks, and returns the resolved product in `result.resolvedProduct`.
- Updated `src/modules/assistant/prompts/assistant-system.prompt.ts` to describe the leaflet product-resolution step.
- Added/updated `assistant-tool-leaflet-read.service.spec.ts` tests for product resolution, `productId` filter, `sourceField` filter, and resolved-product envelope shape.
- Fixed `scripts/dev/meal-analysis-grounding-smoke.ts` to construct the now-required `AiSafetyPolicyService` dependency for `MealAnalysisVisionService`.
- Smoke-tested both RAG pipelines with 100-record limits:
  - DrugBank: imported 77 drugs, chunked into 790 `drugbank_passage_chunks`, embedded 100 chunks.
  - Medical QA: filtered 100 records into `medical_qa_chunks`, embedded 99 safe/caution chunks.
- Deleted completed plan file `plans/2026-07-01-assistant-rag-drugbank-plan.md`.

## Documentation drift cleanup

- Aligned cross-source CN -> DrugBank mapping language across `docs/public/data-sources.md`, `docs/Current_State.md`, and `docs/TODO.md` with the current product decision: Lucent does **not** maintain a runtime mapping table or alias map.
- Removed `medicine_source_matches` from the recommended durable tables list in `docs/public/data-sources.md`; the table is not in the Prisma schema and represents an abandoned manual-mapping approach.
- Replaced references to a “future reviewed bridge” or “future mapping pipeline” with the source-split assistant tool chain (`search_cn_medicine_products`, `get_cn_medicine_detail`, `get_drugbank_detail`, `search_medicine_leaflets`, `search_drugbank_passages`).
- Clarified that `ProductsEnriched.drugbank_ids` exists in the source workbook but is not populated and is not used as a runtime bridge.

## Report Export async worker hardening

- Extracted a shared `DataExportProcessorService` that owns the export processing sequence: status -> `processing`, report fetch, PDF generation, COS upload, status -> `completed`, and notification.
- Refactored `DataExportQueueService` to delegate BullMQ jobs to `DataExportProcessorService` instead of duplicating the PDF/upload/status logic.
- Refactored `DataExportService` inline fallback to call `DataExportProcessorService` and then read the completed request row, removing the duplicated inline processing logic.
- Registered `DataExportProcessorService` in `DataExportModule` providers.
- Added `DataExportProcessorService` unit tests covering not-found, hospital/monthly/print PDF dispatch, failure handling, and notification-error swallowing.
- Added `DataExportQueueService` unit tests covering disabled-queue behavior, worker initialization, enqueue options, and job-to-processor delegation.
- Updated `DataExportService` unit tests to assert the new processor delegation path and the queue-configured async path.
