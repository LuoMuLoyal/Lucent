# Migration Log - 2026-07-02

## Lucent

- Consolidated duplicated local utility functions into shared common utils:
  - `normalizeNullableText` moved to `src/common/utils/string.utils.ts` and reused across meal-analysis worker, vision, and dish-decomposition services.
  - `extractJsonObject` extracted to new `src/common/utils/json.utils.ts` and reused by vision and dish-decomposition response parsing.
  - `asNullableString` / `asNullableNumber` replaced with common `normalizeNullableText` and new `normalizeNullableNumber` in `src/common/utils/number.utils.ts`.
  - `roundNumber` moved to `src/common/utils/number.utils.ts`.
  - `commonCharacterCount` moved to `src/common/utils/string.utils.ts` and fixed its duplicate-counting bug by using character-frequency maps.
- Replaced the hacky `DishTemplatePrismaAccess` type in `MealDishDecompositionService` with the proper Prisma-generated payload type.
- Hardened silent `catch { return null; }` blocks in meal-analysis vision and dish-decomposition parsers so JSON parse failures are logged with context instead of swallowed.
- Fixed the fuzzy-ingredient query in `MealIngredientGroundingService` to filter candidates with an indexed `startsWith` predicate on `normalizedName`/`searchText` instead of scanning the whole table.
- Extracted hard-coded meal-analysis thresholds (fuzzy scores, portion grams, nutrition commentary thresholds) into named constants with TODO markers for future configuration.
- Updated the meal-ingredient-grounding test mock to distinguish exact and filtered fuzzy queries now that both carry `where` clauses.
- Verified the backend with `pnpm typecheck`, `pnpm lint:check`, and the full Jest suite.
- Removed campus-scoped support resources from the public contract: `support-resources` now only exposes `help` / `about`, reference-data `updatedAt` was bumped to `2026-07-02T00:00:00.000Z`, and the exported OpenAPI/client contract was regenerated to drop the `campus` scope.
- Synced public/current docs with the new runtime boundary: Mine/Settings contract, environment contract, and current-state docs now describe campus-service removal and the remaining `help` / `about` support-resource scope.
- Recorded the current CN -> DrugBank bridge status in `docs/public/data-sources.md`: the local V2 workbook snapshot still has 0 populated `drugbank_ids`, so cross-source mapping is not treated as solved runtime data.
