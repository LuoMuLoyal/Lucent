# 2026-07-17 迁移日志

## 部署加固计划落地（plans/2026-07-16-deployment-hardening.md）

15 项审查发现中除「LLM 熔断器」外全部落地。代码变更随本次提交，文档同步完成。

### 单 slot 停机部署（#1/#2/#3，含 ADR-0004 修订）

砍掉蓝绿双 slot，改单 slot 停机部署（决策见 ADR-0004 的 2026-07-17 修订）：

- `deploy/deploy.ts` 全面重写为 12 步单 slot 流程：
  1. 前置检查 + 补齐目录 + 运行 `render-configs.sh`（失败仅警告）
  2. 读取当前 `LUCENT_IMAGE` 作为回滚目标
  3. `.env` → `.env.previous` 快照移到**任何修改之前**（修复旧蓝绿流程回滚拿到新版本的 bug）
  4. 启动 postgres + redis 并等健康
  5. 部署前 `pg_dump | gzip` 快照（`data/backups/pre-deploy-*`，保留 10 份；失败即中止发布，app 未停、零影响）
  6. `docker compose stop app`（停机窗口开始，15~45s）
  7. 新镜像独立容器跑 `prisma migrate deploy`（失败则重启旧 app）
  8. 行级更新 `.env` 的 `LUCENT_IMAGE`（保留注释格式，`chmod 600`）
  9. 启动 app + 健康门禁（轮询容器健康状态 ~150s，失败打印 200 行日志并自动恢复上一镜像）
  10. `nginx -s reload`（重解析 upstream 缓存的容器 IP，否则全站 502）
  11. smoke test（失败同样自动恢复）
  12. 完成
- `deploy/compose.yml`：`app-blue`/`app-green` 双服务合并为单 `app` 服务（`lucent-app`）
- `deploy/nginx/nginx.conf`：upstream 简化为单后端 `app:3000`，注释说明 reload 必要性
- `deploy/smoke.ts`：移除双 slot 判定，固定容器名 `lucent-app`，required services 加 `app`
- 回滚：`node deploy.ts --rollback` 从 `.env.previous` 取上一镜像 tag 重部；**schema 不回退**，文档已明确
- Migration 纪律写入 deployment.md：stop → migrate → start；migration 计入停机窗口；破坏性变更建议 expand-contract（降级为优化项）；CI 有破坏性 migration 检测（见下）

### 生产改人工发布（#11）

- `lucent-production.yml` 删除 `workflow_run` 自动触发（原逻辑：main 分支 CI 成功即自动上生产），仅保留 `workflow_dispatch`，`action` 输入 `deploy`/`rollback`，限 main 分支
- staging（`lucent-staging.yml`）保持 CI 成功自动部署不变
- 理由：单 slot 下任何自动发布都会在意料外时间制造停机窗口

### 告警通道（#4）

- 新增 `deploy/alertmanager/alertmanager.yml`（模板）：Alertmanager 原生 `wechat_configs` 企业微信应用消息，无第三方 webhook adapter；compose 新增 alertmanager 服务（`profiles: ['alerting']`，默认不启动）
- 新增 `deploy/prometheus/rules/lucent.yml`：`LucentDown`、`LucentHigh5xxRate`（>5%/5m）、`BullMQJobFailures`（15m>5）、`BullMQWaitingBacklog`（>100/10m）、`NodeEventLoopLagHigh`（p99>0.5s/10m）、`HostDiskSpaceLow/Critical`（15%/10%）、`LucentCertExpiryWarning/Critical`（14/7 天）
- 新增 `deploy/render-configs.sh`：prometheus/alertmanager 配置改为含 `${VAR}` 占位符的模板，宿主机渲染到 `.rendered/`（含密钥，gitignore，不离开服务器）；`deploy.ts` pre-flight 自动调用
- `deploy.ts` 新增 `notifyDeploy`：发布失败/回滚经 `WECOM_WEBHOOK_URL` 企业微信群机器人通知（best-effort，不阻塞）

### 数据库备份（#5）

- 新增 `deploy/backup.sh`：每日 `pg_dump | gzip` → `data/backups/daily-*`（本地保留 7 份）；`.env` 配齐 `COS_*` 且有 coscli（优先）/coscmd 时上传 COS `backups/` 前缀；COS 侧 30 天保留走 bucket 生命周期规则
- 部署前快照与 deploy.ts 复用同一 `data/backups/` 目录，互不影响
- 新增 runbook `docs/01-reference/how-to/restore-database-backup.md`：每季度恢复演练流程（选文件 → 清库 → 灌入计时 → 验证 → 记录）+ 生产灾难恢复差异点

### SSE 优雅关闭（#6）

- 新增 `src/common/api/sse-connection-registry.service.ts` + `sse.module.ts`：注册表追踪全部活跃 SSE 连接，`beforeApplicationShutdown` 先向每个连接推终止 `error` 事件（`{ message, reason: 'server_shutdown' }`，复用现有 error 事件结构，客户端无合同变更）再 `end()`
- assistant / reports / today-analysis 三个 SSE 控制器接入注册表
- `deploy/compose.yml`：`stop_grace_period` 30s → 60s
- 文档约定低峰发布

### 基础设施指标与端口（#7/#8/#10/#12）

- compose 新增 `postgres-exporter`、`redis-exporter`、`node-exporter`（textfile collector 目录 `./data/node-exporter-textfile`）；prometheus 模板新增 postgres/redis/node 三个 job
- Grafana `127.0.0.1:3001:3000`、Prometheus `127.0.0.1:9090:9090` 本地回环发布（修复文档指示 SSH 隧道但端口未发布的问题）
- `src/common/queue/queue.factory.ts`：30s interval 轮询 `queue.getJobCounts()` 刷新 `bullmq_active_jobs` / `bullmq_waiting_jobs` gauge
- postgres compose command 追加 `-c log_min_duration_statement=500`；`src/prisma/prisma.service.ts` log 配置 `['warn','error']`

### 请求级日志（#9）

- `src/setup-app.ts`：Fastify `onResponse` hook 写结构化完成日志（reqId/method/route/status/durationMs）
- `src/common/logger/logger.config.ts`：Winston format 从 AsyncLocalStorage 注入顶层 `requestId` 字段；`api-exception.filter.ts` 配合改造（reqId 不再只嵌在 message 文本）

### CI 加固（#3.3/#13）

- `lucent-ci.yml` 新增 PR 破坏性 migration 检测：`prisma migrate diff` 对比 base schema，检出 `DROP TABLE|DROP COLUMN|TRUNCATE|SET DATA TYPE` 打 warning（不阻断）
- `lucent-ci.yml` 新增 docker job：`docker build`（不推送）+ Trivy `--severity HIGH,CRITICAL --exit-code 1` 严格扫描

### 零散硬化（#14/#15）

- 新增 `deploy/check-cert.sh`：解析证书 `notAfter`，原子写 textfile 指标 `lucent_cert_expiry_days`（<14 天 stderr 警告）；配合证书过期告警规则；续期仍为手工运维
- nginx：`limit_req_zone`（每 IP 20r/s）+ `limit_conn_zone`，`location /` 限 `burst=40 nodelay` + 50 并发；SSE 路径只限连接数不限速率
- 限流确认为进程内存存储：`app.module.ts` 加设计注释，`environment.validation.ts` 修正「REDIS_URL required for distributed rate limiting」的错误注释（Redis 用于 BullMQ）
- `.env` 写入改为行级替换 + `chmod 600`（见单 slot 流程步骤 8）

### 文档同步

- `docs/01-reference/deployment.md` 全面重写：单 slot 流程、停机窗口与 migration 纪律、SSE、备份、TLS 证书监控、告警、可观测性组件表、模板渲染、触发方式
- `docs/01-reference/adr/0004-deployment-model.md`：补记 2026-07-17 拓扑变更决策（蓝绿 → 单 slot）；ADR 索引同步
- `docs/01-reference/how-to/deploy.md`：修正触发方式、手动部署路径、回滚命令（移除不存在的 `deploy:assets:check`）
- `docs/01-reference/environment.md`：生产服务列表更新（单 app + exporters + alertmanager profile）
- `.env.production.example`：新增 `GRAFANA_ADMIN_PASSWORD`、`WECOM_*`（告警）、`WECOM_WEBHOOK_URL`（发布通知）、`COS_*`（备份异地副本）
- `ROADMAP.md`：Backup & Recovery 标记完成

### 遗留

- LLM 调用熔断器未实现（已有 `AI_MODEL_TIMEOUT_MS=10s` 超时 + 错误分类重试），保留在加固计划文件中

## 跨模块数据访问治理（架构审查计划 #1）

### ADR-0009：跨模块数据访问治理

- 新增 `docs/01-reference/adr/0009-cross-module-data-access.md`：表归属表（User/UserIdentity/UserSession → user/auth 域，且 User 表字段分组——`securityPin*`/`securityElevationVersion` 归 security-pin 域；其余各表归属见 ADR）；读规则（跨模块读允许，软删除模型必须用 `nonDeleted`，高频跨读表收敛提供方 reader port）；写规则（跨模块写必须经 owning module 导出 service；具名例外：testing-support 夹具、AdminJS、security-pin 字段组、`common/llm` 读 userSetting）；`forwardRef` 过渡说明
- `docs/01-reference/adr/README.md` 索引同步；`AGENTS.md` Module Export Rules 增加指向 ADR-0009 的条目

### Reader port 收敛（8 处直查 → 2 个 port）

- `daily-records/repositories/daily-record.repository.ts`：新增 `DailyRecordFact` 与 `DailyRecordReaderPort.listFactsInRange(userId, from, to, kinds?)`（`userId + nonDeleted + occurredAt 区间`，规范排序 `occurredAt asc, createdAt asc`），实现挂 `DailyRecordRepository`
- `medicine-dose-logs/repositories/dose-log.repository.ts`：新增 `DoseLogFact` 与 `MedicineDoseLogReaderPort.listFactsInRange(userId, from, to)`（`scheduledFor` 区间，`scheduledFor asc`）；`repositories/index.ts` 同步导出
- 消费方改接（查询语义不变；原 `createdAt desc` 场景改为内存重排）：
  - `today-suggestion`：`collectors/record.service.ts`（2 处）、`collectors/medication.service.ts`（1 处）、`lifecycle/baseline.service.ts`（1 处，传 kinds）
  - `today-analysis/services/context.service.ts`（2 处）
  - `reports/dashboard/context.service.ts`（2 处，原排序与 reader 规范排序一致）
- 模块接线：`today-suggestion.module.ts` 新增 `forwardRef(DailyRecordsModule/MedicineDoseLogsModule)`（反向边为缓存失效调用，计划 #2 落地后移除）；`today-analysis.module.ts`、`reports.module.ts` 直接 imports 两模块；`medicine-dose-logs.module.ts` 新增 `exports: [MedicineDoseLogReaderPort]`；`daily-records.module.ts` repository 注册统一为具体类 + `useExisting` 并导出 reader port；新建 `daily-records/repositories/index.ts` barrel

### account 写路径改道 UserService

- `user/services/user.service.ts`：新增 `findByIdWithIdentities(id)`（含 identities 升序）与 `unlinkIdentity(identityId)`
- `account/services/account.service.ts`：移除 `PrismaService` 注入，全部改经 `UserService`；last-method 保护等业务规则留在 account；`account.module.ts` 新增 `UserModule` import

### 测试

- 7 个 spec 同步改 mock（collectors×2、baseline、today-analysis context、reports dashboard context、account、user）；两个 repository spec 新增 reader 用例
- 验证：`pnpm lint:check` ✓、`pnpm typecheck` ✓、`pnpm build` ✓（734 文件）、`pnpm test` ✓（232 文件 / 2396 测试）；e2e 未跑（本机 Docker 守护进程不可用），需在有 Docker 的环境补跑 `pnpm test:e2e`

## 代码审查安全修复（plans/lucent-review-2026-07-17.md）

根据 2026-07-17 全仓库代码审查报告，修复 3 个 🔴 严重问题和 2 个 🟡 警告。

### #1 数据导出 setting key（🔴 — 不适用）

- 审查报告提及 `dataExportMonthlyRemindersEnabled` 硬编码在 `data-export.service.ts:78`，但经 `git log -S` 全历史搜索和 `rg` 全仓库扫描，该字符串从未出现在源代码中。`export.service.ts` 使用 `kind: 'hospital'` / `kind: 'monthly'` 作为 DataExportRequest 模型的枚举值，不涉及 userSetting 表。**无需修复。**

### #2 activateConversation IDOR 越权（🔴 修复）

- `src/modules/assistant/repositories/conversation.repository.ts`：`activateConversation` 方法中 `tx.assistantConversation.update` 的 `where` 条件从 `{ id: conversationId }` 改为 `{ id: conversationId, userId }`，与同文件 `archiveConversation` 的写法保持一致。攻击者无法通过遍历 conversationId 激活他人对话。

### #3 persistTurn IDOR 越权（🔴 修复）

- `src/modules/assistant/repositories/conversation.repository.ts`：`persistTurn` 方法中 `tx.assistantConversation.update` 的 `where` 条件从 `{ id: input.conversationId }` 改为 `{ id: input.conversationId, userId: input.userId }`。攻击者无法向他人对话注入消息。

### #4 刷新令牌竞态条件（🟡 修复）

- `src/modules/auth/repositories/session.repository.ts`：新增 `claimSessionForRefresh(id)` 方法，使用 `deleteMany({ where: { id, revokedAt: null, expiresAt: { gt: now() } } })` 原子性声明会话——只有第一个并发请求成功删除会话，后续请求返回 `false`。
- `src/modules/auth/services/token.service.ts`：`refresh` 方法从「先生成新令牌再删除旧会话」改为「先原子性声明旧会话再生成新令牌」。若声明失败（会话已被并发请求占用），抛出 `REFRESH_TOKEN_INVALID`。这消除了同一 refresh token 产生多个有效会话的竞态窗口。

### #5 通知去重防御性解析加固（🟡 修复）

- `src/modules/notifications/services/notifications.service.ts`：`matchesScope` 方法对 `payload['source']` 和 `payload['date']` 增加显式 `typeof === 'string'` 类型检查，防止恶意构造的非字符串嵌套对象导致误判匹配。`duplicateIds` 增加 `.slice(0, 50)` 显式上限，与 `findMany` 的 `take: 50` 构成双重防护。

### 验证

- `pnpm lint:check` 零问题
- `pnpm build` 编译成功（733 文件）
- `pnpm test -- src/modules/auth src/modules/assistant src/modules/notifications`：全部 402 个测试通过（token.service 22 + session.repository 16 + auth.service 246 + assistant 全模块 331 + notifications 49 - 重叠后实际 402 个独立测试）

## 建议引擎缓存失效：事件驱动重构（架构审查 #2）

来源：`plans/2026-07-16-architecture-review.md` 高优先级 #2。

- **安装 `@nestjs/event-emitter`**，`app.module.ts` 注册 `EventEmitterModule.forRoot()`
- **定义 domain event 常量与类型**：`src/common/events/domain-events.ts`，5 个事件（`DAILY_RECORD_CHANGED` / `DOSE_LOG_CHANGED` / `REMINDER_CHANGED` / `HEALTH_CONTEXT_CHANGED` / `SETTINGS_CHANGED`）及对应 payload 类型
- **创建 `SuggestionCacheInvalidationListener`**：`today-suggestion/services/cache/suggestion-cache-invalidation.listener.ts`，`@OnEvent` 订阅 5 个事件，触发 `invalidateSignals` / `invalidateBaseline`；catch + logger.warn 保证事件失败不阻塞
- **写路径改为 emit 事件**：
  - `daily-records/records.service.ts`：移除 `SuggestionCacheService` 直接注入，写路径后 `emitAsync(DAILY_RECORD_CHANGED)`
  - `medicine-dose-logs/dose-logs.service.ts`：同上，emit `DOSE_LOG_CHANGED`
  - `medicine-reminders/reminders.service.ts`：新增 `emitReminderChanged`，create/update/delete 后 emit
  - `user-health-context/health-context.service.ts`：新增 `emitHealthContextChanged`，profile/allergy/condition/medicine 写后 emit
  - `user-settings/user-settings.service.ts`：新增 `emitSettingsChanged`，updateSettings 后 emit
- **移除反向依赖**：`today-suggestion.module.ts` 移除 `forwardRef(DailyRecordsModule)` 和 `forwardRef(MedicineDoseLogsModule)`，移除 `SuggestionCacheService` export；资源模块不再 import 聚合层
- **ESLint override**：`*-invalidation.listener.ts` 禁用 `no-unsafe-*`（`@OnEvent` 装饰器类型推断限制）
- **测试**：新增 `suggestion-cache-invalidation.listener.spec.ts`；5 个 spec 文件 mock 从 `SuggestionCacheService` 改为 `EventEmitter2`
- **验证**：`pnpm lint:check` ✓、`pnpm typecheck` ✓、`pnpm build` ✓（738 文件）、7 个受影响 spec 全部通过（94 测试）

## PrismaService 软删除扩展 + 慢查询日志（架构审查 #3）

来源：`plans/2026-07-16-architecture-review.md` 高优先级 #3。

- **`$extends` 软删除扩展**：新建 `src/prisma/prisma.extension.ts`，使用 `Prisma.defineExtension` 为含 `deletedAt` 的 4 个模型（User、UserDailyRecord、UserMedicineReminder、UserMedicineDoseLog）添加 `nonDeleted` 查询变体命名空间，每个提供 `findMany`/`findFirst`/`findFirstOrThrow`/`findUnique`/`findUniqueOrThrow`/`count` 六个方法，自动注入 `deletedAt: null` 到 `where` 子句
- **`PrismaService` 改造**：
  - 构造函数注入 `WINSTON_MODULE_PROVIDER`（Winston logger），新增 `SLOW_QUERY_THRESHOLD_MS` 环境变量读取（默认 500ms）
  - `log` 配置从 `['warn', 'error']` 改为 `['warn', 'error', { emit: 'event', level: 'query' }]`
  - `$on('query')` 注册慢查询处理器：duration ≥ 阈值时调用 `winstonLogger.warn`，记录 `durationMs`/`query`（参数化 SQL，不含实际参数值）/`target`/`requestId`（从 AsyncLocalStorage 读取）
  - 新增 `_extended` 字段存储 `$extends` 后的客户端，`nonDeleted` getter 暴露 4 个模型的 `nonDeleted` 命名空间
  - `$on` 调用使用类型断言绕过 Prisma 7 `LogOpts` 泛型推断限制（`extends PrismaClient` 时 `LogOpts` 默认为 `never`，运行时事件正常触发）
- **环境变量**：`EnvKey.SLOW_QUERY_THRESHOLD_MS` 新增；`constants.ts` 新增 `DEFAULT_SLOW_QUERY_THRESHOLD_MS = 500`；`environment.validation.ts` 新增 Joi schema（10–60000ms）
- **测试**：`prisma.service.spec.ts` 全量重写，13 个测试覆盖构造、log 配置、`$on` 注册、`$extends` 调用、`nonDeleted` getter、生命周期、慢查询日志触发/不触发；使用 `makeConfigService` helper 避免 enum 比较 lint 错误
- **验证**：`pnpm lint:check` ✓、`pnpm typecheck` ✓、`pnpm build` ✓（739 文件）、`pnpm test -- src/prisma/prisma.service.spec.ts` ✓（13 测试通过）
- **迁移路径**：现有 20+ 处 `deletedAt: null` 手写查询点可逐步迁移到 `prisma.nonDeleted.<model>.findMany(...)`，但非破坏性变更，旧写法继续工作

## 架构文档与代码漂移修复（架构审查 #4）

来源：`plans/2026-07-16-architecture-review.md` 高优先级 #4。

- **architecture.md 依赖图补 today-suggestion 节点**：新增 `todaySuggestion` 节点到 mermaid 图，标注"75 files, 44 providers, 第二大 feature module"；补齐 daily-records/dose-logs → today-suggestion 依赖、today-suggestion → llm/daily/doseLogs/reminders/healthCtx/settings/notifications/prisma 全部边
- **architecture.md 新增队列拓扑图**：mermaid 图展示 `BullmqQueueFactory` + `BaseAsyncQueueService` 基础设施与 7+1 个队列服务（meal-analysis、data-export、medicine-recognition、report-summary、clinic-pdf、today-analysis、suggestion-explanation、mail）的关系；附表格列出每个队列的模块、并发数、结果 TTL、用途
- **architecture.md common/ 目录清单补全**：`common/llm/` 修正（原文已是 `llm/` 但未列全），新增 `common/queue/`、`common/metrics/`、`common/events/`、`common/storage/`、`common/types/`
- **AGENTS.md `ai/` → `llm/`**：`common/` 目录清单中的 `ai/` 修正为 `llm/`；新增 `queue/`、`metrics/`、`events/`、`storage/`、`types/` 五个缺失目录
- **合并 `common/queue` 与 `common/queues`**：`base-async-queue.service.ts` 和 spec 从 `common/queues/` 移入 `common/queue/`，删除空目录；`common/queue/index.ts` barrel 新增 `export * from './base-async-queue.service'`；5 个消费方 import 路径从 `common/queues/base-async-queue.service` 改为 `common/queue` barrel
- **验证**：`pnpm typecheck` ✓、`pnpm lint:check` ✓、`pnpm build` ✓（739 文件）、`pnpm test -- src/common/queue` ✓（35 测试通过）

## 向量检索工具去重 + 队列可观测性增强（架构审查 #5/#7/#8）

来源：`plans/2026-07-16-architecture-review.md` 中优先级 #5、#7、#8。

### #5: VectorStoreFactory 抽取（三个向量检索工具去重）

- **新建 `VectorStoreFactory`**：`src/modules/assistant/tools/vector-store.factory.ts`，注入 `LlmRuntimeService`（复用 `createEmbeddingModel()`）+ `ConfigService`（读取 `DATABASE_URL`），按 `tableName` 懒初始化 + 缓存 `PGVectorStore` 实例
- **重构三个工具**：
  - `leaflet/read.service.ts`：移除 `vectorStore`/`initPromise`/`getVectorStore`/`initializeStore`/`createEmbeddings`（≈ 40 行），改为注入 `VectorStoreFactory` + 调用 `getStore(EMBEDDINGS_TABLE)`
  - `drugbank/search.service.ts`：同上（≈ 40 行 → 1 行调用）
  - `knowledge/medical.service.ts`：同上（≈ 40 行 → 1 行调用）
- **消除三份 `OpenAIEmbeddings` 实例**：三个工具各自 `new OpenAIEmbeddings` → 统一由 `LlmRuntimeService.createEmbeddingModel()` 创建
- **消除三份 pg 连接池**：`PGVectorStore` 各自创建 `postgresConnectionOptions` → `VectorStoreFactory` 按 tableName 缓存，同表只创建一次
- **模块注册**：`assistant.module.ts` providers 新增 `VectorStoreFactory`；`tools/index.ts` barrel 新增导出
- **测试**：3 个 spec 文件 mock 从 `configService`（返回 DB URL + AI 配置）改为 `mockVectorStoreFactory`（直接返回 mock `PGVectorStore`），不再需要 mock `@langchain/openai` 的 `OpenAIEmbeddings` 构造
- **验证**：54 测试通过（leaflet 12 + drugbank 3+2 + knowledge 2 + queue 23+12+2）

### #7: ADR-0004 补队列拓扑章节

- **ADR-0004 新增 "BullMQ Worker Topology" 章节**：记录当前所有 worker 跑在 API 进程内的设计决策、单 slot 部署消除了 cron 双触发顾虑、Prometheus 指标和告警规则已就位；记录中期 worker 拆分计划（同镜像不同 command、`--worker-only` flag、cron 移入 worker 进程、compose.yml 新增 worker service）
- **不涉及代码变更**——纯文档更新，明确设计意图和未来演进路径

### #8: 异步任务 failed job 结构化 error 日志

- **`BaseAsyncQueueService.processJob`** catch 块：从 `this.logger.error(message)` 升级为 `this.logger.error(message, stack, { queue, jobId, errorLabel, errorMessage })` 结构化日志，Winston JSON 输出含完整上下文
- **`BullmqQueueFactory` worker `failed` 事件**：从 `logger.error(message, stack)` 升级为 `logger.error(message, stack, { queue, jobId, attemptsMade, failedReason })`，记录重试次数
- **Grafana alert rules 检查**：`deploy/prometheus/rules/lucent.yml` 已有 `BullMQJobFailures`（15 分钟内 >5 失败 → warning）和 `BullMQWaitingBacklog`（等待 >100 持续 10 分钟 → warning），无需新增
- **验证**：`pnpm lint:check` ✓、`pnpm typecheck` ✓、`pnpm build` ✓（740 文件）、`pnpm test -- src/modules/assistant/tools src/common/queue` ✓（54 测试通过）
