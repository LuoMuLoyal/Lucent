# Migration Log — 2026-08-14

## Persist Clinic Summary Shares — 可撤销的就诊摘要分享表

- 新增 `UserClinicSummaryShare` 模型（`user_clinic_summary_shares` 表）：`id`、`userId`（FK → users，onDelete Cascade）、`tokenHash`（@unique，仅存 sha256 哈希，明文 token 只在创建时返回一次）、`eventId`/`dateFrom`/`dateTo`（eventId 或 dateFrom+dateTo 二选一的 scope）、`selectedFields`（`ClinicSummaryShareField[]`：event_overview/symptom_changes/medication_slots/water/sleep/notes）、`expiresAt`、`revokedAt`、`firstAccessedAt`、`lastAccessedAt`、`accessCount`（默认 0）、`createdAt`；`@@index([userId, createdAt])`。
- 不存储摘要 JSON 与明文 token；响应在读取时按当前授权 scope 重建，访问记录（accessCount/firstAccessedAt/lastAccessedAt）在成功打开时原子更新。
- 迁移 `20260814000000_persist_clinic_summary_shares` 已部署到 dev（127.0.0.1:15432/lucent）与 test（127.0.0.1:5432/lucent）。

## Visit Summary and Product Measurement — Task 2 可撤销的就诊摘要分享服务

- 新增 `ShareService`（`src/modules/reports/services/clinic-summary/share.service.ts`）并注册到 ReportsModule providers（暂不导出）：`createShare(userId, input)` 生成高熵 token（`randomBytes(32).toString('base64url')`，创建时返回且仅返回一次），持久化 sha256 hex 哈希与 `expiresAt`（`DEFAULT_SHARE_TTL_DAYS = 7`）；`getSharedSummaryByToken(token)` 按哈希查询，unknown/expired/revoked 一律返回 null，成功打开时单次 prisma update 原子记录 `accessCount: { increment: 1 }` 与 `lastAccessedAt`（首次打开同时写 `firstAccessedAt`）；`revokeShare(userId, shareId)` 以 `updateMany({ where: { id, userId } })` 做所有权限定撤销，非本人或未知 id 返回 false。
- 输入校验（`BadRequestException`）：`selectedFields` 非空、仅允许固定枚举 `ClinicSummaryShareField`（event_overview/symptom_changes/medication_slots/water/sleep/notes）；scope 为 eventId XOR (dateFrom AND dateTo)，同时指定或两者皆缺均拒绝。
- 存储面只含 token 哈希与 scope/字段选择，不存摘要 JSON、不存明文 token；读取返回 share 记录（scope + selectedFields），响应由 Task 4 按当前授权 scope 重建。spec 16 tests（含哈希≠明文、未知/过期/撤销为 null、原子访问记录单次调用、所有权撤销、字段与 scope 校验）通过；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check` 通过。Task 1 的 13 条红测试（VS3/VS4 归属）保持红。
