# Migration Log — 2026-08-14

## Persist Clinic Summary Shares — 可撤销的就诊摘要分享表

- 新增 `UserClinicSummaryShare` 模型（`user_clinic_summary_shares` 表）：`id`、`userId`（FK → users，onDelete Cascade）、`tokenHash`（@unique，仅存 sha256 哈希，明文 token 只在创建时返回一次）、`eventId`/`dateFrom`/`dateTo`（eventId 或 dateFrom+dateTo 二选一的 scope）、`selectedFields`（`ClinicSummaryShareField[]`：event_overview/symptom_changes/medication_slots/water/sleep/notes）、`expiresAt`、`revokedAt`、`firstAccessedAt`、`lastAccessedAt`、`accessCount`（默认 0）、`createdAt`；`@@index([userId, createdAt])`。
- 不存储摘要 JSON 与明文 token；响应在读取时按当前授权 scope 重建，访问记录（accessCount/firstAccessedAt/lastAccessedAt）在成功打开时原子更新。
- 迁移 `20260814000000_persist_clinic_summary_shares` 已部署到 dev（127.0.0.1:15432/lucent）与 test（127.0.0.1:5432/lucent）。

## Visit Summary and Product Measurement — Task 2 可撤销的就诊摘要分享服务

- 新增 `ShareService`（`src/modules/reports/services/clinic-summary/share.service.ts`）并注册到 ReportsModule providers（暂不导出）：`createShare(userId, input)` 生成高熵 token（`randomBytes(32).toString('base64url')`，创建时返回且仅返回一次），持久化 sha256 hex 哈希与 `expiresAt`（`DEFAULT_SHARE_TTL_DAYS = 7`）；`getSharedSummaryByToken(token)` 按哈希查询，unknown/expired/revoked 一律返回 null，成功打开时单次 prisma update 原子记录 `accessCount: { increment: 1 }` 与 `lastAccessedAt`（首次打开同时写 `firstAccessedAt`）；`revokeShare(userId, shareId)` 以 `updateMany({ where: { id, userId } })` 做所有权限定撤销，非本人或未知 id 返回 false。
- 输入校验（`BadRequestException`）：`selectedFields` 非空、仅允许固定枚举 `ClinicSummaryShareField`（event_overview/symptom_changes/medication_slots/water/sleep/notes）；scope 为 eventId XOR (dateFrom AND dateTo)，同时指定或两者皆缺均拒绝。
- 存储面只含 token 哈希与 scope/字段选择，不存摘要 JSON、不存明文 token；读取返回 share 记录（scope + selectedFields），响应由 Task 4 按当前授权 scope 重建。spec 16 tests（含哈希≠明文、未知/过期/撤销为 null、原子访问记录单次调用、所有权撤销、字段与 scope 校验）通过；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check` 通过。Task 1 的 13 条红测试（VS3/VS4 归属）保持红。

## Visit Summary and Product Measurement — Task 2 代码质量修复

- `getSharedSummaryByToken` 返回值收敛为类型化读模型 `ShareReadModel`（shareId/scope/selectedFields/expiresAt/revokedAt/firstAccessedAt/lastAccessedAt/accessCount），不再暴露原始实体的 `tokenHash` 与 `userId`，Task 4 响应序列化不可能泄漏哈希或属主；`accessCount` 返回记录后的视图（读时值 +1）。
- 撤销竞态关闭：访问记录由单次守卫 `updateMany({ where: { id, revokedAt: null, expiresAt: { gt } } })` 完成——读取与写入之间被撤销/过期则 `count === 0` 返回 null 且不记访问，`updateMany` 不会在行消失时抛 P2025；保留 `increment: 1` 单调用语义，首次打开写 `firstAccessedAt`、后续仅刷新 `lastAccessedAt`。
- 错误抛出改用 common barrel 的 `badRequest()` 助手（`{ code: BAD_REQUEST, message }` 信封，与 ai-summary/auth 服务一致，不再 `throw` 裸 BadRequestException）。
- 校验边界补强：非法日期（`Number.isNaN(getTime())`）拒绝，`dateFrom > dateTo` 拒绝；`selectedFields` 持久化前去重（`[...new Set()]`，保留首次出现顺序）。
- 注释补记：token 哈希唯一索引碰撞概率 ~2^-256，冲突时插入失败而非泄漏，不做重试；分享 TTL 7 天为有意区别于旧 24h 摘要缓存 TTL（持久化授权可随时撤销/过期）。
- `migration.sql` 去除 EOF 多余空行；`git diff --check` 干净。spec 扩至 22 tests（新增 createShare 结果形状、去重、非法日期、dateFrom>dateTo、读-写窗口撤销、读模型形状断言；抽取 `mockCreatedShare` 助手消重三个相同 mockImplementation）；`pnpm test` 3160 passed / 13 failed（13 条仍为 Task 1 红测试）、`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check` 通过。

## Visit Summary and Product Measurement — Task 3 就诊摘要支持事件范围与字段选择

- 新增 `ClinicSummaryRequestDto`（`src/modules/reports/dto/clinic-summary-request.dto.ts`）：scope 二选一 `eventId` 或 `dateFrom/dateTo`（event 优先，`@ValidateIf` 在 eventId 缺失时才校验日期对）；`selectedFields` 仅接受固定枚举 `ClinicSummaryShareField`（event_overview/symptom_changes/medication_slots/water/sleep/notes），空数组与未知值拒绝；导出 `CLINIC_SUMMARY_MAX_RANGE_DAYS = 30`（对齐产品既有最大固定范围 `REPORT_RANGE_LAST_30_DAYS`，report-dashboard-query.dto.ts:5，旧摘要默认即 last_30_days），自定义日期跨度超限在 service scope 解析处 `badRequest` 拒绝。
- `ClinicSummaryDto` 响应扩展：`scopeLabel`（last_7_days/last_30_days/custom 或事件标题）、真实 `start`/`end`（ISO）、`selectedFields`（实际包含的 section 键）、`coverage`（checkIns/water/dose/sleep 统一 observed-metric 条目，water/sleep 同源 dailyRecords、dose 源 doseLogs）、`generatedAt`；`dataRange` 保留为兼容别名（范围请求返回真实范围而非硬编码 last_30_days，事件 scope 为 `event`）。新增 `ClinicSummaryCoverageEntryDto`/`ClinicSummaryCoverageDto`。
- 共享 selected-field view model（`src/modules/reports/services/clinic-summary/summary-view.model.ts`）：`applySelectedFields(summary, selectedFields)` 是 preview/PDF/share 三路径唯一过滤点，未选 section 以显式 `undefined` 属性保留在对象中（Vitest `objectContaining({key: undefined})` 要求键存在）；`resolveSectionKeys` 同时接受 section 键与六枚举值，`CLINIC_SUMMARY_SHARE_FIELD_SECTIONS` 为 Task 4 控制器翻译 request DTO 的唯一点（water/sleep/notes 暂无独立 DTO section，映射为空 → 仅元数据摘要，文档化）。
- `ClinicSummaryService`：`buildClinicSummary(userId, locale, options)` 新增 options（range/eventId/dateFrom/dateTo/selectedFields）；scope 解析 event 优先（`EventReviewService.buildForEvent`，scopeLabel=事件标题），默认 `last_30_days`；findings 只复用 event review 的结构化事实码与 reason code（available→facts.code，unknown→reasonCode），无 review 或全空时输出固定 `insufficient_coverage`（资料不足），不补泛化 AI 结论；构造函数经 `@Optional()` 注入 `EventReviewService`（spec 五参构造仍可用）。
- `createShareLink` 分享 URL 改为 `/api/v1/user/reports/clinic-summary/shared/:token`（与 e2e/controller spec 锁定的实际公开路由一致，Task 4 接线）；`getSharedSummary` 增加持久化 share 记录门禁：cache 命中后查 `userClinicSummaryShare`，revoked/expired 返回 null，有效记录原子记 accessCount/lastAccessedAt（首次打开记 firstAccessedAt），无 store 记录视为 Task 2 前 legacy 分享（缓存即唯一事实源，TTL 处理过期）。
- spec 更新：summary.service.spec 补 store 依赖 mock（findFirst/update），新增字段漂移锁测试（同一 request 的 preview 结果、pdfService 输入、cache 分享载荷 section 键集合完全一致，无选择时保留全部 section）；pdf.service.spec makeSummary 补齐新响应字段。Task 1 红测试 11 条转绿（VS3 全部 + VS2 两条 getSharedSummary），仅剩 controller 两条 NOT_FOUND 红测试归 Task 4（当前控制器抛 GONE）。

## Visit Summary and Product Measurement — Task 3 PDF 按字段视图渲染并补充免责页脚

- `ClinicSummaryPdfService.buildPdf` 输入类型改为 `ClinicSummarySectionView`（section 可缺省），四个 section 仅在存在时渲染（`!= null` 守卫）——service 已按共享 view model 过滤，取消选择的字段不会出现在 PDF 内容中，也不再对 `.length` 崩溃。
- 新增「要点发现 / Key Findings」section：findings 存在时逐行渲染事件回顾的结构化事实码/change code（原始码，Luminous 在 UI 本地化，PDF 不虚构文案），页脚免责声明改为「资料来自用户记录，可能不完整，仅供就诊参考，不能代替专业医疗诊断。」（en 对应文案），不声称医生已查看。
- pdf.service.spec 的 makeSummary 补齐新响应字段（scopeLabel/start/end/selectedFields/coverage）；Task 1 红测试「renders findings into the PDF when the summary contains them」转绿（缓冲区随 findings 变化）。全量 `pnpm test` 3173 passed / 2 failed（仅剩 controller 两条 NOT_FOUND 红测试，归 Task 4）；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check` 通过；`pnpm export:openapi` 已导出并提交（ClinicSummaryDto 新增 scopeLabel/start/end/selectedFields/coverage + 两个 coverage schema；文件含脚本序列化格式变更导致的 tags 数组换行 churn，无新 path）。

## Visit Summary and Product Measurement — Task 3 代码质量复审修复

- 视图模型文件更名 `summary-view.model.ts` → `summary-view.ts`（`.model.ts` 后缀在本仓库无先例，读作裸类型词），导入同步更新，类/函数名不变。
- OpenAPI 修复：`ClinicSummaryDto.selectedFields` 的 `@ApiProperty` 补 `type: String`（原仅 `isArray: true` 会序列化成 `{"type":"array"}` 的破损 items schema，生成的 Flutter client 会坏）；重新导出 `docs/openapi.json` 并提交。
- 访问门禁 TOCTOU 收敛（ShareService 同款模式）：`getSharedSummary` 的访问写入由「findFirst + 无条件 update」改为守卫 `updateMany({ where: { id, revokedAt: null, expiresAt: { gt: now } } })`，`count === 0` 返回 null（读-写窗口内被撤销/过期不记访问也不放行缓存副本）；无 store 记录的 legacy 分享保持缓存即事实源。spec 相应改为断言 `updateMany`（含 where 守卫）并新增 mid-flight 竞态用例。
- 日期范围语义明确化并文档化：dateFrom..dateTo 按「含首含尾的 UTC 日历日」计（响应 `end` 为排他上界 = dateTo + 1 天 00:00 UTC）；跨度校验改为包含式计数（`spanDays + 1 > CLINIC_SUMMARY_MAX_RANGE_DAYS` 拒绝，dateFrom == dateTo 为合法单日窗口）；固定范围（last_7_days/last_30_days）同样改为 [今日-(n-1) 00:00, 明日 00:00) 的日历日窗口。语义与「findings/coverage 仍绑定当前/相关事件回顾，未按自定义日期窗口取值（资料不足时输出固定语句），内容窗口绑定留待后续任务」的限制说明写入 request DTO 与 `resolveScope` 注释。
- request DTO 补 `@IsDefined()`：eventId 缺失时 dateFrom/dateTo 必须成对存在（此前只校验「存在时的格式」，单个 dateFrom 能通过 DTO 边界到 service 才被拒）。
- PDF：固定 `insufficient_coverage`（资料不足）语句按 locale 本地化渲染（zh「资料不足」/ en「Insufficient data」），其余 findings 码保持原样输出（客户端本地化），新增本地化 smoke 测试。
- 新增 service 测试锁定：eventId scope 路径（buildForEvent 调用、event 优先于日期、scopeLabel/dataRange/start/end）、日期对缺失 → 400、31 包含日跨度 → 400 与 30 包含日边界 OK（含排他 end 断言）、单日窗口、无 review / buildCurrent 为 null 时 `insufficient_coverage` 兜底、water/sleep/notes 仅选 → 仅元数据视图（selectedFields 为空数组）、`CLINIC_SUMMARY_SHARE_FIELD_SECTIONS` 六枚举→四 section 翻译。全量 `pnpm test` 3176 passed / 2 failed（仍仅 controller 两条 NOT_FOUND 红测试归 Task 4）；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check` 通过。

## Visit Summary and Product Measurement — Task 4 就诊摘要分享接口接入可撤销模型

- preview/share/export 端点统一使用 `ClinicSummaryRequestDto`（全局 ValidationPipe：scope 必填 eventId 或完整 dateFrom/dateTo 二选一、event 优先；selectedFields 仅收六个 `ClinicSummaryShareField` 枚举值且非空）。`GET clinic-summary/preview/pdf` 改为 `POST`（Fastify 在 bodyParser:false 下不解析 GET body，实测注入请求 body 为 undefined；`@HttpCode(200)` 保持 200 直接下载语义），scope/字段选择随 body 传递。
- `POST clinic-summary/share` 接入可撤销模型编排：`buildClinicSummary` 构建单一过滤视图 → `ShareService.createShare` 持久化记录（controller 只转发胜出 scope，严格 XOR 在存储边界满足；selectedFields 缺省补全六个枚举值）→ 视图按 `clinic-share:{sha256(token)}` 写入缓存（TTL 7 天，对齐 `DEFAULT_SHARE_TTL_DAYS`，缓存副本不越过持久化授权生命周期）→ 响应 `{shareId, token, shareUrl, expiresAt, scope, selectedFields}`（明文 token 仅创建时返回一次）。缓存键方案镜像 `ClinicSummaryService` 私有 scheme（`clinic-share:` 前缀 + sha256 hex，注释与 `sharedViewKey` 集中标注），service 生产逻辑零改动。
- shareUrl base URL 取自现有 `ConfigKey.App.publicBaseUrl`（`PUBLIC_BASE_URL`，缺省 localhost:3000），不硬编码域名；token 只作为 path 参数，不进 query string。
- 新增 `DELETE clinic-summary/shares/:shareId`：`ShareService.revokeShare` 按当前 user 所有权撤销，成功 `successEnvelope(null)`（@HttpCode 200）；未知/非本人返回 404，新增 i18n 键 `reports-clinic-summary.share_not_found`（zh/en）。
- 公开 shared GET/PDF 失效语义由 410 GONE 改为 404 NOT_FOUND（未知/过期/撤销统一 404，复用 `share_link_expired` i18n）——Task 1 两条 VS4 controller 红测试转绿。公开路由 OpenAPI security 修正为 `security: []`（`@ApiOperation({ security: [] })` 覆盖类级 `@ApiBearerAuth`），preview/share/export/DELETE 保持 `access-token`。
- 单次访问记录约束：公开 GET 只走 `ClinicSummaryService.getSharedSummary`（唯一记录门：缓存视图 + 持久化记录门禁 + 守卫 `updateMany` 原子记录 accessCount/lastAccessedAt）；公开 PDF 走 `exportSharedPdf`（内部同一门、同一视图，无字段漂移）。`ShareService.getSharedSummaryByToken` 不在任何 controller 请求路径上调用，保留为存储层读原语（Task 6 产品事件上报用）。
- 导出端点：`POST clinic-summary/export/async` 接受同一 DTO；队列 job 载荷只有 userId+locale（`PdfExportJobData`，pdf-queue 不在本次改动范围），带自定义 scope/字段选择的请求直接同步导出（options 生效、队列路径永不静默丢弃请求 scope），默认请求保持入队 + fallback 行为。
- token 卫生：token 不进入任何日志、错误消息或 query string（仅 path 参数与创建响应体一次返回）；本次未新增任何含 token 的日志语句。
- spec：controller spec 22 tests 全绿——更新 preview/share/PDF 既有用例，新增事件胜出转发锁（日期对随 eventId 提交时存储层只见 eventId）、selectedFields 缺省锁、DELETE 200/404、scoped export 同步导出断言、DTO→service options 转发锁。

## Visit Summary and Product Measurement — Task 4 e2e 与 OpenAPI

- reports e2e 36 tests 全绿（test runtime PostgreSQL 实跑，29 files / 424 tests 全量通过）：410→404 断言修正（shared GET/PDF 无效 token）；新增 DTO 校验锁（日期对缺一 400、31 包含日跨度 400、未知 selectedFields 400、空 selectedFields 400）；事件胜出锁（独立用户建 health event + eventId 与日期同时提交 → 201 且 share 记录只存 eventId，避免污染主用户事件状态）；DELETE 流程（撤销后 shared GET 404、未知/他人 shareId 404、未认证 401）；有效 token GET 200 + selectedFields 生效（未选 section 不出现在响应）；preview/pdf 改 POST 后 401/200 用例。
- `pnpm export:openapi`：118 paths / 276 schemas；新增 path `DELETE /api/v1/user/reports/clinic-summary/shares/{shareId}`；`preview/pdf` 由 GET 变 POST；新增 schema `ClinicSummaryRequestDto`、`ClinicSummaryShareScopeDto`，扩展 `ClinicSummaryShareResponseDto`（shareId/token/scope/selectedFields，旧字段保持必需、新字段 optional 以兼容 legacy `createShareLink` 返回值类型）；shared GET/PDF 的 security 为空数组（公开），其余 clinic-summary 路径保持 `access-token`。
- 全量验证：`pnpm test` 300 files / 3195 tests 全绿（此前 3176 passed / 2 failed，Task 1 红测试清零）；`pnpm test:e2e` 29 files / 424 tests 全绿；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check`、`git diff --check` 通过。

## Visit Summary and Product Measurement — Task 4 复审修复（恢复导出异步路径并修正范围可选语义）

- `ClinicSummaryRequestDto` scope 改为可选：eventId 或完整 dateFrom/dateTo 仍二选一、event 优先，空 scope 现在合法——`@ValidateIf` 条件改为 `eventId == null && (dateFrom != null || dateTo != null)` + `@IsDefined` 保持日期对一致性（partial pair 仍 400；eventId 存在时日期对照旧忽略），空 body 回落到服务端默认 `last_30_days`（legacy 兼容：preview 空 body e2e 恢复 201 + last_30_days 断言，另加显式日期范围 201 + custom + start/end 窗口锁）。
- 修复 `export/async` 死分支：scope 可选后默认（无 scope）请求重新走异步队列路径（BullMQ 可用时入队返回 jobId、否则同步 fallback 返回 pdfBase64），显式 scope 请求仍同步导出（options 生效、队列永不静默丢弃 scope）；OpenAPI 描述与注释改为真实行为。controller spec 与 e2e 各新增两条分支可达性锁（默认路径不再 400）。
- `POST clinic-summary/share` 空 scope 时把默认 last_30_days 窗口物化为显式日期对（today-29..today，UTC 日历日）写入 strict-XOR share 记录，与默认视图窗口一致（unit 锁断言物化日期、e2e 锁断言 30 包含日跨度），记录永不缺 scope。
- 分享缓存写入失败补偿：`shareClinicSummary` 的 `cacheManager.set` 包 try/catch，失败时 best-effort `revokeShare`（撤销错误忽略）后 rethrow——不再产生「记录已持久化但视图缓存缺失、URL 永久 404」的孤儿分享（unit 锁覆盖）。
- 缓存键派生收敛：summary.service 导出 `SHARE_CACHE_KEY_PREFIX` 与 `sharedSummaryCacheKey(token)`（`clinic-share:` + sha256 hex），`createShareLink`/`getSharedSummary` 与 controller 共用同一派生（controller 私有 `sharedViewKey` 删除），controller spec 与 summary.service.spec 的键断言改为导入同一 builder（协调漂移不可能通过测试）；服务端私有 `hashToken` 随之删除。
- `createShareLink`（legacy 缓存分享）在注释中标记为 Task 10 清理项（未删除，spec 继续覆盖）；controller spec 移除不再使用的 `createShareLink` mock。
- spec 风格收敛：controller spec 三处 404 断言由「`rejects.toThrow(HttpException)` + 二次 try/catch 取 `getStatus()`」改为单次 `rejects.toMatchObject({ status: 404 })`（仓库既有惯例，graph.spec.ts 同款）。
- 单记录锁 e2e：创建 share → shared GET ×2 → shared PDF ×1 → 直查 test DB `user_clinic_summary_shares.accessCount === 3`（每次公开读取恰好一次记录；若双 recorder 同跑会翻倍）。
- 全量验证：`pnpm test` 300 files / 3198 tests、`pnpm test:e2e` 29 files / 429 tests 全绿；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check`、`git diff --check` 通过；`pnpm export:openapi` 语义 diff 仅 `ClinicSummaryRequestDto` 描述变更（118 paths / 276 schemas 不变）。

## Visit Summary and Product Measurement — Task 5 低敏产品事件表（schema + migration）

- 新增 `UserProductEvent` 模型与三个固定枚举：`ProductEventName`（11 个事件名）、`ProductEventSurface`（today/record/review/more/notification/system）、`ProductEventResult`（success/failure/improved/unchanged/worsened）；字段仅白名单属性，**无 metadata JSON 列**（隐私硬保证，不存症状/药名/note/token 等任何健康内容）；`eventStatus` 复用既有 `HealthEventStatus`（active/ended，可空），`platform` 复用 `UserDevicePlatform`，`suggestionRuleCode` 可空 String。
- 幂等：`clientEventId` + `@@unique([userId, clientEventId])`（schema 注释标注客户端重试幂等语义）；索引 `[occurredAt]`（90 天清理扫描）与 `[userId, occurredAt]`（账户删除与后续日聚合查询）；FK `onDelete: Cascade`，账户 30 天硬删除路径即时级联清除该用户全部原始事件（DataRetentionService 既有 `prisma.user.deleteMany` 级联覆盖）。
- migration `20260814100000_add_product_events`：经 `prisma migrate diff --from-schema <git show HEAD:prisma/schema.prisma> --to-schema 工作区 --script` 生成（与上次 `persist_clinic_summary_shares` 同法，迁移目录含 `migration_lock.toml`）；首次 deploy 曾因 CLI 输出污染 SQL 首行失败（"Already up to date" 混入 migration.sql），修复后 `migrate resolve --rolled-back`（dev/test 各一次）再 deploy 成功；development（127.0.0.1:15432）与 test（127.0.0.1:5432）`migrate status` 均 up to date，`pnpm prisma:generate` 完成，`pnpm exec prisma validate` 通过。

## Visit Summary and Product Measurement — Task 5 产品事件存储模块（service/controller/retention/e2e/OpenAPI）

- `product-events` 模块：`POST /api/v1/user/product-events` 批量上报（单请求 1..50 条，`MAX_PRODUCT_EVENTS_PER_REQUEST`）；userId 一律取自 session（`@CurrentUser`），DTO 无 userId 字段，全局 ValidationPipe（whitelist + forbidNonWhitelisted，setup-app.ts 既有配置）拒绝客户端伪造 userId、自由文本与任何 metadata 字段；信封 `{ code, message, data: { received, recorded } }`，recorded 低于 received 表示同批/跨请求重复 clientEventId 被去重。
- `ProductEventsService.recordBatch` 只写原始事件、请求路径零聚合；幂等经 `createMany(skipDuplicates: true)`（Prisma 7 PostgreSQL 支持，已核验生成客户端）命中 `(userId, clientEventId)` 唯一约束；`suggestionRuleCode` 仅接受服务端 allowlist（`SUGGESTION_RULE_CODE_ALLOWLIST`，7 个 ruleId 镜像 today-suggestion 规则注册表，常量文件注明须同步，spec 直接实例化 7 个规则服务类锁死防漂移），未知码整批 `badRequest`（typed 400）拒绝且不写库。
- 保留策略绑定：DataRetentionService 每日 03:00 UTC cron 新增 `cleanupExpiredProductEvents`——`occurredAt < now-90d` 批量删除；按事件发生时间（而非服务端接收时间）计，离线缓冲上报不延长保留期；账户硬删除路径不变，FK 级联即时清除。
- 测试：service spec 8 tests + controller spec 13 tests（白名单拒绝 userId/metadata、未知枚举、非法日期、长度边界、缺失 clientEventId、空批、>50 批拒绝与 =50 边界通过、未知 rule code、幂等）+ retention spec 新增 2 tests；e2e 10 tests（401 / 201 计数 / 落库字段仅白名单断言 / 幂等重试 recorded=0 且 DB count=1 / userId 与 metadata 400 / 超限 400 / 未知 rule code 整批拒写 / 非法枚举 400 / 空批 400）。全量 `pnpm test` 302 files / 3220 tests、`pnpm test:e2e` 30 files / 439 tests 全绿；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check`、`git diff --check` 通过。
- `pnpm export:openapi`：119 paths（+1）/ 282 schemas（+6）；新增 path `POST /api/v1/user/product-events` 与 schema `ProductEventName`/`ProductEventSurface`/`ProductEventResult`/`UserDevicePlatform`/`CreateProductEventDto`/`CreateProductEventBatchDto`，语义 JSON diff 仅这 7 项（无既有 schema 改动）。

## Visit Summary and Product Measurement — Task 5 复审修复（时间边界与注释）

- `occurredAt` 增加未来偏移上限：`MAX_PRODUCT_EVENT_FUTURE_SKEW_MS = 24h`（DTO 常量，注释说明动机——无限未来的时间戳永远不会命中 90 天保留清理，破坏隐私硬保证）；按仓库既有约定在 `ProductEventsService.assertValidEvents` 服务侧 `badRequest`（typed 400）整批拒绝，`now()` 在批处理开始时取一次作为一致 cutoff。新增 service spec 2 tests（fake timers 确定性：+24h1s 拒绝且不写库、+23h59m59s 边界通过）+ controller spec 1 test（skew 400 传播与参数转发锁）。
- `SUGGESTION_RULE_CODE_ALLOWLIST` 注释改为描述真实同步保证：drift-guard spec 实例化注册规则服务类并断言集合相等，注册表新增规则需同时更新 allowlist 与 spec 的规则列表，测试将两侧锁在一起（原注释「不加则 build 红」仅当 spec 同步更新才成立）。
- 修正迁移日志 Task 5 模块条目测试计数（controller spec 12 → 13，该 spec 实际 13 个 `it` 块）。
- 全量验证：`pnpm test` 302 files / 3223 tests（+3）、`pnpm test:e2e` 30 files / 439 tests 全绿；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm docs:check`、`git diff --check` 通过。
