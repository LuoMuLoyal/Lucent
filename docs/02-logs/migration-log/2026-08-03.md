# 2026-08-03 代码审查修复

## 按 `plans/Lucent-review-2026-08-03.md`（已删）修复（审查文档保留，6 🟡 经代码验证全部属实，无 🔴）

### 追踪启动环境加载路径稳健性

- `src/tracing.ts`
  - `loadTracingEnvironment()` 由 `join(process.cwd(), envFilePath)` 改为 `join(__dirname, '..', envFilePath)`：`__dirname` 在 dev（`src/`）与 `nest build` 产物（`dist/`）下均指向项目根的子目录，单级 `'..'` 即定位到项目根，规避 PM2 / systemd / Docker 下工作目录非项目根导致 `.env.<NODE_ENV>` 无法定位、OTel 无声失效的问题（与 08-02 已修复的 Scalar 路由同一模式）。
- `src/tracing.spec.ts`
  - 测试 mock 内由直接赋值 `process.env['OTEL_ENABLED'] = 'true'` 改为 `vi.stubEnv('OTEL_ENABLED', 'true')`：`vi.unstubAllEnvs()` 现在能完整清理，避免环境变量残留导致后续测试非确定性行为（`OTEL_ENABLED=true` 意外启动 OTel SDK）。

### Scalar bundle 缓存版本化

- `src/setup-app.ts`
  - `/scalar/standalone.js` 的 `cdn` 路径改为嵌入 `@scalar/api-reference` 解析版本号（`?v=<version>`，去掉 `^`/`~` 前缀）：依赖升级后 URL 变化，浏览器 `immutable` 缓存自然失效，不再长期缓存旧 bundle。读取失败时回退无版本 URL。
  - 路由路径不变（query string 不参与 Fastify 路由匹配），`Cache-Control: public, max-age=31536000, immutable` 头保持。

### 文档检查脚本健壮性

- `scripts/hooks/check-links.ts`
  - `isLineInFence` 正则由仅 ` ``` ` 扩展为 ` ``` ` 与 `~~~` 两种围栏：`~~~` 围栏或缩进代码块内的链接样文本不再被误判为断链。
  - 移除 3 处冗余的 `.replace(/\//g, '\\')`（`resolve()` 在 Windows 与 POSIX 均正确处理正斜杠），简化跨平台路径逻辑。
  - `main()` 通过 `git rev-parse --show-toplevel` 显式解析仓库根目录（非 git 环境回退 `process.cwd()`），不再隐式依赖调用目录。
- `scripts/hooks/check-docs-updated.ts`
  - 新增 `resolveRepoRoot()`：`main()` 开头经 `git rev-parse --show-toplevel` 解析仓库根，`run()` 支持 `cwd` 参数，`getChangedFiles` / `getLastModifiedMap` / `checkMigrationLogOverwrite` 全部以仓库根为工作目录执行 git 命令，支持从子目录调用（如 `node Lucent/scripts/hooks/check-docs-updated.ts`）。

### 验证

- `pnpm test`（`src/tracing.spec.ts` + `src/setup-app.spec.ts` 9 用例）✅
- `pnpm test:tools`（`scripts/hooks/doc-coverage-lib.spec.ts` 25 用例）✅
- `pnpm docs:links` ✅（100 markdown 文件无断链）
- `pnpm docs:check --warning-only` ✅（输出本轮规则命中）
- 说明：`pnpm typecheck` 全仓级报错均集中在 `generated/prisma/internal/prismaNamespace` 缺失类型（postinstall 中 prisma 生成的 EEXIST 既有环境问题），与本次改动文件无关。
