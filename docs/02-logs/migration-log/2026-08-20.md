# Migration Log — 2026-08-20

## feat(notifications): 持久化用户通知偏好并开放 API

- 新增独立 `UserNotificationPreference` Prisma 模型、迁移和
  `GET/PATCH /api/v1/user/notification-preferences`；缺少记录时返回默认值、
  `configured=false`，PATCH 支持部分更新并限制睡眠分钟值为 0..1439。
- 同步更新 OpenAPI 合同和通知偏好文档；`configured=false` 仅用于客户端首次迁移
  旧 SharedPreferences 值。

## feat(notifications): 让健康规则通知服从用户偏好门禁

- Today suggestion escalation 注入通知偏好服务，明确
  `sleep_shortfall` / `event_check_in_trend` / `deteriorating_symptom` →
  `healthAlertsEnabled`、`water_behind_target` → `waterRemindersEnabled`，
  `missed_dose_pending` 不受两者门禁影响。

## feat(notifications): 生成每周纵向洞察并发送通知

- 复用 Reports 7 天 summary 与既有 BullMQ cron queue 新增周洞察任务；用户 profile timezone
  的周一 09:00 运行，无真实序列不生成 `ai_weekly_insight`，通知 scope 按用户/周唯一。
- 周洞察单用户偏好读取失败不再中断整批调度；通知 scope key 的 upsert 幂等路径补充
  单测。
- 已执行 Prisma generate、窄单测、`pnpm typecheck`、`pnpm build`、OpenAPI 导出；OpenAPI
  导出后由 Luminous bootstrap 重新生成客户端。

## fix(notifications): 恢复周洞察 scheduler 模块注入

- 修复提交拆分后 `WeeklyInsightSchedulerService` 未重新声明在
  `NotificationPreferencesModule` 中，确保 Nest 启动和 cron 周洞察任务可正常解析依赖。

## feat(support): 删除废弃 support-resources 接口并拆分 app-info

- 删除公开 `GET /api/v1/public/support-resources` 及其静态资源 DTO、查询参数、常量、
  服务和测试；保留 FAQ 的本地 Markdown 方案。
- 将 `GET /api/v1/public/app-info` 抽出为独立 `AppInfoModule`，OpenAPI 客户端改由
  `AppInfoApi` 提供应用元数据。

## docs(engineering): 修正工程底座文档与测试守卫注释漂移

- `architecture.md` 更新为 12 条 Queue 实例（6 条 BaseAsync、4 条业务直建、2 条调度）及
  实际并发配置，并将 metrics 说明改为 `setup-app.ts` 内联 Fastify hook。
- 删除 testing-support shared-secret guard 关于必须叠加 JwtAuthGuard 的陈旧注释；无运行逻辑变化。

## feat(storage): 对象存储抽象层 — 开发环境 SeaweedFS / 生产腾讯云 COS

- 引入 `ObjectStorageRuntime` 抽象类作为唯一 DI token，`StorageModule`
  根据 `STORAGE_PROVIDER` 绑定 `TencentCosStorageRuntime`（默认）或
  `S3StorageRuntime`。
- 将原 `CosStorageRuntime` 重命名为 `TencentCosStorageRuntime`，实现抽象类；
  新增 `S3StorageRuntime` 使用 AWS SDK v3，维护 internal/client/external
  三个 `S3Client` 端点。
- 改造 4 个消费方（`DailyRecordImageUploadService`、`FilesService`、
  `DataExportStorageService`、`MealAnalysisWorkerService`）使用抽象注入，
  `provider` 字段由 runtime 填充，错误文案改为厂商无关。
- `createSignedPutUrl` / `createSignedGetUrl` 改为 `async`，controller
  和 `DataExportService.toDto` 同步适配。
- 新增 `S3StorageConfig` 和 Zod 校验规则：`STORAGE_PROVIDER` 默认
  `tencent-cos`，设为 `s3` 时要求 endpoint/accessKey/secretKey/bucket
  同时存在。
- `docker-compose.dev.yml` 添加 SeaweedFS 4.41 服务，`up-local-stack.ts`
  默认服务列表加入 `seaweedfs`。
- DTO 描述改为厂商中立，重新生成 OpenAPI。
- ADR-0014 记录架构决策；环境文档和 README 更新本地栈说明。
- `pnpm-workspace.yaml` 添加 `find-my-way: 9.6.0` override，将
  `@nestjs/platform-fastify` 间接依赖的 `find-my-way` 从 9.7.0
  锁定回 9.6.0，避免 9.7.0 的路由兼容性回归。
- `storage.module.spec.ts` 改用 NestJS `Test.createTestingModule`
  验证真实 DI 绑定（`ObjectStorageRuntime` token 在
  `STORAGE_PROVIDER` 为 undefined / tencent-cos / s3 / 未知值时
  的解析结果），替代原先手动复制 factory 分支的测试方式。
- Luminous 客户端已通过 `bootstrap_generated_sources.dart` 重新
  生成，`verify_lucent_openapi_sync.dart` 同步检查通过。
