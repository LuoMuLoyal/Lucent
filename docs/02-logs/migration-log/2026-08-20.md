# Migration Log — 2026-08-20

## feat(notifications): 持久化用户通知偏好并开放 API

- 新增独立 `UserNotificationPreference` Prisma 模型、迁移和
  `GET/PATCH /api/v1/user/notification-preferences`；缺少记录时返回默认值、
  `configured=false`，PATCH 支持部分更新并限制睡眠分钟值为 0..1439。
- 同步更新 OpenAPI 合同和通知偏好文档；`configured=false` 仅用于客户端首次迁移
  旧 SharedPreferences 值。
