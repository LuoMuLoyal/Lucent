# 2026-08-30

## 08-29 审查报告修复

基于 `plans/Lucent-review-08-29.md` 的 2 项 Warning + 9 项 Suggestion，逐项处理。

### S-1/S-2/S-3/S-4：buildTrend 重构

- `computation.service.ts`：`filter().flatMap()` 合并为单次 `flatMap`（S-1）；
  `windowStart`/`windowEnd` 改为必填 `Date`（S-2）；
  `trendSources()` 替换为模块级 `TREND_SOURCES` 常量表（S-3）；
  嵌套三元抽为 `classifyCoverage()` 私有方法（S-4）。

### S-5：state 字段 DTO 加 description

- `report-dashboard-response.dto.ts`：`ReportObservedMetricDto.state` 的 `@ApiProperty`
  增加 description 说明与 `coverage` 字段的关系。

### S-6：buildTrend sleep 路径单测补充

- `computation.service.spec.ts`：新增用例验证 sleep trend 在无 `observedSleepSeries` 时
  不携带 `observedMetric` 字段（与 medication/water 区分）。

### S-7：dose-logs 测试注释简化 + 断言加强

- `dose-logs.service.spec.ts`：将 5 行注释简化为单行 `// Regression guard for old 'where == null' bug.`，
  追加 `expect(repository.findReminderById).toHaveBeenCalledWith('u1', 'reminder-1')` 断言。

### W-1：trend values 语义 breaking change 标注

> **BREAKING: `trend.values` 语义变化** — 原实现按日期窗口全量输出（缺数补 0），
> 新实现改为 observed-only（unknown 天省略），`values.length` 不再与窗口长度一一对应。
> 前端/客户端需按 `observedMetric.observedCount` / `expectedCount` 对齐日期。

- `report-dashboard-response.dto.ts`：`ReportTrendDto.values` 的 `@ApiProperty`
  description 追加 BREAKING 语义说明。
- `legacyValues` 短期兼容方案标注 `TODO(W-1-legacy)`，留待 R-4 路线图后续处理。

### W-2：prisma.config.ts 占位 DATABASE_URL 容错

- `prisma.config.ts`：新增占位值识别，当 `DATABASE_URL` 为占位值时将 `datasource.url` 设为 `undefined`，
  避免 prisma 尝试解析/连接占位 URL。CI/Docker build 阶段使用占位值，e2e 阶段使用真实值不受影响。

### S-8：acorn9 TODO 跟踪注册

- `package.json`：`acorn` devDependency 旁加 `//acorn` TODO 引用。
- `plans/2026-08-29-acorn9-migration.md`：新建跟踪计划文件，含升级条件与迁移步骤。

### S-9：pnpm/action-setup 缺少兜底

- `lucent-ci.yml`：审查报告建议加守卫步骤，经评估不做修改。

## 模块边界卫生审计 Phase 1 + 2

基于 `plans/2026-08-30-module-boundary-hygiene-audit.md` 的审计计划，执行 Phase 1（补全 barrel export）与 Phase 2（修复跨模块深引用）。

### Phase 1：补全 index.ts 导出（纯增量）

- `data-retention/index.ts`：新建，导出 `DataRetentionService` 和 `DATA_RETENTION_CRON`。
- `today-suggestion/index.ts`：新建，导出 `LifecycleService` 和 `LIFECYCLE_REFRESH_CRON`。
- `medicine-reminders/index.ts`：追加导出 `ReminderSchedulerService` 和 `REMINDER_SCHEDULER_CRON`。
- `notification-preferences/index.ts`：已有 `WeeklyInsightSchedulerService` 导出，无需修改。
- `reports/index.ts`：追加导出 `ReportsAiSummaryService`。
- `daily-records/index.ts`：追加导出 `CreateDailyRecordDto`、`UpdateDailyRecordDto` 类型。

### Phase 2：修复跨模块深引用

- `common/queue/cron-jobs.service.ts`：4 处深引用（data-retention、today-suggestion、medicine-reminders、notification-preferences）改为 barrel import；spec 文件同步修改。
- `reports/services/clinic-summary/summary.service.ts`：`DailyRecordFact` 改为从 `../../daily-records` barrel 导入。
- `notification-preferences/services/weekly-insight-scheduler.service.ts`：`ReportsAiSummaryService` 改为从 `../../reports` barrel 导入。
- `assistant/services/core.service.ts`：`CreateDailyRecordDto`、`UpdateDailyRecordDto` 改为从 `../../daily-records` barrel 导入。
- `product-events/services/events.service.spec.ts`：7 处对 `today-suggestion/services/rules/` 的深引用经评估为合理（rule-code allowlist 同步验证），保留深引用并添加注释说明原因。
- `cron-jobs.module.ts`：确认模块引用指向 `.module.ts` 文件，符合 NestJS 惯例，不算违规。

### 验证结论

- `pnpm lint:check`（修改文件）：通过。
- `pnpm typecheck`：通过。
- 单元测试（修改文件相关 spec）：全部通过。
- 预先存在的 `today-analysis/trigger.listener.spec.ts` 15 个失败与本改动无关（`invalidateContext` 方法不存在）。

## pnpm 12.0.0 PowerShell 兼容性修复

### 问题一：`package.json` 中的 `//acorn` 注释条目导致 supply-chain policy 检查失败

- pnpm 12.0.0 在 `pnpm run` 前新增 lockfile supply-chain policy 预检，不接受 `//` 开头的依赖名。
- S-8 修复时在 `devDependencies` 中添加的 `"//acorn": "TODO(acorn9)..."` 触发 `ERR_PNPM_INVALID_DEPENDENCY_NAME`，
  导致所有 `pnpm run` 命令在预检阶段失败。
- 修复：从 `package.json` 删除 `"//acorn"` 条目。acorn9 的 TODO 信息已在 `plans/2026-08-29-acorn9-migration.md` 中记录。

### 问题二：`pnpm.ps1` wrapper 不拼接 `$exe` 变量，导致 PowerShell 中输出丢失

- pnpm 12.0.0 从 Node.js 脚本改为 Rust 原生二进制（39MB `pnpm.exe`）。
- npm 全局安装生成的 `pnpm.ps1` wrapper 虽然计算了 `$exe=".exe"`（Windows 环境），但调用行
  `& "$basedir/node_modules/pnpm/pnpm"` 未拼接 `$exe`，导致 PowerShell 调用不带 `.exe` 扩展名的 Rust 二进制时
  stdout/stderr 管道不正确——输出丢失且 exit code 返回 0 而非实际值。
- 修复：将 `pnpm.ps1` 中的调用行改为 `& "$basedir/node_modules/pnpm/pnpm$exe"`。
- 此文件位于全局 npm 目录（`%APPDATA%\npm\pnpm.ps1`），全局更新可能覆盖；如复发，重新拼接 `$exe` 即可。
