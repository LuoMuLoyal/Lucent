# 2026-08-26

## 代码审查修复 — 08-24 + 08-25 审查报告

基于 08-24 和 08-25 两份审查报告中的 Critical 和 Warning 项，执行修复。

### C-1(8-24)：better-auth.adapter trustedProviders 双写 → 抽常量单源

- `src/modules/auth/adapters/better-auth.adapter.ts`：
  - 提取 `BETTER_AUTH_TRUSTED_PROVIDERS` 常量，确保 `accountLinking` 配置和 `oauth.service.ts` 引用同一数据源。
- `src/modules/auth/services/oauth/oauth.service.ts`：
  - `linkOAuthProfileToUser` 中硬编码的 trusted providers 列表改为引用 `BETTER_AUTH_TRUSTED_PROVIDERS` 常量。
- `src/modules/auth/index.ts`：导出 `BETTER_AUTH_TRUSTED_PROVIDERS`。

### C-2(8-24)：unlinkIdentity TOCTOU → 包 $transaction

- `src/modules/account/services/account.service.ts`：
  - `unlinkIdentity` 方法中 `hasPassword` 检查、`accounts` 查询和 `deleteMany` 操作包裹在 Prisma `$transaction` 中，关闭 TOCTOU 竞态窗口。
  - `hasPassword` 方法新增可选 `tx` 参数，支持事务内调用。
  - 移除未使用的 `deleteAccountIdentity` 私有方法。
- `src/modules/account/services/account.service.spec.ts`：
  - PrismaService mock 新增 `$transaction` 方法，传入 `prismaMock` 本身作为事务客户端。
  - 4 个 `unlinkIdentity` 测试的 `findMany` mock 序列从 2 次扩展为 3 次（getActiveAccountUser + tx 内 re-check + getAccount）。
  - `revokeBetterAuthSessions` 断言新增 `expect.anything()` 匹配事务客户端参数。

### W-3(8-24)：data-export controller @ApiBody 补全

- `src/modules/data-export/data-export.controller.ts`：
  - `createRequest` 方法新增 `@ApiBody({ type: CreateDataExportRequestDto })` 装饰器，改善 Swagger 文档。

### W-4(8-24)：token.service revoke 事务内 throw 文档化

- `src/modules/auth/services/token.service.ts`：
  - `revoke` 和 `revokeAll` 方法新增注释，解释 Prisma 事务内 `throw DomainFailureException` 的"短路"模式及错误码保留机制。

### C-1(8-25)：logger.config.ts 直接读 process.env → 改走 ConfigService

- `src/common/logger/logger.config.ts`：
  - `createLoggerOptions` 参数从独立字符串改为 `LoggerOptionsInput` 对象接口，移除对 `process.env` 和 `EnvKey` 的直接引用。
  - `transports` 数组类型从 `winston-transport` 的 `TransportStream` 改为 `NonNullable<WinstonModuleOptions['transports']>`，避免 pnpm 未 hoist `winston-transport` 导致的 TS2307。
  - VictoriaLogs transport 的 `.on('warn')` 监听器保留 `console.error` fallback 并添加 eslint-disable 注释。
- `src/common/logger/logger.module.ts`：
  - `useFactory` 从 `ConfigService` 读取 `nodeEnv`、`logLevel`、`logFormat`、`victoriaLogsUrl`，以 `LoggerOptionsInput` 对象传入 `createLoggerOptions`。

### W-4(8-25)：VictoriaLogs transport emit('warn') 无消费者 → 加 .on('warn') 监听

- `src/common/logger/logger.config.ts`：
  - VictoriaLogs transport 创建后添加 `.on('warn', ...)` 监听器，将 HTTP ingest 错误通过 `console.error` 输出，避免静默吞错。

### C-2(8-25)：domain-failure.ts/mapper 用 DomainFailureException 当 throw → 改回 throw new Error

- `src/common/result/domain-failure.ts`：
  - `createDomainFailure` 不变式校验失败时 throw `Error` 而非 `DomainFailureException`，添加 eslint-disable 注释。
- `src/common/result/domain-failure.mapper.ts`：
  - `toProblemDetails` 校验失败时 throw `Error` 而非 `DomainFailureException`，添加 eslint-disable 注释。

### W-1(8-25)：console.warn → 注入 Logger

- `src/modules/assistant/tools/vector/vector-cursor.ts`：
  - `console.warn` 改为模块级 `Logger` 实例。
- `src/modules/assistant/tools/drugbank/entity-resolve.service.ts`：
  - 新增 `Logger` 实例，传入 `parseSearchPayload`，替换 `console.warn`。
- `src/modules/assistant/tools/drugbank/search.service.ts`：新增 `Logger` 实例，传入 `parseSearchPayload`。
- `src/modules/assistant/tools/knowledge/medical.service.ts`：新增 `Logger` 实例，传入 `parseSearchPayload`。
- `src/modules/assistant/tools/leaflet/read.service.ts`：新增 `Logger` 实例，传入 `parseSearchPayload`。
- `src/modules/assistant/tools/medicine/lookup.service.ts`：新增 `Logger` 实例，传入 `parseLookupPayload`。
- `src/modules/assistant/tools/tool.service.ts`：`parseSearchPayload` 调用传入 `this.logger`。
