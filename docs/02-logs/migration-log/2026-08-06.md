# 2026-08-06 增量审查修复

## 极光推送配置

- 新增 `JPUSH_APP_KEY`、`JPUSH_MASTER_SECRET`、`JPUSH_APNS_PRODUCTION`、`JPUSH_API_BASE_URL` 环境变量与 `JpushConfig`。
- 根 `ConfigModule` 注册 JPush 配置；AppKey 与 Master Secret 缺一时启动校验失败，两者都为空时保持静默禁用。

## 极光推送提供方

- 新增 `PushProvider`/`PushMessage` 端口和 `JpushPushProvider`。
- 通过 JPush REST v3 `/v3/push` 使用 Basic Auth 按 alias 投递，单次最多 1000 个 alias，Android/iOS extras 共用业务数据。
- 提供方覆盖空配置、空 alias、请求 payload、分批和非 2xx 错误测试；请求失败交给上层业务服务处理。

## 按 `plans/Lucent-review-2026-08-06.md` 修复

本轮修复针对 2026-08-06 增量审查中的 2 个 🔴 严重问题与 4 个 🟡 警告项。
审查范围：`32e4ad20..aa365cd7`（dev 分支最近 5 个 commit）。

### `streamModelResponse` onText catch 收紧为仅传输层错误（🔴 #2）

- `src/modules/assistant/agent/runtime/model-stream.ts`
  - 新增 `isTransportError` 函数，仅放行传输层错误（`ECONNRESET`、`EPIPE`、
    `ERR_STREAM_PREMATURE_CLOSE`、`ERR_STREAM_WRITE_AFTER_END`、`ERR_HTTP_HEADERS_SENT`、
    `AbortError`），编程错误（TypeError / ReferenceError / SyntaxError）和业务逻辑错误
    继续抛出，避免回调内部 Bug 被静默掩盖。
  - catch 块日志消息增加 `(transport)` 标记以区分。
- `src/modules/assistant/agent/runtime/model-stream.spec.ts`
  - 原 "continues aggregating when onText callback throws" 测试改为使用 `EPIPE` 传输层错误。
  - 新增 "rethrows non-transport errors from onText" 测试，验证 TypeError 被重抛。

### `extractMessageText` 原型链属性安全检查（🟡 #3）

- `src/modules/assistant/agent/runtime/message-text.utils.ts`
  - `'text' in part` 改为 `Object.prototype.hasOwnProperty.call(part, 'text')`，
    避免原型链属性误判。属性访问改为 `(part as Record<string, unknown>)['text']`
    满足 ESLint `no-unsafe-assignment` 规则。

### `nodes.ts` 移除不可达代码（🟡 #4）

- `src/modules/assistant/agent/runtime/nodes.ts`
  - `streamModelResponse` 返回类型为 `Promise<AIMessage>`，`response instanceof AIMessage`
    永远为 `true`，移除冗余 if/else 检查及不可达的 `stopReason: 'no_match'` 分支。
  - 移除不再使用的 `AIMessage` import，添加注释说明类型保证。

### `respond.ts` 缓存 key 防御空值（🟡 #5）

- `src/modules/assistant/agent/runtime/respond.ts`
  - `cacheable` 条件增加 `typeof state.userMessage === 'string' && state.userMessage.length > 0`
    防御，避免上游状态构造 Bug 导致 `makeShortHash(undefined)` 产生缓存污染。

### `setup-app.ts` Scalar 版本解析 catch 增加日志（🟡 #6）

- `src/setup-app.ts`
  - `resolveScalarStandaloneUrl` 的 catch 块新增 `Logger.warn` 调用，记录
    `package.json` 读取/解析失败原因，便于 serverless / 只读文件系统环境排查。
  - 新增 `Logger` import。

### 🔴 #1 确认已修复

- `src/common/helpers/infra/queue-helpers.ts`
  - `isQueueConnectionError` 已在 2026-08-05 修复中收紧为 errno 代码 + 窄化消息模式，
    本轮确认无需额外修改。

## 极光推送投递接线

- `PushDeliveryService` 不再查询已移除计划中的 `UserDevice` 表，改为将用户 ID
  作为 JPush alias 投递。
- `NotificationsModule` 通过配置工厂注入 `JpushPushProvider`；未配置凭证时跳过网络请求，
  已配置时 provider 异常仍按 best-effort 语义记录并吞咽，不阻塞站内通知。
- 补充服务单元测试，覆盖未配置跳过、alias 传递和 provider 异常降级。

## 推送契约文档

- 更新提醒契约与代码质量参考，明确当前推送实现使用 JPush alias 和 best-effort
  降级语义；`UserDevice` 移除及客户端契约变更留待后续迁移步骤处理。

## 移除旧设备注册模块

- 在 development/test 数据库中只读确认 `user_devices` 均为 0 行后，移除后端
  `user-devices` 模块、路由、i18n 文案、E2E 测试及测试 helper 引用。
- Prisma schema 删除 `User.devices` 关系和 `UserDevice` 模型，保留
  `UserDevicePlatform`，因为 `UserSession.platform` 仍使用该枚举。
- 已获用户确认后，因 development 数据库存在既有 migration checksum 漂移，手工创建并
  检查 `remove-user-devices` 迁移；迁移只删除空的 `user_devices` 表及其自动级联的
  索引/外键，不改写既有 migration 文件，也不执行数据库 reset。

## OpenAPI 导出

- 使用项目的 Prisma 生成和 build 流程后，在 `NODE_ENV=test` 且不启用 Redis 的应用
  生命周期中重新导出 `docs/openapi.json`，避免本地开发 Redis 配置干扰契约生成。
- 重新导出后，旧 `user-devices` 路由与 DTO schema 已移除，`UserDevicePlatform`
  仍因 `UserSession.platform` 保留。
