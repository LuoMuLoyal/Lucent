# Lucent Migration Log — 2026-07-11

## 审查报告修复（7-11 回查报告中 🔴/🟡 级别问题）

### LUC-2026-0711-01: `mark` 方法查找条件过宽修复（🔴）

- `medicine-dose-logs.service.ts` `mark` 方法入口新增运行时校验：当 `reminderId` 和 `currentMedicineId` 均为 null 时抛出 `BadRequestException`
- `buildMarkLookupWhere` 第三分支（fallback）替换为 `badRequest` 抛出，不再使用可能为 null 的 `currentMedicineId` 做宽泛查找
- 新增 i18n 键 `missing_slot_identifier`（en/zh-CN）
- 新增测试用例：验证无标识符时 mark 抛出 `BadRequestException`

### LUC-2026-0711-02: barrel export 导出 .spec 文件修复（🔴）

- `src/common/helpers/index.ts` 移除三行 `.spec` 文件的通配符导出（`array.utils.spec`、`error-info.utils.spec`、`string.utils.spec`）
- 生产构建不再意外包含测试模块

### LUC-2026-0711-03: 通知失败静默修复（🟡）

- `today-analysis/services/analysis.service.ts` `createNotificationSafely` catch 块新增 `this.logger.warn(...)` 日志
- 通知服务故障时可通过日志排查，不再完全无迹可寻

### LUC-2026-0711-04: `createOrReplaceScoped` 分页限制（🟡）

- `notifications/services/notifications.service.ts` `createOrReplaceScoped` 的 `findMany` 调用新增 `take: 50` 限制
- 避免用户某类通知数量极大时全量加载
- 更新测试断言匹配新增的 `take` 参数

### LUC-2026-0711-05: `matchesScope` 兼容数组 payload（🟡）

- `notifications/services/notifications.service.ts` `matchesScope` 方法重构：数组 payload 时遍历元素检查 scope 匹配
- 未来通知 payload 使用数组结构时去重逻辑不再失效
- 新增测试用例：验证数组 payload 的去重行为

## 测试覆盖审查与补全

### LUC-2026-0711-06: 为 assistant/tools 纯函数补全单元测试

审查发现 `assistant/tools` 下多个纯函数文件完全没有测试覆盖，新增以下 spec 文件：

- `date-resolver.spec.ts`（54 条测试）：覆盖 `resolveSingleDate`（ISO/中文/斜杠/相对日期）、`resolveDateRange`（显式范围/最近N天/截断/默认）、`resolveReportRangeFromKey`、`extractReportRangeKey`、`normalizeRange`、`diffDaysInclusive`、`enumerateDates` 等全部导出函数
- `presenters.spec.ts`（56 条测试）：覆盖 `buildReadEnvelope`、`buildDailyRecordCoverage`、`buildDailyRecordRangeCoverage`、`buildReadConfidence`、`buildProposalExpiryIso`、preview field builders、locale helpers、summary descriptions 等全部导出函数
- `vector-cursor.spec.ts`（14 条测试）：覆盖 cursor 编解码 round-trip、base64url 安全性、无效输入处理、query hash 确定性、page 构建与 nextCursor 逻辑
- `read-helpers.spec.ts`（12 条测试）：覆盖 `describeReminderFrequency`（空数组/daily/days-per-week/多时间/补零）和 `mapSleepQuality`（poor/fair/good/null/未知值）

### LUC-2026-0711-07: 为 medicines/utils/helpers.ts 补全单元测试

- 新增 `medicines/utils/helpers.spec.ts`（33 条测试）
- 覆盖 `toStringList`（null/非数组/字符串数组/对象提取 name|title|code|id/trim/空值过滤/混合数组）
- 覆盖 `uniqueNonEmptyStrings`（去重/trim/limit 参数）
- 覆盖 `composeSubtitle`、`firstNonEmpty`、`truncateText`（默认长度/自定义长度/ellipsis）、`detectMatchedBy`（大小写不敏感/空查询/null 值跳过）、`toPagination`（totalPages 计算/0 total/精确整除）

### LUC-2026-0711-08: 为 auth/controllers/auth-response.helper.ts 补全单元测试

- 新增 `auth-response.helper.spec.ts`（6 条测试）
- 覆盖 success envelope 结构、user 字段序列化（id/email/nickname/avatar/emailVerified/emailVerifiedAt/createdAt/updatedAt）
- 覆盖 token 字段序列化（accessToken/refreshToken/expiresIn）
- 覆盖 emailVerifiedAt 为 null 时 emailVerified=false
- 覆盖 null nickname/avatar 处理

### LUC-2026-0711-09: environment.service.spec.ts 边界用例补全

- 原有 2 条测试仅覆盖默认快照和中国温带区域
- 新增 7 条边界测试：仅 lat/仅 lon 降级为默认、热带/高纬度/南北中纬度区域选择、dataSource 恒为 static
- 测试从 2 条增长到 9 条，覆盖全部 6 个区域分支

### 测试统计

| 指标       | 变更前 | 变更后 | 增量 |
| ---------- | ------ | ------ | ---- |
| 测试套件数 | 191    | 197    | +6   |
| 测试用例数 | 1567   | 1743   | +176 |

### LUC-2026-0711-10: 已有测试文件边界用例补全

审查已有测试文件的覆盖缺口，为以下 15 个 spec 文件补充未覆盖的边界情况：

**today-suggestion 模块：**

- `feedback.service.spec.ts`（+4 条）：未知 feedback 类型的 `noted` 效果、事务失败错误传播、返回结果字段验证、静态方法 `getAcceptedBoostPercent` / `getNotApplicableReductionPercent`
- `feedback-stats.service.spec.ts`（+6 条）：最小样本量边界(=5)、`not_applicable` 在 switch 中计数、boost 上限封顶 1.5、reduction 下限封顶 0.5、multiplier 两位小数舍入、多规则单次查询
- `scoring.service.spec.ts`（+4 条）：0 分+LOW、负分、超大分值(1M)、超大分值+LOW
- `arbitration.spec.ts`（+5 条）：单候选直接选 primary、全 LOW → primary 为 null、undefined 与空字符串 subtype 去重等价、不同 subtype 共存、恰好 MAX_SECONDARY_CARDS+1
- `suppression.spec.ts`（+5 条）：动态 multiplier 应用、静态+动态叠加、multiplier=1.0 不变、score 不低于 0、多次 reduction 不为负
- `escalation.service.spec.ts`（+4 条）：LOW 置信度不升级、priorityScore 恰好 700 边界升级、suggestion 不存在仍升级、update 失败不升级
- `suggestion-cache.service.spec.ts`（+5 条）：单元素 excludeKey、重复 ID、输入数组不被修改、特殊字符 ID
- `lifecycle.service.spec.ts`（+1 条）：非 ACTIVE 状态的 dismiss 静默处理

**common/llm 模块：**

- `llm-safety-policy.service.spec.ts`（+14 条）：空字符串安全、减药/加药/增量/减量/剂量/药量/治愈/治疗方案 中文禁词、cure/treatment plan/change dose/adjust dose/increase dose/decrease dose/stopping medication 英文禁词
- `llm-retry.helper.spec.ts`（+4 条）：HTTP 504、HTTP 599、400 错误仍重试（onRetry 回调验证）、429 重试成功
- `base-llm-generator.service.spec.ts`（+2 条）：多 chunk 流式 summary 去重、成功流式记录 metric
- `base-llm-summary.service.spec.ts`（+4 条）：安全策略拒绝最终输出返回 fallback、流失败后发射 fallback summary、已发射 summary 后不重复发射、whitespace-only fallback 不发射

**common/helpers 模块：**

- `retry.utils.spec.ts`（+3 条）：网络故障重试、fetch init 透传、持续 503 重试耗尽后抛错
- `number.utils.spec.ts`（+9 条）：NaN/Infinity/-Infinity/大精度/0.5 舍入、数组/boolean/function/MAX_VALUE/MIN_VALUE 归一化
- `json.utils.spec.ts`（+7 条）：多 JSON 对象贪婪匹配、数组内嵌、仅有开括号/闭括号、嵌套数组对象、大字符串

**测试统计更新：**

| 指标       | 变更前 | 变更后 | 增量 |
| ---------- | ------ | ------ | ---- |
| 测试套件数 | 197    | 197    | 0    |
| 测试用例数 | 1743   | 1857   | +114 |

### LUC-2026-0711-11: 扩大审查范围 — 第二轮已有测试边界用例补全

在第一轮基础上扩大审查范围，为以下 19 个 spec 文件补充未覆盖的边界情况：

**common/helpers 模块（+38 条）：**

- `string.utils.spec.ts`（+12 条）：`truncate` 精确等于 maxLength 不截断、空 suffix、空字符串、maxLength=0；`normalizeEmail` 未测试的 trim+lowercase；`commonCharacterCount` 重复字符不双计、空串、完全相同、大小写敏感
- `array.utils.spec.ts`（+8 条）：`isEmptyArray` 伪值数组 `[0]/[null]/[undefined]/[false]`；`shuffleArray` 空数组、单元素、不修改原数组、字符串数组、对象数组
- `date-time.utils.spec.ts`（+6 条）：`calculateExpiresIn` 当前时间边界和无效日期返回 NaN；`formatDateTime` 当前时间；`formatDateOnly` 年边界/12月31日；`parseDateOnly` 1月1日/12月31日；`calculateAge` 今天生日和12月31日生日
- `error-info.utils.spec.ts`（+5 条）：空消息 Error、自定义子类、boolean/NaN/Infinity/array 值
- `search-text.utils.spec.ts`（+4 条）：trim 后 join、全空白返回 null、混合空值、单元素 trim
- `api-errors.spec.ts`（+6 条）：每个函数空消息输入、全部函数抛出断言
- `client-ip.spec.ts`（+3 条）：socket=null 抛 TypeError、trustProxy=true 全源缺失返回 unknown-client、仅 socket.remoteAddress 降级

**common/api 模块（+9 条）：**

- `api-envelope.spec.ts`（+6 条）：`successEnvelope` undefined/空对象/嵌套对象/消息恒空；`errorEnvelope` 空消息/CONFLICT/TOKEN_EXPIRED
- `sse.spec.ts`（+5 条）：boolean/undefined/嵌套对象/数组 data 序列化、event 写入顺序

**common/metrics 模块（+7 条）：**

- `metrics.service.spec.ts`（+7 条）：`recordBullmqJob`/`setBullmqActiveJobs`/`recordLlmCall`/`recordLlmTokens` disabled 时不抛错、gauge 设 0、LLM duration=0

**common/filters 模块（+3 条）：**

- `api-exception.filter.spec.ts`（+3 条）：requestId 写入日志、单元素数组消息、CONFLICT status 正确映射 CONFLICT code

**auth 模块（+12 条）：**

- `token.service.spec.ts`（+5 条）：context=undefined 不设置 context 属性、多次调用产生不同 refreshToken、revoke token hash 验证、hashRefreshToken 不同输入/64位 hex/一致性
- `rate-limit.service.spec.ts`（+7 条）：lockout 过期后放行并删除、resetAt 过期删除、recordLoginFailure 过期 bucket 重置为 count=1、已锁定 bucket 重置、无效 bucket shape 重置、clearLoginFailures 邮箱被 hash

**medicines 模块（+15 条）：**

- `helpers.spec.ts`（+11 条）：`toStringList` boolean/number 跳过/空数组/嵌套数组；`uniqueNonEmptyStrings` limit=0/limit>unique/插入顺序；`composeSubtitle` 全空/无参数；`truncateText` maxLength=1/0；`detectMatchedBy` 空候选/多字段匹配；`toPagination` pageSize=1/大数/total=1
- `cache.service.spec.ts`（+4 条）：缓存命中 detail、load 返回 null 也缓存、search bypass=true、特殊字符编码

**daily-records 模块（+4 条）：**

- `mapper.service.spec.ts`（+4 条）：多附件映射、空数组、objectKey trim、空记录 summaries、同 kind 单组

**security-pin 模块（+9 条）：**

- `pin.service.spec.ts`（+9 条）：enable 5位/7位/空 PIN 拒绝；change 未启用拒绝/新 PIN 格式校验；disable 未启用拒绝/错误 PIN/格式校验；getStatus 未启用返回 null；verifyElevationToken PIN 禁用后拒绝

**assistant 模块（+6 条）：**

- `presenters.spec.ts`（+3 条）：`buildReadConfidence` truncated+多歧义=low、truncated+1歧义=medium、`buildProposalExpiryIso` TTL=0
- `vector-cursor.spec.ts`（+3 条）：`buildVectorQueryHash` 空字符串一致性/空 vs 非空不同；`buildVectorPage` offset=0；`decodeVectorCursor` 错误 shape 不返回 null

**测试统计更新：**

| 指标       | 变更前 | 变更后 | 增量 |
| ---------- | ------ | ------ | ---- |
| 测试套件数 | 197    | 197    | 0    |
| 测试用例数 | 1857   | 1955   | +98  |

### LUC-2026-0711-12: 修复 defaultCode 缺少 CONFLICT 映射（🔴）

- `api-exception.filter.ts` `defaultCode` 方法新增 `HttpStatus.CONFLICT → ResultCode.CONFLICT` 映射分支
- 修复前：HTTP 409 Conflict 异常被错误映射为 `INTERNAL_ERROR`（500001），客户端误判为服务端故障
- 修复后：正确返回 `CONFLICT`（409001），与 `ResultCode` 枚举定义一致
- 更新对应测试用例：从期望 `INTERNAL_ERROR` 改为期望 `CONFLICT`

### LUC-2026-0711-13: 扩大审查范围 — 第三轮已有测试边界用例补全

在第二轮基础上继续扩大审查范围，为以下 17 个 spec 文件补充未覆盖的边界情况：

**common/interceptors 模块（+7 条）：**

- `api-envelope.interceptor.spec.ts`（+5 条）：boolean `true`/`false`/numeric `42` 数据包装、带 `meta` 字段的 envelope 透传、有 `code` 但无 `data` 的对象不被识别为 envelope
- `slow-request.interceptor.spec.ts`（+2 条）：POST 方法慢请求日志记录、`durationMs` 和 `threshold` 字段验证

**common/helpers 模块（+10 条）：**

- `prisma-ownership.helper.spec.ts`（+3 条）：`notFound` 异常包含 `ResultCode.NOT_FOUND` code、`userId` 为空字符串时抛出、`userId` 精确匹配时不抛
- `error-payload.spec.ts`（+4 条）：空数组 message join 为空字符串、单元素数组、Error 子类自定义属性不提取 code、code=0 的数字 code
- `localized-copy.spec.ts`（+2 条）：`buildUserPrompt` 空对象 context、含 null/undefined 值的 context

**config 模块（+3 条）：**

- `env-file-paths.spec.ts`（+3 条）：`NODE_ENV` 为 undefined 时默认 `development`、development 环境路径、production 环境路径

**common/logger 模块（+4 条）：**

- `logger.config.spec.ts`（+2 条）：4xx 状态码错误日志、5xx 状态码错误日志
- `request-context.service.spec.ts`（+2 条）：嵌套上下文不同 requestId 隔离、`getStore` 返回完整 store 对象

**common/metrics 模块（+4 条）：**

- `metrics.middleware.spec.ts`（+4 条）：POST 方法记录、500 状态码记录、UUID + 数字 ID 混合路径归一化、`originalUrl` 优先于 `url`

**auth 模块（+9 条）：**

- `jwt-auth.guard.spec.ts`（+3 条）：`user=false` 抛出、error+user 同时存在时 error 优先、info 为字符串时抛出
- `notification.service.spec.ts`（+5 条）：`wechat_mobile` 登录通知、`apple`/`qq`/未知 provider 绑定通知
- `session.repository.spec.ts`（+1 条）：无活跃会话时返回空数组
- `state.service.spec.ts`（+1 条）：`link` purpose 创建 state

**user-settings 模块（+2 条）：**

- `user-settings.service.spec.ts`（+2 条）：空 payload 不调用 upsert、`waterTargetCount` 设置 upsert

**user-health-context 模块（+3 条）：**

- `ownership.service.spec.ts`（+3 条）：allergy/condition/currentMedicine 记录为 null 时抛出 NotFoundException

**files 模块（+4 条）：**

- `files.service.spec.ts`（+4 条）：`sizeBytes=0` 被接受、精确等于 `maxUploadBytes` 被接受、`image/png` 扩展名、`image/gif` 扩展名

**medicine-reminders 模块（+2 条）：**

- `mapper.service.spec.ts`（+2 条）：`capDeliveryLimit` 边界值 1 和 100

**测试统计更新：**

| 指标       | 变更前 | 变更后 | 增量 |
| ---------- | ------ | ------ | ---- |
| 测试套件数 | 197    | 197    | 0    |
| 测试用例数 | 1955   | 1999   | +44  |

### LUC-2026-0711-14: 为无测试覆盖的源文件新增 6 个 spec 文件

审查 `src/` 下所有 `.ts` 源文件与对应 `.spec.ts` 文件的匹配情况，识别出 6 个含业务逻辑但完全无测试覆盖的文件，新增对应 spec：

**config 模块（+24 条）：**

- `jwt.config.spec.ts`（8 条）：覆盖 `parseTtl` 解析逻辑——s/m/h/d 单位、大小写不敏感、纯数字字符串、零/负数回退默认值、缺失回退默认值、自定义 issuer/audience
- `app.config.spec.ts`（9 条）：覆盖 `parseCorsOrigin` 逻辑——`*` 通配、逗号分隔数组、空白过滤、空值返回 false；覆盖 production 默认 127.0.0.1、test 环境 trustProxy、自定义 publicBaseUrl trim
- `ai.config.spec.ts`（11 条）：覆盖 `readOptionalEnv` 空白 trim/null、`readOptionalNumericEnv` NaN 回退、`buildRoleConfig` 多角色配置、`readForbiddenPatterns` 逗号/换行/混合分隔、空值过滤、空白 only 返回空数组

**mail 模块（+9 条）：**

- `mail-transport.service.spec.ts`（9 条）：覆盖 log 驱动不创建 transporter、smtp 驱动创建 transporter（465 secure / 587 non-secure）、log 模式日志输出、smtp 模式 sendMail 调用参数、成功日志、transporter 未初始化抛错、sendMail 错误传播

**common/storage 模块（+6 条）：**

- `cos-storage.runtime.spec.ts`（6 条）：覆盖 COS 客户端初始化、getConfig 返回、createSignedPutUrl 参数透传（PUT/Sign/Expires/Headers）、createSignedGetUrl 参数透传（GET/Sign/Expires）、uploadBuffer 调用 putObject 参数、putObject 错误传播

**common/validators 模块（+27 条）：**

- `jsonb-schemas.spec.ts`（27 条）：覆盖 `safeParseJsonb` 成功解析/失败回退/null/undefined/string/array 输入；`mealAnalysisPayloadSchema` 完整 payload/空对象/null 字段/无效枚举/无效 source/loose schema 保留未知键；`mealRecordPayloadSchema` 完整/空/null/loose；`suggestionEvidenceSchema` 完整/空/loose；`suggestionActionSchema` 完整/仅 type/缺 type 拒绝/loose；`assistantUsedToolsSchema` 数组/空数组/缺 name 拒绝/非数组拒绝/仅 name

**测试统计更新：**

| 指标       | 变更前 | 变更后 | 增量 |
| ---------- | ------ | ------ | ---- |
| 测试套件数 | 197    | 203    | +6   |
| 测试用例数 | 1992   | 2067   | +75  |

### LUC-2026-0711-15: 高 ROI 薄弱 spec 边界用例补全

针对已有测试用例数 ≤ 2 的薄弱 spec 文件，补充核心业务逻辑的边界测试：

**assistant 模块（+7 条）：**

- `policy.service.spec.ts`（+7 条）：assistantEnabled=false 全部工具禁用且 disabledReason=chat_disabled；全部 context 关闭时 enabledContextSources 为空但无 context 依赖工具仍允许；implementedToolNames 不包含时 disabledReason=not_implemented；chatModelConfigured=false 时 disabledReason=model_not_configured；executableToolNames 仅包含已实现且已授权工具；foundation.interactiveChatReady=false 时 interactiveChatReady=false；仅 sleep_records 开启时工具权限正确

**reports/ai-summary 模块（+4 条）：**

- `generator.service.spec.ts`（+4 条）：LLM 调用失败时记录 error metric 并 rethrow、成功调用后记录 success metric 且 duration ≥ 0、hasAnalysisModel 委托 hasRoleConfig('analysis')、role 未配置时 hasAnalysisModel 返回 false

**today-analysis 模块（+4 条）：**

- `generator.service.spec.ts`（+4 条）：LLM 调用失败时记录 error metric 并 rethrow、成功调用后记录 success metric 且 duration ≥ 0、hasAnalysisModel 委托 hasRoleConfig('analysis')、role 未配置时 hasAnalysisModel 返回 false

**mail 模块（+3 条）：**

- `mail-queue.service.spec.ts`（+3 条）：Redis 可用时 enqueue 调用 queue.add 且不直接发送、queue.add 失败时错误传播、mail config 缺失时构造函数使用默认 job options 不抛错

**daily-records/meal-analysis 模块（+8 条）：**

- `matcher.service.spec.ts`（+8 条）：空输入返回 coverage=none + null 诊断/评论、complete 覆盖全匹配场景、none 覆盖含 unresolved dishes 诊断、高蛋白+高脂+低碳水阈值触发评论、无阈值触发时保守估算评论、portion text 含"克"时精确解析克数、"少量"返回 smallPortionGrams=30、空白名称被 normalizeMealEntityName 过滤

**测试统计更新：**

| 指标       | 变更前 | 变更后 | 增量 |
| ---------- | ------ | ------ | ---- |
| 测试套件数 | 203    | 203    | 0    |
| 测试用例数 | 2067   | 2093   | +26  |

## 文档更新

### Today Suggestion Engine 前端接入状态校准

- `docs/00-current/Today_Suggestion_Engine.md` 前端接入状态与实际代码不符，进行校准：
- Phase 3（Riverpod Provider + UI 重构）实际已完成，标记 `[x]`：`todaySuggestionProvider` 已升级为 `AsyncNotifier`，主卡/次卡/观察项区从 provider 直接取数据
- Phase 4（反馈按钮 + AI 解释）实际已完成，标记 `[x]`：`submitFeedback` / `dismiss` / `refresh` 已实现，`suggestionExplanationProvider` 按需加载 AI 解释
- Phase 5 拆分为两项：深层链接导航已完成 `[x]`（`_openRoute` → `primaryAction.route`）；历史建议回顾接入 `GET /today/suggestions/history` 未完成 `[ ]`（data source 已就绪，无 UI 消费）
- 对应计划文件 `Luminous/plans/2026-07-09-today-suggestion-engine-backend.md` 已删除，剩余项移入 `Luminous/docs/00-current/TODO.md`

### LUC-2026-0711-16: 修复 meal-analysis-grounding-smoke 脚本类型错误

- `scripts/dev/meal-analysis-grounding-smoke.ts` 修复 3 个 TS 编译错误：
  - `LlmRuntimeService` 导入路径由 `../../src/modules/llm-runtime/services/llm-runtime.service` 修正为 `../../src/llm-runtime/services/llm-runtime.service`（移除不存在的 `modules/` 层级）
  - `AiSafetyPolicyService` 导入修正为 `LlmSafetyPolicyService`，路径由 `../../src/common/ai/ai-safety-policy.service` 改为 `../../src/common/llm/llm-safety-policy.service`（类已重命名且迁移至 `common/llm` 目录）
  - `MealIngredientGroundingService` 构造补传第二个参数 `configService`（构造函数签名要求 `PrismaService` + `ConfigService`）
  - `scripts/tsconfig.json` 新增 `"rootDir": ".."` 显式指定项目根目录，修复 TS6059（脚本导入 `../src/` 下文件时 TypeScript 推断 `rootDir` 为 `scripts/` 导致越界报错）
