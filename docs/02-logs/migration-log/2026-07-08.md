# Migration Log - 2026-07-08

## Lucent — 7.8 审查问题修复（P1 + P2）

基于 `plans/lucent-review-2026-07-08.md` 审查报告，修复以下问题：

### P1-1: 环境变量 HOST 默认值安全风险 (LUC-2026-0708-01)

- **问题**：`app.config.ts` 中 HOST 默认 `0.0.0.0`，生产环境若未设置会监听所有接口。
- **修复**：
  - `app.config.ts`：默认值改为环境感知——生产环境 `127.0.0.1`，开发/测试环境 `0.0.0.0`。
  - `environment.validation.ts`：Joi schema 使用 `Joi.when('NODE_ENV', ...)` 条件默认值，保持验证与运行时一致。
- **文件**：`src/config/app.config.ts`、`src/config/environment.validation.ts`

### P1-2: JWT 配置魔法数字 (LUC-2026-0708-02)

- **问题**：`jwt.config.ts` 中令牌 TTL 使用 `2 * 3600` / `30 * 86400` 魔法数字。
- **修复**：提取到 `constants.ts` 为命名常量 `DEFAULT_JWT_ACCESS_TTL_SECONDS` / `DEFAULT_JWT_REFRESH_TTL_SECONDS`。
- **文件**：`src/config/constants.ts`、`src/config/jwt.config.ts`

### P2-1: 邮件队列硬编码值 (LUC-2026-0708-03)

- **问题**：`mail-queue.service.ts` 中重试次数、延迟、清理间隔等魔法数字散落在代码中。
- **修复**：提取为模块级命名常量（`MAIL_MAX_ATTEMPTS`、`MAIL_BACKOFF_DELAY_MS`、`MAIL_WORKER_CONCURRENCY`、`MAIL_COMPLETE_AGE_SECONDS` 等）。
- **文件**：`src/mail/mail-queue.service.ts`

### P2-2: 健康探针 TTL 硬编码 (LUC-2026-0708-04)

- **问题**：`app.service.ts` 中探针 TTL `5_000` 内联在方法调用中。
- **修复**：提取为模块级常量 `PROBE_TTL_MS`。
- **文件**：`src/app.service.ts`

### P2-3: DTO 开发地址残留 (LUC-2026-0708-05)

- **问题**：`auth-responses.dto.ts` 和 `oauth.dto.ts` 中 3 处 `example: 'http://127.0.0.1:49152/oauth/wechat'` 开发地址。
- **修复**：改为生产风格示例 `https://api.lumos.app/oauth/wechat` / `https://api.lumos.app/oauth/qq`。
- **文件**：`src/modules/auth/dto/auth-responses.dto.ts`、`src/modules/auth/dto/oauth.dto.ts`

### P2-4: 餐食分析阈值硬编码 + TODO (LUC-2026-0708-06)

- **问题**：`matcher.service.ts` 中 `DEFAULT_PORTION_GRAMS`、`HIGH_PROTEIN_THRESHOLD_G` 等 5 个业务阈值硬编码为模块常量，且 TODO 标注需移至配置。
- **修复**：
  - 新增 5 个环境变量：`MEAL_DEFAULT_PORTION_GRAMS`、`MEAL_SMALL_PORTION_GRAMS`、`MEAL_HIGH_PROTEIN_THRESHOLD_G`、`MEAL_LOW_CARBOHYDRATE_THRESHOLD_G`、`MEAL_HIGH_FAT_THRESHOLD_G`。
  - 默认值集中到 `constants.ts`，Joi 验证添加到 `environment.validation.ts`。
  - `MealAnalysisMatcherService` 注入 `ConfigService`，构造函数读取配置值（含 fallback 到默认常量）。
  - 移除 TODO 注释。
- **文件**：`src/config/env-keys.enum.ts`、`src/config/constants.ts`、`src/config/environment.validation.ts`、`src/modules/daily-records/services/meal-analysis/matcher.service.ts`、`src/modules/daily-records/services/meal-analysis/matcher.service.spec.ts`

### 验证

- `pnpm lint:check` — 0 errors
- `pnpm build` — 0 issues, 497 files compiled
- `pnpm test:ci` — 654/654 passed (104 test suites)
