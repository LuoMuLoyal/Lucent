# 2026-07-31 代码审查修复

## Auth — OAuth 登录修复

- `src/modules/auth/services/oauth/oauth.service.ts`
  - 修复 `updateOAuthLoginUser` 中 `lastLoginAt` 硬编码为 `new Date('2026-01-01T00:00:00Z')` 的问题，改为 `new Date()`。
- `src/modules/auth/services/oauth/state.service.ts`
  - `providerCallbackPath` 补充 `weibo` 和 `google` 的 case 分支，避免落入 default 返回 `/login/oauth/wechat` 导致非 loopback 场景下回调 URI 验证失败。

## Daily Records — Candidate Schema 修复

- `src/modules/daily-records/schemas/daily-record-candidates.schema.ts`
  - `DAILY_RECORD_CANDIDATE_KINDS` 新增 `vital` 和 `activity` 类型，与 DTO/Service 保持同步。
  - `dailyRecordCandidateSchema.payload` 的 `z.union` 新增 `vitalPayloadSchema` 和 `activityPayloadSchema`，为 AI 生成的 vital/activity 记录提供结构化校验。
