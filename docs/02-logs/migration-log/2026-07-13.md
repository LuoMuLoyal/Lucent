# Lucent Migration Log — 2026-07-13

## 审查问题修复（2026-07-13 审查报告）

### LUC-2026-0713-01: meal-analysis worker 失败更新字段不一致（🔴）

- `src/modules/daily-records/services/meal-analysis/worker.service.ts`
- `buildFailureUpdate` 方法补齐 `mealAnalysisCoverage: null` 和 `mealAnalysisUpdatedAt: now()` 字段
- 与成功路径的字段更新逻辑保持一致，避免前端状态判断混乱

### LUC-2026-0713-02: QQ OAuth JSON.parse 缺乏防护（🟡）

- `src/modules/auth/providers/qq-oauth.provider.ts`
- 新增 `safeJsonParse` 方法，包装 `JSON.parse` 并在解析失败时记录 warn 日志并抛出 unauthorized
- `fetchOpenId` 和 `extractJsonp` 中的 3 处 `JSON.parse` 调用全部替换为 `safeJsonParse`

### LUC-2026-0713-03: auth.service.ts refresh 方法吞掉原始错误（🟡）

- `src/modules/auth/services/auth.service.ts`
- `refresh` 方法的 `catch` 块中，给抛出的 `UnauthorizedException` 添加 `cause: error` 属性
- 保留原始错误链，便于排查 token 刷新失败的根本原因

### LUC-2026-0713-04: buildMarkLookupWhere 职责混淆（🟡）

- `src/modules/medicine-dose-logs/services/dose-logs.service.ts`
- `buildMarkLookupWhere` 返回类型从 `Prisma.UserMedicineDoseLogWhereInput` 改为 `... | null`
- 无安全查询条件时返回 `null`，由 `mark` 方法负责抛出 `badRequest`
- 分离"构建查询条件"和"验证输入"两个职责

### LUC-2026-0713-05: 缓存失效静默吞错（维护隐患）

- `src/modules/daily-records/services/records.service.ts`
- `src/modules/medicine-dose-logs/services/dose-logs.service.ts`
- 两处 `invalidateSuggestionCache` 的 `catch` 块从静默吞错改为记录 `logger.warn`
- 添加 `Logger` 实例到两个 Service 类

### LUC-2026-0713-06: 提取公共 mealAnalysis 热字段提取函数（🟡）

- `src/modules/daily-records/services/records.service.ts`
- 新增 `extractMealAnalysisHotFields` 私有方法，统一提取 `mealAnalysisStatus`、`mealAnalysisCoverage`、`mealAnalysisUpdatedAt`、`mealAnalysisFailureReason`、`mealSourceRevision`
- `withMealHotFields` 和 `buildMealCreateFields` 均改为调用此公共方法，消除重复逻辑

### LUC-2026-0713-07: 测试文件 `any` 类型替换为 `Mocked<T>`（🟡）

以下 8 个测试文件中 `let repository: any` 替换为 `Mocked<RepositoryPort>` 类型安全 mock：

- `src/modules/user-health-context/services/health-context.service.spec.ts`
- `src/modules/user-health-context/services/profile-write.service.spec.ts`
- `src/modules/user-health-context/services/ownership.service.spec.ts`
- `src/modules/user-health-context/services/medicine-write.service.spec.ts`
- `src/modules/user-health-context/services/condition-write.service.spec.ts`
- `src/modules/user-health-context/services/allergy-write.service.spec.ts`
- `src/modules/medicine-dose-logs/services/dose-logs.service.spec.ts`
- `src/modules/medicine-reminders/services/reminders.service.spec.ts`

同步修复了 `Mocked<T>` 类型检查暴露的 mock 数据不完整问题：

- `health-context.service.spec.ts`：`mockUserBase` 补齐 `securityPinEnabled`、`securityPinHash`、`securityPinChangedAt`、`securityElevationVersion` 字段
- `health-context.service.spec.ts`、`ownership.service.spec.ts`：`findCurrentMedicineById` mock 返回值补齐 `endedAt: null`
- `dose-logs.service.spec.ts`：dose log mock 返回值补齐 `deletedAt: null`、`takenAt: null`；`findReminderById` mock 返回值移除多余 `id` 字段

所有 5 个文件中的 `/* eslint-disable @typescript-eslint/no-unsafe-call */` 已由 ESLint --fix 自动移除（不再需要）。

### LUC-2026-0713-08: 统一验证库 zod/joi → 移除 joi 依赖（🟡）

- `src/config/environment.validation.ts`
- 将 `joi` schema 完全翻译为 `zod` schema：
  - `EnvironmentVariables` 接口改为 `z.infer<typeof envSchema>` 类型推导
  - `NodeEnvironment` 从 `enum` 改为 `as const` 对象 + 联合类型
  - Joi 的 `.allow('')` 用 `z.union([z.literal(''), ...])` 或 `z.enum(['', ...])` 表达
  - Joi 的 `.uri({ scheme: [...] })` 用 `z.refine()` + `startsWith` 检查表达
  - Joi 的 `Joi.when('NODE_ENV', ...)` 条件默认值在 `validateEnvironment` 中用命令式代码替代
  - 数值字段统一使用 `z.coerce.number()` 自动转换字符串环境变量
  - 使用 Zod 4 的 `z.url()` / `z.email()` 替代已弃用的 `.url()` / `.email()` 方法
- `package.json` 移除 `joi` 依赖（-7 packages）

### LUC-2026-0713-09: 移除死依赖 ts-node + tsconfig-paths

- `package.json` devDependencies 移除 `ts-node` 和 `tsconfig-paths`（-14 packages 含传递依赖）
- 项目无任何文件引用这两个包，所有脚本通过 `node` 直接运行 `.ts` 文件（Node 24 原生 type stripping）
- 路径别名通过 `package.json` `imports` 字段实现，不需要 `tsconfig-paths`
- NestJS 构建使用 SWC builder，与 `ts-node` 无关

### 验证结果

- `pnpm lint:check` ✅ 通过（0 errors, 0 warnings）
- `pnpm typecheck` ✅ 通过
- `pnpm typecheck:tools` ✅ 通过
- `pnpm test:ci` ✅ 205 test files, 2105 tests 全部通过

## 安全审查修复（2026-07-12 安全审查报告）

来源：`plans/2026-07-12-security-audit-fixes.md`，P0/P1/P2 共 12 项全部修复。

### P0 — 高危

#### LUC-SEC-01: 导出任务状态查询 IDOR

- `src/modules/reports/reports.controller.ts` — `generateSummaryStatus()` / `exportClinicSummaryPdfStatus()`
- `src/modules/reports/services/ai-summary/summary-queue.service.ts` — `getStatus()` 增加 `userId` 参数
- `src/modules/reports/services/clinic-summary/pdf-queue.service.ts` — `getStatus()` 增加 `userId` 参数
- 从 BullMQ job `data.userId` 校验归属，不匹配返回 `null`（controller 返回 `not_found`）

#### LUC-SEC-02: `/metrics` 端点无认证

- `src/setup-app.ts` — `/metrics` 路由添加 Basic Auth 中间件
- `src/config/env-keys.enum.ts` — 新增 `METRICS_USER` / `METRICS_PASSWORD`
- `src/config/app.config.ts` — 读取 `metricsUser` / `metricsPassword`
- `src/config/environment.validation.ts` — 新增可选 `METRICS_USER` / `METRICS_PASSWORD` schema
- 未配置时 `/metrics` 保持开放（dev/test），配置后使用 `timingSafeEqual` 常量时间比较

#### LUC-SEC-03: 未使用 Helmet

- `src/setup-app.ts` — `app.use(helmet())` 在 CORS 之前
- `package.json` — 新增 `helmet 8.3.0` 依赖

#### LUC-SEC-05: 测试支持端点缺少认证守卫

- `src/modules/testing-support/guards/testing-shared-secret.guard.ts` — 新建 `TestingSharedSecretGuard`
- `src/modules/testing-support/testing-support.controller.ts` — 添加 `@UseGuards(JwtAuthGuard, TestingSharedSecretGuard)`
- `src/modules/testing-support/testing-support.module.ts` — 注册 guard
- `src/config/env-keys.enum.ts` — 新增 `TESTING_SHARED_SECRET`
- 客户端须在 `x-testing-secret` header 中传递共享密钥

### P1 — 中危

#### LUC-SEC-06: Admin 面板明文比较时序攻击

- `src/admin/services/auth-router.service.ts` — `===` 比较改为 `crypto.timingSafeEqual` 常量时间比较

#### LUC-SEC-07: JWT Secret 无最小长度校验

- `src/config/environment.validation.ts` — `JWT_ACCESS_SECRET` / `JWT_REFRESH_SECRET` 添加 `.min(32)`
- `.env.development.example` / `.env.test.example` — dev/test secret 值更新为 ≥32 字符

#### LUC-SEC-08: 诊所摘要分享 Token 强度与存储

- `src/modules/reports/services/clinic-summary/summary.service.ts`
- `randomBytes(16)` → `randomBytes(32)`（256 位 token）
- 缓存键改为存储 SHA-256(token)，`getSharedSummary` 按哈希查询

#### LUC-SEC-09: 无 Redis 限流退化为单实例

- `src/config/environment.validation.ts` — `assertProductionEnvironment` 新增明确的 `REDIS_URL is required in production for distributed rate limiting` 错误

### P2 — 低危

#### LUC-SEC-10: 验证码明文存储

- `src/modules/auth/services/verification-code.service.ts`
- 缓存中存储 `SHA-256(code)` 而非明文
- `verify` 时对输入 code 哈希后用 `timingSafeEqual` 常量时间比较

#### LUC-SEC-12: 开发环境 CORS 通配符

- `.env.development.example` — `CORS_ORIGIN=*` 改为 `CORS_ORIGIN=http://localhost:3000,http://localhost:8080`
- `.env.test.example` — 同上

#### LUC-SEC-13: `TRUST_PROXY` 测试环境默认开启

- `src/config/app.config.ts` — 移除 `env === 'test'` 自动开启逻辑，仅显式 `TRUST_PROXY=true` 时生效

### P2 — 延后

#### LUC-SEC-11: Refresh Token 轮换非原子

- 当前设计已注释接受此风险，后续迭代改进。

## 部署配置同步安全变更

### deploy/prometheus/prometheus.yml

- 抓取配置添加 `basic_auth`，使用 `${METRICS_USER}` / `${METRICS_PASSWORD}` 环境变量
- Prometheus 直接在 Docker 内部网络抓取 `app-blue:3000` 和 `app-green:3000`，不经过 Nginx

### deploy/compose.yml

- `x-app-common` anchor 的 `environment` 新增 `METRICS_USER`、`METRICS_PASSWORD`、`TRUST_PROXY: 'true'`
- `METRICS_USER` / `METRICS_PASSWORD` 从 `.env` 文件通过 Compose 变量替换注入

### deploy/nginx/nginx.conf

- HTTPS server block 新增 `location = /metrics { return 403; }`，阻止外部通过 Nginx 访问 `/metrics`
- Prometheus 在 Docker 内部网络直接抓取 app 容器，不经过 Nginx

### deploy/smoke.ts

- 新增 `runCurlStatus` 辅助函数（仅返回 HTTP 状态码）
- `runCurl` / `checkHttp` 新增 `headers` 参数支持
- Smoke test 新增两个检查阶段：
  1. `/metrics` 通过 Nginx 访问应返回 `403`（Nginx 层拦截）
  2. 如果配置了 `METRICS_USER` / `METRICS_PASSWORD`，直连 app 容器 `:3000/metrics` 验证：无认证返回 `401`，有认证返回 `200` 且包含 `lucent_` 前缀指标

### 文档更新

- `docs/01-reference/deployment.md`：
  - 安全加固章节重构为「容器与网络」「Nginx 层」「应用层（Helmet + 认证）」三部分
  - `.env` 示例新增 `METRICS_USER` / `METRICS_PASSWORD` 区块
  - GitHub Secrets 新增 `METRICS_USER` / `METRICS_PASSWORD`
  - 最低上线检查新增 `/metrics` 的 403/401/200 三步验证
  - Smoke Test 检查项新增 `/metrics` Nginx 拦截和 Basic Auth 验证
- `docs/01-reference/environment-variables.md`：
  - Required Production Variables 新增 `METRICS_USER` / `METRICS_PASSWORD`
  - Observability 区块新增 `METRICS_USER` / `METRICS_PASSWORD` 说明
  - 新增 Security 区块，包含 `TESTING_SHARED_SECRET` / `TRUST_PROXY` 说明
- `docs/01-reference/environment.md`：
  - Runtime Notes 新增 Helmet 中间件、`/metrics` Basic Auth、测试端点守卫、`TRUST_PROXY` 说明
