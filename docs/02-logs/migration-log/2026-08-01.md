# 2026-08-01 代码审查修复

## Auth — OAuth 常量风格统一

- `src/modules/auth/services/oauth/state.service.ts`
  - `providerCallbackPath` 中 `'qq'` 字符串字面量改为使用已定义的 `OAUTH_PROVIDER_QQ` 常量，与同文件其他 provider（`OAUTH_PROVIDER_WECHAT_WEB` / `OAUTH_PROVIDER_WEIBO` / `OAUTH_PROVIDER_GOOGLE`）风格一致，避免未来重构遗漏。

## Assistant — Proposal 类型同步

- `src/modules/assistant/types/assistant.types.ts`
  - `AssistantCreateDailyRecordProposalPayload.draft.kind` 联合类型补充 `vital` 和 `activity`，与 `DAILY_RECORD_CANDIDATE_KINDS`（07-31 已新增这两个类型）保持一致，修复 `proposal.service.ts` 中 `first.kind` 赋值时的 TS 编译错误。

---

# 2026-08-01 LangGraph 图编排全方位升级

按 `plans/2026-07-31-langgraph-orchestration-upgrade.md` 实施，充分发挥 LangGraph 1.4 特性（缓存、重试、分支、循环、校验）。

## 意图分类与路由

- 新增 `src/modules/assistant/agent/runtime/classify.ts`
  - 纯规则 `classifyIntent(userMessage, allowedTools)`，复用现有关键词路由 `selectRelevantToolsForMessage`，输出 `simple_chat / read_data / write_proposal / knowledge / mixed` 五类意图 + 收窄后的 `relevantTools`。
  - write 意图的辅助 read（如 propose_create 附带 get_today_records）不视为 mixed；仅 read×knowledge / write×knowledge 才算 mixed（审查修订 R2/R3）。
- `graph.ts` 新增 `classify_intent` 节点：调用 classifyIntent、按意图选择差异化 system prompt 并重写 messages[0]。
- 条件边完整路由：
  - `simple_chat → respond`（跳过 agent）
  - `read_data → read_subgraph`
  - `write_proposal → write_subgraph`
  - `knowledge → knowledge_subgraph`
  - `mixed / fallback → agent ↔ tools`

## 意图差异化 System Prompt

- `src/modules/assistant/prompts/system.prompt.ts`
  - 新增 `buildReadSystemPrompt` / `buildWriteSystemPrompt` / `buildKnowledgeSystemPrompt` / `buildSimpleChatSystemPrompt`（复用 `BASE_SYSTEM_LINES` / `toolListLine()`）；`buildAssistantSystemPrompt` 保留为 fallback。

## Respond 节点 + simple_chat 直通

- 新增 `src/modules/assistant/agent/runtime/respond.ts`
  - `finalContent` 非空 → 输出侧非空校验后直通；`null`（simple_chat 快路径 / 循环无文本）→ 无工具轻量 LLM 调用（审查修订 R4），空内容抛错，与 `generateStream` 语义一致。

## 图级默认（setNodeDefaults）

- `graph.ts` 用 `setNodeDefaults` 配置全图节点默认：
  - `retryPolicy: { retryOn: isRetryableLlmError, maxAttempts: 3 }` —— 显式白名单，非瞬态错误（400/401）不重试
  - `timeout: AI_MODEL_TIMEOUT_MS`
  - `cachePolicy: false`（LLM 节点不缓存）

## Read / Write / Knowledge 子图 + 校验

- 新增 `src/modules/assistant/agent/runtime/subgraphs/read.ts` / `write.ts` / `knowledge.ts`
  - read：`read_agent ↔ read_tools → read_validate`；partial/empty 覆盖时追加 SystemMessage 指引 + `stopReason: 'no_data'`
  - write：`write_agent ↔ write_tools → write_validate`；无 proposedActions → `stopReason: 'no_target'` + 追加指引
  - knowledge：`knowledge_route` 按依赖链排序工具（产品→详情→说明书 / entity→detail→passages）→ `knowledge_agent ↔ knowledge_tools → knowledge_validate`；零证据 → `stopReason: 'no_evidence'`
- 新增 `src/modules/assistant/agent/runtime/validate.ts`：`validateReadResults` 解析 envelope（`data.coverage.status` / `data.ambiguities`）产出 `AssistantValidationFlags`。
- 新增 `src/modules/assistant/agent/runtime/nodes.ts`：共享 `createAgentNode` / `createToolsNode` 工厂，主图与子图复用。
- `state.ts`：`AssistantRuntimeState` 扩展 `intent / relevantTools / activeSubGraph / validationFlags / memoryEnabled / isNewConversation / memoryInjected`；`stopReason` 扩展 `no_data / no_target / no_evidence`。

## Mixed 意图

- 按审查修订 R2 不构建 mixed 子图：`mixed` 走全量 `agent ↔ tools`，绑定 `relevantTools` 全量并集。

## 迁移 core.service 逻辑到图内（Phase 7）

- `graph.ts` deps 新增 `buildMemoryBlock`；`prepare_context` 在 `memoryEnabled && isNewConversation` 时注入 memory block（HumanMessage，classify_intent 重写 system 后保留），并设置 `memoryInjected`。
- `runtime.service.ts`：`runConversation` 接收 `memoryEnabled / isNewConversation / buildMemoryBlock`，返回新增 `validationFlags`；注入 CACHE_MANAGER 组装 respondCache。
- `core.service.ts`：删除 `buildGenerationMessages` / `appendToolContextMessage`（memory/tool context 注入已入图）；`generateStream` fallback 直接传原始会话消息。

## 三层缓存（Phase 8.5）

- 节点级：`classify_intent` 配置 LangGraph 原生 `cachePolicy: { ttl: 3600 }`，`compile({ cache: 进程级共享 InMemoryCache })` 跨请求生效；`prepare_context` 因输出含用户 memory block 未缓存（隐私 + 记忆新鲜度优先，对计划的工程修正）。
- 工具级：`tool.service.ts` 对 7 个 knowledge 检索工具按 `(tool, locale, parseSearchPayload(query+filters))` + 1h TTL 缓存结果，命中跳过底层向量检索/DB 读；缓存损坏时降级重执行。
- 响应级：`respond.ts` 仅缓存 `simple_chat`（无 memory 注入、无工具结果）回复，key = `locale + promptVersion + messageHash`；memory 注入路径永不缓存。
- 指标：`metrics.service.ts` 新增 `assistant_cache_accesses_total{kind,hit}` 与 `recordCacheAccess`，tool / response 层命中率已接入。

## 依赖

- `package.json`：新增直接依赖 `@langgraph/langgraph-checkpoint@^1.1.1`（langgraph 的 `InMemoryCache` / `BaseCache` 提供方）。

## 收尾（Phase 8）

- `validate.ts` 移除 `data == null` 冗余判空（`data` 类型为非空 Record，消除 lint no-unnecessary-condition）。
- `Assistant_Runtime.md` 当前状态文档同步 4 分支意图路由、图级默认、三层缓存与校验 stopReason 说明。

## 测试缺口修复（按 plans/2026-08-01-test-gap-fix.md）

- `pdf.service.spec.ts`：两个多页 PDF 渲染用例超时从 30s 放宽到 120s，消除 CI flaky（实测首个用例 4.7s，之前 30s 超时随机失败）。
- 新增 `medicines/utils/ingredient-canonicalization.spec.ts`（20 用例）：token 归一/括号与剂量剥离/canonical 映射与扩展/drugbank 工具函数全覆盖；断言以实现语义为准修正两处（括号别名被剥离、命中 canonical key 后原始 token 不保留）。
- 新增 `medicines/utils/allergy-severity.spec.ts`（4 用例）：anaphylaxis 关键词推断、severity 回退与 unknown 归一。
- 新增 `medicines/services/risk/risk-detection.service.spec.ts`（12 用例）：评分/等级边界、过敏 token 匹配与 severity 映射、酒精/咖啡因食物相互作用、药物对双向 interaction、canonical 重复成分、三类 coverage issue、severeAllergy/informationGap 红旗；断言以实现语义为准修正 reverse 分支构造（正向命中时 primary 为 A）。
- 新增 `medicines/prompts/risk-check.prompt.spec.ts`（3 用例）：system 安全边界与 user prompt 各 section 序列化/(none) 占位。
- 新增 `medicines/services/risk/risk-context-builder.service.spec.ts`（4 用例）：用户为空、detail 组装与 reject 跳过、reminder 过滤与 daysOfWeek 类型过滤、static findings 拼装；断言修正两处（reaction null 字段省略、drugInteractions 仅过滤非字符串类型）。
- 新增 `medicines/services/risk/risk-llm-generator.service.spec.ts`（2 用例）：模型可用性与 generate 委托 structured-output 模型。
- 新增 `medicines/services/risk/risk-check.service.spec.ts`（6 用例）：getRecords 缓存命中/DB 回源、runStaticCheck 空用户安全响应、runLlmCheck 未配置抛错/全链路持久化、markStale 缓存失效容错；断言按实现修正（upsert mock 携带完整 llm result、user 为空时不断言 evaluateStaticRisk 调用）。
- 新增 `medicines/services/risk/risk-check.listener.spec.ts`（5 用例）：5s debounce、事件突发合并、markStale 失败仍调度、异步检查 reject 吞掉、模块销毁清 timer；vitest 4 下 advanceTimersByTimeAsync 不能包 .resolves，改直接 await。
- `medicines.controller.spec.ts` 扩展至 10 用例：risk-check GET/POST 分发、recognize 委托、recognize/async 同步回退、recognize/status 未知 job。
- 新增 `test/e2e/medicines/risk-check.e2e-spec.ts`（5 用例）：未认证 401、首查空记录、static 检查持久化后再次 GET 可见、llm 未配置 500；本地 runtime 实测通过（cleanupDatabase + medicineRiskCheckRecord 清理）。
- `today-analysis.controller.spec.ts` 扩展至 12 用例：generateAsync 同步回退/入队 jobId/enqueue null 回退、generateStatus not_found/已决。
- `assistant/agent/runtime.service.spec.ts` 扩展至 9 用例：runConversation 成功映射/失败重抛、generateStream 空内容抛错/数组 chunk 展平、describeFoundation 无模型分支。
- `auth/services/auth.service.spec.ts` 扩展至 26 用例：credential 全委托、refresh 失败 REFRESH_TOKEN_INVALID 业务码与 i18n 断言。
- `auth/controllers/oauth.controller.spec.ts` 扩展至 12 用例：weibo/google authorize/callback、OAuth 失败传播。
- `app.controller.spec.ts` 扩展至 7 用例：health/live/ready/deep 各探针的 200 与 503 变体。
- `today-suggestion.controller.spec.ts` 扩展至 17 用例：getSuggestions/feedback/explain/history 错误路径、explain/async 同步回退与入队 jobId/enqueue null 回退、explain/status not_found 与已决。
- `today-analysis/services/pipeline/copy.service.spec.ts` 扩展至 6 用例：medication-only/hydration-only 摘要变体、空数据 default 摘要与 done bullet、summariesDisabled 委托；copy.service.ts 行覆盖 68.8% → 100%。
- `medicines/services/risk/risk-check.service.spec.ts` 扩展至 11 用例：现有用户 eligible 过滤与 uncovered 拼装、detail 获取失败降级、persist 缓存删除失败容错、llm 无 secondaryMedicine/无 overallRecommendation 映射、无历史记录 null 映射；risk-check.service.ts 行覆盖 68.1% → 100%（branches 90.6%）。

## 依赖

- `package.json`：新增直接依赖 `@langchain/langgraph-checkpoint-postgres@^1.0.4`（checkpoint 方案预留，Postgres 持久化存储提供方）。

## Assistant — Checkpoint 持久化服务

- 新增 `src/modules/assistant/agent/checkpointer.service.ts`（`AssistantCheckpointerService`）
  - 基于 `DATABASE_URL` 构建 `PostgresSaver`（与 vector store 工厂同款 pg Pool 模式，max=5）；
    `DATABASE_URL` 缺失或初始化失败时 `getSaver()` 返回 `null`，调用方降级为无状态行为。
  - `onModuleInit` 执行 idempotent `setup()`（建表/迁移）；失败时关闭 pool 并重置状态；
    `onModuleDestroy` 释放 pool。
- 新增 `assistant/agent/checkpointer.service.spec.ts`（4 用例）：URL 缺失返回 null、构建成功并 setup、
  setup 失败回退并关闭 pool、模块销毁释放 pool；`pg.Pool` 用 class mock 保证可 `new`。

---

# 2026-08-01 LangGraph Checkpoint 持久化 + 图内审批（HITL）

按 `plans/2026-08-01-langgraph-checkpoint-hithl.md` 实施。

## Phase 1 — Checkpointer 服务

- 新增 `src/modules/assistant/agent/checkpointer.service.ts`（`AssistantCheckpointerService`）
  - 用 `DATABASE_URL` 建 `pg.Pool`（复用 vector-store 模式），`new PostgresSaver(pool)` + `setup()`（幂等建表与迁移；1.0.4 构造签名为 `(pool, serde?, options?)`，非 `{ pool }` 对象）。
  - 进程级单例，`getSaver()` 返回 `BaseCheckpointSaver | null`；`DATABASE_URL` 缺失或初始化失败 → 返回 `null`，调用方降级为旧行为；`onModuleDestroy` 释放 pool。
- 新增 `checkpointer.service.spec.ts`（4 用例）：缺 URL、setup 成功、setup 失败降级关池、destroy 关池（mock `PostgresSaver` / `pg.Pool`）。
- `assistant.module.ts` providers 注册 `AssistantCheckpointerService`。
