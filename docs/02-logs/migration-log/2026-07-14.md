# 2026-07-14 迁移日志

## 文档变更

- 将 `plans/Lucent-2026-07-14.md` 从增量代码审查报告改写为 Bug 修复计划文档
  - 逐条核实报告中的 15 个问题，全部在源码中确认（其中 #4 报告分析有误，实际问题为 `.env` 引号未剥离）
  - 按 P0/P1/P2 优先级重新组织为 15 个 BUG 修复任务（BUG-01 ~ BUG-15）
  - 每个任务包含：问题描述、涉及文件、修复方案、验证方式、预估工作量
  - 附执行顺序建议表（含依赖关系）和总工作量估算

## Bug 修复执行（15 项）

### P0 — 重复代码消除

- **BUG-01**：新建 `src/common/helpers/crypto.utils.ts`，导出 `safeCompare` 函数；`src/setup-app.ts`、`src/admin/services/auth-router.service.ts`、`src/modules/testing-support/guards/testing-shared-secret.guard.ts` 三处删除本地定义，统一导入。
- **BUG-02**：新建 `src/common/queues/base-async-queue.service.ts` 抽象基类，封装 `isConfigured`、`resultKey`、`storeResult`、`pollStatus`、`processJob` 通用逻辑；5 个队列服务（medicine-recognition、today-analysis、explanation、report-summary、clinic-summary-pdf）改为继承基类，消除约 350 行重复代码。
- **BUG-03**：新建 `src/common/helpers/queue-helpers.ts`，导出 `enqueueOrFallback` 泛型工具函数；`src/modules/reports/reports.controller.ts` 两处 async/sync fallback 改为调用工具函数。

### P1 — 部署脚本安全与可靠性

- **BUG-04**：`deploy/deploy.ts` `compose()` 从 `execSync` 字符串拼接改为 `spawnSync('docker', ['compose', ...args])` 数组参数形式，消除 shell 注入风险。
- **BUG-05**：`deploy/smoke.ts` `runCurl`、`runCurlStatus`、`runCurlInContainer`、`runCurlStatusInContainer` 四个函数从 `execSync` 字符串拼接改为 `spawnSync` 数组参数形式。
- **BUG-06**：`deploy/deploy.ts` `sleep()` 从 `Atomics.wait` 阻塞改为 `execSync('sleep N')`。
- **BUG-07**：`src/modules/testing-support/guards/testing-shared-secret.guard.ts` 未配置时和 header 缺失时的错误消息统一为 `'Invalid testing secret'`，消除测试端点信息泄露。
- **BUG-08**：`deploy/deploy.ts` smoke test 失败后 `rollback()` 调用包装 try-catch，rollback 失败时输出错误但不覆盖原始 smoke test 错误。
- **BUG-09**：`deploy/deploy.ts` `runMigrate` 中 `postgresPassword` 使用 `encodeURIComponent()` 编码。

### P2 — 代码质量与安全加固

- **BUG-10**：`deploy/deploy.ts` `readEnvFile()` 增加配对引号剥离逻辑。
- **BUG-11**：`src/modules/daily-records/services/records.service.ts` 提取 `validateSleepPayload()` 私有方法，`ensureValidSleepPayload` 和 `ensureValidSleepFinalState` 改为调用。
- **BUG-12**：`src/modules/reports/services/ai-summary/summary-queue.service.ts` 和 `clinic-summary/pdf-queue.service.ts` 的 `getStatus` 方法在 userId 不匹配时添加 `logger.debug` 日志（已随 BUG-02 重构合并到基类 `pollStatus` 中）。
- **BUG-13**：`src/modules/auth/providers/qq-oauth.provider.ts` `parseQueryString` 从自定义实现改为 `Object.fromEntries(new URLSearchParams(qs))`。
- **BUG-14**：`src/modules/auth/services/verification-code.service.ts` `hashCode` 方法签名增加 `scene` 和 `email` 参数，使用 `${scene}:${email}:${code}` 作为 SHA256 输入；`safeCompareCode` 同步更新；测试 spec 同步更新 mock 哈希值。
- **BUG-15**：测试文件和 DTO 中的硬编码 `lucent-1250000000` 替换为 `lucent-test-bucket`；新建 `src/common/constants/test-constants.ts`。

## 验证

- `pnpm lint:check`：0 errors
- `pnpm build`：704 files 编译成功，0 issues
- `pnpm test`：2090 passed | 11 failed（全部为 pre-existing，非本次变更引入）
  - 修复前 15 failed，修复后 11 failed（修复了 verification-code spec 3 个 + testing-support spec 2 个）
