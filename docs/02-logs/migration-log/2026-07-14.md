# 2026-07-14 迁移日志

## 文档变更

- 将 `plans/Lucent-2026-07-14.md` 从增量代码审查报告改写为 Bug 修复计划文档
  - 逐条核实报告中的 15 个问题，全部在源码中确认（其中 #4 报告分析有误，实际问题为 `.env` 引号未剥离）
  - 按 P0/P1/P2 优先级重新组织为 15 个 BUG 修复任务（BUG-01 ~ BUG-15）
  - 每个任务包含：问题描述、涉及文件、修复方案、验证方式、预估工作量
  - 附执行顺序建议表（含依赖关系）和总工作量估算

## Bug 修复执行（15 项）

### P0 — 重复代码消除

- **BUG-01**：新建 `src/common/helpers/crypto.utils.ts`，导出 `safeCompare` 函数；`src/setup-app.ts`、`src/admin/services/auth-router.service.ts`、`src/modules/testing-support/guards/testing-shared-secret.guard.ts` 三处删除本地定义，统一导入。
- **BUG-02**：新建 `src/common/queues/base-async-queue.service.ts` 抽象基类，封装 `isConfigured`、`resultKey`、`storeResult`、`pollStatus`、`processJob` 通用逻辑；5 个队列服务（medicine-recognition、today-analysis、explanation、report-summary、clinic-summary-pdf）改为继承基类，消除约 350 行重复代码。
- **BUG-03**：新建 `src/common/helpers/queue-helpers.ts`，导出 `enqueueOrFallback` 泛型工具函数；`src/modules/reports/reports.controller.ts` 两处 async/sync fallback 改为调用工具函数。

### P1 — 部署脚本安全与可靠性

- **BUG-04**：`deploy/deploy.ts` `compose()` 从 `execSync` 字符串拼接改为 `spawnSync('docker', ['compose', ...args])` 数组参数形式，消除 shell 注入风险。
- **BUG-05**：`deploy/smoke.ts` `runCurl`、`runCurlStatus`、`runCurlInContainer`、`runCurlStatusInContainer` 四个函数从 `execSync` 字符串拼接改为 `spawnSync` 数组参数形式。
- **BUG-06**：`deploy/deploy.ts` `sleep()` 从 `Atomics.wait` 阻塞改为 `execSync('sleep N')`。
- **BUG-07**：`src/modules/testing-support/guards/testing-shared-secret.guard.ts` 未配置时和 header 缺失时的错误消息统一为 `'Invalid testing secret'`，消除测试端点信息泄露。
- **BUG-08**：`deploy/deploy.ts` smoke test 失败后 `rollback()` 调用包装 try-catch，rollback 失败时输出错误但不覆盖原始 smoke test 错误。
- **BUG-09**：`deploy/deploy.ts` `runMigrate` 中 `postgresPassword` 使用 `encodeURIComponent()` 编码。

### P2 — 代码质量与安全加固

- **BUG-10**：`deploy/deploy.ts` `readEnvFile()` 增加配对引号剥离逻辑。
- **BUG-11**：`src/modules/daily-records/services/records.service.ts` 提取 `validateSleepPayload()` 私有方法，`ensureValidSleepPayload` 和 `ensureValidSleepFinalState` 改为调用。
- **BUG-12**：`src/modules/reports/services/ai-summary/summary-queue.service.ts` 和 `clinic-summary/pdf-queue.service.ts` 的 `getStatus` 方法在 userId 不匹配时添加 `logger.debug` 日志（已随 BUG-02 重构合并到基类 `pollStatus` 中）。
- **BUG-13**：`src/modules/auth/providers/qq-oauth.provider.ts` `parseQueryString` 从自定义实现改为 `Object.fromEntries(new URLSearchParams(qs))`。
- **BUG-14**：`src/modules/auth/services/verification-code.service.ts` `hashCode` 方法签名增加 `scene` 和 `email` 参数，使用 `${scene}:${email}:${code}` 作为 SHA256 输入；`safeCompareCode` 同步更新；测试 spec 同步更新 mock 哈希值。
- **BUG-15**：测试文件和 DTO 中的硬编码 `lucent-1250000000` 替换为 `lucent-test-bucket`；新建 `src/common/constants/test-constants.ts`。

## 验证

- `pnpm lint:check`：0 errors
- `pnpm build`：704 files 编译成功，0 issues
- `pnpm test`：2090 passed | 11 failed（全部为 pre-existing，非本次变更引入）
  - 修复前 15 failed，修复后 11 failed（修复了 verification-code spec 3 个 + testing-support spec 2 个）

## 测试补充

### P0 — 修复已有失败测试

- `config/environment.validation.spec.ts`：JWT secret 常量值从不足 32 字符更新为 42 字符，修复 8 个用例
- `config/app.config.spec.ts`：`trustProxy` 测试从期望 test 环境自动启用改为期望默认 `false`，修复 1 个用例
- `modules/reports/services/clinic-summary/summary.service.spec.ts`：`getSharedSummary` 断言从明文 token 改为 SHA-256 hash，修复 1 个用例
- `.env.test`：JWT secret 从不足 32 字符更新为 34/35 字符，修复 E2E 环境启动失败

### P1 — 核心基础设施单元测试

- 新建 `src/common/queues/base-async-queue.service.spec.ts`（23 tests）：覆盖 `isConfigured`、`resultKey`、`storeResult`、`processJob`（成功/失败/无 job.id）、`pollStatus`（缓存命中/缺失/userId 校验/BullMQ 状态轮询）
- 新建 `src/common/helpers/crypto.utils.spec.ts`（8 tests）：覆盖 `safeCompare` 相同/不同/空串/Unicode/长字符串场景
- 新建 `src/common/helpers/queue-helpers.spec.ts`（6 tests）：覆盖 `enqueueOrFallback` 队列可用/不可用/enqueue 返回 null/自定义 fallbackKey 场景

### P2 — 安全守卫与 E2E 补充

- 新建 `src/modules/testing-support/guards/testing-shared-secret.guard.spec.ts`（8 tests）：覆盖 secret 未配置/为空/header 缺失/为空/数组/不匹配/匹配/大小写敏感场景
- 新建 `test/e2e/legal-documents/legal-documents.e2e-spec.ts`（9 tests）：覆盖列表查询（中文/英文/排除 inactive/400 非法 lang）、详情查询（中文/英文/404 未知/404 inactive）

### P3 — 队列子类冒烟测试与路由器测试

- 新建 `src/modules/today-suggestion/services/explanation/queue.service.spec.ts`（5 tests）
- 新建 `src/modules/today-analysis/services/analysis-queue.service.spec.ts`（4 tests）
- 新建 `src/modules/reports/services/ai-summary/summary-queue.service.spec.ts`（4 tests）
- 新建 `src/modules/reports/services/clinic-summary/pdf-queue.service.spec.ts`（4 tests）
- 新建 `src/modules/medicines/services/medicine-recognition-queue.service.spec.ts`（4 tests）
- 新建 `src/modules/assistant/agent/runtime/router.spec.ts`（30 tests）：覆盖 `selectAllowedToolsForContextSources` 和 `selectRelevantToolsForMessage` 的关键词匹配/中英文/CN 产品路由/写意图/摘要路由/回退行为/边界场景

### 测试统计

- 单元测试文件：205 → 215（+10）
- 单元测试用例：2105 → 2201（+96）
- 单元测试通过率：2095/2105 → 2201/2201（100%）
- E2E 测试文件：24 → 25（+1）
- E2E 测试用例：新增 9 个 legal-documents 测试

## 测试缺口补充（第二轮）

### 配置文件测试

- 新建 `src/config/oauth.config.spec.ts`（6 tests）：覆盖各 OAuth provider env 读取、默认空值、Apple 固定 URL
- 新建 `src/config/mail.config.spec.ts`（4 tests）：覆盖 driver/host/port 默认值、SMTP 自定义值、队列参数默认/自定义
- 新建 `src/config/tencent-cos.config.spec.ts`（5 tests）：覆盖 COS 凭证默认空值、bucket/region 读取、上传/下载过期时间默认/自定义

### 装饰器测试

- 新建 `src/modules/auth/decorators/current-user.decorator.spec.ts`（6 tests）：覆盖全量 user 返回、字段提取（sub/email/status）、user 缺失返回 undefined
  - 源码改动：`current-user.decorator.ts` 导出 `currentUserFactory` 函数用于直接测试
- 新建 `src/modules/auth/decorators/public.decorator.spec.ts`（3 tests）：覆盖方法/类 metadata 设置、无装饰器时不设置
- 新建 `src/common/interceptors/skip-api-envelope.decorator.spec.ts`（3 tests）：覆盖方法/类 metadata 设置、无装饰器时不设置
- 新建 `src/modules/security-pin/decorators/require-elevation.decorator.spec.ts`（3 tests）：覆盖方法/类 metadata 设置、无装饰器时不设置

### Zod Schema 测试

- 新建 `src/modules/daily-records/schemas/daily-record-candidates.schema.spec.ts`（23 tests）：覆盖 `sleepPayloadSchema` 独立验证（duration 正整数/上限/strict 模式/quality 长度）、candidate schema（kind 枚举/日期 regex/rationale 长度/payload union）、candidates schema（数组 1-5 限制）
  - 源码改动：`daily-record-candidates.schema.ts` 导出 `sleepPayloadSchema` 用于独立测试
- 新建 `src/modules/reports/schemas/report-summary.schema.spec.ts`（11 tests）：覆盖 bullet kind 枚举、summary 长度限制、bullet 数量 2-3 限制、actionLabel/action/confidenceNote 长度
- 新建 `src/modules/today-analysis/schemas/analysis.schema.spec.ts`（9 tests）：覆盖 bullet kind 枚举、summary 长度限制（120）、bullet 数量 2-3 限制、confidenceNote 长度（80）
- 新建 `src/modules/today-suggestion/schemas/explanation.schema.spec.ts`（6 tests）：覆盖 reason/boundary 非空和长度限制、whitespace trim

### setup-app 验证错误格式化测试

- 新建 `src/setup-app.spec.ts`（8 tests）：覆盖 `collectValidationMessages`（flat/nested/deeply nested/无 constraints）和 `formatValidationErrors`（多错误 join/空数组/嵌套 flatten/混合 constraints+children）
  - 源码改动：`setup-app.ts` 导出 `formatValidationErrors` 和 `collectValidationMessages` 函数

### 第二轮测试统计

- 单元测试文件：215 → 227（+12）
- 单元测试用例：2201 → 2288（+87）

## 测试缺口补充（第三轮）

### PDF 报表主题函数测试

- 新建 `src/modules/data-export/utils/report-pdf.theme.spec.ts`（28 tests）：覆盖 `kindLabel`（hospital/monthly/print × 中英文）、`statusPalette`（good/stable/needs_attention/insufficient_data/未知状态）、`metricLabel`（medication/water/sleep/未知 × 中英文）、`statusLabel`（good/stable/needs_attention/insufficient_data/未知 × 中英文）

### Assistant Schema 测试

- 新建 `src/modules/assistant/schemas/message.schema.spec.ts`（11 tests）：覆盖 `assistantMessageRoleSchema` 枚举（system/user/assistant/非法值）、`assistantMessageSchema`（合法消息、8000 字符边界、超长/空/纯空白拒绝、role 校验、content trim）

### 环境静态快照区域选择测试

- 新建 `src/modules/environment/config/reference.spec.ts`（14 tests）：覆盖 `getStaticEnvironmentSnapshot` 的区域选择逻辑（无坐标→default、仅 lat/仅 lon→default、上海→china_temperate、边界值、赤道→tropical、北极→high_latitude、欧洲→northern_mid_latitude、澳洲→southern_mid_latitude）、深拷贝验证

### 验证装饰器测试

- 新建 `src/common/validators/auth.decorators.spec.ts`（23 tests）：覆盖常量导出（密码长度限制、验证码长度、密码正则模式）、`IsStrongPassword`（合法/过短/超长/缺大写/缺小写/缺数字/空值/optional/custom prefix）、`IsVerificationCode`（6 位合法/过短/超长/空值/optional/exactLength=false）、`IsEmailAddress`（合法/非法/空值/optional）

### Assistant Tool Definitions 测试

- 新建 `src/modules/assistant/tools/tool-definitions.spec.ts`（7 tests）：覆盖 `buildToolDefinitions`（空数组、单个工具、全量工具、名称映射、描述非空、no-params schema、顺序保持）

### 用户设置常量测试

- 新建 `src/modules/user-settings/constants/constants.spec.ts`（17 tests）：覆盖 `USER_SETTING_KEYS`（键存在/值类型）、`ASSISTANT_CONTEXT_SETTING_KEYS`（4 个来源）、`USER_SETTINGS_DEFAULTS`（默认值）、`ASSISTANT_CONTEXT_DEFAULTS`（全 true）、`listDefaultBooleanUserSettings`（7 条目/键值对完整/aiSummariesEnabled/assistantEnabled/assistantMemoryEnabled/4 个 context 键/值匹配）

### 第三轮测试统计

- 单元测试文件：227 → 233（+6）
- 单元测试用例：2288 → 2388（+100）

## 测试缺口补充（第四轮：契约/安全/压力测试）

### 契约测试扩展

- 扩展 `test/contract/contract.e2e-spec.ts`（+15 tests）：
  - 新增 GET 端点结构验证：`/health/deep`、`/medicines`（搜索）、`/notifications/unread-count`、`/daily-records/summary`、`/today/suggestions/history`、`/today-analysis/recommendations`、`/assistant/conversations`
  - 新增 POST 响应结构验证：`POST /daily-records`（返回 id+kind）、`POST /health-context/allergies`（返回 allergies 数组）、`POST /notifications`（返回 id）
  - 新增 PATCH 响应结构验证：`PATCH /user/settings`（返回更新后设置）
  - 新增分页元数据结构验证：`/medicines` 分页 meta 形状、`/daily-records` items 数组

### 安全测试补充

- 新建 `test/security/elevation-and-idor.e2e-spec.ts`（12 tests）：
  - 安全提升守卫（4 tests）：无 elevation token 拒绝、无效 token 拒绝、有效 token 通过、GET/latest 无 token 拒绝
  - 服药记录 IDOR（5 tests）：跨用户列表隔离、按 ID 读取/更新/删除均返回 404
  - 质量分配防护（3 tests）：PATCH /account 忽略 `role`/`isVerified`/`status`、POST /daily-records 忽略 `userId` 注入

### 压力测试补充

- 新建 `test/performance/daily-records.k6.ts`：写入路径压力测试
  - 5 VU 渐进式加压，25s 持续
  - 覆盖 POST（创建）、PATCH（更新）、GET（查询）三个操作
  - 阈值：p95 < 500ms（创建/更新）、p95 < 400ms（查询）、错误率 < 5%
  - 更新 `test/performance/README.md`：添加新脚本说明

## Pre-push TypeScript 错误修复

- `src/common/queues/base-async-queue.service.spec.ts`：修复 `Mock<Procedure | Constructable>` 不可调用错误（TS2348），在 `execute` 和 `testProcessJob` 中将 `executeMock` 显式 cast 为 `(data: TestJobData) => Promise<TestResult>`
- `src/modules/assistant/agent/runtime/router.spec.ts`：修复 `'medicine_knowledge'` 不在 `AssistantContextSource` 类型中错误（TS2322），改为 `[]` 验证知识工具始终可用（知识工具 required sources 为空数组）
