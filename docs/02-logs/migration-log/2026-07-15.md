# 2026-07-15 迁移日志

## 审查报告修复（7-15 审查报告）

### JSON.parse 保护（7-14 遗留 — 确认已修复）

- 7-15 审查报告标注 3 个文件的 `JSON.parse` 无 try-catch 保护仍未修复，经源码级验证，**3 个文件实际均已修复**，报告过时：
  - `vision.service.ts`：`parseRecognitionResponse` 函数已封装 try-catch（第 207-265 行）
  - `decomposition.service.ts`：`parseDecompositionResponse` 函数已封装 try-catch（第 215-280 行）
  - `medicines.service.ts`：`recognizeMedicine` 方法已封装 try-catch（第 65-78 行）

### 🔴 严重问题修复（3 项）

- **health-context.repository.ts TOCTOU 风险消除**：`findAllergyById`、`findConditionById`、`findCurrentMedicineById` 三个方法签名从 `(id)` 改为 `(userId, id)`，查询从 `findUnique({ where: { id } })` 改为 `findFirst({ where: { id, userId } })`，在 DB 层过滤 userId，消除 TOCTOU 竞态窗口。`ownership.service.ts` 同步更新调用方传入 `userId`。Repository 测试和 ownership service 测试同步更新。
- **notifications.controller.ts 系统通知伪造防护**：`CreateNotificationDto` 的 `@IsIn` 从 `USER_NOTIFICATION_TYPES` 改为 `USER_CREATABLE_NOTIFICATION_TYPES`（排除 `system_announcement`），普通用户无法通过 `POST /notifications` 创建系统公告类型通知。
- **daily-records.controller.ts generateCandidates 关联用户**：`generateCandidates` 方法的 `_user`（未使用）改为 `user`，传入 `user.sub` 到 `DailyRecordCandidatesService.generate`。Service 签名新增 `userId` 参数用于日志。`IDailyRecordCandidateGenerator` 接口同步更新。Assistant `proposal.service.ts` 调用方同步更新传入 `context.userId`。

### 🟡 警告问题修复（1 项）

- **medicine-dose-logs.service.ts 列表查询分页**：`list` 方法新增 `page`（默认 1）和 `pageSize`（默认 50）参数，Repository 新增 `findManyWithCount` 方法返回 `{ items, total }`，Controller 新增 `page`/`pageSize` 查询参数（`ParseIntPipe` optional），DTO `DoseLogListDataDto` 新增 `total` 字段。Repository/Service/Controller 测试同步更新。

### 附带修复

- **OpenAPI 导出脚本 CJS/ESM 互操作修复**：`scripts/contract/export-openapi.ts` 的 `import()` 解构改为兼容 CJS 模块的 `default` 属性访问模式，修复 `setupApp is not a function` 错误，`pnpm export:openapi` 恢复正常。
- **medicine-dose-logs TOCTOU 消除**：`dose-log.repository.ts` 的 `findReminderById` 和 `findCurrentMedicineById` 签名从 `(id)` 改为 `(userId, id)`，查询条件添加 `userId` 过滤（`findReminderById` → `where: { id, userId, deletedAt: null }`；`findCurrentMedicineById` 从 `findUnique` 改为 `findFirst` + `where: { id, userId }`）。`dose-logs.service.ts` 的 `ensureOwned` 方法查询条件从 `{ id, deletedAt: null }` 改为 `{ id, userId, deletedAt: null }`，`ensureReminderOwned` 和 `resolveCurrentMedicineId` 调用方同步传入 `userId`。三个跨用户拒绝测试用例的 mock 从返回 `{ userId: 'other' }` 改为返回 `null`（DB 层过滤后跨用户记录不存在）。Repository 和 Service 测试同步更新。

## ADR-0008: CN↔DrugBank 药品不进行映射关系

- **决策**：不维护 CN↔DrugBank 药品映射关系。跨源查询由 LLM 自主通过工具链完成。详见 [ADR-0008](../../01-reference/adr/0008-no-cn-drugbank-medicine-mapping.md)。
- **删除字段**：`cn_medicine_products.drugbank_ids`（Prisma schema + DTO + service + spec + import 脚本）
- **涉及文件**：
  - `prisma/schema.prisma` — 删除 `drugbankIds` 列
  - `src/modules/medicines/dto/medicine-detail.dto.ts` — 删除 `CnMedicineDetailDto.drugbankIds` 字段
  - `src/modules/medicines/adapters/cn.service.ts` — 删除 `parseDrugbankIds()` 方法 + 字段赋值
  - `src/modules/medicines/adapters/cn.service.spec.ts` — 删除 3 个 drugbankIds 测试用例 + mock 字段
  - `scripts/import/medicine/parsers/cn_products.py` — 删除 `drugbank_ids` 列解析
  - `scripts/import/medicine/import-medicine-knowledge.ts` — 删除 2 处 `drugbank_ids` 列定义
- **文档同步**：`data-sources-cn-products.md`、`Medicine_Data_RAG.md`、ADR README 索引
- **注意**：schema 变更后需运行 `pnpm prisma migrate dev --name remove_cn_drugbank_ids` 生成迁移，并重新 `pnpm export:openapi`

## 技术债务修补（批次 0-6）

### 数据库迁移 + OpenAPI 重新导出

- `pnpm prisma db execute` 直接执行 `ALTER TABLE cn_medicine_products DROP COLUMN IF EXISTS drugbank_ids;`（避免 `migrate dev` 因 pgvector 表 drift 而 reset 整个数据库）
- `pnpm prisma:generate` 重新生成 Prisma client
- `pnpm export:openapi` 重新导出 OpenAPI 规范（101 paths, 210 schemas, build 0 issues）

### #3 嵌套事务修复 — P0

- **问题**：`DailyRecordsService.update` 的 `repository.transaction()` callback 内调用 `mealDishTemplateLearningService.learnFromConfirmedAnalysis()`，该方法内部使用 `$transaction` — Prisma 嵌套事务会静默降级为独立连接，丢失原子性
- **修复**：将 `learnFromConfirmedAnalysis`、`enqueueMealAnalysisIfNeeded`、`invalidateSuggestionCache` 三个副作用调用从事务 callback 内移到事务外部（`create` 和 `update` 两个方法均已修复）
- **预防**：`DailyRecordRepositoryPort.transaction()` 方法添加 JSDoc 警告，说明 callback 内禁止嵌套 `$transaction` 和副作用调用
- **涉及文件**：`src/modules/daily-records/services/records.service.ts`、`src/modules/daily-records/repositories/daily-record.repository.ts`

### #2 提取 safeParseLlmJson — P1

- **问题**：3 个文件重复实现 "提取 JSON 子串 + parse + try-catch + 日志" 模式
- **修复**：在 `src/common/helpers/json.utils.ts` 中新增 `safeParseLlmJson()` 函数，替换以下文件的内联实现：
  - `medicines/services/medicines.service.ts` — `recognizeMedicine`
  - `daily-records/services/meal-analysis/vision.service.ts` — `parseRecognitionResponse`
  - `daily-records/services/meal-dish/decomposition.service.ts` — `parseDecompositionResponse`
- 移除了 `vision.service.ts` 和 `decomposition.service.ts` 中不再使用的 `extractErrorInfo` 导入

### #5 JSON.stringify 深比较处理 — P1

- **问题**：`meal-analysis.types.ts` 中 `hasMealDishInputChanges` 使用 `JSON.stringify(a) !== JSON.stringify(b)` 做深比较，键顺序不同会误判
- **修复**：改用 Node.js 内置的 `isDeepStrictEqual`（`node:util`），无新依赖
- **涉及文件**：`src/modules/daily-records/types/meal-analysis.types.ts`

### #9a Allergy severity null-handling — P1

- **问题**：`allergy-write.service.ts:28` 写入 `severity: dto.severity ?? null`，但 DTO 描述为 "Defaults to unknown"，Prisma schema 默认值为 `unknown` — 传 `null` 覆盖了默认值
- **修复**：改为 `dto.severity ?? UserAllergySeverity.unknown`
- **涉及文件**：`src/modules/user-health-context/services/allergy-write.service.ts`

### #9e DrugBank synonym 搜索优化 — P1

- **问题**：`searchText` 包含同义词，`matchedBy` 无法区分匹配来源
- **修复**（Option B）：在 `toSearchItem` 的 `detectMatchedBy` 调用中增加 `synonyms` 候选项，让调用方区分匹配来自 `name`/`casNumber`/`unii`/`searchText`/`synonyms`。在 `buildWhere` 方法上添加 JSDoc 说明 `searchText` 的构建逻辑
- **涉及文件**：`src/modules/medicines/adapters/drugbank.service.ts`

### 预存测试修复

- `daily-records.controller.spec.ts:163` — `generateCandidates` 测试期望参数从 `(dto, 'zh-CN')` 修正为 `('user-1', dto, 'zh-CN')`，与 controller 实际调用一致

### 验证结果

- `pnpm lint:check` — 0 errors
- `pnpm build` — 0 issues, 732 files
- `pnpm test` — 233 test files passed, 2383 tests passed

## Express → Fastify 迁移计划审查更新

- **逐文件对照代码库**：对 `plans/express-to-fastify-migration.md` 中列出的所有源文件进行逐行验证，确认计划与实际代码一致
- **修正项**：
  - 控制器数量：实际 22 个控制器中仅 7 个需改动（原计划数字正确），补充列出 15 个无需改动的完整清单（含 `files.controller.ts`、`data-export.controller.ts` 等新增控制器）
  - E2E 测试数量：从 24 修正为 27 个（含 `test/security/` 下 4 个 + `test/contract/` 下 1 个）
  - 测试文件位置澄清：单元测试全部在 `src/` 下（非 `test/`），共 233 个 spec 文件，其中 14 个需 Express→Fastify 类型替换
  - 移除依赖列表：移除 `request-ip` 和 `@types/request-ip`（当前 `package.json` 未安装此包）
  - 补充风险 §5.7：`@nestjs/throttler` Fastify 兼容性注意事项
  - 新增 §8 回滚方案：建议每 Phase 结束后打 git tag
  - 改动统计更新：删除文件从 5 增至 ~8（含对应的 spec 文件）

## Express → Fastify 迁移 — Phase 1: 基础设施切换

### 依赖变更

- `package.json` 新增 Fastify 相关包（不移除 Express 包，待全部迁移完成后统一移除）：
  - `@nestjs/platform-fastify` ^11.1.0
  - `@fastify/helmet` ^13.0.0、`@fastify/cors` ^11.0.0、`@fastify/static` ^9.0.0、`@fastify/view` ^11.0.0
  - `fastify` ^5.0.0
  - `@adminjs/fastify` ^4.2.0、`@fastify/session` ^11.1.0（为 Phase 4 预装）

### 新增文件

- `src/common/middleware/request-id.types.ts` — `REQUEST_ID_HEADER` 常量 + `FastifyRequestWithId` 接口（从 `request-id.middleware.ts` 提取类型定义，供 `setup-app.ts` 使用）
- `src/common/metrics/metrics.utils.ts` — `normalizeRoute` + `shouldSkip` 工具函数（从 `metrics.middleware.ts` 提取，供 `setup-app.ts` 使用）

### 修改文件

- `src/main.ts` — 切换为 `NestFactory.create<NestFastifyApplication>(AppModule, new FastifyAdapter({ trustProxy }), { bufferLogs: true })`，`trustProxy` 通过 `process.env[EnvKey.TRUST_PROXY]` 传入 `FastifyAdapter` 构造函数；`setupApp` 和 `registerAdminPanel` 均改为 `await`
- `src/setup-app.ts` — 全面重写：
  - 函数签名从 `sync (app: INestApplication)` 改为 `async (app: NestFastifyApplication): Promise<void>`
  - 移除 Express trust proxy 代码（已移至 `main.ts` 的 `FastifyAdapter` 构造参数）
  - `helmet()` → `await app.register(fastifyHelmet)`
  - `requestIdMiddleware` → `fastify.addHook('preHandler', ...)` 内联
  - `bindRequestContextMiddleware` → `fastify.addHook('preHandler', ...)` 内联
  - `createMetricsMiddleware` → `preHandler` + `onResponse` hooks 内联
  - `/metrics` 端点从 `app.use('/metrics', ...)` 改为 `fastify.get('/metrics', ...)`
  - `/api/docs` Scalar API Reference 从 `app.use('/api/docs', ...)` 改为 `fastify.get('/api/docs', ...)`，使用 `withFastify: true` + handler 类型转换
  - 移除所有 Express 类型导入（`Express`, `Request`, `Response`）
- `scripts/contract/export-openapi.ts` — 切换为 `FastifyAdapter`，`setupApp` 改为 `await`
- `test/helpers/e2e-helpers.ts` — `E2eApp` 类型从 `INestApplication<App>` 改为 `NestFastifyApplication`，`createNestApplication` 添加 `FastifyAdapter`，`setupApp` 改为 `await`
- `test/e2e/core/app.e2e-spec.ts` — 同步 `E2eApp` 类型 + `FastifyAdapter` + `await setupApp`
- `test/e2e/daily-records/daily-records.e2e-spec.ts` — 同上
- `test/e2e/medicines/medicines.e2e-spec.ts` — 同上
- `test/e2e/testing-support/testing-support.e2e-spec.ts` — 同上

### 验证结果

- `pnpm build` — 0 issues, 734 files ✅
- `pnpm typecheck` — 0 issues ✅
- `pnpm typecheck:tools` — 0 issues ✅
- `pnpm lint:check` — 0 errors ✅

### 已知限制（Phase 2+ 处理）

- Express 包仍保留在 `package.json` 中，待所有 Express 类型引用迁移完毕后统一移除
- `ApiExceptionFilter` 仍使用 `response.status().json()`（Express 风格），运行时异常响应可能出错 — Phase 2 修复
- `app.controller.ts` 的 `@Res({ passthrough: true }) response: Response` 仍使用 Express 类型 — Phase 3 修复
- `admin/setup.ts` 仍使用 `@adminjs/express` 和 `app.use()` — Phase 4 修复
- 旧中间件文件（`request-id.middleware.ts`、`metrics.middleware.ts`）仍存在但不再被 `setup-app.ts` 引用 — Phase 2 删除

## Express → Fastify 迁移 — Phase 2: 公共工具迁移

### 修改文件

- `src/common/api/sse.ts` — 类型从 `express.Response` 改为 `node:http.ServerResponse`，`status()` + `setHeader()` + `flushHeaders()` 改为 `writeHead()`，不引入 Fastify 类型依赖（控制器通过 `reply.raw` 传入）
- `src/common/api/sse.spec.ts` — mock 从 `{ status, setHeader, flushHeaders, write, end }` 改为 `{ writeHead, write, end }`
- `src/common/filters/api-exception.filter.ts` — `Request/Response` 从 express 改为 `FastifyRequest/FastifyReply`，`response.status().json()` 改为 `response.status().send()`，`request.originalUrl || request.url` 改为 `request.url`
- `src/common/filters/api-exception.filter.spec.ts` — mock response 从 `json` 改为 `send`，移除 `originalUrl`
- `src/common/interceptors/slow-request.interceptor.ts` — `Request` 改为 `FastifyRequest`，`request.originalUrl || request.url` 改为 `request.url`
- `src/common/interceptors/slow-request.interceptor.spec.ts` — mock request 移除 `originalUrl`
- `src/common/helpers/client-ip.ts` — `Request` 改为 `FastifyRequest`，`request.socket.remoteAddress` 改为 `request.raw.socket.remoteAddress`
- `src/common/helpers/client-ip.spec.ts` — mock request 从 `{ socket }` 改为 `{ raw: { socket } }`

### 删除文件（6 个）

- `src/common/middleware/request-id.middleware.ts` + `.spec.ts` — 逻辑已在 Phase 1 迁移到 `request-id.types.ts` + `setup-app.ts` Fastify hooks
- `src/common/logger/request-context.middleware.ts` + `.spec.ts` — 逻辑已在 Phase 1 迁移到 `setup-app.ts` Fastify hooks
- `src/common/metrics/metrics.middleware.ts` + `.spec.ts` — 逻辑已在 Phase 1 迁移到 `metrics.utils.ts` + `setup-app.ts` Fastify hooks

### Barrel export 更新

- `src/common/middleware/index.ts` — 从 `request-id.middleware` + `.spec` 改为仅 `request-id.types`
- `src/common/logger/index.ts` — 移除 `request-context.middleware` 导出
- `src/common/metrics/index.ts` — 从 `metrics.middleware` 改为 `metrics.utils`

### 附带完成项（提前从 Phase 3 拉入）

因 `client-ip.ts` 类型变更导致 build 失败，3 个 auth 控制器提前迁移：

- `src/modules/auth/controllers/local.controller.ts` — `@Req() request: Request` → `FastifyRequest`
- `src/modules/auth/controllers/oauth.controller.ts` — `@Req() request: Request` → `FastifyRequest`，`@Res() response: Response` → `FastifyReply`，`response.redirect(code, url)` → `reply.redirect(url, code)`（Fastify 5 签名变更）
- `src/modules/auth/controllers/session.controller.ts` — `@Req() request: Request` → `FastifyRequest`
- 对应 3 个 `.spec.ts` 文件同步更新 mock 类型

### 验证结果

- `pnpm build` — 0 issues, 728 files ✅
- `pnpm typecheck` — 0 issues ✅
- `pnpm typecheck:tools` — 0 issues ✅
- `pnpm lint:check` — 0 errors ✅
