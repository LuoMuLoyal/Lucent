# 2026-07-27 迁移日志

来源：`Lucent/plans/Lucent-review-2026-07-27.md`（全仓库审查报告修复）

## 🔴 #1: copy batch Map key 冲突修复

文件：`src/modules/today-suggestion/services/copy/copy.service.ts`、`src/modules/today-suggestion/services/suggestion.service.ts`

- `SuggestionCopyService` 新增静态方法 `buildResultKey(templateKey, params)`，以 `templateKey:sortedParams` 作为 Map key。
- `getOrEnqueueBatch` 和 `generateSyncBatch` 改用 `buildResultKey` 而非 `templateKey`，避免相同 templateKey 不同 params 的请求互相覆盖。
- `SuggestionService.resolveCopy` 新增 `params` 参数，通过 `buildResultKey` 查找对应文案结果。
- 更新 `suggestion.service.spec.ts` 的 mock Map 以使用 `buildResultKey`。

## 🟡 #2: limit 参数 NaN 校验

文件：`src/modules/today-suggestion/today-suggestion.controller.ts`

- `getHistory` 中 `parseInt(limit, 10)` 后增加 `Number.isNaN` 检查，NaN 时回退到默认值 100（由 `LifecycleService` 处理）。

## 🟡 #3: 日期边界 UTC → 本地时区

文件：`src/modules/today-suggestion/today-suggestion.controller.ts`、`src/modules/today-suggestion/services/lifecycle/service.ts`、`src/modules/today-suggestion/services/cache/suggestion-cache-invalidation.listener.ts`

- Controller 默认 endDate 从 `new Date().toISOString().slice(0, 10)` 改为 `formatDateOnly(now())`（使用 date-fns 本地时区格式化）。
- `LifecycleService.getDefaultStartDate()` 从 `setUTCDate/getUTCDate/toISOString` 改为 `setDate/getDate/formatDateOnly`。
- Cache invalidation listener 中 3 处 `new Date().toISOString().slice(0, 10)` 同步改为 `formatDateOnly(now())`。
- 更新对应 spec 测试以使用 `formatDateOnly(now())` 计算预期日期。

## 🟡 #4: 历史去重 key 移除 AI 生成字段

文件：`src/modules/today-suggestion/services/lifecycle/service.ts`

- `getHistory` 去重 key 从 `${ruleId}:${subtype ?? ''}:${title}:${reason}` 改为 `${ruleId}:${subtype ?? ''}`。
- `title` 和 `reason` 来自 LLM 生成，同一规则不同天的文案可能不同，导致本应去重的记录未被去重。
- 与 `getActiveSuggestionIds` 的 `ruleId:subtype` 去重逻辑保持一致。

## 🟡 #5: 缓存失效清除所有 excludeKey 变体

文件：`src/modules/today-suggestion/services/cache/suggestion-cache.service.ts`

- 新增 `excludeKeysRegistryKey` 和 `trackExcludeKey` 方法，在 `setSuggestions` 时将 excludeKey 记录到 registry。
- `invalidateSignals` 和 `invalidateSuggestions` 改用 `invalidateAllSuggestions`，读取 registry 后删除所有已记录的 suggestion cache 变体，再删除 registry 本身。
- Registry 过期时回退到删除 `none` key。
- 更新 `suggestion-cache.service.spec.ts` 验证多 excludeKey 变体的清除。

## 🟡 #6: excludeIds 过滤不再依赖数组顺序

文件：`src/modules/today-suggestion/services/suggestion.service.ts`

- 将 `activeItems` 数组拆分为 `primaryItem` 和 `secondaryItems`，不再通过 `activeItems[0]` 隐式假设 primary。
- `excludeIds` 过滤逻辑直接引用 `primaryItem` 和 `secondaryItems`，消除对 push 顺序的隐式依赖。

## 🟡 #7: Reports 阈值提取为命名常量

文件：`src/modules/reports/dashboard/presenter.service.ts`

- 将 `buildFindings` 和 `buildPatterns` 中的硬编码阈值提取为 `private static readonly` 常量：
  - `LOW_WATER_THRESHOLD_LITERS` (1.5L)
  - `HYDRATION_LOW_DAYS_THRESHOLD` (4 天)
  - `MEDICATION_STRONG_ADHERENCE_PERCENT` (80%)
  - `MEDICATION_STRONG_DAYS_THRESHOLD` (5 天)
  - `HYDRATION_STABLE_DAYS_THRESHOLD` (4 天)
  - `HYDRATION_STABLE_LITERS` (1.5L)
  - `SLEEP_GOOD_HOURS` (7h)
  - `SLEEP_STABLE_HOURS` (5h)
- 每个常量附带 JSDoc 注释说明其含义。

## 验证

- `pnpm lint:check` 通过（0 errors, 0 warnings）
- `pnpm test -- src/modules/today-suggestion src/modules/reports` 全部 394 测试通过
- `pnpm typecheck` 通过（pre-existing 的 `environment.validation.ts` LOG_FORMAT 错误已由下方日志优化修复）

## 🟢 #8: 日志格式优化 — 开发环境彩色 printf + 生产 JSON 时间戳

文件：`src/common/logger/logger.config.ts`、`src/common/logger/logger.config.spec.ts`、`src/config/env-keys.enum.ts`、`src/config/environment.validation.ts`、`.env.development.example`、`docs/01-reference/environment.md`

- 新增 `LOG_FORMAT` 环境变量（`pretty` | `json`），可在任何环境手动覆盖日志格式。
- 开发环境（`NODE_ENV=development`）默认使用彩色 `printf` 格式：时间戳、彩色级别、`[context]`、绿色 `[requestId]`（前 8 位）、消息、`{key=value}` 元数据、堆栈。
- 生产/测试环境保持 JSON 格式，新增 `timestamp` 字段。
- `winston.format.colorize()` 负责级别着色，`printf` 负责布局，`requestId` 用手动绿色 ANSI 突出。
- 环境校验（`environment.validation.ts`）新增 `LOG_FORMAT` 枚举校验。
- 新增 8 个格式选择测试（dev=pretty / test=json / prod=json / LOG_FORMAT 覆盖 / 时间戳 / requestId 标签 / 元数据）。
- 全部 28 个 logger 测试通过。
