# Migration Log - 2026-07-07

## 代码变更

### 代码审查修复 — 超时集中配置、错误处理工具函数、DTO 边界防御

依据 `plans/lucent-review-2026-07-07.md` 审查报告，修复了 3 项代码质量问题。

#### 问题 #2: AI 模型超时硬编码集中配置

- `src/config/constants.ts` 新增 `AI_MODEL_TIMEOUT_MS = 10_000` 常量
- `src/common/ai/base-ai-generator.service.ts` 的 `MODEL_OPTIONS.timeout` 改为引用常量
- `src/modules/assistant/agent/runtime.service.ts` 的 `CHAT_MODEL_OPTIONS.timeout` 改为引用常量
- 3 个测试文件的断言同步引用 `AI_MODEL_TIMEOUT_MS`：
  `llm-runtime.service.spec.ts`、`today-analysis/generator.service.spec.ts`、
  `reports/ai-summary/generator.service.spec.ts`

#### 问题 #3: 错误处理重复模式抽取为工具函数

- 新增 `src/common/helpers/error-info.utils.ts`，导出 `extractErrorInfo(error: unknown): { message: string; stack?: string | undefined }`
- 新增对应单元测试 `error-info.utils.spec.ts`（4 个测试用例）
- 已注册到 `src/common/helpers/index.ts` barrel 导出
- 替换了 13 处 `error instanceof Error ? error.message : String(error)` 重复模式，涉及 11 个文件：
  - `src/app.service.ts`（2 处：数据库探针、缓存探针）
  - `src/common/ai/base-ai-summary.service.ts`（2 处：generate、generateStream）
  - `src/modules/today-analysis/today-analysis.controller.ts`
  - `src/modules/reports/reports.controller.ts`
  - `src/modules/data-export/services/processor.service.ts`
  - `src/modules/daily-records/services/candidates/candidates.service.ts`
  - `src/modules/auth/providers/wechat-base-oauth.provider.ts`
  - `src/modules/auth/providers/qq-oauth.provider.ts`
  - `src/modules/auth/services/oauth/state.service.ts`
  - `src/modules/daily-records/services/meal-dish/decomposition.service.ts`
  - `src/modules/daily-records/services/meal-analysis/vision.service.ts`

#### 问题 #4: DTO 可选字符串字段添加 @IsNotEmpty() 边界防御

对关键字段添加 `@IsNotEmpty()`，防止空字符串绕过 `@IsString()` 校验。
保留 `UpdateAccountDto` 和 `UpdateHealthContextProfileDto` 中显式支持空字符串清除的字段不变。

修改的 DTO 文件：

- `CreateDailyRecordDto`: `title`、`value`、`unit`
- `UpdateDailyRecordDto`: `title`、`value`、`unit`
- `CreateDoseLogDto`: `currentMedicineId`、`doseText`
- `UpdateDoseLogDto`: `doseText`、`note`
- `CreateMedicineReminderDto`: `currentMedicineId`、`label`
- `UpdateMedicineReminderDto`: `currentMedicineId`、`label`、`note`

#### 问题 #1: 测试覆盖率补充

审查报告指出 5 个模块测试覆盖率偏低：`llm-runtime`、`user`、`account`、`notifications`、`medicine-reminders`。
本次为缺口最大的 `llm-runtime` 和 `medicine-reminders` 补充了测试文件，其余 3 个模块经
排查已有较完善的测试覆盖（service + controller spec 均在），不再重复补充。

##### llm-runtime（3 → 18 个测试）

- `llm-runtime.service.spec.ts` 从 3 个测试扩展到 18 个，按 `hasRoleConfig`、`createChatModel`、
  `createEmbeddingModel` 三个方法分组
- 新增覆盖：各角色缺 apiKey/baseUrl/model 的边界、provider 非 openai-compatible、
  无选项和部分选项的 createChatModel、createEmbeddingModel 完整路径（null/有值/dimension）

##### medicine-reminders（新增 3 个测试文件，40 个测试）

- 新增 `services/mapper.service.spec.ts`（29 个测试）：覆盖 toCreateData / toUpdateData /
  toDeliveryWhere / capDeliveryLimit / toItem / toDeliveryItem 全部公开方法
- 新增 `services/ownership.service.spec.ts`（7 个测试）：覆盖 ensureCurrentMedicineOwnedByUser /
  ensureOwnedByUser 的正常和异常路径
- 新增 `reminder-deliveries.controller.spec.ts`（5 个测试）：覆盖默认 limit、date+limit 参数、
  非数字 limit 回退、空字符串 limit 回退

##### account / notifications / user（评估结论：已较完善）

- `account`: service spec 12 个测试覆盖 getAccount/updateAccount/unlinkIdentity 全路径，
  controller spec 12 个测试覆盖全部端点
- `notifications`: service spec 14 个测试覆盖 create/findAll/findOne/markAsRead/markAsUnread/
  markAllAsRead/remove/getUnreadCount，controller spec 9 个测试覆盖全部端点
- `user`: service spec 13 个测试覆盖 findById/findByEmail/findByIdentity/findByProviderUnionId/
  create/createOAuthUser/linkIdentity/update/updateByEmail

## 验证

- `pnpm lint:check` ✅
- `pnpm typecheck` ✅
- `pnpm test:ci` ✅ (104 suites, 654 tests)
- `pnpm build` ✅
