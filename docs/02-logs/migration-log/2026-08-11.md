# Migration Log — 2026-08-11

## Proactive Suggestion Runtime — Task 7 final hardening

- Today Analysis 增加 stale claim 的新 activeVersion fence、用户时区 localDate resolver、物化状态与历史摘要 source/computed version fence；daily record 更新事件按 before/after kind/date 发送，普通记录不会误触发分析。
- REST DTO/OpenAPI 已显式描述 envelope、版本、状态、`computedAt` 和 `retryAfterSeconds`；SSE done event 使用明确的空 object schema。OpenAPI 已重新导出并同步 Flutter generated client。
- 验证：Today Analysis 及相关模块 73 files、755 tests；`pnpm format:check`、`pnpm lint:check --max-warnings=0`、`pnpm typecheck`、`pnpm build`、`pnpm exec prisma validate`、`pnpm docs:verify`、`pnpm docs:links` 通过。

## Today Analysis claim fence hardening

- 修正普通与流式生成在 claim 成功后使用 `claim.activeVersion` 写回 ready/failed 状态；claim fence 缺失时显式失败。生成失败的状态清理改为 best-effort，清理异常不覆盖原始生成异常；claim 类型收紧为判别联合，`releaseClaim` 显式接收 active fence。
- 增加同步/流式成功与失败、缺失 fence、清理失败保留原异常及 release fence 的回归测试。
- 规格/质量复审通过；定向 Today Analysis/materialization 测试 33/33、Prettier、ESLint、TypeScript typecheck 通过。

## Proactive Suggestion Runtime — Task 8/9 cross-repo checkpoint

- Luminous Today 已完成 `ready / stale / pending / failed / empty` 状态消费：GET 只读，相关数据变更 300ms 去抖刷新，resume 检查 `sourceVersion`，cold-start 恢复缓存，所有 GET FIFO 串行；对应提交为 `c1a8f9cbc`、`c071e35df`。
- Task 9 metrics 正在收尾：为 suggestion recompute 增加 enqueue/dedupe、job duration、ready/failed 和 stale age 的低基数 Prometheus 观测；标签不包含用户或健康内容。

## Proactive Suggestion Runtime — Task 9 完成

- Task 9 已完成并提交 `4428f67c`：Lucent 增加 suggestion recompute enqueue/dedupe、成功/失败 duration、materialization ready/failed 和 stale age 指标；所有 label 固定为低基数 operational label，不携带 userId、日期或健康内容。
- 验证通过：Task 9 定向测试 4 个 spec、49 tests；Today suggestion API E2E 11 tests；全量 `pnpm test`、`pnpm lint:check`（`--max-warnings=0`）、`pnpm typecheck`、`pnpm build`、`pnpm format:check`、Prisma validate、`pnpm docs:check`、`pnpm docs:verify`、`pnpm docs:links`。
- PostgreSQL `127.0.0.1:5432` 与 Redis `127.0.0.1:6379` live acceptance 已完成：写入 symptom record 后 worker 生成 ready materialization，首个 GET 只读取 ready/stale 结果；连续 10 次写入后最终 `computedVersion === sourceVersion` 且版本增加 10。
- `Active_Product_Loop.md` 已切换到 Sparse Record Semantics 下一阶段；本计划 Task 9 已执行完毕，临时 acceptance 脚本已删除。

## Sparse Record Semantics — Water canonicalization

- Today suggestion collector 与 Report dashboard context 共同使用纯 Water mapper；`ml`、`L`、`liter`、`litre` 统一为整数 ml，非法/缺失/负数/非有限值进入 `ignoredCount`，明确 `0 ml` 保留为 observed zero。
- Today 与 Report 新增 `ObservedMetric<number>` 水量投影，明确 `value/state/coverage/sources/observedCount/expectedCount/windowStart/windowEnd`；旧 Report 升数序列继续由同一 ml 投影派生，避免无记录日进入平均值。
- water-shortfall rule 对 unknown、partial 和无 value 的水量观察 abstain；旧 count 字段保留兼容，规则不再把 sparse unknown 当成不足事实。
- 验证：Water mapper 14 tests、Record collector Water 4 tests、Report context Water 1 test、Report computation Water 7 tests、water-shortfall 6 tests；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm build` 通过。

## Sparse Record Semantics — Water downstream alignment

- Today Analysis context 与 Report AI context 也已消费共享 `ObservedMetric<number>`；unknown 不再产生补水缺口，Report AI 的 tracked days 包含明确 observed zero。
- water-shortfall 的 canonical 路径按 `observedMl / targetMl` 判定并使用 ml evidence；旧 target count 仅作为兼容字段，来源标记为 derived。
- `ObservedMetric` 的 summary factory 已提取到 shared metrics helper，Today、Report、AI context 不再各自复制 state/coverage/window 构造。

## Sparse Record Semantics — Water report and copy boundary

- Report findings、patterns、trend 和 sparkline 只消费 sufficient observed 水量；明确 `0 ml` 保留为 0，unknown/partial 不再投影为 0，也不进入平均值。现有升数数组继续作为兼容投影，unknown 点被排除，直到 observed metric DTO 在后续合同阶段公开。
- water-shortfall 的 copy params 已收敛为 `observedMl`、`targetMl`、`completionRate`、`consecutiveDays`；LLM 不再接收未标注的 completed/target/remaining record-count 字段。
- water trend 的兼容 `dailyCounts` 与 `targetCount` 增加 `semantics: legacy_record_count`、`source: daily_record` 标记。

## Sparse Record Semantics — Medication reminder slots

- 服药 collector 与 Report dashboard 统一以 `reminderId + scheduledFor + scheduledTime` 识别计划槽位；同药同日多次提醒不再合并。
- `planned` 在消费合同中映射为 `unconfirmed`；taken 为 adherence 分子，skipped 与 overdue-unconfirmed 保留独立计数，部分观察返回 `coverage: partial`。
- 无 reminder 的临时 dose log 每条独立保存，不进入计划槽位分母；无计划窗口的 `observedMetric` 返回 `unknown`，不伪造 0%。legacy log 仅在 medicine + scheduledTime 唯一时兼容匹配提醒。
- 验证：dose-log、medication collector、Report dashboard 定向测试 4 files / 80 tests；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm build`、`pnpm format:check` 通过。

## Sparse Record Semantics — Sleep episodes

- 睡眠 payload 新增 `sleepType: nightSleep|nap`、`startedAt`、`endedAt`、`durationMinutes` 和可选 `quality` 校验；结束时间必须晚于开始时间，跨午夜区间有效，旧 `startAt/endAt` 继续按 nightSleep 读取。
- Today record collector 保留同日所有睡眠 episode，分别计算 night、nap、all-sleep 总时长并返回 `ObservedMetric`；区间重叠只产生 `sleep_episode_overlap` data-quality warning，不删除任一记录。
- 验证：daily-record service 与 Today record collector 定向测试 2 files / 59 tests；`pnpm typecheck`、`pnpm lint:check --max-warnings=0`、`pnpm format:check` 通过。

## Sparse Record Semantics — Observed metric OpenAPI contract

- Report metric、Today suggestion item 与 Today Analysis data 新增同构 `observedMetric` schema，统一描述 `value`、`state`、`coverage`、`sources`、观察/期望计数及时间窗；`value` 与 `expectedCount` 明确为 required nullable，unknown 不会由 schema 默认成 0。
- Report 旧 scalar metric 字段保留一个兼容周期并标记 deprecated；OpenAPI 由 `pnpm export:openapi` 重新生成，生成结果为 114 paths / 258 schemas。
- 验证：DTO typecheck、`pnpm lint:check --max-warnings=0`、`pnpm format:check`、`pnpm build` 和 OpenAPI 语义检查通过。

## Sparse Record Semantics — Task 9 文档收口

- 依据已落地实现复核并收口 `Active_Product_Loop.md`、`TODO.md` 与相关合同文档：water ml/coverage、服药 reminder slot、sleep episode 和 `ObservedMetric` OpenAPI contract 均作为已完成事实保留；TODO 删除对应的 water/sleep 条目，继续保留尚未实现的 Review 读模型、旧 Report 指标修正、报告出口降级和闭环测量。
- 关联实施计划已执行完毕，计划文件已删除（实施完毕文件已删）；本次文档收口不新增计划文件，也不改变既有 `.obsidian` 工作区改动。
- 已核对 `reminder-contract.md` 与 `data-export-contract.md`：槽位身份、临时 dose log、unknown/partial 覆盖率、canonical ml 与 deprecated scalar 投影的边界与当前实现一致。

## Today Analysis fallback specs

- 补齐 `TodayAnalysisCopyService` fallback specs 的 `observedMetric` 水量夹具；unknown water 保持不触发 hydration fallback，修正 f3f0b210 水量语义迁移遗漏的测试覆盖。
