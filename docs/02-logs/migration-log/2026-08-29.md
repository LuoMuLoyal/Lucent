# 2026-08-29

## 代码审查修复 — 08-28 审查报告

基于 08-28 审查报告（`plans/Lucent-review-08-28.md`）中的 3 项 Suggestion，执行修复。

### S-1：dose-logs 临时打卡测试覆盖偏窄

- `dose-logs.service.spec.ts`：将原「临时打卡正向用例」重命名为更精确的 `should create mark when currentMedicineId is provided without scheduledTime (temporary log without reminder)`，并新增第 2 个用例 `should reject mark with RESOURCE_NOT_FOUND when reminder exists but currentMedicineId resolves to null`。
- 新用例覆盖「有 reminder + `findCurrentMedicineById` 返回 null」分支，确保 `buildMarkLookupWhere` 返回非 null `where` 时不会误入 `where == null && currentMedicineId == null` 的 validationFailed 分支，而是正确返回 `RESOURCE_NOT_FOUND`。

### S-2：acorn 版本范围收紧

- `package.json`：`acorn` devDependency 范围从 `^8.0.0` 收紧为 `^8.18.0`，与当前实际安装版本一致，避免 8.0.0 ~ 8.17.x 范围内引入回归。

### S-3：acorn 类型断言 hack 标注 TODO（持续跟踪）

- `eslint-plugins/error-handling.spec.ts`：在 `as unknown as import('estree').Program` 断言处新增 `TODO(acorn9)` 注释，标注未来升级 acorn 9.x 时应迁移到 `@types/estree` 或 acorn 自身 `ParseResult` 类型。本次不修代码，仅标注跟踪。

### 验证

- `dose-logs.service.spec.ts`：23 个测试全部通过（原 22 + 新增 1）。
- `eslint-plugins/error-handling.spec.ts`：9 个测试全部通过。
- `pnpm lint:check`（修改文件）：通过。

## R-4 #21: legacy 图表改按 observedMetric 口径输出

- `src/modules/reports/dto/report-dashboard-response.dto.ts`:
  `ReportTrendDto` 新增可选 `observedMetric: ReportObservedMetricDto` 字段，
  `values` 的 `@ApiProperty` 增加 description 标注 observed-only 语义。
- `src/modules/reports/dashboard/computation.service.ts`:
  新增 `buildTrend()` 私有方法，从 observedSeries 提取 observed-only values
  （unknown 天省略），附带 `observedMetric` 汇总（observedCount/expectedCount/coverage）。
  三个 trend（medication/water/sleep）均改为通过 `buildTrend()` 构建。
  sleep 无 `observedSleepSeries` 时走 scalar fallback。
- `src/modules/reports/dashboard/context.service.ts`:
  更新 medication/water scalar series 注释，明确 scalar 仅用于 metric sparkline 和 presenter，
  trend values 在 computation 中从 observedSeries 直接提取（不再经过 0 投影）。
- `src/modules/reports/dashboard/computation.service.spec.ts`:
  更新 trend 断言为 `objectContaining`（允许新增 observedMetric 字段），
  新增 water trend observedMetric 覆盖率断言（observedCount=2, expectedCount=3, coverage=partial）。
- `docs/openapi.json`: 重新导出。

## CI 修复：pnpm 版本冲突 + Docker build 缺少 DATABASE_URL

- `.github/workflows/lucent-ci.yml`:
  移除三个 job 的 `pnpm/action-setup@v4` `version: 11.9.0` 参数，
  让 action 自动从 `package.json#packageManager` (pnpm@12.0.0) 读取版本，
  消除 "Multiple versions of pnpm specified" 错误。
  给 `ci-lint-typecheck` 和 `ci-unit` job 的 env 添加占位 `DATABASE_URL`，
  使 `pnpm install` 的 postinstall（prisma generate）和显式 prisma:generate 步骤
  能通过 `prisma.config.ts` 的环境变量校验。
- `Dockerfile`:
  在 `RUN pnpm prisma:generate` 前添加
  `ENV DATABASE_URL=postgresql://placeholder:...` 占位值，
  解决 Docker build 阶段 prisma config 校验失败。
