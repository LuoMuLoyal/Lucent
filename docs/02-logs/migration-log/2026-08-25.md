# 2026-08-25

## 错误处理基础设施层扫尾 — P0 修复

基于三轮代码审查报告（08-23、08-24、08-25）的根因分析，启动错误处理重构遗漏的基础设施层修复。
计划文档：[`plans/2026-08-25-error-boundary-infra-sweep.md`](../../plans/2026-08-25-error-boundary-infra-sweep.md)

### P0-1：RedisService.onModuleDestroy 静默吞错修复

- `src/common/redis/redis.service.ts`：
  - `onModuleDestroy` 中 `.catch(() => undefined)` 改为 `.catch(error => this.logger.warn(...))`。
  - graceful shutdown 时 Redis quit 失败不再被静默吞掉，运维可感知关闭失败。
- `src/common/redis/redis.service.spec.ts`：
  - 新增测试：quit 失败时输出 warn 日志且不抛出。

### P0-2：AssistantToolService 缓存错误保护

- `src/modules/assistant/tools/tool.service.ts`：
  - 新增 `cacheGet<T>(key)` 包装器：Redis 故障时降级为 cache miss（返回 `undefined`），记录 warn 日志。
  - 新增 `cacheSet(key, value, ttl)` 包装器：Redis 故障时记录 warn 日志，不影响结果返回。
  - `executeOne` 中裸 `this.cache.get` / `this.cache.set` 调用改为 `this.cacheGet` / `this.cacheSet`。
  - 降级策略与 `SuggestionCacheService`（catch 后 rethrow）不同——AssistantToolService 的缓存是纯加速（miss 直接走 `executeUncached`），所以降级为无缓存执行更合理。
- `src/modules/assistant/tools/tool.service.spec.ts`：
  - 新增测试：cache get 失败时降级为无缓存执行，调用底层服务并返回结果。
  - 新增测试：cache set 失败时不影响结果返回。

### 验证

- `npx vitest run` 两个 spec 文件：19 测试全部通过。
- `npx eslint` 4 个文件：无错误。
- `pnpm typecheck`：通过。

### 提交结构

- P0-1（RedisService.onModuleDestroy）与 P0-2（AssistantToolService 缓存保护）分别原子化提交。
- 迁移日志和 `code-quality.md` 参考文档随 P0-1 提交，P0-2 追加本条目。

## 错误处理基础设施层扫尾 — P1 裸 throw 收口

将基础设施层四处裸 `throw new Error` 改为抛 `DomainFailureException`，使 `ApiExceptionFilter` 能正确
映射 HTTP status（503 `DEPENDENCY_UNAVAILABLE` 或 500 `INTERNAL_ERROR`），而非无差别 500。

### P1-1：domain-failure.ts 裸 throw

- `src/common/result/domain-failure.ts`：`createDomainFailure` 校验失败时，由裸 `throw new Error`
  改为 `throw new DomainFailureException({ _tag, kind: 'internal', code: 'INTERNAL_ERROR', ... })`。
  避免递归调用 `createDomainFailure`，直接构造字面量对象。

### P1-2：domain-failure.mapper.ts 裸 throw

- `src/common/result/domain-failure.mapper.ts`：`toProblemDetails` 校验失败时，由裸 `throw new Error`
  改为 `throw new DomainFailureException`，携带 `INTERNAL_ERROR` DomainFailure。

### P1-3：RedisService.atomicIncrement 裸 throw

- `src/common/redis/redis.service.ts`：Redis 不可用时由裸 `throw new Error('Redis is not available')`
  改为 `throw new DomainFailureException(createDomainFailure({ kind: 'dependency', code: 'DEPENDENCY_UNAVAILABLE', retryable: true }))`。
  调用方 `VerificationCodeService` 和 `AuthRateLimitService` 通过 `lift` 包装后，
  `DomainFailureException` 被 `mapUnknownToDependencyFailure` 正确识别为 503 而非 500。

### P1-4：S3StorageRuntime.createSignedGetUrl 裸 throw

- `src/common/storage/s3.runtime.ts`：外部端点未配置时由裸 `throw new Error` 改为
  `throw new DomainFailureException(createDomainFailure({ kind: 'dependency', code: 'DEPENDENCY_UNAVAILABLE' }))`。

### 测试

- `domain-failure.spec.ts`：kind/code 拒绝测试改为验证抛出 `DomainFailureException` 类型。
- `domain-failure.mapper.spec.ts`：mapper 拒绝测试改为验证抛出 `DomainFailureException` 类型。
- `redis.service.spec.ts`：`atomicIncrement` 不可用测试改为验证抛出 `DomainFailureException`。
- `verification-code.service.spec.ts`：17 个测试全部通过，调用方不受影响。
- 共 37 个测试通过，lint 和 typecheck 通过。

### P1 提交结构

- P1-1/P1-2（domain-failure + mapper）与 P1-3/P1-4（Redis + S3）分两批原子化提交。

## 错误处理基础设施层扫尾 — P2 降级日志与内省防御

### P2-1：SuggestionCacheInvalidationListener 失败日志升级

- `src/modules/today-suggestion/services/cache/suggestion-cache-invalidation.listener.ts`：
  - 新增 `consecutiveFailures` 计数器和 `ERROR_THRESHOLD = 3`。
  - 提取 `onCacheSuccess()` 重置计数器，`handleCacheError()` 统一处理失败日志。
  - 连续失败 3 次后日志从 warn 升级为 error，触发监控告警。
  - 成功时计数器重置，避免瞬时故障累积。
- `src/modules/today-suggestion/services/cache/suggestion-cache-invalidation.listener.spec.ts`：
  - 新增测试：连续失败 3 次后日志升级为 error。
  - 新增测试：成功后计数器重置。

### P2-2：MedicinesCacheAdminService 内省防御增强

- `src/modules/medicines/cache/admin.service.ts`：
  - `listMedicineKeys` 中 stores 为空时新增 warn 日志。
  - rawStore 不暴露 keys() 时新增 warn 日志。
  - cache-manager 版本升级后内省失败可观测，不再静默降级。
- `src/modules/medicines/cache/admin.service.spec.ts`：
  - 新增测试：stores 为空时记录 warn 日志。

### 验证

- 18 个测试全部通过，lint 和 typecheck 通过。

### P2 提交结构

- P2-1（SuggestionCacheInvalidationListener）与 P2-2（MedicinesCacheAdminService）分别原子化提交。

## 错误处理预防性扫尾 — 残留缓存保护与裸 throw 收口

全仓库扫描发现前轮扫尾仍有遗漏：`risk-check.service.ts` 缓存调用无保护、4 处业务代码裸 `throw new Error`、12 处空 catch 块缺少日志。
本轮修复全部残留，并新增 ESLint 自定义规则与 AGENTS.md 规范从源头预防。

### 残留缓存保护

- `src/modules/medicines/services/risk/risk-check.service.ts`：
  - `getRecords` 中 `cache.get` / `cache.set` 裸调用改为 try-catch 包裹。
  - `cache.get` 失败时 warn 日志 + rethrow（与 `SuggestionCacheService` 一致）。
  - `cache.set` 失败时 warn 日志 + 降级（不影响返回值）。

### 业务代码裸 throw 收口

将 4 处业务代码中的裸 `throw new Error` 改为 `throw new DomainFailureException`，
使 `ApiExceptionFilter` 能正确映射 HTTP status：

- `src/modules/reports/services/event-review/review.service.ts`：
  `toEventDto` 中 `event.kind == null` 不变式违反 → `INTERNAL_ERROR`。
- `src/modules/reports/services/clinic-summary/summary.service.ts`：
  `resolveScope` 中 `eventReview == null` 前置条件 → `INTERNAL_ERROR`。
- `src/modules/medicine-reminders/services/delivery-receipts.service.ts`：
  `createLocalReceipt` 中写入后行缺失不变式 → `INTERNAL_ERROR`。
- `src/modules/data-export/services/queue.service.ts`：
  `enqueue` 中 queue 未配置 → `DEPENDENCY_UNAVAILABLE`（503 可重试）。

### 空 catch 块补充日志

按 ADR-0012「No catch may silently discard an error」要求，为 9 处空 catch 块补充 warn 日志：

- `src/modules/today-suggestion/services/collectors/medication.service.ts`：时区校验失败。
- `src/modules/notification-preferences/services/weekly-insight-scheduler.service.ts`：时区日期解析失败。
- `src/modules/notification-preferences/services/notification-preferences.service.ts`：偏好读取失败。
- `src/modules/assistant/services/memory.service.ts`：JSON 数组解析失败。
- `src/modules/assistant/tools/medicine/lookup.service.ts`：查询 payload 解析失败。
- `src/modules/assistant/tools/vector/vector-cursor.ts`：游标解码失败。
- `src/modules/assistant/tools/drugbank/entity-resolve.service.ts`：解析 payload 失败。
- `src/modules/assistant/tools/tool.service.ts`：缓存条目损坏。
- `src/common/llm/base-llm-generator.service.ts`：流清理回调失败。

### ESLint 自定义规则 — 错误处理预防机制

新增两条 ESLint 自定义规则，从源头预防 ADR-0012 违规：

- `eslint-plugins/error-handling.ts`：本地 ESLint 插件，定义两条规则：
  - `no-bare-throw-error`：禁止非 spec 文件中使用 `throw new Error(...)`，
    强制使用 `DomainFailureException` 或 NestJS `HttpException` 子类。
  - `no-silent-catch`：禁止空 catch 块或无日志/无 re-throw 的 catch 块。
- `eslint.config.ts`：
  - 注册 `error-handling` 插件，规则级别 `error`。
  - 测试文件（`*.spec.ts`、`*.test.ts`）关闭两条规则。
  - 新增 override 块：20 个现有违规文件暂时关闭规则，每修复一个文件就从
    列表中删除对应条目。TODO 注释标记清理完毕后删除整个 override 块。
  - `eslint-plugins/` 目录加入 ignores 列表。
- 已修复文件添加 `// eslint-disable-next-line` 豁免注释：
  - `prisma.service.ts`（构造函数 env 校验）
  - `registry.service.ts`（启动阶段不变式）
  - `worker.service.ts`（4 处控制流异常 + 1 处延迟 catch）
  - `processor.service.ts`（BullMQ worker re-throw）

### AGENTS.md 错误处理规则

- `AGENTS.md`：新增 "Error Handling Rules (ADR-0012)" 章节，明确四条规则：
  1. 禁止裸 `throw new Error()`（构造函数/init 阶段豁免）
  2. 禁止静默 catch（延迟 catch 需豁免注释）
  3. 缓存调用必须有 try-catch 保护
  4. 控制流异常应重构为判别联合返回
