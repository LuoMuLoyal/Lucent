# 2026-08-22

## Today Analysis 响应封装与失败可观测性

Today Analysis 控制器移除显式 `successEnvelope`，统一交给全局 interceptor 包装；版本化生成/流式生成的持久化失败补充 Logger 与 OTel 记录，保持原有回退和重抛语义，不改变 API 合同。

验证：Today Analysis 相关 spec、typecheck、build 通过。

## Assistant 再生与工具详情安全边界

再生流程无内容时返回稳定的内部错误码和安全文案，并记录 Logger/OTel；工具详情 metadata 改用 strict Zod schema 校验，畸形数据告警后按无详情降级；补齐 SSE DTO 与运行时类型职责说明。

验证：assistant runtime、core service 相关 spec 通过。

## Assistant 记忆与工具执行可观测性

为记忆提取增加独立超时、warning 与 OTel event；工具迟到结算记录工具名和耗时；标题生成失败提升为 warning 并记录 OTel，避免异步失败静默。

验证：assistant memory、tool、conversation service 相关 spec 通过。

## Assistant 记忆清理审计与事务边界

清除助手记忆成功后写入审计事件，助手会话交互事务增加 `maxWait` 和 `timeout`，避免数据库等待无限延长；补充 controller/repository 回归断言。

验证：assistant controller 与 conversation repository 相关 spec 通过。

## 跨模块缓存失败日志与审查文档清理

为认证、用药、报告、Today suggestion、用户设置和分析上下文的缓存/异步失败路径补齐 warning 日志，保留原有降级行为。8-18 整改计划已执行完毕并从计划索引移除；8-22 审查报告仅作为本次输入，结论已沉淀在本迁移日志中，现一并删除。

验证：相关代码通过 lint、typecheck、build、format、test:ci、docs:check 和 OpenAPI 导出验证。

## 提交钩子暂存区边界

Pre-commit 文档覆盖检查改为显式使用 `--staged`，只根据当前提交暂存区判断代码与文档是否成对，不再因为工作区中尚未准备提交的下一批代码阻断当前原子提交。

验证：hook 配置与工具链文档同步，后续提交按独立意图拆分。

## 响应契约硬切：Problem Details HTTP 边界

新增 Lucent 目标态 Problem Details builder，并将全局异常 filter 切换为
`application/problem+json`。错误体使用稳定字符串 `code`、`type`、`title`、`detail`，验证错误使用结构化 `errors`；未知 5xx 只返回安全的内部错误文案并保留日志/trace 关联，不输出 `statusCode`、`requestId` 或内部异常详情。保留旧数字错误码仅作为服务端内部输入到稳定字符串的暂时映射，客户端不再看到旧格式。

本阶段未移除全局成功 envelope 或批量迁移 controller；成功资源/OpenAPI 切换作为下一阶段继续执行。SSE 错误 payload 和生成客户端同步仍待对应契约切换完成。

验证：Problem Details 与 filter 目标 spec、error payload 相关 spec、Lucent typecheck 通过；文档检查仅保留可选 Runtime Snapshot 建议。

## 响应契约硬切：首批公共资源 controller

将 app-info、environment snapshot、files upload、legal documents、notification preferences、product events 和 testing-support controller 的成功返回改为直接资源，不再在 controller 中显式调用 `successEnvelope`。对应 controller spec 已改为断言资源本身；全局 success interceptor 暂留，等待剩余 controller 完成后统一删除。

验证：首批 7 个 controller 的定向 spec 通过；未导出 OpenAPI，等待全量成功响应切换完成。

## 响应契约硬切：Auth/Account controller 批次

将认证注册、登录、OAuth 回调与授权地址、会话、刷新令牌，以及账户资料、密码、邮箱和身份关联 controller 的成功响应切换为直接资源；无 body 的操作改为 `204 No Content`。对应 controller、共享认证响应 helper 和单测不再依赖成功 envelope 断言。全局成功 interceptor、旧成功 DTO 与 OpenAPI 导出继续保留到全部 controller 迁移完成后统一删除和更新。

验证：Auth/Account controller 相关定向 spec 和文档覆盖检查通过。

## 响应契约硬切：日常记录、健康事件与用户域 controller 批次

将 daily-records、health-events、user-health-context 和 user-settings controller 的成功响应切换为直接资源；删除操作改为 `204 No Content`。对应单测移除成功 envelope 断言，保留用户归属、参数转发和资源映射行为。同步更新 Mine/Settings 合同与健康事件当前状态文档；全局成功 interceptor、旧成功 DTO 与 OpenAPI 导出继续等待全部 controller 迁移完成后统一处理。

验证：4 个 controller spec、typecheck 和文档覆盖检查通过。

## 响应契约硬切：药品、剂量、提醒与通知 controller 批次

将 medicines、medicine-dose-logs、medicine-reminders、reminder-deliveries 和 notifications controller 的成功响应切换为直接资源；dose log 与 reminder 删除操作改为 `204 No Content`。对应单测移除旧成功 envelope 断言，保留缓存绕过、分页、提醒槽位、投递回执、通知归属和未找到错误语义。同步更新 reminder/notification 合同；全局成功 interceptor、旧成功 DTO 与 OpenAPI 导出继续等待全部 controller 迁移完成后统一处理。

验证：5 个 controller spec、typecheck 和文档覆盖检查通过。

## 响应契约硬切：助手、建议与数据导出 controller 批次

将 assistant、today-suggestion 和 data-export controller 的成功 JSON 响应切换为直接资源；SSE 端点继续使用独立的事件流结构，不套用普通 HTTP 成功 envelope。对应单测移除旧 envelope 断言，保留助手会话、提案确认、记忆清理、建议反馈/解释/轮询、历史查询和数据导出空结果语义。同步更新助手与数据导出合同；全局成功 interceptor、旧成功 DTO 与 OpenAPI 导出继续等待 reports controller 完成后统一处理。

验证：3 个 controller spec、typecheck 和文档覆盖检查通过；SSE 失败路径测试产生预期的 controller error 日志。

## 响应契约硬切：Reports controller 批次

将 reports controller 的 dashboard、AI summary、clinic summary、分享管理、PDF 异步状态和 event review 成功 JSON 响应切换为直接资源；撤销分享改为 `204 No Content`。PDF 二进制下载和 SSE 事件流保持各自媒体类型与事件结构，不混入普通 JSON envelope。对应 reports spec 已移除旧 envelope 断言，并保留分享缓存回滚、未找到、异步回退、事件 review 和 SSE 错误路径。

验证：reports controller 定向 spec、typecheck 和文档覆盖检查通过；SSE 失败路径测试产生预期的 controller error 日志。
