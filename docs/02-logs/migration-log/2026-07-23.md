# 2026-07-23 — 代码风格一致性大重构

## 概要

系统性消除 Lucent 后端代码中的风格不一致问题，使全部 `src/` 代码严格遵守 `AGENTS.md` 中的 Barrel Exports、File Naming、Module Subdirectory Whitelist 等规则。

## 执行的阶段

### 阶段 1 — Barrel 补全（P0）

为 17 个缺失 `index.ts` 的模块子目录补齐 barrel 导出：

- `user/services/`, `account/services/`, `testing-support/services/`
- `auth/types/`, `daily-records/constants|prompts|schemas|types/`
- `medicine-dose-logs/services/`, `notifications/services/`, `files/services/`
- `environment/services/`, `data-export/constants|utils/`
- `user-settings/constants|services/`, `support-resources/services/`

同时补全 6 个已有 barrel 缺失的 service 导出：

- `medicines/services/index.ts` — 补 `medicine-recognition-queue.service`
- `medicines/dto/index.ts` — 补 `recognize-medicine.dto`
- `today-analysis/services/index.ts` — 补 `analysis-queue.service`
- `reports/services/ai-summary/index.ts` — 补 `summary-queue.service`
- `reports/services/clinic-summary/index.ts` — 补 `pdf-queue.service`
- `today-suggestion/services/explanation/index.ts` — 补 `queue.service`
- `today-suggestion/services/cache/index.ts` — 补 `suggestion-cache-invalidation.listener`

### 阶段 2 — 跨模块深路径导入统一（P0）

将所有跨模块深路径导入替换为 barrel 导入，涵盖：

- `UserPayload` / `AuthService` — 统一从 `../auth/services` 导入（~22 个文件）
- `CurrentUser` / `Public` 装饰器 — 统一从 `../auth/decorators` 导入（~13 个文件）
- `PrismaModule` / `PrismaService` — 统一从 `../../prisma` 导入（~12 个文件）
- `common/helpers/*` — 统一从 `../../common/helpers` 导入（~150+ 个文件）
- `common/interceptors/*` — 统一从 `../../common/interceptors` 导入
- `common/api/*` — 统一从 `../../common/api` 导入
- 模块内部深路径 — 替换为子目录 barrel 导入

同时合并所有重复导入（从同一 barrel 的多个 import 合并为一个）。

3 个 `vi.mock` spec 文件更新为 `importOriginal` 模式以保留真实导出。

### 阶段 3 — 裸 `types.ts` / `constants.ts` 文件重命名（P1）

将 8 个违反 AGENTS.md 规则 3 的裸类型词文件名重命名：

| 原路径                                   | 新路径                    |
| ---------------------------------------- | ------------------------- |
| `daily-records/types/types.ts`           | `record.types.ts`         |
| `assistant/types/types.ts`               | `assistant.types.ts`      |
| `admin/types/types.ts`                   | `admin.types.ts`          |
| `medicine-reminders/types/types.ts`      | `reminder.types.ts`       |
| `user-health-context/types/types.ts`     | `health-context.types.ts` |
| `legal-documents/constants/constants.ts` | `legal.constants.ts`      |
| `admin/constants/constants.ts`           | `admin.constants.ts`      |
| `user-settings/constants/constants.ts`   | `settings.constants.ts`   |

所有引用已同步更新为 barrel 导入。

### 阶段 4 — `@ApiBearerAuth` 放置位置统一（P1）

将 4 个 controller 的 `@ApiBearerAuth('access-token')` 从方法级别移到类级别：

- `daily-records.controller.ts`（7 处 → 类级别）
- `medicine-reminders.controller.ts`（4 处 → 类级别）
- `medicine-dose-logs.controller.ts`（5 处 → 类级别）
- `medicines.controller.ts`（3 处 → 类级别，`@Public()` 方法覆盖）

### 阶段 5 — `UserPayload` 规范导出路径确定（P1）

确定 `UserPayload` 的规范导出路径为 `../auth/services`（通过 barrel），在阶段 2 中一并完成。

### 阶段 7 — Spec 文件归位（P2）

删除 `data-export/data-export.service.spec.ts`（重复，正确版本已在 `services/export.service.spec.ts`）。

### 阶段 9 — 构造函数命名统一（P2）

- `medicine-dose-logs.controller.ts`: `this.service` → `this.doseLogsService`
- `medicine-reminders.controller.ts`: `this.service` → `this.remindersService`

### 阶段 10 — `assistant.service.ts` 移入 `services/`（P2）

删除遗留的 `assistant/assistant.service.spec.ts`（对应文件已重命名为 `services/core.service.ts`，spec 已在 `services/core.service.spec.ts`）。

### 阶段 13 — `support-resources-reference.ts` 归属调整（P3）

将非 `@Injectable()` 的纯数据文件从 `services/` 移到 `constants/`：

- `services/support-resources-reference.ts` → `constants/support-resources-reference.ts`
- `services/support-resources-reference.spec.ts` → `constants/support-resources-reference.spec.ts`
- 创建 `constants/index.ts` barrel
- 更新 `services/index.ts` 移除对应导出
- 更新 `resources.service.ts` 导入路径

### 阶段 14 — ESLint `no-restricted-imports` 扩展（收尾）

在 `eslint.config.ts` 中新增 7 个 barrel 强制规则，防止深路径导入回退：

- `**/auth/services/*`
- `**/auth/decorators/*`
- `**/auth/types/*`
- `**/prisma/prisma.*`
- `**/common/helpers/*`
- `**/common/interceptors/*`
- `**/common/api/*`

### 阶段 11 — `import type` 语法统一（P3）

在 `eslint.config.ts` 中将 `@typescript-eslint/consistent-type-imports` 规则显式配置为 `inline-type-imports` 风格：

```typescript
'@typescript-eslint/consistent-type-imports': [
  'error',
  { fixStyle: 'inline-type-imports' },
]
```

新代码中混合类型/值导入将使用 `import { Foo, type Bar }` 内联写法，替代分离的 `import type` 语句。

### 阶段 12 — `today-suggestion` 规则 Service 独立 Spec（P3）

将 932 行的 `rules.spec.ts` 拆分为 9 个独立 spec 文件，与各自 Service 一一对应：

- `missed-dose.service.spec.ts`（5 tests）
- `water-shortfall.service.spec.ts`（4 tests）
- `sleep-shortfall.service.spec.ts`（2 tests）
- `deteriorating-trend.service.spec.ts`（2 tests）
- `coverage.service.spec.ts`（3 tests）
- `caffeine-sleep.service.spec.ts`（8 tests）
- `mood-sleep.service.spec.ts`（7 tests）
- `rule-version-registry.service.spec.ts`（8 tests）
- `registry.service.spec.ts`（2 tests）

共享辅助函数 `buildContext()` / `buildSignal()` 提取到 `test-helpers.ts`。

## 未执行阶段

- **阶段 8** — `medicines.controller.ts` 响应信封修复（涉及 API 变更，需同步更新前端）

## `localizeEvidenceValue` 类型修复

### 背景

`pnpm typecheck` 报错 TS2322：`SuggestionService.localizeEvidenceValue()` 中 `this.i18n.t()` 返回类型被 `tsc` 推断为 `unknown`（变量赋值丢失了泛型上下文推断），导致三元表达式 `translated === key ? value : translated` 中的 `translated` 无法赋给函数的 `string` 返回类型。

### 修复

对 `i18n.t()` 的返回值添加 `as string` 类型断言。由于 ESLint 的 `@typescript-eslint/no-unnecessary-type-assertion` 规则认为该断言多余（ESLint 推断返回类型为 `string`），添加 `eslint-disable-next-line` 注释说明 `tsc` 与 ESLint 的推断分歧。

**文件:** `src/modules/today-suggestion/services/suggestion.service.ts`

## 验证

- `pnpm lint:check` — 通过（0 errors）
- `pnpm typecheck` — 通过
- `pnpm test` — 通过（254 files, 2627 tests）
- `pnpm docs:check` — 通过（warning-only）
