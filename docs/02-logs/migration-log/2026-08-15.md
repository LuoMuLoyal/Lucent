# Migration Log — 2026-08-15

## 2026-08-14 增量代码审查修复 — 批量上报端点专属限流

基于 `plans/Lucent-review-2026-08-14.md`（实施完毕文件已删）审查报告修复 5 项 🟡 警告与 1 项维护建议（未发现 🔴 严重问题），按原子化提交逐项落地，本日志逐提交追加对应条目。

- `POST /product-events`（`product-events.controller.ts`）在全局 throttler（100 req/min）之上叠加 `@Throttle({ default: { ttl: 60_000, limit: 10 } })`：单批最多 50 事件 × 10 req/min 将持续写入速率封顶，防止恶意高频小批量放大数据库写入压力；限流参数风格与 today-suggestion 控制器既有端点一致。
- 写入面防护说明同步至 `data-retention.md` 产品事件保留章节。
- 验证：product-events 控制器 spec 通过；`pnpm typecheck`、`pnpm lint:check`（`--max-warnings=0`）通过。

## 2026-08-14 增量代码审查修复 — 漏斗窗口跨度精确日历日差计算

- `funnel.service.ts` `resolveWindow` 的跨度计算由 `Math.round((end - start) / MS_PER_DAY)` 改为 `utcDayNumber(end) - utcDayNumber(start)`：两端点均为 UTC 午夜瞬间（`utcDayStart` 强制），`Math.floor(t / MS_PER_DAY)` 的差值即精确日历日数，不再依赖毫秒四舍五入，杜绝 DST 边界的 1 天误差；新增 `utcDayNumber` 辅助函数并注释说明其精确性（ms 纪元无闰秒）。
- 验证：funnel spec 既有窗口边界用例（单日/30 包含日上限/dateFrom>dateTo/时区偏移归日）全部通过；`pnpm typecheck` 通过。

## 2026-08-14 增量代码审查修复 — ProductEventName 映射全覆盖强制测试

- `funnel.service.ts` 新增导出 `isFunnelEventNameAccountedFor`：枚举值须映射到核心阶段、optional 计数或显式声明不计数（`INTENTIONALLY_UNCOUNTED_EVENT_NAMES`，目前仅 `visit_summary_share_revoked`）；DEV NOTE 同步更新说明强制方式。
- `funnel.service.spec` 新增遍历 `Object.values(ProductEventName)` 的全覆盖用例：新增枚举值未映射/未声明时测试失败——此前「新增枚举值时可能遗漏映射导致数据静默丢失」的维护隐患从注释约束升级为可执行强制。
- `Active_Product_Loop.md` 当前状态补充本轮审查修复总览（限流/日历日差/日志白名单/映射强制）。
- 验证：funnel spec 全部通过；`pnpm typecheck`、`pnpm lint:check`（`--max-warnings=0`）通过。

## 2026-08-14 增量代码审查修复 — 事件发射失败日志白名单化

- `emitServerEvent` 失败日志不再拼接原始 `error.message`（Prisma/驱动错误可能内嵌连接串、主机、表名甚至事件载荷），改为仅记录固定低敏标识：`Prisma.PrismaClientKnownRequestError` 输出稳定错误码（如 `prisma P2002`），其余错误输出类名（`Error`/`PrismaClientValidationError` 等），非 Error 输出固定 `unknown error`；方法注释同步更新，`product_event_emission_failure_total` 指标标签不变。
- `events.service.spec` 对应断言收紧：日志精确等于固定标识、原始 message 不得出现、Prisma 已知错误码用例新增。
- 验证：events.service spec 全部通过；`pnpm typecheck`、`pnpm lint:check`（`--max-warnings=0`）通过。

## 2026-08-14 增量代码审查修复 — ShareService 统一时间源

- `share.service.ts` 引入 `common` barrel 的 `now()`（与 `summary.service.ts` 等既有约定一致），替换 `createShare` 的 `Date.now()` 过期时间计算、`getSharedSummaryByToken` 的守卫/访问时间戳（局部变量改名 `nowDate`/`accessedAt` 避免遮蔽）与 `revokeShare` 的 `revokedAt`——时间在测试中可通过 fake timers 冻结，风格与项目其余服务一致。
- 验证：share.service spec 全部通过；`pnpm typecheck` 通过。

## 2026-08-14 增量代码审查修复 — 摘要范围标签集中配置

- `summary.service.ts` 新增导出配置对象 `SUMMARY_RANGE_LABELS`（`last7Days`/`last30Days`/`event`/`custom`），`DEFAULT_RANGE`、`RANGE_DAY_COUNTS` 键与 event/custom 标签全部改由其派生，删除分散的 `EVENT_SCOPE_RANGE_LABEL` 字面量；`INSUFFICIENT_COVERAGE_CODE` 提升为导出常量，与 `RANGE_DAY_COUNTS` 同层集中管理，注释注明与 dashboard 的 `REPORT_RANGE_*` 常量保持同步。
- 合同值不变：`scopeLabel`/`dataRange` 响应字符串与既有 DTO 文档一致，无需 `pnpm export:openapi`。
- 验证：clinic-summary + reports controller spec 全部通过；全量 `pnpm test`、`pnpm typecheck`、`pnpm lint:check`（`--max-warnings=0`）、`pnpm format:check`、`pnpm docs:check`、`pnpm docs:verify` 通过。

## 方向计划修订:SaaS 工作台技术选型调研论证(无代码变更)

- `plans/2026-08-14-saas-modules-and-node-monorepo.md` 修订:桌面工作台 = Next.js 独立 web 客户端(Lucent 薄客户端),不复刻手机五大 Tab;monorepo 合并动机更正为「web 客户端 + 营销站 + 文档站与后端同仓同 CI」,不再新建伞仓(按 2026-08-14 讨论结论,以 Lucent 本体为 monorepo 根、apps/ 子包收编,原伞仓方案废弃);SaaS 模块优先级重排——web 微信扫码 OAuth 第一优先,`subscription`/`billing` 延后到商业化落地,角色/多租户不做(现有可撤销分享覆盖照护者场景);执行顺序按 Phase 0-3 分阶段(见 Luminous 侧临时草稿)。
- 调研结论(2026-08-15):判据「用户打开电脑端是为了看清手机上看不清的东西」→ 核心能力为复杂数据表/筛选/图表;web 生态(TanStack Table/AG Grid + ECharts)对此碾压 Flutter(核心判据,一票决定);WebView 性能澄清(WebView2 = Chromium,瓶颈在数据层工程不在引擎,第一版纯 web 连 WebView 都不存在);升级路径不对称(web→Tauri 加壳 UI 零重写,Flutter→web 全量重写);Tauri 加壳设两个触发条件(本地目录导出/离线历史数据;托盘/通知/自动更新),未满足不加壳。
- 范围:方向计划修订,无代码变更,不影响发布门禁。

## fix(notifications): 响应 DTO 补全 data 信封层

全局 `ApiEnvelopeInterceptor` 只放行带 `data` 键的对象;`NotificationListResponseDto`/`UnreadCountResponseDto`
此前为扁平结构(code/message/业务字段),运行时被再包一层 data,OpenAPI 契约与前端生成客户端不匹配,
通知列表/未读数解析抛 CheckedFromJsonException 并触发前端自动重试,页面长期显示骨架屏。

- `notifications/dto/response.dto.ts`:`NotificationListResponseDto` 新增 `data: NotificationListDataDto {items,total}`;`UnreadCountResponseDto` 新增 `data: UnreadCountDataDto {count}`。
- 验证:`pnpm typecheck`、`pnpm lint:check`(--max-warnings=0)通过;notifications 模块测试通过。
