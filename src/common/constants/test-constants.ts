/**
 * Shared test-only constants.
 *
 * Using a fake bucket name in tests avoids leaking real production
 * bucket identifiers into test code and DTO examples.
 */
export const TEST_BUCKET_NAME = 'lucent-test-bucket';
