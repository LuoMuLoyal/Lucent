# ── 服务器 ──
HOST=0.0.0.0
PORT=3000
CORS_ORIGIN=*

# ── JWT ──
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=14d

# ── 邮件 ──
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_FROM=noreply@example.com

# ── 日志 ──
LOG_LEVEL=debug
