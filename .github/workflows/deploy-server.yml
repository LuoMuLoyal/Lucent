name: lucent-ci-cd

on:
  workflow_dispatch:
  pull_request:
  push:

concurrency:
  group: lucent-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      HUSKY: 0
      NODE_ENV: test
      HOST: 0.0.0.0
      PORT: 3000
      CORS_ORIGIN: http://127.0.0.1:3000
      DATABASE_URL: postgresql://lucent:lucent_dev@127.0.0.1:5432/lucent?schema=public
      REDIS_URL: redis://127.0.0.1:6379
      JWT_ACCESS_SECRET: ci_access_secret
      JWT_REFRESH_SECRET: ci_refresh_secret
      JWT_ACCESS_TTL: 15m
      JWT_REFRESH_TTL: 14d
      MAIL_DRIVER: log
      MAIL_FROM: noreply@example.com
      ADMIN_EMAIL: admin@example.com
      ADMIN_PASSWORD: ci_admin_password
      ADMIN_COOKIE_SECRET: ci_admin_cookie_secret_32chars_plus
      LOG_LEVEL: error
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: lucent
          POSTGRES_PASSWORD: lucent_dev
          POSTGRES_DB: lucent
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U lucent -d lucent"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      redis:
        image: redis:8-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm exec prisma generate

      - name: Apply test migrations
        run: pnpm exec prisma migrate deploy

      - name: Lint
        run: pnpm lint:check

      - name: Type check
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Unit tests
        run: pnpm test:ci

      - name: E2E tests
        run: pnpm test:e2e:ci

  docker:
    name: Build and Push Image
    runs-on: ubuntu-latest
    needs: ci
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    timeout-minutes: 30
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      REGISTRY_HOST: ${{ vars.REGISTRY_HOST }}
      REGISTRY_NAMESPACE: ${{ vars.REGISTRY_NAMESPACE }}
      REGISTRY_IMAGE_NAME: ${{ vars.REGISTRY_IMAGE_NAME }}
    outputs:
      image_ref: ${{ steps.meta.outputs.image_ref }}
      postgres_image_ref: ${{ steps.meta.outputs.postgres_image_ref }}
      redis_image_ref: ${{ steps.meta.outputs.redis_image_ref }}
      prometheus_image_ref: ${{ steps.meta.outputs.prometheus_image_ref }}
      grafana_image_ref: ${{ steps.meta.outputs.grafana_image_ref }}
      nginx_image_ref: ${{ steps.meta.outputs.nginx_image_ref }}
      registry_host: ${{ steps.meta.outputs.registry_host }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve image metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          registry_host="${REGISTRY_HOST:-ghcr.io}"
          default_namespace="$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          default_image_name="$(echo "${GITHUB_REPOSITORY##*/}" | tr '[:upper:]' '[:lower:]')"
          namespace="${REGISTRY_NAMESPACE:-$default_namespace}"
          image_name="${REGISTRY_IMAGE_NAME:-$default_image_name}"
          image_repo="${registry_host}/${namespace}/${image_name}"
          image_ref="${image_repo}:latest"
          postgres_image_repo="${registry_host}/${namespace}/${image_name}-postgres"
          postgres_image_ref="${postgres_image_repo}:18-alpine"
          redis_image_repo="${registry_host}/${namespace}/${image_name}-redis"
          redis_image_ref="${redis_image_repo}:8-alpine"
          prometheus_image_repo="${registry_host}/${namespace}/${image_name}-prometheus"
          prometheus_image_ref="${prometheus_image_repo}:v3.12.0"
          grafana_image_repo="${registry_host}/${namespace}/${image_name}-grafana"
          grafana_image_ref="${grafana_image_repo}:13.0.2"
          nginx_image_repo="${registry_host}/${namespace}/${image_name}-nginx"
          nginx_image_ref="${nginx_image_repo}:1.29.1-alpine"

          {
            echo "registry_host=${registry_host}"
            echo "image_repo=${image_repo}"
            echo "image_ref=${image_ref}"
            echo "postgres_image_repo=${postgres_image_repo}"
            echo "postgres_image_ref=${postgres_image_ref}"
            echo "redis_image_repo=${redis_image_repo}"
            echo "redis_image_ref=${redis_image_ref}"
            echo "prometheus_image_repo=${prometheus_image_repo}"
            echo "prometheus_image_ref=${prometheus_image_ref}"
            echo "grafana_image_repo=${grafana_image_repo}"
            echo "grafana_image_ref=${grafana_image_ref}"
            echo "nginx_image_repo=${nginx_image_repo}"
            echo "nginx_image_ref=${nginx_image_ref}"
          } >> "$GITHUB_OUTPUT"

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ steps.meta.outputs.registry_host }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build image
        shell: bash
        run: |
          set -euo pipefail
          docker build \
            -f ./Dockerfile \
            -t "${{ steps.meta.outputs.image_ref }}" \
            .

      - name: Push image tag
        shell: bash
        run: |
          set -euo pipefail
          docker push "${{ steps.meta.outputs.image_ref }}"

      - name: Mirror monitoring base images
        shell: bash
        run: |
          set -euo pipefail

          docker pull prom/prometheus:v3.12.0
          docker tag prom/prometheus:v3.12.0 "${{ steps.meta.outputs.prometheus_image_ref }}"
          docker push "${{ steps.meta.outputs.prometheus_image_ref }}"

          docker pull grafana/grafana-oss:13.0.2
          docker tag grafana/grafana-oss:13.0.2 "${{ steps.meta.outputs.grafana_image_ref }}"
          docker push "${{ steps.meta.outputs.grafana_image_ref }}"

          docker pull nginx:1.29.1-alpine
          docker tag nginx:1.29.1-alpine "${{ steps.meta.outputs.nginx_image_ref }}"
          docker push "${{ steps.meta.outputs.nginx_image_ref }}"

  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: docker
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    timeout-minutes: 20
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      SERVER_APP_DIR: ${{ vars.SERVER_APP_DIR }}
      LUCENT_IMAGE: ${{ needs.docker.outputs.image_ref }}
      POSTGRES_IMAGE: ${{ needs.docker.outputs.postgres_image_ref }}
      REDIS_IMAGE: ${{ needs.docker.outputs.redis_image_ref }}
      PROMETHEUS_IMAGE: ${{ needs.docker.outputs.prometheus_image_ref }}
      GRAFANA_IMAGE: ${{ needs.docker.outputs.grafana_image_ref }}
      NGINX_IMAGE: ${{ needs.docker.outputs.nginx_image_ref }}
      REGISTRY_HOST: ${{ needs.docker.outputs.registry_host }}
      REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
      REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
    steps:
      - name: Configure SSH
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf '%s\n' "${{ secrets.SERVER_KNOWN_HOSTS }}" > ~/.ssh/known_hosts

      - name: Sync deployment files to server
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SERVER_APP_DIR"

          ssh -p "${{ secrets.SERVER_PORT }}" "${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}" \
            "mkdir -p '$SERVER_APP_DIR/scripts/deploy' '$SERVER_APP_DIR/monitoring' '$SERVER_APP_DIR/deploy/nginx'"

          tar czf - docker-compose.yml scripts/deploy/deploy-server.sh monitoring deploy/nginx | \
            ssh -p "${{ secrets.SERVER_PORT }}" "${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}" \
              "tar xzf - -C '$SERVER_APP_DIR'"

          ssh -p "${{ secrets.SERVER_PORT }}" "${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}" \
            "chmod +x '$SERVER_APP_DIR/scripts/deploy/deploy-server.sh'"

      - name: Deploy on server
        shell: bash
        run: |
          set -euo pipefail
          password_b64="$(printf '%s' "$REGISTRY_PASSWORD" | base64 -w0)"
          printf -v remote_env 'SERVER_APP_DIR=%q LUCENT_IMAGE=%q POSTGRES_IMAGE=%q REDIS_IMAGE=%q PROMETHEUS_IMAGE=%q GRAFANA_IMAGE=%q NGINX_IMAGE=%q REGISTRY_HOST=%q REGISTRY_USERNAME=%q REGISTRY_PASSWORD_B64=%q' \
            "$SERVER_APP_DIR" \
            "$LUCENT_IMAGE" \
            "$POSTGRES_IMAGE" \
            "$REDIS_IMAGE" \
            "$PROMETHEUS_IMAGE" \
            "$GRAFANA_IMAGE" \
            "$NGINX_IMAGE" \
            "$REGISTRY_HOST" \
            "$REGISTRY_USERNAME" \
            "$password_b64"

          ssh -p "${{ secrets.SERVER_PORT }}" "${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}" \
            "$remote_env sh -s" <<'EOF'
          set -eu
          export REGISTRY_PASSWORD="$(printf '%s' "$REGISTRY_PASSWORD_B64" | base64 -d)"
          cd "$SERVER_APP_DIR"
          sh scripts/deploy/deploy-server.sh
          EOF
